---
phase: 03-cli-auth-crud
plan: 05
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - cli/apollo_cli/entities/rotina.py
  - cli/apollo_cli/entities/log_inferencia.py
  - cli/tests/test_crud_rotina_template.py
  - cli/tests/test_rotina_instancia.py
  - cli/tests/test_log_inferencia.py
autonomous: true
requirements: [CLI-06, CLI-07, CLI-10]
user_setup: []

must_haves:
  truths:
    - "`apollo rotina template criar|editar|deletar|listar` performs full CRUD on `templatesRotina` against the live app, with `donoId` equal to the session `user_id`."
    - "`apollo rotina instancia listar` returns `instanciasRotina` records, and `apollo rotina instancia status --id <id> --status <novo>` updates only the `status` field of an existing instance."
    - "There is NO command that creates an `instanciasRotina` record: `apollo rotina instancia criar` exits non-zero with click's `No such command` error, and `apollo rotina instancia --help` lists exactly `listar` and `status`."
    - "`apollo rotina instancia status` changes ONLY `status` — reading the record back shows `dedupeKey`, `dataPrevista`, `competencia`, `tipoPrazo`, and `donoId` all byte-identical to their pre-update values."
    - "`apollo log-inferencia registrar` creates a `logInferenciaClaude` record with `createdAt` set automatically, and `apollo log-inferencia listar` returns it."
    - "`apollo rotina template criar --tipo-geracao <invalid>` exits 2 listing the three valid values `du_fixo`, `corrido_fixo`, `encadeado`."
    - "`apollo rotina template criar --antecessor-id <id>` writes the `templateAntecessor` self-link, verified by reading the record back with the nested `antecessor`."
    - "`apollo rotina gerar-instancias` does NOT exist in this phase — `apollo rotina --help` does not list it (it lands in Phase 5, JOB-02); no stub, no placeholder, no `NotImplementedError` command."
    - "`apollo rotina instancia status --id <unused uuid>` exits non-zero with `not_found` and creates no phantom instance — critical here, since a phantom `instanciasRotina` without a valid `dedupeKey` would corrupt Phase 5's idempotency."
    - "Both groups appear in `apollo --help` with no edit to `cli.py`, and the `log-inferencia` group name is kebab-case even though its module is `log_inferencia.py`."
    - statement: "Restricting `instancia status` to the `status` field alone (rather than allowing date edits) is the right boundary for v1, given Phase 5 owns instance generation and any date edit would desynchronize a record from its `dedupeKey`."
      verification: backstop
  prohibitions:
    - "The CLI must never expose a way to create an `instanciasRotina` record directly. Creation belongs exclusively to the Phase 5 dedupeKey-based upsert; a hand-created instance would carry a wrong or absent dedupeKey and would then be duplicated by the job on the very next run — silently breaking the one idempotency guarantee this system promises."
    - "`apollo rotina instancia status` must never be widened into a general-purpose instance editor. Allowing `dataPrevista` or `competencia` edits would leave the record inconsistent with the `dedupeKey` derived from those exact values, so the next job run would regenerate a duplicate alongside it."
    - "`logInferenciaClaude` is an append-only audit trail of what Claude inferred and why. The CLI must never offer an edit or delete command for it — a rewritable audit log cannot serve its purpose of letting the user check the AI's reasoning after the fact."
    - "No stub or placeholder for `apollo rotina gerar-instancias` may be added in this phase. A command that exists but does nothing is indistinguishable from a working one at the call site, and a Claude-operated workflow would report the job as run."
  artifacts:
    - path: "cli/apollo_cli/entities/rotina.py"
      provides: "`apollo rotina template ...` (CRUD) and `apollo rotina instancia listar|status` (no create)"
      exports: ["group"]
      min_lines: 80
    - path: "cli/apollo_cli/entities/log_inferencia.py"
      provides: "`apollo log-inferencia registrar|listar` (append-only)"
      exports: ["group"]
      min_lines: 40
    - path: "cli/tests/test_crud_rotina_template.py"
      provides: "Live CRUD + self-link proof for templatesRotina (CLI-06)"
    - path: "cli/tests/test_rotina_instancia.py"
      provides: "Live list/status proof + structural no-create proof (CLI-07)"
    - path: "cli/tests/test_log_inferencia.py"
      provides: "Live registrar/listar proof + structural no-edit/no-delete proof (CLI-10)"
  key_links:
    - from: "cli/apollo_cli/entities/rotina.py"
      to: "cli/apollo_cli/crud_helpers.py"
      via: "create_entity/update_entity/list_entities for templatesRotina; update_entity for instanciasRotina status"
      pattern: "from apollo_cli.crud_helpers import"
    - from: "cli/apollo_cli/entities/log_inferencia.py"
      to: "cli/apollo_cli/crud_helpers.py"
      via: "create_entity(etype='logInferenciaClaude', ...) and list_entities"
      pattern: "logInferenciaClaude"
---

<objective>
Deliver the recurring-routine surface (`templatesRotina` full CRUD, `instanciasRotina` list + status-only) and the Claude inference audit log — including the deliberate absences that protect Phase 5's idempotency guarantee.

Purpose: This plan is as much about what is NOT built as what is. `instanciasRotina` has a unique `dedupeKey` that Phase 5's upsert depends on; any CLI path that creates or re-dates an instance would break idempotency before the job that guarantees it even exists. The structural tests here pin those absences so a future change cannot quietly add them.
Output: two entity modules and three live test modules, added without touching any file another wave-3 plan touches.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-cli-auth-crud/03-RESEARCH.md
@.planning/phases/03-cli-auth-crud/03-02-SUMMARY.md
@shared/instant.schema.ts
@cli/apollo_cli/crud_helpers.py
@cli/apollo_cli/entities/fundo.py
@cli/tests/conftest.py
@cli/tests/test_crud_fundo.py
</context>

<interfaces>
<!-- Command surface for this plan. Shapes LOCKED by PROJECT.md C-07; field names LOCKED by C-04. -->

    apollo rotina template criar   --nome TEXT [req]
                                   --tipo-geracao [du_fixo|corrido_fixo|encadeado] [req]
                                   --regra-competencia TEXT [req]
                                   [--propagar-atraso-soft/--nao-propagar-atraso-soft, default off]
                                   [--ativo/--inativo, default --ativo]
                                   [--fundo-id TEXT] [--antecessor-id TEXT]
    apollo rotina template editar  --id TEXT [req] [--nome] [--tipo-geracao] [--regra-competencia]
                                   [--propagar-atraso-soft/--nao-propagar-atraso-soft]
                                   [--ativo/--inativo] [--fundo-id] [--antecessor-id]
    apollo rotina template deletar --id TEXT [req]
    apollo rotina template listar  [--fundo-id TEXT] [--ativo/--inativo] [--limit INT]

    apollo rotina instancia listar [--template-id TEXT] [--status TEXT] [--limit INT]
    apollo rotina instancia status --id TEXT [req] --status TEXT [req]
    # NO `criar`. NO `deletar`. NO other mutating subcommand. NO `gerar-instancias` (Phase 5).

    apollo log-inferencia registrar --campo TEXT [req] --valor-inferido TEXT [req]
                                    --entidade-tipo TEXT [req] --entidade-id TEXT [req]
                                    [--trecho-motivador TEXT]
    apollo log-inferencia listar    [--entidade-tipo TEXT] [--entidade-id TEXT] [--limit INT]
    # NO `editar`. NO `deletar`. Append-only audit trail.

Schema fields (LOCKED, `shared/instant.schema.ts`):
- `templatesRotina`: nome, tipoGeracao, regraCompetencia, propagarAtrasoSoft(bool), ativo(bool), donoId
- `instanciasRotina`: dedupeKey(unique+indexed), dataPrevista(date), dataPrevistaEstimada?(date), competencia, tipoPrazo, status(indexed), donoId
- `logInferenciaClaude`: campo, valorInferido, trechoMotivador?, entidadeTipo, entidadeId, donoId, createdAt(date)

Link labels (forward direction — pass these to `.link({...})`):
- `templatesRotina.fundo` → a `fundos` id (link `fundoTemplatesRotina`)
- `templatesRotina.antecessor` → another `templatesRotina` id (self-link `templateAntecessor`)
- `instanciasRotina.template` → a `templatesRotina` id (link `templateInstancias`) — READ-ONLY from the CLI

Group naming: the module is `log_inferencia.py` but the click group MUST be named `log-inferencia`
(`click.Group("log-inferencia", ...)`), because the discovery mechanism registers the group under its
click name, not the module name.

`rotina` is a nested group: `group = click.Group("rotina")`, with `template` and `instancia` as
sub-`click.Group`s added to it. `apollo rotina --help` therefore lists `template` and `instancia`.

Helpers to use unchanged (from `crud_helpers`, plan 03-02): `require_session`, `client_for_session`,
`create_entity`, `update_entity`, `delete_entity`, `get_entity`, `list_entities`, `emit`,
`drop_none`, `validate_iso_date`, `now_iso`, `EXIT_NO_SESSION`, `EXIT_API_ERROR`, `EXIT_NETWORK_ERROR`.

Discovery contract (plan 03-02): each module exports `group: click.Group` and lives directly in
`cli/apollo_cli/entities/`. **Do not edit `cli/apollo_cli/cli.py`** — other wave-3 plans run in
parallel and any edit there is a merge conflict.

Test-fixture note for `instanciasRotina`: since the CLI cannot create instances, the CLI-07 tests
must seed one directly through `live_client.tx.instanciasRotina[new_id()].create({...})` in the test
body (with a `dedupeKey` like `f"test-{unique_suffix()}"` and `donoId=live_session.user_id`), then
exercise `listar`/`status` through the CLI. Seeding in the test is correct; a CLI seeding command
would not be.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `apollo rotina template` full CRUD with fundo and antecessor links (CLI-06)</name>

  <read_first>
    - `cli/apollo_cli/entities/fundo.py` — module shape, help-text style, emit shapes to mirror
    - `cli/apollo_cli/crud_helpers.py` — signatures, `links` parameter
    - `shared/instant.schema.ts` lines 47-58, 106-113, 130-139 — `templatesRotina` fields plus the `fundoTemplatesRotina` and `templateAntecessor` links
    - `.planning/PROJECT.md` §Constraints C-06 — what the three `tipoGeracao` values mean to the Phase 5 job
  </read_first>

  <files>cli/apollo_cli/entities/rotina.py, cli/tests/test_crud_rotina_template.py</files>

  <action>
Create `cli/apollo_cli/entities/rotina.py` with `group = click.Group("rotina", ...)` and a `template` sub-group holding the four subcommands from `<interfaces>`. (The `instancia` sub-group is added in Task 2 in the same file.)

`--tipo-geracao` is `click.Choice(["du_fixo", "corrido_fixo", "encadeado"])`; the option help explains each in one clause (business-day offset, calendar-day offset, chained off a predecessor template). `--regra-competencia` is a free-form string; document the expected shape in help without enforcing it. `--fundo-id` and `--antecessor-id` are each validated with `get_entity` (against `fundos` and `templatesRotina` respectively) before being merged into `links` as `{"fundo": ...}` / `{"antecessor": ...}`. On `editar`, both boolean flag pairs must be declared `default=None` so an unspecified flag does not silently reset `ativo` or `propagarAtrasoSoft`.

Group help for `rotina` states that it covers recurring-routine templates and their generated instances, and explicitly notes that instances are created only by the generation job, never by hand.

Create `cli/tests/test_crud_rotina_template.py` (marked `@pytest.mark.live`, using `conftest.py` fixtures) covering: create → read back, `donoId == live_session.user_id`, all three required fields plus both booleans round-trip with correct types; create with `--fundo-id` (throwaway fundo, cleaned up) → nested `fundo` resolves; create a second template with `--antecessor-id <first id>` → nested `antecessor` resolves to the first (self-link proof); `criar --tipo-geracao semanal` → exit 2 with all three valid values named; `listar --ativo` filters; `editar --nome X` alone leaves `ativo` and `propagarAtrasoSoft` unchanged (the default-`None` regression); delete removes it; `editar --id <unused uuid4>` → `not_found`, no phantom record.
  </action>

  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run pytest tests/test_crud_rotina_template.py -q && uv run apollo rotina template --help && uv run ruff check . && uv run ruff format --check . && uv run ty check</automated>
  </verify>

  <acceptance_criteria>
    - `cd cli && uv run apollo rotina template --help` exits 0 and lists exactly `criar`, `editar`, `deletar`, `listar`.
    - `cd cli && uv run apollo rotina template criar --nome N --tipo-geracao semanal --regra-competencia R` exits 2 and stderr contains all of `du_fixo`, `corrido_fixo`, `encadeado`.
    - `cd cli && uv run pytest tests/test_crud_rotina_template.py -q` reports `0 failed`, `0 skipped`.
    - `git diff --stat cli/apollo_cli/cli.py` shows no changes.
    - `grep -n 'donoId\|dono_id\|INSTANT_APP_ADMIN_TOKEN\|Instant(' cli/apollo_cli/entities/rotina.py` returns zero matches.
  </acceptance_criteria>

  <done>`apollo rotina template` does full CRUD live, writes both the fundo link and the antecessor self-link, and its boolean flags cannot be reset by omission.</done>
</task>

<task type="auto">
  <name>Task 2: `apollo rotina instancia listar|status` with structurally-enforced no-create (CLI-07)</name>

  <read_first>
    - `cli/apollo_cli/entities/rotina.py` — Task 1 output, same file gets the `instancia` sub-group
    - `shared/instant.schema.ts` lines 59-71 and 130-133 — `instanciasRotina` fields and the `templateInstancias` link
    - `.planning/phases/03-cli-auth-crud/03-RESEARCH.md` §"Pattern 4"
    - `.planning/PROJECT.md` §Constraints C-06 — why `dedupeKey` integrity matters
  </read_first>

  <files>cli/apollo_cli/entities/rotina.py, cli/tests/test_rotina_instancia.py</files>

  <action>
Add an `instancia` sub-group to `rotina.py` with exactly two commands:

- `listar [--template-id TEXT] [--status TEXT] [--limit INT]` → `list_entities(etype="instanciasRotina", where=...)`, emitting the JSON array. When `--template-id` is given, filter via the link: `client.query({"instanciasRotina": {"$": {"where": {"template": template_id}}}})`.
- `status --id TEXT [req] --status TEXT [req]` → `update_entity(etype="instanciasRotina", eid=id, fields={"status": status})`. The payload dict must contain the single key `status` and nothing else; add an inline comment stating that widening it would desynchronize the record from its `dedupeKey`.

Add a module-level docstring in `rotina.py` stating explicitly that `instanciasRotina` has no `criar`
and no `deletar` command by design (C-06), and that `gerar-instancias` arrives in Phase 5.

Create `cli/tests/test_rotina_instancia.py` (marked `@pytest.mark.live`) covering:
- **Structural (no session needed):** import the `rotina` group and assert `set(rotina.group.commands["instancia"].commands) == {"listar", "status"}`. Assert `"criar" not in ...` and `"deletar" not in ...` with a failure message citing C-06. Assert `"gerar-instancias" not in rotina.group.commands` — Phase 5 owns it and no stub may pre-empt it.
- **CLI-surface:** `apollo rotina instancia criar ...` exits non-zero with `No such command`.
- **Live:** seed one `instanciasRotina` directly via `live_client.tx` (per `<interfaces>`, with a unique `dedupeKey`, linked to a throwaway template, registered for cleanup); `apollo rotina instancia listar` includes it; `listar --template-id <id>` returns it; `apollo rotina instancia status --id <id> --status concluida` exits 0; read back and assert `status == "concluida"` AND that `dedupeKey`, `dataPrevista`, `competencia`, `tipoPrazo`, and `donoId` are all identical to the seeded values (field-by-field equality assertions, not a single dict comparison, so a failure names the drifted field).
- **Not-found:** `status --id <unused uuid4> --status x` exits non-zero with `not_found`, and a follow-up query for that id returns nothing — no phantom instance with a missing `dedupeKey`.
  </action>

  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run pytest tests/test_rotina_instancia.py -q && uv run apollo rotina instancia --help && uv run ruff check . && uv run ty check</automated>
  </verify>

  <acceptance_criteria>
    - `cd cli && uv run apollo rotina instancia --help` exits 0 and its command list contains exactly `listar` and `status` — no `criar`, no `deletar`.
    - `cd cli && uv run apollo rotina instancia criar` exits non-zero and stderr contains `No such command`.
    - `cd cli && uv run apollo rotina --help` does NOT contain `gerar-instancias`: `uv run apollo rotina --help | grep -c 'gerar-instancias'` returns 0.
    - `cd cli && uv run pytest tests/test_rotina_instancia.py -q` reports `0 failed`, `0 skipped`.
    - `grep -n 'instanciasRotina' cli/apollo_cli/entities/rotina.py | grep -i 'create_entity'` returns zero matches — `create_entity` is never called for this etype.
  </acceptance_criteria>

  <done>`instanciasRotina` can be listed and status-updated from the CLI and cannot be created, deleted, or re-dated — the absences are pinned by structural tests, and a status update provably touches only `status`.</done>
</task>

<task type="auto">
  <name>Task 3: `apollo log-inferencia registrar|listar` append-only audit log (CLI-10)</name>

  <read_first>
    - `cli/apollo_cli/entities/fundo.py` — module shape to mirror
    - `cli/apollo_cli/crud_helpers.py` — `now_iso()` and `create_entity`
    - `shared/instant.schema.ts` lines 90-99 — `logInferenciaClaude` fields
  </read_first>

  <files>cli/apollo_cli/entities/log_inferencia.py, cli/tests/test_log_inferencia.py</files>

  <action>
Create `cli/apollo_cli/entities/log_inferencia.py` exporting `group = click.Group("log-inferencia", ...)` (kebab-case click name, snake_case module name) with exactly two commands per `<interfaces>`: `registrar` and `listar`. No `editar`, no `deletar`.

`registrar` builds `fields = {"campo": ..., "valorInferido": ..., "trechoMotivador": ..., "entidadeTipo": ..., "entidadeId": ...}` (dropping the optional `trechoMotivador` when absent) and lets `create_entity` supply `donoId` and `createdAt`. Emit `{"id": eid}`. `listar` supports `--entidade-tipo`/`--entidade-id` filters and `--limit`.

The group docstring must state that this is an append-only record of values Claude inferred and the text that justified them, and that it is deliberately not editable or deletable so the user can always audit the AI's reasoning after the fact. `--entidade-tipo` help should note the expected values are entity names as used in the schema (e.g. `tarefas`, `tickets`).

Create `cli/tests/test_log_inferencia.py` covering:
- **Structural (no session needed):** `set(log_inferencia.group.commands) == {"registrar", "listar"}` — asserting `editar` and `deletar` are absent, with a failure message explaining the append-only rationale.
- **Live:** `registrar` a record with all five fields → exit 0, JSON has an `id`; read back and assert `donoId == live_session.user_id`, `createdAt` is present and parses as an ISO timestamp, and all supplied fields round-trip. `registrar` without `--trecho-motivador` → the key is absent, not null. `listar --entidade-tipo tarefas --entidade-id <the id>` returns exactly the created record. Register created records for cleanup (cleanup uses `live_client.tx` directly, which is correct — the CLI intentionally offers no delete).
  </action>

  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run pytest tests/test_log_inferencia.py -q && uv run apollo log-inferencia --help && uv run ruff check . && uv run ruff format --check . && uv run ty check</automated>
  </verify>

  <acceptance_criteria>
    - `cd cli && uv run apollo log-inferencia --help` exits 0 and its command list contains exactly `registrar` and `listar`.
    - `cd cli && uv run apollo log-inferencia deletar --id x` exits non-zero with `No such command`.
    - `cd cli && uv run apollo --help` lists `log-inferencia` (kebab-case) and not `log_inferencia`.
    - `cd cli && uv run pytest tests/test_log_inferencia.py -q` reports `0 failed`, `0 skipped`.
    - `cd cli && uv run pytest -q` (full suite) reports `0 failed`.
    - `grep -n 'donoId\|dono_id\|INSTANT_APP_ADMIN_TOKEN\|Instant(' cli/apollo_cli/entities/log_inferencia.py` returns zero matches.
    - `cd cli && uv run pytest tests/test_auth_rejection.py -q -k confinement` still reports `1 passed`.
  </acceptance_criteria>

  <done>`apollo log-inferencia registrar` writes an audited inference record with an automatic `createdAt` and `donoId`, `listar` reads it back, and the absence of edit/delete is pinned by a structural test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags → transaction payload | Includes `status` values written onto records the generation job owns |
| CLI command surface → Phase 5 idempotency contract | Any create/date-edit path on `instanciasRotina` silently breaks `dedupeKey` uniqueness |
| Claude → audit log | The subject of the audit log is also the agent writing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Tampering | Hand-created `instanciasRotina` with a wrong/absent `dedupeKey` | mitigate | No `criar` command exists; a structural test asserts the `instancia` sub-group's command set is exactly `{listar, status}` and fails citing C-06 if widened |
| T-03-24 | Tampering | `instancia status` widened to edit `dataPrevista`/`competencia`, desynchronizing `dedupeKey` | mitigate | Update payload is the single key `status`; the live test asserts field-by-field that `dedupeKey`, `dataPrevista`, `competencia`, `tipoPrazo`, `donoId` are unchanged after a status update |
| T-03-25 | Tampering | `update_entity`'s upsert behavior creating a phantom instance from a bad `--id` | mitigate | `update_entity` calls `get_entity` first; the not-found test asserts no record exists afterwards |
| T-03-26 | Repudiation | Claude editing or deleting its own inference audit trail | mitigate | `log-inferencia` exposes only `registrar` and `listar`; structural test asserts `editar`/`deletar` are absent. Note: the InstantDB perms rule still permits deletes at the data layer for the owner — the CLI-level absence is the control, and Phase 6 may revisit whether a perms-level restriction is warranted |
| T-03-27 | Elevation of Privilege | New entity modules bypassing `crud_helpers` | mitigate | Per-task grep gates plus the AST confinement test from 03-02 re-run in Task 3's acceptance criteria |
</threat_model>

<verification>
```
cd cli && uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run ty check
cd cli && uv run apollo --help                     # must list rotina and log-inferencia
cd cli && uv run apollo rotina instancia --help    # exactly listar + status
cd cli && uv run apollo rotina --help | grep -c 'gerar-instancias'   # must be 0
git diff --stat cli/apollo_cli/cli.py              # must be empty
```
</verification>

<success_criteria>
- CLI-06, CLI-07, CLI-10 satisfied by live round trips with `donoId` from the session.
- `instanciasRotina` is provably list+status-only, protecting Phase 5's idempotency contract.
- `logInferenciaClaude` is provably append-only from the CLI.
- Zero edits to `cli.py` or any file owned by another wave-3 plan.
</success_criteria>

## Artifacts this phase produces

| Artifact | Purpose | Consumed by |
|----------|---------|-------------|
| `cli/apollo_cli/entities/rotina.py` | `apollo rotina template ...` + `apollo rotina instancia listar\|status` (CLI-06, CLI-07) | end user, Phase 4 parity, Phase 5 (adds `gerar-instancias` to this same group) |
| `cli/apollo_cli/entities/log_inferencia.py` | `apollo log-inferencia registrar\|listar` (CLI-10) | end user, Phase 4 parity, Phase 6 |
| `cli/tests/test_crud_rotina_template.py`, `test_rotina_instancia.py`, `test_log_inferencia.py` | Live proofs + structural absence proofs | 03-06 verification script, Phase 5 |

<output>
After completion, create `.planning/phases/03-cli-auth-crud/03-05-SUMMARY.md`. Record the exact option names, the nested-group structure of `rotina` (so Phase 5 knows where to attach `gerar-instancias`), the seeded-instance fixture approach, and the exact `tipoGeracao` / `status` values used in tests.
</output>
