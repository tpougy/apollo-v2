---
phase: 30-ciclo-de-vida-e-higiene-de-dados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/apollo_cli/entities/rotina.py
  - cli/tests/test_crud_rotina_template.py
  - cli/tests/test_rotina_instancia.py
  - cli/tests/test_cli_surface.py
autonomous: true
requirements: [LIFE-01, LIFE-02]
user_setup: []

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`apollo rotina template deletar --id <eid>` on a template with N linked `instanciasRotina` and no `--force` exits 2, reports the exact count N in the error JSON, and performs zero writes (template and instances both untouched) — live-proven (D-01, ROADMAP Success Criterion 1)."
    - "The same command with `--force` deletes the template, never cascades onto the linked instances, and the instance(s) become live-provably orphaned — re-querying with the `template` link expanded returns a row where the `template` key is entirely absent (not `[]`, not `null`), matching 30-PATTERNS.md's live experiment exactly (D-01, D-02)."
    - "`apollo rotina instancia limpar-orfas` (no `--confirmar`) lists every `instanciasRotina` whose `template` link is absent and writes nothing — live-proven against a real orphan (D-02, ROADMAP Success Criterion 2)."
    - "`apollo rotina instancia limpar-orfas --confirmar` deletes exactly the listed orphans and leaves a simultaneously-existing, validly-linked instance completely untouched — live-proven in the same test run (D-02, C-06 scope)."
    - "Running `apollo rotina instancia limpar-orfas --confirmar` against the real production account (`tp@rbrasset.com.br`) removes every currently-orphaned `instanciasRotina` row, including any whose `dedupeKey` starts with `phase23-e2e-dedupe-weekday`/`phase23-e2e-dedupe-weekend`, and the total `instanciasRotina` count drops by exactly the number of orphans removed — no other row is touched (D-03, ROADMAP Success Criterion 3)."
    - "`apollo rotina instancia` exposes exactly `{listar, status, limpar-orfas}` — no bare `criar`/`deletar` ever reopens for instances outside the orphan-only path (C-06, D-02)."
  artifacts:
    - path: "cli/apollo_cli/entities/rotina.py"
      provides: "_count_linked_instances, deletar's --force/--no-force guard, instancia.limpar_orfas's --confirmar/--no-confirmar"
      contains: "_count_linked_instances"
    - path: "cli/tests/test_crud_rotina_template.py"
      provides: "live proof of the block-by-default guard and the force+no-cascade+orphan-shape behavior"
      contains: "test_deletar_with_linked_instances_blocks_by_default_with_exact_count"
    - path: "cli/tests/test_rotina_instancia.py"
      provides: "live proof of limpar-orfas list/confirm precision + the Success Criterion 3 real-production cleanup"
      contains: "test_limpar_orfas_confirmar_removes_real_production_residue_success_criterion_3"
    - path: "cli/tests/test_cli_surface.py"
      provides: "EXPECTED_SURFACE['instanciasRotina'] extended to include limpar-orfas"
      contains: "limpar-orfas"
  key_links:
    - from: "cli/apollo_cli/entities/rotina.py deletar's --force guard"
      to: "cli/apollo_cli/entities/rotina.py _count_linked_instances's InstaQL reverse-link ('instancias') expansion"
      via: "client.query({'templatesRotina': {'instancias': {}, '$': {'where': {'id': eid}}}})"
      pattern: "reverse-link-expansion-count"
    - from: "cli/apollo_cli/entities/rotina.py limpar_orfas"
      to: "InstaQL forward-link ('template') expansion, falsy-or-absent orphan check"
      via: "client.query({'instanciasRotina': {'template': {}, '$': {'where': {'donoId': session.user_id}}}}); orphans = [r for r in rows if not r.get('template')]"
      pattern: "forward-link-expansion-orphan-filter"
---

<objective>
`apollo rotina template deletar` gains an explicit, exact-count block-by-default guard so a
template with linked `instanciasRotina` is never deleted silently, and `apollo rotina instancia
limpar-orfas` becomes the ONE narrow, orphan-only exception to this project's "no `criar`/`deletar`
for instances" rule (PROJECT.md C-06) — proven end-to-end, live, against the real production
InstantDB app, including the one deliberate, pre-authorized exception of actually cleaning the
already-known real orphan/residue rows onboarding surfaced (ROADMAP Success Criterion 3).

This is the phase's Wave 1 (CLI-only) plan. Task 1 is the tracer: it wires the guard's InstaQL
reverse-link count query through `deletar`'s existing `delete_entity` call, all the way to a real
InstantDB write/non-write, for both the blocked and the `--force` path — including the live proof
of 30-PATTERNS.md's key finding (an orphaned instance's `template` link comes back with the key
entirely ABSENT under link expansion, never `[]`/`null`). Task 2 expands to `instancia
limpar-orfas` (list vs. `--confirmar`, orphan-only precision proven against a simultaneously
existing valid instance) and folds in the phase's one real-production-data acceptance step
(Success Criterion 3), plus repairs two pre-existing structural tests this phase's new command
necessarily breaks (`test_cli_surface.py`'s `EXPECTED_SURFACE` exact-match tuple and
`test_rotina_instancia.py`'s `instancia` command-set assertion).

Purpose: REQUIREMENTS.md LIFE-01/LIFE-02 — the onboarding report found `template deletar` silently
orphaning instances with zero warning, and no command existed to find/remove the resulting debris
(3 real orphans + 1 test residual already present in production).
Output: `deletar --force/--no-force` (block-by-default guard), `instancia limpar-orfas
--confirmar/--no-confirmar` (list-by-default orphan cleanup), both live-proven against the real
account, plus the real onboarding residue actually removed as part of this plan's own live
verification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ciclo-de-vida-e-higiene-de-dados/30-CONTEXT.md
@.planning/phases/30-ciclo-de-vida-e-higiene-de-dados/30-PATTERNS.md

@cli/apollo_cli/entities/rotina.py
@cli/apollo_cli/routine_job.py
@cli/apollo_cli/crud_helpers.py
@cli/tests/test_crud_rotina_template.py
@cli/tests/test_rotina_instancia.py
@cli/tests/test_cli_surface.py
@cli/tests/conftest.py
@shared/instant.schema.ts

## D-NN decision-ID mapping (30-CONTEXT.md's `<decisions>` block uses plain numbers 1-6, not a
"D-NN" prefix — this plan assigns the mapping below once, for citation traceability; both forms
refer to the SAME locked decisions)

- D-01 = decision 1: `deletar` blocks by default on linked instances, exact count in the error,
  zero writes; `--force` proceeds WITHOUT cascading onto the instances (they become orphans,
  cleanable later); `--force` with zero linked instances is a silent no-op.
- D-02 = decision 2: `instancia limpar-orfas` is a dedicated, list-by-default,
  `--confirmar`-to-execute command; an instance is orphan when its `template` link expansion comes
  back falsy/absent (LIVE-PROVEN by 30-PATTERNS.md's experiment: the key is entirely ABSENT, not
  `[]`); never touches an instance with a valid `template` link (C-06 still governs the normal
  flow).
- D-03 = decision 3: the 3 real orphans + the `phase23-e2e-dedupe-weekday-...` residual must be
  removable by this same command, proven as part of THIS plan's live verification (not a separate
  manual step).
- D-04 = decision 4: LIFE-03 revised — no second InstantDB app; same production app, a reliable
  post-test sweep flow instead (implemented entirely in Wave 2 / plan 30-02, not this plan).
- D-05 = decision 5 (root cause + two required consequences, both scoped to Wave 2 / plan 30-02):
  5a = every `web/e2e/*.spec.ts` `rotina template deletar` call site needs `--force`; 5b = e2e sweep
  needs a DIRECT `instanciasRotina`-by-`dedupeKey`-prefix sweep, since `limpar-orfas` alone cannot
  serve this (an instance mid-sweep may still have a currently-valid `template` link).
- D-06 = decision 6: whether to centralize e2e sweep logic is a documented engineering call, made
  in plan 30-02 (this plan makes no e2e change).

## Existing tests this plan MUST repair (broken by this plan's own new command, not pre-existing bugs)

`cli/tests/test_cli_surface.py`'s `EXPECTED_SURFACE["instanciasRotina"]` is
`(["rotina", "instancia"], {"listar", "status"}, True)` with `exact_match=True` (line 49) — Task 2
adding `limpar-orfas` under `instancia` makes `test_schema_entity_coverage[instanciasRotina]` fail
immediately unless this tuple's required-set is extended to
`{"listar", "status", "limpar-orfas"}`. `cli/tests/test_rotina_instancia.py`'s
`test_instancia_command_set_is_exactly_listar_and_status` (lines 30-38) asserts
`set(rotina.instancia.commands) == {"listar", "status"}` — same failure mode, same fix (Task 2
renames it to `test_instancia_command_set_is_exactly_listar_status_and_limpar_orfas` and updates
the expected set + assertion message; the `"deletar" not in commands` assertion stays valid
unchanged — there is still no BARE `deletar`, only the orphan-scoped `limpar-orfas`). Both repairs
are grounded by direct reads of both files this session, not inferred.

`cli/tests/test_crud_rotina_template.py`'s existing `test_deletar_unknown_id_is_not_found` (a
phantom id, `deletar` with no `--force`) needs NO change: a nonexistent template id makes
`_count_linked_instances` return 0 (`templatesRotina` where-clause matches nothing), so the new
guard is skipped entirely and `delete_entity`'s pre-existing `not_found` exit (`EXIT_API_ERROR = 3`)
fires exactly as before — confirmed by reading this task's own planned implementation against that
test's exact assertions.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end guarded template delete — count query, --force flag, live proof of block + force-orphan (LIFE-01, D-01)</name>
  <files>cli/apollo_cli/entities/rotina.py, cli/tests/test_crud_rotina_template.py</files>
  <read_first>
    - cli/apollo_cli/entities/rotina.py:1-33 (module docstring — the "instanciasRotina has NO
      criar/deletar" paragraph this plan must NOT silently contradict; append a new paragraph
      after it documenting LIFE-01/LIFE-02 as the two narrow, named exceptions), :63-68
      (`_ETYPE_TEMPLATE`/`_ETYPE_INSTANCIA`/`_COMPETENCIA_RE: Final[re.Pattern[str]]` — the
      module's own local-`Final`-constant convention `_EXIT_INSTANCES_LINKED` mirrors; place it
      immediately after `_COMPETENCIA_RE`), :71-86 (`_resolve_ref` — the EXACT
      `click.echo(json.dumps({...}, sort_keys=True), err=True)` error-emission shape already used
      in THIS file for a query-then-fail guard; mirror this verbatim, NOT
      `crud_helpers._emit_err` — that helper is never imported into this module, confirmed by this
      session's read of the current import block at :45-56), :304-413 (`editar` then the current
      `deletar` at :408-413 — the exact 6-line body to replace; `_count_linked_instances` is a new
      module-level function placed immediately before `deletar`, mirroring
      `_query_active_templates`'s own "helper immediately before its caller" placement in
      `routine_job.py`)
    - cli/apollo_cli/routine_job.py:778-793 (`_query_active_templates` — the InstaQL link-expansion
      query shape `_count_linked_instances` copies, expanding the schema's REVERSE link
      `instancias` instead of `antecessor`; `_normalize_antecessor` — the falsy-or-absent
      normalization convention to mirror: an absent/non-list value counts as zero, never raises)
    - shared/instant.schema.ts:153-154 (`forward: {on: "instanciasRotina", has: "one", label:
      "template"}`, `reverse: {on: "templatesRotina", has: "many", label: "instancias"}` — confirms
      `_count_linked_instances` must expand `instancias` on `templatesRotina`, the reverse side of
      the SAME link `limpar-orfas` (Task 2) expands from the forward side)
    - cli/apollo_cli/crud_helpers.py:220-228 (`delete_entity`'s own not-found guard — the
      query-then-conditionally-fail-then-proceed shape `deletar`'s new guard extends; `deletar`
      keeps calling this function completely unchanged for the actual delete)
    - cli/tests/test_crud_rotina_template.py:1-48 (imports — `json`, `uuid`, `Any`/`cast`,
      `pytest`, `Instant`, `Session`, `CliInvocation`/`RunCli`/`unique_suffix`; `_query_template`/
      `_single`/`_create_fundo` helpers — `_seed_linked_instancia` is a new local helper added here
      mirroring `test_rotina_instancia.py`'s own `_seed_instancia` shape, using
      `from instantdb import id as new_id` — add this import), :475-480
      (`test_deletar_unknown_id_is_not_found` — confirm unchanged per this plan's own context note
      above; do not modify it)
    - cli/tests/test_rotina_instancia.py:61-79 (`_seed_instancia` — the EXACT
      `live_client.tx["instanciasRotina"][eid].create(fields).link({"template": template_id})`
      shape `_seed_linked_instancia` mirrors in the sibling test file, adapted to return only the
      new id since this file's tests do not need the full fields dict back)
  </read_first>
  <precondition>A persisted `apollo auth login` session exists for the real InstantDB production
  account (existing project setup — `.planning/PROJECT.md`'s established live-test convention,
  not created by this plan); `live_session`/`live_client` in `cli/tests/conftest.py` skip cleanly
  without it, but this task's live proofs require them to actually run, not skip.</precondition>
  <behavior>
    - `deletar --id <eid>` on a template with N &gt; 0 linked `instanciasRotina`, no `--force`:
      exit 2, stderr JSON `{"error": "instances_linked", "template_id": eid, "instance_count": N,
      "hint": ...}`, template still exists afterward, all N instances still exist afterward.
    - Same command with `--force`: exit 0, template gone, EVERY previously-linked instance still
      exists as a row but its `template` link, re-queried with expansion, has the `template` key
      entirely ABSENT (not `[]`, not `null` — the live-verified 30-PATTERNS.md shape).
    - Same command with `--force` and N == 0 linked instances: exit 0, template gone, response JSON
      `instances_linked_at_delete == 0` — a documented no-op on the flag itself, not an error.
    - `deletar --id <nonexistent>` (no `--force`): unchanged pre-existing behavior — `not_found`,
      `EXIT_API_ERROR`.
  </behavior>
  <action>
    In `cli/apollo_cli/entities/rotina.py`:

    Add a module-level `_EXIT_INSTANCES_LINKED: Final[int] = 2` immediately after
    `_COMPETENCIA_RE`, with a one-line comment noting D-01 mandates exit code 2 specifically (a new
    named constant, deliberately distinct from `crud_helpers.EXIT_API_ERROR = 3` — this is a
    business-rule/state guard discovered only after a query, not a Click argument-parsing failure,
    so it is emitted manually rather than raised as a `click.UsageError`).

    Add `_count_linked_instances(template_id: str) -> int` immediately before `deletar` (after
    `editar`). Calls `client_for_session()` for the client only (no `session.user_id` needed — this
    query filters by `id`, mirroring `get_entity`'s own no-donoId-filter convention, relying on
    InstantDB's server-side perms for ownership scoping exactly like every other single-record
    lookup in this module). Queries `{"templatesRotina": {"instancias": {}, "$": {"where": {"id":
    template_id}}}}`, reads `result.get("templatesRotina", [])`; returns `0` when that list is
    empty (id not found — the guard must never fire for a template that does not exist, preserving
    `test_deletar_unknown_id_is_not_found`'s existing behavior unchanged); otherwise reads
    `rows[0].get("instancias")` and returns `len(...)` when it is a list, else `0` (falsy-or-absent
    normalization, mirroring `_normalize_antecessor`'s own convention — an empty/absent reverse
    link must count as zero, never raise).

    Replace `deletar`'s current 6-line body (`--id` option + function). Add a second
    `@click.option("--force/--no-force", default=False, ...)` decorator (this module's established
    paired-boolean-flag convention — every existing boolean option here is `--x/--no-x`, e.g.
    `--ativo/--inativo`, `--dry-run/--no-dry-run`; there is no bare `is_flag=True` precedent
    anywhere in this codebase, confirmed by a full-codebase grep this session). Help text: states
    D-01's guard-bypass semantics, that it NEVER cascades onto linked instances (they become
    orphans, cleanable via `apollo rotina instancia limpar-orfas`), and that it is a silent no-op
    with zero linked instances. Extend `deletar`'s signature to `(eid: str, force: bool) -> None`.
    Body: `linked_count = _count_linked_instances(eid)`; when `linked_count > 0 and not force`,
    emit the `instances_linked` error via the EXACT `click.echo(json.dumps({...}, sort_keys=True),
    err=True)` shape `_resolve_ref` already uses in this file (fields: `error`, `template_id`,
    `instance_count`, `hint`), then `raise SystemExit(_EXIT_INSTANCES_LINKED)` — strictly BEFORE
    calling `delete_entity` (D-01's "nenhuma escrita realizada" — the guard must short-circuit
    before any write path, mirroring `_resolve_range_override`'s own "raise before any network
    call" precedent from Phase 29, though here the query itself already happened by necessity to
    compute the count). Otherwise (force, or zero linked instances): call `delete_entity(etype=
    _ETYPE_TEMPLATE, eid=eid)` completely unchanged, then `emit({"id": eid, "deleted": True,
    "force": force, "instances_linked_at_delete": linked_count})` — the two new keys are a purely
    additive extension of the existing `{"id", "deleted"}` JSON contract (mirrors JOB-03's own
    precedent of additive report fields), so no existing consumer of this command's stdout breaks.
    Extend `deletar`'s docstring: block-by-default with exact count, `--force` bypasses without
    cascading, orphans are cleanable via `limpar-orfas` (cite D-01).

    Append one new paragraph to the module's top docstring (after the existing `--antecessor-id`
    paragraph, before the closing `"""`): documents that LIFE-01/LIFE-02 (Phase 30) add exactly two
    narrow exceptions to the "no criar/deletar for instances" rule above — `deletar` now blocks by
    default on linked instances (exact count, zero writes) unless `--force`, which never cascades;
    `limpar-orfas` (Task 2) is the ONLY command that may ever delete an `instanciasRotina` row, and
    only when its `template` link is confirmed absent. Neither reopens general instance
    `criar`/`deletar` in the normal flow.

    In `cli/tests/test_crud_rotina_template.py`:

    Add `from instantdb import id as new_id` to the imports. Add a local helper
    `_seed_linked_instancia(live_client: Instant, live_session: Session, template_id: str, suffix:
    str) -> str`, mirroring `test_rotina_instancia.py:61-79`'s `_seed_instancia` exactly (same
    `dedupeKey`/`dataPrevista`/`competencia`/`tipoPrazo`/`status`/`donoId` field shape, same
    `.create(fields).link({"template": template_id})` chain), using `dedupeKey =
    f"phase30-life01-{suffix}-{n}"` (n distinguishes multiple seeded instances in one test) and
    returning only the new instance id.

    Add four new tests (all use `run_cli`/`live_client`/`cleanup_records`/`unique_suffix`, mirror
    this file's existing `test_full_crud_round_trip`-style structure — create fundo-free templates
    via `run_cli(["rotina","template","criar", ...])`, append every created id to
    `cleanup_records`):
    - `test_deletar_with_linked_instances_blocks_by_default_with_exact_count`: create a template,
      seed exactly 2 linked instances via `_seed_linked_instancia` (append both ids to
      `cleanup_records` as `("instanciasRotina", id)`), call `deletar --id <template_id>` with NO
      `--force`. Assert `exit_code == 2`. Parse the error JSON from `result.output or
      result.stderr` (mirrors this file's own `test_deletar_unknown_id_is_not_found` parsing
      idiom); assert `error == "instances_linked"`, `template_id == <template_id>`,
      `instance_count == 2`. Re-query the template via `_query_template` and assert it is NOT
      `None` (still exists — zero writes). Re-query both seeded instances directly and assert both
      still exist.
    - `test_deletar_with_force_deletes_template_and_orphans_linked_instance`: create a template,
      seed exactly 1 linked instance, call `deletar --id <template_id> --force`. Assert
      `exit_code == 0`; parse the JSON response and assert `deleted is True`, `force is True`,
      `instances_linked_at_delete == 1`. Assert `_query_template(live_client, template_id) is
      None`. THEN query the instance directly WITH link expansion (`live_client.query({
      "instanciasRotina": {"template": {}, "$": {"where": {"id": instance_id}}}})`), take
      `rows[0]`, and assert `"template" not in rows[0]` — the live-verified exact key-absence proof
      from 30-PATTERNS.md's experiment, now a permanent regression test.
    - `test_deletar_force_with_zero_linked_instances_is_noop_and_still_succeeds`: create a template
      with no seeded instances, call `deletar --id <template_id> --force`. Assert `exit_code == 0`,
      `instances_linked_at_delete == 0`, `force is True`, and `_query_template(...) is None`.
    - `test_deletar_help_documents_force_and_orphan_consequence`: `run_cli(["rotina", "template",
      "deletar", "--help"])`, assert `exit_code == 0`, assert `"--force"` in the output, assert
      `"orf"` in the lowercased output (catches "orfa"/"orfas" — this codebase's Portuguese help
      text is written without diacritics throughout, confirmed by this session's reads).
  </action>
  <reversibility rating="reversible">The guard is opt-out-by-default-behavior-preserving in the
  opposite direction of Phase 29's flags: EVERY existing `deletar --id <eid>` invocation with zero
  linked instances (the common case for every pre-existing test/caller) keeps its exact prior
  behavior — the new guard only changes behavior when instances ARE linked, which is precisely the
  silent-data-loss case D-01 exists to close. `--force` restores the old unconditional-delete
  behavior explicitly, opt-in. No schema, no perms, no destructive default — a straightforward
  `git revert`.</reversibility>
  <acceptance_criteria>
    - `grep -c '_count_linked_instances' cli/apollo_cli/entities/rotina.py` returns at least 2 (def
      + call site).
    - `grep -c '_EXIT_INSTANCES_LINKED' cli/apollo_cli/entities/rotina.py` returns at least 2 (def
      + raise site).
    - `grep -c -- '--force/--no-force' cli/apollo_cli/entities/rotina.py` returns 1.
    - `uv run --project cli apollo rotina template deletar --help` output contains `--force` and,
      lowercased, `orf`.
    - `uv run --project cli pytest cli/tests/test_crud_rotina_template.py -k "not live" -v` exits
      0 (no non-live tests exist in this file today or added by this task — this asserts the
      collection itself is clean, zero errors).
    - `uv run --project cli pytest cli/tests/test_crud_rotina_template.py -m live -v` exits 0,
      including the four new tests and every pre-existing test in this file unmodified.
    - `uv run --project cli ruff check cli/apollo_cli/entities/rotina.py
      cli/tests/test_crud_rotina_template.py && uv run --project cli ruff format --check
      cli/apollo_cli/entities/rotina.py cli/tests/test_crud_rotina_template.py && uv run --project
      cli ty check cli/apollo_cli/entities/rotina.py` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run ruff check apollo_cli/entities/rotina.py tests/test_crud_rotina_template.py && uv run ruff format --check apollo_cli/entities/rotina.py tests/test_crud_rotina_template.py && uv run ty check apollo_cli/entities/rotina.py && uv run pytest tests/test_crud_rotina_template.py -m live -v && cd /home/thomaz/pessoal/apollo-v2 && uv run --project cli apollo rotina template deletar --help | grep -q -- "--force" && uv run --project cli apollo rotina template deletar --help | grep -qi "orf" && echo TASK1_OK</automated>
  </verify>
  <done>`apollo rotina template deletar` blocks by default with an exact linked-instance count and
  zero writes; `--force` deletes the template without ever cascading onto its instances, and the
  resulting orphan's `template` link is live-proven to come back with the key entirely absent;
  zero-linked-instance `--force` is a documented no-op; the pre-existing `not_found` behavior is
  unchanged; every new and pre-existing test in this task's two files is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: instancia limpar-orfas (LIFE-02, D-02) + Success Criterion 3 real-production cleanup + repair of two pre-existing structural tests</name>
  <files>cli/apollo_cli/entities/rotina.py, cli/tests/test_rotina_instancia.py, cli/tests/test_cli_surface.py</files>
  <read_first>
    - cli/apollo_cli/entities/rotina.py:174-188 (the `template`/`instancia` `click.Group(...)`
      definitions — `instancia`'s current help text at :181-185 says "No `criar`, no `deletar`";
      extend it to name `limpar-orfas` as the one narrow exception, mirroring this task's own
      module-docstring paragraph from Task 1), :431-469 (`listar_instancia`/`status` — the
      `@instancia.command()` decorator + `emit(...)`-JSON-output convention `limpar_orfas` copies;
      place the new command immediately after `status`, before the module-level `gerar_instancias`
      group command)
    - This task's own Task 1 output: `_EXIT_INSTANCES_LINKED` is NOT reused here — `limpar-orfas`
      never exits non-zero on finding orphans (listing/removing orphans is success, not failure);
      only Click's own default argument-parsing failures apply to this command.
    - cli/tests/test_cli_surface.py:23-49 (`EXPECTED_SURFACE` table + its own doc comment at
      :27-30 explaining `exact_match` — `"instanciasRotina": (["rotina", "instancia"], {"listar",
      "status"}, True)` at line 49 is the EXACT line to update, extending the required set to
      `{"listar", "status", "limpar-orfas"}`; :92-104 `test_schema_entity_coverage` is the
      parametrized test that fails today without this change once `limpar-orfas` exists), :131-146
      (`test_every_command_has_rich_help_text`/`test_every_option_has_help_text` — both walk EVERY
      command/option automatically; `limpar_orfas`'s docstring and `--confirmar`'s help text must
      each independently satisfy the `_MIN_HELP_LEN = 20` floor and non-empty option help,
      confirmed by reading these two tests' exact assertions this session)
    - cli/tests/test_rotina_instancia.py:1-8 (module docstring — the "no criar and no deletar...by
      design" claim to amend with a `limpar-orfas` narrow-exception note, mirroring Task 1's own
      module-docstring addition in `rotina.py`), :30-38
      (`test_instancia_command_set_is_exactly_listar_and_status` — the EXACT test to rename +
      extend: this is a pre-existing test this task's own new command breaks, not a bug this task
      introduces), :61-79 (`_seed_instancia`/`_create_template`/`_query_instancia` — reuse verbatim
      for this task's own new live tests, exactly as Task 1's `_seed_linked_instancia` already
      mirrors this same shape in the sibling file)
  </read_first>
  <behavior>
    - `limpar-orfas` (no `--confirmar`) against an account holding one genuine orphan (a template
      already force-deleted per Task 1's `--force`) AND one instance with a currently-valid
      `template` link: response lists exactly the orphan (id + dedupeKey), `count` matches,
      `confirmado` is `false`, and BOTH instances still exist afterward (zero writes).
    - `limpar-orfas --confirmar` in the same scenario: the orphan is deleted, the validly-linked
      instance is completely untouched (still exists, `template` link still resolves).
    - Run against the real production account: every currently-orphaned row (any dedupeKey
      pattern, including `phase23-e2e-dedupe-weekday`/`-weekend`) is removed; total
      `instanciasRotina` count drops by exactly the orphan count found; a second `limpar-orfas`
      call immediately after reports `count == 0`.
  </behavior>
  <action>
    In `cli/apollo_cli/entities/rotina.py`:

    Extend `instancia`'s `click.Group(..., help=(...))` text: after "records" and before "No
    `criar`", insert a clause naming `limpar-orfas` as a narrow exception (deletes only instances
    whose `template` link no longer resolves); change "no `deletar`" to "no general `deletar`" so
    the help text does not contradict the new command's existence.

    Add `@instancia.command(name="limpar-orfas")` with a single
    `@click.option("--confirmar/--no-confirmar", default=False, ...)` (this module's established
    paired-boolean-flag convention, same as Task 1's `--force/--no-force`; help text states D-02's
    list-by-default/confirm-to-execute semantics). Function `limpar_orfas(confirmar: bool) -> None`
    with a docstring stating: strictly orphan-only scope (D-02, C-06 still governs the normal
    flow — never creates, edits, or deletes an instance with a valid `template` link); default
    lists only; `--confirmar` actually removes; the orphan definition is the link's expanded value
    coming back falsy/absent (live-verified this phase, never a dangling reference or error). Body:
    `client, session = client_for_session()`; query `{"instanciasRotina": {"template": {}, "$":
    {"where": {"donoId": session.user_id}}}}`; `rows = result.get("instanciasRotina", [])`;
    `orphans = [row for row in rows if not row.get("template")]` (the falsy-or-absent check,
    mirroring Task 1's `_count_linked_instances`/`_normalize_antecessor` convention, now applied to
    the FORWARD side of the same schema link Task 1 expanded from the reverse side). When
    `confirmar` is true, loop `orphans` calling `delete_entity(etype=_ETYPE_INSTANCIA,
    eid=row["id"])` for each — reusing the existing generic helper unchanged, exactly like every
    other delete in this module. Always `emit({"orphans": [{"id": row["id"], "dedupeKey":
    row.get("dedupeKey")} for row in orphans], "count": len(orphans), "confirmado": confirmar})`
    — the response is identical shape whether listing or confirming, only `confirmado` and whether
    the rows still exist afterward differ (this is the audit-trail mitigation for
    T-30-04 in `&lt;threat_model&gt;` below — every deletion is self-documenting in the command's
    own output).

    In `cli/tests/test_cli_surface.py`: change line 49's tuple from `(["rotina", "instancia"],
    {"listar", "status"}, True)` to `(["rotina", "instancia"], {"listar", "status",
    "limpar-orfas"}, True)`. No other line in this file changes — `test_every_command_has_rich_help_text`/
    `test_every_option_has_help_text` pass automatically once `limpar_orfas`'s docstring and
    `--confirmar`'s help text are non-trivial (already true per this task's own action above).

    In `cli/tests/test_rotina_instancia.py`:

    Amend the module docstring's opening claim (lines 3-7) with one clause: `limpar-orfas` (Phase
    30, LIFE-02) is the sole, narrow exception — it may delete an instance, but only when its
    `template` link is confirmed absent.

    Rename `test_instancia_command_set_is_exactly_listar_and_status` to
    `test_instancia_command_set_is_exactly_listar_status_and_limpar_orfas`. Change its assertion
    from `commands == {"listar", "status"}` to `commands == {"listar", "status", "limpar-orfas"}`
    and update the failure message to name all three. Keep the `"criar" not in commands` and
    `"deletar" not in commands` assertions and their existing messages unchanged (both remain true
    — `limpar-orfas` is a distinctly-named command, not a bare `deletar`).

    Add three new live tests:
    - `test_limpar_orfas_lists_orphan_without_confirmar_writes_nothing`: create a template via
      `_create_template`, seed one linked instance via `_seed_instancia`, force-delete the template
      (`run_cli(["rotina", "template", "deletar", "--id", template_id, "--force"])`, asserting exit
      0 — this reuses Task 1's own `--force`, already landed earlier in this same plan). Call
      `run_cli(["rotina", "instancia", "limpar-orfas"])`; assert exit 0; parse the JSON response
      and assert `confirmado is False`, `count &gt;= 1`, and the seeded instance's id appears in
      `orphans`. Re-query the instance directly via `_query_instancia` and assert it is NOT `None`
      (still exists — zero writes). Append the instance id to `cleanup_records` (it was never
      deleted by this test).
    - `test_limpar_orfas_confirmar_deletes_only_orphans_leaves_valid_linked_instance_untouched`:
      create TWO templates (A and B) via `_create_template`, seed `instance_orphan` linked to A and
      `instance_valid` linked to B via `_seed_instancia`. Append template B's id and
      `instance_valid`'s id to `cleanup_records` (template A is force-deleted by this test itself;
      `instance_orphan` is deleted by `limpar-orfas --confirmar` itself). Force-delete template A.
      Call `run_cli(["rotina", "instancia", "limpar-orfas", "--confirmar"])`; assert exit 0,
      `confirmado is True`, `instance_orphan`'s id in `orphans`. Assert
      `_query_instancia(live_client, instance_orphan_id) is None` (deleted). Assert
      `_query_instancia(live_client, instance_valid_id)` is NOT `None`, AND re-query it WITH
      `template` link expansion and assert the row's `template` is truthy/non-empty (still validly
      linked — the precise orphan-only-scope proof D-02 requires).
    - `test_limpar_orfas_help_documents_confirmar_and_orphan_definition`: `run_cli(["rotina",
      "instancia", "limpar-orfas", "--help"])`; assert exit 0, `"--confirmar"` in output, `"orf"`
      in the lowercased output.

    Add the phase's one deliberate real-production-data live test (Success Criterion 3, D-03) —
    the SAME single production account every other `@pytest.mark.live` test in this project already
    targets (LIFE-03/D-04: no second app), and the ONLY test in this entire phase permitted to
    touch pre-existing (non-test-prefixed) rows, because `limpar-orfas`'s own definition makes this
    safe by construction (it can only ever act on a row whose `template` link is already
    confirmed absent — a row already broken/unusable in production, never a row with a valid
    link):
    - `test_limpar_orfas_confirmar_removes_real_production_residue_success_criterion_3`: capture
      `total_before = len(cast("list[dict[str, Any]]", run_cli(["rotina", "instancia",
      "listar"]).json_out()))`. Call `limpar-orfas` (no confirm), capture `before_body =
      json_out()`, `orphan_count = before_body["count"]` (best-effort visibility only — do NOT hard
      -assert the specific residue is present, since a prior run of this same idempotent test may
      already have cleaned it: a soft note, not an assertion, that
      `[k for k in (o.get("dedupeKey") for o in before_body["orphans"]) if k and
      str(k).startswith("phase23-e2e-dedupe-week")]` is informational only). Call `limpar-orfas
      --confirmar`; assert exit 0, `confirm_body["count"] == orphan_count`, `confirmado is True`.
      Call `limpar-orfas` again (no confirm); assert `after_body["count"] == 0`, and assert none of
      `after_body["orphans"]`' dedupeKeys start with `"phase23-e2e-dedupe-weekday"` or
      `"phase23-e2e-dedupe-weekend"` (the named residue from ROADMAP Success Criterion 3, now
      provably absent). Capture `total_after = len(cast("list[dict[str, Any]]",
      run_cli(["rotina", "instancia", "listar"]).json_out()))`; assert `total_before - total_after
      == orphan_count` — the exact-delta invariant proving no other row was affected. This test
      needs NO `cleanup_records` entries (it deletes exactly the rows it lists, nothing more, and
      those rows were already broken residue, not created by this test).
  </action>
  <reversibility rating="costly">`--confirmar` against the real production account performs an
  actual, one-time deletion of currently-orphaned rows (D-03, explicitly pre-authorized by
  CONTEXT.md decision 3 — not a fresh decision this task re-asks). This is scoped safely by
  construction (only rows whose `template` link is already confirmed absent — already-broken,
  unreachable production data, never a validly-linked row) and is idempotent (a second run finds
  zero orphans and is a no-op), but an InstantDB row deletion has no built-in undo, so it is rated
  `costly` rather than `reversible` for the record. No checkpoint is inserted — the decision was
  already made by the user in `30-CONTEXT.md`, not re-opened here.</reversibility>
  <acceptance_criteria>
    - `grep -c 'name="limpar-orfas"' cli/apollo_cli/entities/rotina.py` returns 1.
    - `grep -c -- '--confirmar/--no-confirmar' cli/apollo_cli/entities/rotina.py` returns 1.
    - `grep -c 'limpar-orfas' cli/tests/test_cli_surface.py` returns at least 1 (the updated
      `EXPECTED_SURFACE` entry).
    - `grep -c 'limpar-orfas' cli/tests/test_rotina_instancia.py` returns at least 5 (the renamed
      structural test's assertion + the three new live tests' CLI invocations + the
      production-residue test).
    - `uv run --project cli apollo rotina instancia limpar-orfas --help` output contains
      `--confirmar` and, lowercased, `orf`.
    - `uv run --project cli pytest cli/tests/test_cli_surface.py -v` exits 0 (fully offline, no
      live session required, per this file's own module docstring).
    - `uv run --project cli pytest cli/tests/test_rotina_instancia.py -m live -v` exits 0,
      including all three new orphan-precision tests and the production-residue test.
    - `uv run --project cli pytest cli/tests/test_crud_rotina_template.py cli/tests/test_rotina_instancia.py cli/tests/test_cli_surface.py -m live -v` exits 0 as one combined run (proves Task 1 and Task 2's live tests coexist correctly in one session against the real account).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run ruff check apollo_cli/entities/rotina.py tests/test_rotina_instancia.py tests/test_cli_surface.py && uv run ruff format --check apollo_cli/entities/rotina.py tests/test_rotina_instancia.py tests/test_cli_surface.py && uv run ty check apollo_cli/entities/rotina.py && uv run pytest tests/test_cli_surface.py -v && uv run pytest tests/test_crud_rotina_template.py tests/test_rotina_instancia.py -m live -v && cd /home/thomaz/pessoal/apollo-v2 && uv run --project cli apollo rotina instancia limpar-orfas --help | grep -q -- "--confirmar" && uv run --project cli apollo rotina instancia limpar-orfas --help | grep -qi "orf" && echo TASK2_OK</automated>
  </verify>
  <done>`apollo rotina instancia limpar-orfas` lists orphans by default (zero writes) and deletes
  exactly the listed orphans with `--confirmar`, live-proven to never touch a validly-linked
  instance in the same run; the real production account's orphan/residue rows (including the named
  `phase23-e2e-dedupe-weekday`/`-weekend` residue) are actually removed with an exact-delta proof
  that no other row was affected (ROADMAP Success Criterion 3); both pre-existing structural tests
  this new command necessarily breaks (`test_cli_surface.py`, `test_rotina_instancia.py`) are
  repaired and green; the full combined live suite for this plan's three touched test files passes
  in one run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator `--force`/`--confirmar` CLI flags → irreversible InstantDB writes (template delete, orphan instance delete) against the real production app | Both flags gate a real delete; a wrong default or a wrong orphan definition either silently destroys wanted data or silently fails to protect it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Tampering | `deletar --force` | high | mitigate | `--force` only skips the block — it never cascades onto linked instances; a dedicated live test (Task 1) proves the orphaned instance row still exists after a forced delete, only its `template` link resolves absent |
| T-30-02 | Tampering | `_count_linked_instances` guard-logic correctness | high | mitigate | Live test against a REAL linked instance (not a mock) asserts exit 2, exact count, and zero writes — a silently-broken count (e.g. always 0) fails this test immediately |
| T-30-03 | Elevation of Privilege / Repudiation | `limpar-orfas --confirmar`'s orphan definition | critical | mitigate | Definition is the exact, live-verified shape from 30-PATTERNS.md's experiment (falsy-or-absent `template`, mirroring `_normalize_antecessor`'s proven convention); a dedicated live test creates BOTH a genuine orphan and a still-validly-linked instance in the SAME run and asserts only the orphan is removed |
| T-30-04 | Repudiation | `limpar-orfas --confirmar` acting on real production data with no audit trail | medium | mitigate | Response JSON always lists every affected row's `id`+`dedupeKey`, plus `count`/`confirmado` — self-documenting output, captured in the live test assertions and recorded in the phase SUMMARY |
| T-30-05 | Denial of Service | `limpar-orfas` querying/deleting an unbounded row set in one call | low | accept | Query is `donoId`-scoped to a single owner; this account holds ~168 `instanciasRotina` total today — no pagination needed at this scale, matching every other `list_entities`-based command in this module |
| T-30-06 | Information Disclosure | Error/list responses expose `dedupeKey`/`instance_count` | low | accept | Already visible to the SAME authenticated owner via `rotina instancia listar`/`rotina template listar` — no new information crosses the trust boundary |

</threat_model>

<verification>
- `uv run --project cli pytest cli/tests/test_crud_rotina_template.py -m live -v` — LIFE-01's block
  + force + no-cascade + orphan-shape proof, live.
- `uv run --project cli pytest cli/tests/test_rotina_instancia.py -m live -v` — LIFE-02's
  list/confirm precision proof + the real Success Criterion 3 production cleanup, live.
- `uv run --project cli pytest cli/tests/test_cli_surface.py -v` — fully offline structural gate,
  including the repaired `EXPECTED_SURFACE` exact-match.
- `uv run --project cli ruff check cli && uv run --project cli ruff format --check cli && uv run
  --project cli ty check cli` — CLI gates green.
- `apollo rotina template deletar --help` documents `--force` + the orphan consequence;
  `apollo rotina instancia limpar-orfas --help` documents `--confirmar` + the orphan definition.
</verification>

<success_criteria>
- `apollo rotina template deletar` on a template with linked instances blocks by default, reports
  the exact count, writes nothing; `--force` proceeds without cascading (ROADMAP Success Criterion
  1, live).
- `apollo rotina instancia limpar-orfas` lists (default) or removes (`--confirmar`) exactly the
  instances whose `template` link no longer resolves, never touching a validly-linked instance, and
  never reopening `criar`/general `deletar` for instances (Success Criterion 2, live).
- Running `limpar-orfas --confirmar` against the real production account removes every currently
  -orphaned row, including the named `phase23-e2e-dedupe-weekday`/`-weekend` residue, with an
  exact-delta proof that no other row was affected (Success Criterion 3, live).
- Two pre-existing structural tests this phase's new command necessarily breaks
  (`test_cli_surface.py`'s `EXPECTED_SURFACE`, `test_rotina_instancia.py`'s command-set assertion)
  are repaired and green, not left broken or deleted.
</success_criteria>

<artifacts>
## Artifacts this phase produces (contributed by this plan)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `_count_linked_instances` | pure query helper | `deletar`'s guard |
| `deletar --force/--no-force` | CLI surface | operator use (LIFE-01), Wave 2 / plan 30-02's e2e sweep fix |
| `_EXIT_INSTANCES_LINKED = 2` | exit-code constant | `deletar`'s guard, this plan's own live tests |
| `instancia limpar-orfas --confirmar/--no-confirmar` | CLI surface | operator use (LIFE-02), this plan's own Success Criterion 3 live proof |
| Repaired `EXPECTED_SURFACE["instanciasRotina"]` / renamed command-set test | test fixtures | `test_cli_surface.py`, `test_rotina_instancia.py`, permanently |

</artifacts>

<output>
Create `.planning/phases/30-ciclo-de-vida-e-higiene-de-dados/30-01-SUMMARY.md` when done.
Record: the exact new flag names (`--force`/`--confirmar`) and exit code (`_EXIT_INSTANCES_LINKED =
2`) — plan 30-02 depends on these exact names; the live-verified exact orphan shape (`template` key
absent, not `[]`); the real production orphan count found/removed for Success Criterion 3 (the
actual number, whatever it was at execution time) and confirmation the named
`phase23-e2e-dedupe-weekday`/`-weekend` residue is gone; confirmation both repaired structural
tests pass.
</output>
