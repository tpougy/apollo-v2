---
phase: 22-dashboard-kanbans-rotinas-heatmap
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - web/src/lib/dashboard/RoutinesByFundo.svelte
  - web/src/lib/dashboard/MonthHeatmap.svelte
  - web/src/lib/dashboard/Dashboard.svelte
  - web/e2e/dashboard.spec.ts
autonomous: true
requirements: [DASH-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "This week's routine instances group into cards by fundo via the already-shipped rotinasPorFundo, with the 'Sem fundo vinculado' group always rendered last regardless of the ordenar/status controls' current values"
    - "Each fundo card shows up to 4 routine rows plus a '+N' overflow row when it has more, and each row's bolinha is bg-destructive only when derive.ts's vencido() says that instance is overdue -- never derived from any status string comparison"
    - "agrupar/ordenar/status controls are all present and real (wired Select.Root components); ordenar and status each produce an observable change in which rows are visible or in what order, over the same already-fetched data -- no new query is issued"
    - "Each routine row shows a human-readable label sourced from that instance's template.nome"
    - "The monthly heatmap renders a 7-column grid using only bg-muted / bg-chart-1 / bg-chart-2 / bg-chart-4 / bg-destructive (plus opacity variants) for its 5 fixed intensity bands (0 / 1-2 / 3-4 / 5-7 / 8+), weekend cells always render bg-muted/40 regardless of their own carga count and show no number, and a 'tranquilo -> carregado' legend is always visible"
    - "Each intensity band's markup carries the correct dark-mode chart-token swap (chart-1<->chart-5, chart-2<->chart-4) so the ramp's meaning stays 'quiet is unobtrusive, busy stands out' against a dark background too"
  artifacts:
    - web/src/lib/dashboard/RoutinesByFundo.svelte
    - web/src/lib/dashboard/MonthHeatmap.svelte
  key_links:
    - "Dashboard.svelte's dadosNormalizados.instanciasRotina mapping now also carries template.nome alongside template.fundo -- a Dashboard.svelte-local, additive change; derive.ts and dashboardQuery.ts's DASHBOARD_QUERY object stay byte-for-byte unchanged, since the query's existing template: {} traversal already fetches nome as a scalar and Dashboard.svelte's own TemplateRow type already declares it"
    - "Dashboard.svelte calls rotinasPorFundo/cargaDoMes itself (mirroring how it already calls agendaPorDia/semanaUtil for WeekCalendar) and passes the already-computed grupos/nomeById/carga as props -- RoutinesByFundo.svelte and MonthHeatmap.svelte never call db.useQuery or any derive.ts export themselves"
    - "Dashboard.svelte's existing dash-placeholder-rotinas wrapper (testid/classes unchanged from Phase 21) now mounts both new components stacked, replacing its static placeholder text"
---

<objective>
Build `RoutinesByFundo.svelte` (this week's routine instances grouped by fundo, with functional
agrupar/ordenar/status controls) and `MonthHeatmap.svelte` (the current month's 7-column,
5-band workload grid using only spec-ui.md section 6's exact token table, dark-mode-aware), and
wire both into `Dashboard.svelte`'s existing `dash-placeholder-rotinas` grid cell -- completing
DASH-04 and, with Plan 22-01's `ProjectStrips`, all of the Dashboard's remaining content.

Purpose: close the second and last content gap left over from Phase 21's shell, reusing every
piece of already-shipped, already-unit-tested derivation logic (`rotinasPorFundo`, `cargaDoMes`,
`faixaHeatmap`, `vencido`) without touching `derive.ts` itself.
Output: `RoutinesByFundo.svelte`, `MonthHeatmap.svelte`, `Dashboard.svelte`'s finished
`dash-placeholder-rotinas` slot, and `dashboard.spec.ts` extended with full DASH-04 coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-CONTEXT.md
@.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-RESEARCH.md
@web/src/lib/dashboard/Dashboard.svelte
@web/src/lib/dashboard/derive.ts
@web/src/lib/dashboard/dashboardQuery.ts
@web/src/lib/dashboard/WeekCalendar.svelte
@web/src/lib/sections/ProjetosSection.svelte
@web/e2e/dashboard.spec.ts
@web/e2e/fixtures/instancia-admin-fixture.ts
@web/src/app.css
@spec-ui.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RoutinesByFundo.svelte -- fundo-grouped cards, bolinha, agrupar/ordenar/status controls, wired end-to-end</name>
  <files>web/src/lib/dashboard/RoutinesByFundo.svelte, web/src/lib/dashboard/Dashboard.svelte, web/e2e/dashboard.spec.ts</files>
  <behavior>
    - Given two instancias this week in the same fundo and one instancia with no fundo, the
      rendered fundo cards list that fundo first and the "Sem fundo vinculado" card last,
      regardless of what agrupar/ordenar/status are currently set to.
    - Given a fundo group with 5 instancias, exactly 4 `rotinas-row` elements render plus one
      `rotinas-overflow` row reading "+1".
    - An instancia whose `dataPrevista` is in the past renders its `rotinas-row-bolinha` with
      `bg-destructive`; one dated in the future renders it with `bg-muted-foreground`.
    - Each `rotinas-row` shows the linked template's `nome`, not the instancia's own id.
    - Selecting `status: atrasadas` hides every row whose bolinha is not destructive, without
      re-fetching; selecting `status: todas` restores every row.
    - Selecting `ordenar` to the descending option reverses each group's own row order by
      `dataPrevista`.
  </behavior>
  <action>
Create `web/src/lib/dashboard/RoutinesByFundo.svelte`. Declare local types:
`InstanciaRow = { id: string; dataPrevista: string; tipoPrazo: string }` (deliberately minimal --
no `template`/`fundo` field needed on this row type at all, since fundo grouping arrives already
resolved via `grupos` and the label arrives via the separate `nomeById` map described below),
`Grupo = { fundoId: string | null; fundoNome: string | null; instancias: InstanciaRow[] }`. Props
via `$props()`: `grupos: Grupo[]` (already fundo-grouped, "Sem fundo vinculado" already forced
last -- this is `rotinasPorFundo`'s own, unmodified return value; NEVER re-sort or re-filter the
top-level `grupos` array itself, only ever transform each group's OWN `instancias` list, or the
"Sem fundo vinculado is always last" guarantee breaks), `nomeById: Map<string, string>`,
`hojeIso: string`. This component never imports `"../db"` and never calls `db.useQuery`.

Import `vencido` from `./derive` (do not import `rotinasPorFundo` here -- Dashboard.svelte calls
it, per this plan's `key_links`).

Control state: `type SortBy = "data-asc" | "data-desc"; type StatusFilter = "todas" | "atrasadas";`
`let agrupar = $state<"fundo">("fundo");` (a single fixed value -- fundo grouping is already the
data's own shape from `rotinasPorFundo`, so this control is deliberately a no-op selection over a
one-item set; document this exact reasoning in a code comment, mirroring how CONTEXT.md/22-RESEARCH.md
already frame "agrupar: fundo" as "the only one wired to non-trivial logic this phase" i.e. no
OTHER grouping key exists to switch to). `let ordenar = $state<SortBy>("data-asc");`
`let status = $state<StatusFilter>("todas");` Render all three as `Select.Root type="single"` /
`Select.Trigger` / `Select.Content` / `Select.Item` blocks, mirroring `ProjetosSection.svelte`'s
exact `groupBy` Select idiom (lines 345-360) verbatim in shape: `data-testid="rotinas-agrupar"`
Trigger text `` `agrupar: ${agrupar}` `` with a single `Select.Item value="fundo" label="fundo"`;
`data-testid="rotinas-ordenar"` Trigger text `` `ordenar: ${ordenar}` `` with two items
`"data-asc"`/`"data-desc"` (labels "data (mais proxima)" / "data (mais distante)"); `data-testid="rotinas-status"`
Trigger text `` `status: ${status}` `` with two items `"todas"`/`"atrasadas"`.

Compute the displayed groups via one `$derived.by`: for each `grupo` in `grupos` (in the SAME
order `grupos` already has -- never re-sort this array), build `{ ...grupo, displayed: [...grupo.instancias]
  .filter((i) => status === "todas" || vencido(i.dataPrevista, false, hoje))
  .sort((a, b) => { const cmp = a.dataPrevista === b.dataPrevista ? 0 : a.dataPrevista < b.dataPrevista ? -1 : 1; return ordenar === "data-asc" ? cmp : -cmp; }) }`
where `hoje = new Date(`${hojeIso}T00:00:00.000Z`)`. `vencido`'s second argument is hard-coded
`false` here -- `instanciasRotina` carries no non-string completion signal, exactly the same
documented decision `derive.ts`'s own `agendaPorDia` already made for rotina items (mirrors
REQUIREMENTS.md section 5.3's "consequencia simetrica" pattern; restate this in the SUMMARY, do
not silently repeat it uncredited). The fundo card's own "N rotinas - N atrasadas" meta line uses
the group's ORIGINAL, pre-filter `grupo.instancias` (never the post-status-filter `displayed`
list) -- so that summary line reads as a stable fact about the week regardless of which filter the
user currently has selected.

Per fundo card (keyed by `grupo.fundoId ?? "sem-fundo"`): `data-testid="rotinas-fundo-card" data-eid={grupo.fundoId ?? ""}`
with `class="rounded border bg-card/60 p-3 space-y-2"` (thin border + `bg-card/60`, explicitly NOT
the solid shadcn `Card` component -- CONTEXT.md is explicit the visual weight belongs to the week
band, not here). Inside: a `data-testid="rotinas-fundo-titulo"` `<button type="button">` with no
`onclick` (inert -- dialog 5 is Phase 23's job) showing `grupo.fundoNome ?? "Sem fundo vinculado"`;
a `data-testid="rotinas-fundo-meta"` text reading
`"{grupo.instancias.length} rotinas - {overdue count} atrasadas"` where the overdue count is
`grupo.instancias.filter((i) => vencido(i.dataPrevista, false, hoje)).length`; then, for the first
4 entries of that group's `displayed` list, one `data-testid="rotinas-row" data-eid={instancia.id}`
`<button type="button">` with no `onclick` (inert -- dialog 7 is Phase 23's job) containing a
`data-testid="rotinas-row-bolinha"` span (`class="inline-block size-2 rounded-full"`, plus
`"bg-destructive"` when `vencido(instancia.dataPrevista, false, hoje)` else `"bg-muted-foreground"`),
the day (`instancia.dataPrevista`'s DD/MM slice, same slicing idiom used throughout this codebase),
and the label `nomeById.get(instancia.id) ?? "Rotina"`. When a group's `displayed` list has more
than 4 entries, render one `data-testid="rotinas-overflow" data-eid={grupo.fundoId ?? ""}` row
reading `"+{n}"` where `n = displayed.length - 4`. When a group's `displayed` list is empty (e.g.
the status filter removed everything), render a plain `text-sm text-muted-foreground` line
"Nenhuma rotina corresponde ao filtro" instead of an empty card body (small polish, not a locked
requirement -- no dedicated testid/e2e assertion needed for this line specifically).

Wrap the whole component in `data-testid="dash-rotinas"` with `class="space-y-4"`. When `grupos.length === 0`,
render a plain `text-sm text-muted-foreground` line "Nenhuma rotina esta semana." below the
controls row instead of the (nonexistent) card list -- also not a locked requirement, no dedicated
testid needed.

In `web/src/lib/dashboard/Dashboard.svelte`: extend the existing `dadosNormalizados` `$derived.by`
block's `instanciasRotina` mapping -- inside the `template: i.template ? { ... } : null` object,
add a `nome: i.template.nome` field alongside the existing `fundo: ...` field (this is the ONLY
change this plan makes to satisfy CONTEXT.md's "surface template.nome" decision; `derive.ts` and
`dashboardQuery.ts`'s `DASHBOARD_QUERY` object stay completely unmodified -- `TemplateRow` already
declares `nome: string` and the query's `template: {}` traversal already fetches it as a scalar;
document this exact reasoning -- and that it required zero changes to either of those two files --
prominently in the SUMMARY). Extend the existing `import { agendaPorDia, semanaUtil } from "./derive";`
line to also import `rotinasPorFundo`. Add
`const rotinaGrupos = $derived(rotinasPorFundo(dadosNormalizados.instanciasRotina, semana));` and
`const rotinaNomeById = $derived.by(() => { const data = query.data as DashboardData | undefined; const map = new Map<string, string>(); for (const i of data?.instanciasRotina ?? []) { if (i.template?.nome) map.set(i.id, i.template.nome); } return map; });`.
Import `RoutinesByFundo` from `"./RoutinesByFundo.svelte"`. Inside the existing
`data-testid="dash-placeholder-rotinas"` div (leave that wrapper's own `data-testid`/classes
byte-for-byte unchanged), replace the "Em breve: rotinas da semana e carga do mes" placeholder
text with a `class="space-y-6"` wrapper (this app's one spacing scale between stacked sections)
containing `<RoutinesByFundo grupos={rotinaGrupos} nomeById={rotinaNomeById} hojeIso={hojeIso} />`
-- Task 2 appends `MonthHeatmap` as a sibling inside this same wrapper.

Extend `web/e2e/dashboard.spec.ts` with a `test.describe("DASH-04: rotinas by fundo")` block,
reusing this file's existing `apolloCli`/`uniqueName`/`uniqueCodigo`/`seedInstance`/`deleteInstance`/
`computeSemana` helpers (never re-declare them). In `beforeAll`: create one fundo, one
`rotina template` linked to it, and via the admin fixture's `seedInstance` (this entity has no CLI
create path by design), 5 instances all linked to that template, `dataPrevista` values spread
across this week's own date keys (`semana.dias`/`sabado`/`domingo` from `computeSemana()`) with
one deliberately in the past relative to `hojeIso()` (vencida) and the rest in the future; also
seed one instance with a DIFFERENT template that has NO `--fundo-id` (so it lands in "Sem fundo
vinculado"). Write tests for: (a) fundo-grouping order ("Sem fundo vinculado" card is the last
`rotinas-fundo-card`); (b) the 5-instance fundo's card shows exactly 4 `rotinas-row` plus one
`rotinas-overflow` reading "+1"; (c) the vencida instance's `rotinas-row-bolinha` carries
`bg-destructive` and a future-dated one carries `bg-muted-foreground`; (d) each `rotinas-row`
contains the seeded template's `nome`, not any instance id; (e) selecting `status: atrasadas` via
`rotinas-status` leaves only the vencida row's `rotinas-row` visible within that fundo's card, and
selecting `todas` restores all visible rows (use the same `Select.Root` interaction idiom
`ProjetosSection.svelte`'s existing tests already establish for `project-groupby`); (f) toggling
`rotinas-ordenar` to its descending option reverses that fundo group's row order (compare the
`data-eid` sequence before/after). `afterAll` calls `deleteInstance` for every seeded instance,
`tryDelete` for the templates/fundo.
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps -g "DASH-04"</automated>
  </verify>
  <done>Routine instances group correctly by fundo with "Sem fundo vinculado" always last; the 4-row cap and overflow row are correct; bolinha color is vencido()-driven, never status-string-driven; agrupar/ordenar/status are all present and ordenar/status each provably change what is visible.</done>
</task>

<task type="auto">
  <name>Task 2: MonthHeatmap.svelte -- 5-band grid with dark-mode token swap, weekend override, legend, wired end-to-end</name>
  <files>web/src/lib/dashboard/MonthHeatmap.svelte, web/src/lib/dashboard/Dashboard.svelte, web/e2e/dashboard.spec.ts</files>
  <behavior>
    - A day with 0 afazeres renders `bg-muted`; 1-2 renders `bg-chart-1/40`; 3-4 renders
      `bg-chart-2/70`; 5-7 renders `bg-chart-4`; 8+ renders `bg-destructive`.
    - Every one of the 4 non-zero, non-destructive bands also carries the correct `dark:` variant
      class swapping to the opposite end of the chart-1..5 ramp.
    - A weekend day (Saturday or Sunday) always renders `bg-muted/40` regardless of its own
      carga count, and shows no digit anywhere in its cell.
    - The legend is always visible and mentions both "tranquilo" and "carregado".
    - Every heatmap cell resolves to a real `<button>` tagName.
  </behavior>
  <action>
IMPORTANT correction to 22-RESEARCH.md's own Pattern 5 code example before implementing: that
example's band-4 class `text-destructive-foreground` names a CSS custom property
(`--destructive-foreground`) that does not exist anywhere in `web/src/app.css` (verified: only
`--destructive` is defined, in both the `:root` and dark-mode blocks; no
`--color-destructive-foreground` entry exists in the `@theme inline` block either, and no existing
component in this codebase -- `button.svelte`/`badge.svelte`/`alert.svelte` -- ever pairs
`bg-destructive` with a `-foreground` text token; they all use `text-destructive` at a reduced
background opacity instead). Using `text-destructive-foreground` here would render unstyled,
token-less text, violating spec-ui.md section 0's "toda cor sai de token semantico ja definido"
rule. Use `text-background` for band 4 instead (an already-defined, already-used token in this
exact lookup table for band 3) -- `--background` already differs correctly between the light and
dark blocks, giving adequate contrast against `--destructive` in both themes with zero `dark:`
override needed on the text class itself. Document this correction explicitly in the SUMMARY as a
deviation from 22-RESEARCH.md's own (unverified-against-app.css) example.

Create `web/src/lib/dashboard/MonthHeatmap.svelte`. Props via `$props()`: `carga: Map<string, number>`
(already computed by `cargaDoMes`, keyed by ISO date, ascending), `ano: number`, `mes: number`
(1-indexed). Import `faixaHeatmap` from `./derive` (do not import `cargaDoMes` here -- Dashboard.svelte
calls it, per this plan's `key_links`). This component never imports `"../db"` and never calls
`db.useQuery`.

Define the exact 5-entry lookup table as a `Record<0 | 1 | 2 | 3 | 4, string>` named
`FAIXA_CLASSES`: `0: "bg-muted"`; `1: "bg-chart-1/40 dark:bg-chart-5/40"`;
`2: "bg-chart-2/70 dark:bg-chart-4/70"`; `3: "bg-chart-4 text-background dark:bg-chart-2 dark:text-foreground"`;
`4: "bg-destructive text-background"` (per the correction above -- no `dark:` prefix needed on
either class, since `--destructive` and `--background` already differ correctly per theme on
their own). Compute `dias = $derived([...carga.keys()])` (already ascending ISO strings from
`cargaDoMes`'s own Map insertion order -- do not regenerate this list independently).
Implement `isWeekend(iso: string): boolean` returning
`[0, 6].includes(new Date(`${iso}T00:00:00.000Z`).getUTCDay())` (the local, phase-scoped helper
22-RESEARCH.md's Pitfall 5 recommends -- `derive.ts` has no weekend-of-month export and does not
need one added for this single-component concern). Compute
`leadingBlanks = $derived(dias.length > 0 ? (new Date(`${dias[0]}T00:00:00.000Z`).getUTCDay() + 6) % 7 : 0)`
(the same Monday-first `(dow + 6) % 7` idiom `derive.ts`'s own `semanaUtil` already uses, applied
here purely for calendar-grid alignment -- this app's week always starts Monday).

Render `data-testid="dash-heatmap"` with `class="space-y-2"`. Grid:
`data-testid="dash-heatmap-grid"` with `class="grid grid-cols-7 gap-1"`; render `leadingBlanks`
plain empty `<div aria-hidden="true"></div>` cells first, then one cell per entry in `dias`. Per
day: compute `n = carga.get(iso) ?? 0`, `faixa = faixaHeatmap(n)`, `weekend = isWeekend(iso)`.
Render `<button type="button" data-testid="dash-heatmap-cell" data-eid={iso} class="aspect-square rounded-sm {weekend ? 'bg-muted/40' : FAIXA_CLASSES[faixa]}">`
with no `onclick` (inert -- dialog 2 is Phase 23's job) and NO text content at all -- neither
weekday nor weekend cells ever render the numeric count as a visible digit (this is a pure
color-intensity grid, matching the "so tokens" / no-gradient-library discipline; weekend cells'
own "sem numero" instruction from spec-ui.md section 3.4 is the explicit case, and no OTHER
instruction anywhere says a weekday cell should show a digit either). When `weekend` is true, the
band-driven `FAIXA_CLASSES[faixa]` classes are never applied -- the weekend override always wins,
regardless of how high that day's own `n` is.

Below the grid, render `data-testid="dash-heatmap-legend"` with `class="flex items-center gap-2 text-xs text-muted-foreground"`
containing the text "tranquilo", five small `aspect-square rounded-sm` swatches at `size-3` using
`FAIXA_CLASSES[0]` through `FAIXA_CLASSES[4]` in order (this is the one place `text-background`/
`text-foreground` modifiers on bands 3/4 are irrelevant -- an empty swatch has no text -- so
applying the full class string is harmless), and the text "carregado".

In `web/src/lib/dashboard/Dashboard.svelte`: extend the existing
`import { agendaPorDia, rotinasPorFundo, semanaUtil } from "./derive";` line to also import
`cargaDoMes`. Add
`const anoMes = $derived.by(() => { const d = new Date(`${hojeIso}T00:00:00.000Z`); return { ano: d.getUTCFullYear(), mes: d.getUTCMonth() + 1 }; });`
(the heatmap always shows the real current month, independent of `semanaBase`'s week navigation --
spec-ui.md gives the heatmap no navigation of its own) and
`const carga = $derived(cargaDoMes(dadosNormalizados, anoMes.ano, anoMes.mes));`. Import
`MonthHeatmap` from `"./MonthHeatmap.svelte"`. Inside the `space-y-6` wrapper Task 1 added to
`dash-placeholder-rotinas`, append `<MonthHeatmap carga={carga} ano={anoMes.ano} mes={anoMes.mes} />`
as a sibling immediately after `<RoutinesByFundo ... />`.

Extend `web/e2e/dashboard.spec.ts` with a `test.describe("DASH-04: month heatmap")` block, placed
LAST in the file (after every other describe block's own cleanup has already run, so this block's
own day-specific fixtures cannot collide with any other describe's "this week" fixtures still
being alive). Reuse `apolloCli`/`uniqueName`/`tryDelete`. In `beforeAll`, compute the current
UTC year/month from `hojeIso()`, then create standalone tarefas (no `--etapa-id` needed --
`tarefas.etapa` is optional) with `--data-prevista` pinned to 5 distinct days within that month,
chosen to fall OUTSIDE `computeSemana()`'s own 7 date keys (so this block never touches a day any
earlier describe's fixtures could have populated): 0 created for day A (band 0, nothing to
create), 2 for day B (band 1), 4 for day C (band 2), 6 for day D (band 3), 8 for day E (band 4);
also identify a weekend day (Saturday or Sunday) within the month via `getUTCDay()` and create 3
tarefas dated on it. Write tests for: (a) each of days B/C/D/E's `dash-heatmap-cell` carries its
exact expected `FAIXA_CLASSES` string (light class AND, for bands 1-3, the paired `dark:` class,
via `toHaveClass` regex against the `class` attribute -- both are always present in markup
regardless of runtime color scheme, since Tailwind's `dark:` variant is a conditional style rule,
not a conditional class) and day A's cell carries `bg-muted` only; (b) the identified weekend
day's cell carries `bg-muted/40` (not any `FAIXA_CLASSES` band despite its 3 seeded tarefas) and
its own `textContent` is empty; (c) `dash-heatmap-legend` is visible and contains both "tranquilo"
and "carregado"; (d) a `dash-heatmap-cell`'s tagName is `"button"`. `afterAll` deletes every
created tarefa.
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps -g "DASH-04"</automated>
  </verify>
  <done>All 5 intensity bands map to exactly spec-ui.md section 6's token table with correct dark-mode chart-token swaps; weekend cells always override to bg-muted/40 with no digit; the legend is always visible; heatmap cells are real buttons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-scoped query data -> RoutinesByFundo.svelte / MonthHeatmap.svelte | Both components' props are already owner-scoped by `instant.perms.ts`'s existing `view` rule (unchanged this phase); neither performs additional authorization filtering |
| Client-only agrupar/ordenar/status Select state -> re-render | Never reaches a query, a mutation, or persisted storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-04 | Tampering | `agrupar`/`ordenar`/`status` client-side Select state | low | accept | Every value is constrained to a closed `Select.Item` set (mirroring `ProjetosSection.svelte`'s own `groupBy`); never free text, never reaches a query or mutation |
| T-22-05 | Information Disclosure | A future phase (23) mistakenly wiring these display-only controls as a data-scoping mechanism instead of a client-side re-sort/re-filter | low | mitigate | Documented explicitly in this plan's SUMMARY: these controls must remain re-sort/re-filter over already-fetched, already-owner-scoped data forever; they must never grow into a second, unscoped query path |
| T-22-06 | Information Disclosure | `RoutinesByFundo.svelte`'s `nomeById` map exposes `template.nome` for every instance in `grupos`, sourced from the same already-owner-scoped query result | low | accept | No new data reaches the client beyond what `DASHBOARD_QUERY` (unchanged) already fetches -- `template: {}` already returns `nome` as a scalar; this plan only stops discarding it during normalization |
</threat_model>

<verification>
Run `cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps` after Task 2 and
confirm all DASH-01/02/03/04/07 tests in the file still pass (regression proof that Task 1/2's
`Dashboard.svelte` edits did not disturb Phase 21's existing content). Manually confirm
`derive.ts` and `dashboardQuery.ts` are byte-for-byte unchanged from Phase 21 (`git diff --stat`
should show neither file).
</verification>

<success_criteria>
`RoutinesByFundo.svelte` and `MonthHeatmap.svelte` both exist, are mounted inside
`Dashboard.svelte`'s unchanged `dash-placeholder-rotinas` slot, and every truth in this plan's
`must_haves.truths` is provable by the Playwright suite. DASH-04's ROADMAP success criteria
(fundo grouping with "Sem fundo vinculado" last, functional controls, 5-band heatmap with only
section-6 tokens, weekend override, legend) all hold against live data. Combined with Plan 22-01,
Phase 22's full ROADMAP success-criteria list (1-5) holds.
</success_criteria>

<output>
Create `.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-02-SUMMARY.md` when done.
</output>
