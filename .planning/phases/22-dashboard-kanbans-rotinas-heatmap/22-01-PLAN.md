---
phase: 22-dashboard-kanbans-rotinas-heatmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/dashboard/ProjectStrips.svelte
  - web/src/lib/dashboard/Dashboard.svelte
  - web/e2e/dashboard-kanbans.spec.ts
autonomous: true
requirements: [DASH-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A project with zero etapas never renders a strip; a project with at least one etapa always renders one (em andamento = has >=1 etapa, mirroring REQUIREMENTS.md section 5.3's no-status-string precedent for the ticket queue -- projetos.status is free text and no non-textual completion signal exists on projetos)"
    - "Every kanban column inside one strip has an identical fixed width (w-36) and an identical shared height regardless of how many cards it holds -- proven via equal boundingBox().width and boundingBox().height across a 4-card column and a 1-card column in the same strip"
    - "Each column shows at most 3 tarefa cards, with a '+N tarefas' row when its etapa has more than 3"
    - "The '>' overflow indicator sits position:absolute at the strip's right edge, outside the row's flex flow, and is present only when a strip's own scrollWidth genuinely exceeds its clientWidth -- never for a strip whose columns already fit the viewport"
    - "A project's collapse toggle persists across a full page reload via exactly the localStorage key apollo.dash.collapsed.<projetoId>, and no other localStorage key is written by this feature"
    - "With zero in-progress projects, 'Nenhum projeto em andamento' renders with a working link back to the Projetos section"
    - "Every clickable surface inside a strip (collapse toggle, project name, fundo badge, column header, card) is a real <button> element"
    - "A vencido tarefa's card carries a destructive left border and destructive-colored meta text; a non-vencido card never does"
  artifacts:
    - web/src/lib/dashboard/ProjectStrips.svelte
  key_links:
    - "Dashboard.svelte's existing dash-placeholder-projetos wrapper (testid/classes unchanged from Phase 21) now mounts ProjectStrips with the raw query.data.projetos rows and hojeIso, replacing its static placeholder text"
    - "ProjectStrips.svelte never calls db.useQuery or any dashboardQuery.ts export -- all data arrives as the projetos prop from Dashboard.svelte's single existing query (DASH-07's one-query contract stays intact)"
    - "ProjectStrips.svelte calls derive.ts's already-shipped progressoEtapa/tarefaConcluida/vencido for etapa/card display -- it never reimplements grouping or overdue logic"
---

<objective>
Build `ProjectStrips.svelte` -- DASH-05's stacked mini-kanban strips, one per in-progress project,
with etapa columns ordered by `ordem` ascending, cards capped at 3 per column plus a `+N` overflow
row, and the phase's single highest-priority correctness bar: **fixed-width, non-compressing
columns** (spec-ui.md section 3.5 explicitly calls this "o erro mais repetido no wireframe"). Adds
the measured `scrollWidth > clientWidth` overflow indicator and the per-project `localStorage`
collapse toggle -- both genuinely new mechanisms this codebase has never used before -- and wires
the whole thing into `Dashboard.svelte`'s existing `dash-placeholder-projetos` grid cell.

Purpose: prove, with a real Playwright bounding-box measurement, that this phase's namesake
failure mode (columns silently compressing to fit more of them) cannot happen here, before any
other Dashboard content is added on top of it.
Output: `web/src/lib/dashboard/ProjectStrips.svelte`, `Dashboard.svelte`'s finished
`dash-placeholder-projetos` slot, and `web/e2e/dashboard-kanbans.spec.ts` covering DASH-05 end to
end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-CONTEXT.md
@.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-RESEARCH.md
@web/src/lib/dashboard/Dashboard.svelte
@web/src/lib/dashboard/derive.ts
@web/src/lib/dashboard/dashboardQuery.ts
@web/src/lib/dashboard/TicketQueue.svelte
@web/src/lib/sections/ProjetosSection.svelte
@web/e2e/dashboard.spec.ts
@web/e2e/projetos-section.spec.ts
@web/src/app.css
@spec-ui.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: ProjectStrips.svelte -- filtered strips, fixed-width non-compressing columns, capped/overflowed cards, wired end-to-end</name>
  <files>web/src/lib/dashboard/ProjectStrips.svelte, web/src/lib/dashboard/Dashboard.svelte, web/e2e/dashboard-kanbans.spec.ts</files>
  <behavior>
    - Given a projeto with zero etapas, it never appears as any `project-strip`'s `data-eid`.
    - Given a projeto with two etapas (ordem 1 holding 4 tarefas, ordem 2 holding 1 tarefa,
      ordered ascending by `ordem`), both columns' `boundingBox().width` are identical AND both
      columns' `boundingBox().height` are identical, despite the 4-vs-1 card-count difference.
    - The ordem-1 column shows exactly 3 `project-strip-card` elements plus one
      `project-strip-card-overflow` reading "+1 tarefas"; the ordem-2 column shows exactly 1 card
      and no overflow row.
    - The strip header shows the projeto's nome, its fundo's nome (or "Sem fundo vinculado"), and
      "{N} etapas - {M} tarefas" with the correct counts.
    - `project-strip-nome`, `project-strip-fundo-badge`, `project-strip-column-header`, and
      `project-strip-card` all resolve to a real `<button>` tagName.
    - A tarefa whose `dataPrevista` is in the past and has zero subtarefas (so `tarefaConcluida`
      is false) renders its card with a destructive left border and destructive meta text; a
      tarefa dated in the future does not.
    - With zero projetos that have any etapa, `dash-projetos-empty` renders "Nenhum projeto em
      andamento" and a working link to the Projetos section.
  </behavior>
  <action>
Create `web/src/lib/dashboard/ProjectStrips.svelte`. Declare its own local row types mirroring
Dashboard.svelte's existing inline `ProjetoRow`/`EtapaRow`/`TarefaRow` shapes exactly (same
convention `TicketQueue.svelte`/`WeekCalendar.svelte` already use -- each dashboard leaf component
declares its own row types rather than importing Dashboard.svelte's, since Dashboard.svelte
exports none): `SubtarefaRow = { id: string; concluida: boolean }`, `FundoRow = { id: string; nome: string }`,
`TarefaRow = { id: string; titulo: string; tipoPrazo: string; dataPrevista?: string | null; subtarefas?: SubtarefaRow[] }`,
`EtapaRow = { id: string; nome: string; ordem: number; tarefas?: TarefaRow[] }`,
`ProjetoRow = { id: string; nome: string; fundo?: FundoRow | null; etapas?: EtapaRow[] }`.
Props via `$props()`: `projetos: ProjetoRow[]` (the RAW, unfiltered `query.data.projetos` rows --
this component owns the "em andamento" filter itself, it is not pre-filtered by Dashboard.svelte),
`hojeIso: string`, `onVerProjetos: () => void` (mirrors `TicketQueue.svelte`'s existing
`onVerTodos` prop shape exactly). This component never imports `"../db"` or `dashboardQuery.ts`
and never calls `db.useQuery` -- every row arrives as a prop.

Import `progressoEtapa`, `tarefaConcluida`, `vencido` from `./derive` (same directory, same three
functions `ProjetosSection.svelte` already imports from `../dashboard/derive` -- do not
reimplement any of their logic here).

Implement `filtroEmAndamento(projeto: ProjetoRow): boolean` returning
`(projeto.etapas?.length ?? 0) > 0`. This is the phase's locked, documented resolution of "projeto
em andamento": a project gets a strip solely because it has at least one etapa to render as
columns -- no `projeto.status` value is ever read or compared, mirroring exactly the reasoning
REQUIREMENTS.md's section 5.3 already used for the ticket queue (no non-textual completion signal
exists on `projetos` either). Compute `const emAndamento = $derived([...projetos].filter(filtroEmAndamento).sort((a, b) => a.nome.localeCompare(b.nome)));`.

When `emAndamento.length === 0`, render `data-testid="dash-projetos-empty"` using the exact
`Empty.Root`/`Empty.Header`/`Empty.Media`/`Empty.Title` composition `TicketQueue.svelte` already
uses for `dash-tickets-empty`, swapping the `Inbox` icon for `@lucide/svelte/icons/kanban`'s
default export and the title text for "Nenhum projeto em andamento". Below it, render a real
`<button type="button" data-testid="dash-projetos-ver-todos">` reading "ver projetos ->", styled
identically to `dash-tickets-ver-todos` (`text-sm text-muted-foreground hover:text-foreground`),
with `onclick={onVerProjetos}`.

When `emAndamento.length > 0`, render `data-testid="dash-projetos"` with `class="space-y-4"` (the
app's one spacing scale) and, for each projeto in `emAndamento` (keyed by `projeto.id`), one strip.

Per-strip markup and state -- everything below lives inside the same `{#each emAndamento as projeto (projeto.id)}` block in this one file (no separate per-item component; spec-ui.md section 8's
file list enumerates exactly `ProjectStrips.svelte`, no sibling singular file, so keep per-project
state in `Record<string, T>` maps rather than introducing a new component):

- `let collapsedByProjeto = $state<Record<string, boolean>>({});` -- `collapsedByProjeto[projeto.id]`
  defaults to `undefined` (treated as falsy/"expanded") this task; Task 2 seeds it from
  `localStorage`.
- Compute `etapasOrdenadas = [...(projeto.etapas ?? [])].sort((a, b) => a.ordem - b.ordem)` inline
  per strip (same idiom as `ProjetosSection.svelte`'s own `etapasOrdenadas`, lines 414-416/595).
- Compute `totalTarefas = etapasOrdenadas.reduce((sum, e) => sum + (e.tarefas ?? []).length, 0)`
  (mirrors `ProjetosSection.svelte`'s own `totalTarefas` helper).
- Strip container: `data-testid="project-strip" data-eid={projeto.id}` wrapping a
  `data-testid="project-strip-header"` flex row (`class="flex items-center gap-2"`) containing, in
  order: a `data-testid="project-strip-collapse"` `<button type="button">` (a plain text glyph
  `{collapsedByProjeto[projeto.id] ? "▸" : "▾"}` is sufficient -- no lucide icon required
  -- with `onclick={() => (collapsedByProjeto[projeto.id] = !collapsedByProjeto[projeto.id])}`, a
  real, functional, in-memory-only toggle this task; not yet persisted); a
  `data-testid="project-strip-nome"` `<button type="button">` showing `projeto.nome` with no
  `onclick` (inert this phase -- dialog 4 is Phase 23's job); a
  `data-testid="project-strip-fundo-badge" data-eid={projeto.fundo?.id ?? ""}` `<button type="button">`
  with no `onclick` wrapping a shadcn `Badge` (`variant="outline"`, same component/variant
  `ProjetosSection.svelte`'s own subtarefas chip already uses) showing `projeto.fundo?.nome ?? "Sem fundo vinculado"`;
  and a `data-testid="project-strip-meta"` text node reading
  `"{etapasOrdenadas.length} etapas - {totalTarefas} tarefas"`.

Body (rendered only `{#if !collapsedByProjeto[projeto.id]}`): `data-testid="project-strip-body"`
with `class="relative overflow-x-auto [scroll-snap-type:x_proximity]"` (the `relative` class is
required now even though Task 2 adds the absolutely-positioned overflow indicator -- add it here
so Task 2 never has to touch this element's class list again). Inside it,
`data-testid="project-strip-row"` with `class="flex gap-2"`. For each etapa in `etapasOrdenadas`
(keyed by `etapa.id`), one `data-testid="project-strip-column" data-eid={etapa.id}` div with
`class="w-36 shrink-0 box-border border-r px-2 space-y-2 [scroll-snap-align:start]"` -- `w-36
shrink-0` together are the load-bearing, non-negotiable pair from spec-ui.md section 3.5 / the
already-proven idiom at `ProjetosSection.svelte:594-632` (that precedent uses `w-48`; this phase's
own spec number is `w-36` -- do not reuse `w-48`); never add any class or logic that could
override `shrink-0` or make the width conditional on card count. Rely on the row's default
flexbox `align-items: stretch` (the CSS default -- do not add `items-start`/`self-start` anywhere
on the row or its columns) so every column stretches to the tallest sibling's own natural content
height; because every column's own rendered card list is capped at 3 plus one overflow line, the
tallest column's height is itself bounded, and every shorter column inherits that exact height via
the stretch default, leaving visible empty space below its own shorter card list -- write no
measured-height code for this.

Inside each column: a `data-testid="project-strip-column-header"` `<button type="button">` with no
`onclick` (inert -- dialog 6 is Phase 23's job) showing `etapa.ordem` (`class="font-mono text-xs"`),
`etapa.nome` (`class="text-sm font-medium"`), and, on its own line,
`progressoEtapa(etapa)`'s `{feitas}/{total}` (`class="text-xs text-muted-foreground"`) -- the exact
three-line shape `ProjetosSection.svelte`'s own `etapa-kanban-column` header already uses. Then,
for the first 3 entries of `etapa.tarefas ?? []` (in existing array order, no re-sort), one
`data-testid="project-strip-card" data-eid={tarefa.id}` `<button type="button">` with no `onclick`
(inert -- dialog 3 is Phase 23's job); compute `concluida = tarefaConcluida(tarefa)` and
`atrasada = vencido(tarefa.dataPrevista, concluida, new Date(`${hojeIso}T00:00:00.000Z`))` per
card. Base card class `"block w-full rounded border p-2 text-left space-y-1"`; when `atrasada`,
add `"border-l-[3px] border-destructive"` and give the meta line `"text-xs text-destructive"`;
otherwise give the meta line `"text-xs text-muted-foreground"`. Card content: `tarefa.titulo`
(`class="line-clamp-2 text-sm"`), then a meta line reading
`"{tarefa.tipoPrazo.toUpperCase()} {tarefa.dataPrevista's DD/MM}"` (slice the ISO string's
positions 8-10 for day and 5-7 for month, same slicing idiom `TicketQueue.svelte`/`WeekCalendar.svelte`
already use elsewhere in this codebase -- never a new date-formatting helper) or `"-"` when
`dataPrevista` is absent. When `etapa.tarefas` has more than 3 entries, render one
`data-testid="project-strip-card-overflow" data-eid={etapa.id}` row reading
`"+{n} tarefas"` where `n = etapa.tarefas.length - 3`.

In `web/src/lib/dashboard/Dashboard.svelte`: add
`function projetoRows(): ProjetoRow[] { const data = query.data as DashboardData | undefined; return data?.projetos ?? []; }`
immediately below the existing `ticketRows()` function (identical shape, reusing the file's
already-declared `ProjetoRow`/`DashboardData` types -- no new type needed, `ProjetoRow` already
carries `fundo`/`etapas`/nested `tarefas` in full). Add
`function goToProjetos(): void { document.querySelector<HTMLButtonElement>('[data-testid="nav-projetos"]')?.click(); }`
immediately below the existing `goToTickets()` function (identical shape). Import
`ProjectStrips` from `"./ProjectStrips.svelte"`. Inside the existing
`data-testid="dash-placeholder-projetos"` div (leave that wrapper's own `data-testid`/classes
byte-for-byte unchanged -- it is the one thing the pre-existing "mobile viewport" e2e test already
asserts on), replace the "Em breve: projetos em andamento" placeholder text with
`<ProjectStrips projetos={projetoRows()} hojeIso={hojeIso} onVerProjetos={goToProjetos} />`.

Create `web/e2e/dashboard-kanbans.spec.ts`, mirroring `projetos-section.spec.ts`'s exact
`execFileSync`/`apolloCli`/`uniqueName`/`tryDelete`/`sweepLeftovers` scaffolding (subtarefas before
tarefas before etapas before projetos before fundos on cleanup) with its own `phase22-e2e-`
prefix. In one `test.describe("DASH-05: project strip rendering")` block's `beforeAll`, create:
one fundo; one projeto linked to it (`--status "em andamento"`, matching the free-text value
`dashboard.spec.ts`'s own DASH-03 fixtures already use -- never asserted on, just realistic); one
etapa `--ordem 1` linked to that projeto holding 4 tarefas (3 with `--data-prevista` a week in the
future, 1 with `--data-prevista` a week in the past and no subtarefas so it is genuinely vencida);
one etapa `--ordem 2` linked to the same projeto holding 1 tarefa (future date); and a second,
separate projeto with zero etapas. `afterAll` deletes every created record plus `sweepLeftovers()`.
Write the four tests from this task's `<behavior>` block, reusing this one fixture set: (1) the
zero-etapa projeto's id never appears as any `project-strip`'s `data-eid`; (2) header meta text
plus equal `boundingBox().width` AND equal `boundingBox().height` between the two columns plus the
3+1 card/overflow split on the ordem-1 column and the plain 1-card ordem-2 column (mirror
`projetos-section.spec.ts:808-813`'s exact width-equality assertion, extended with the matching
height check); (3) `project-strip-nome`/`project-strip-fundo-badge`/`project-strip-column-header`/
`project-strip-card` each `.evaluate((el) => el.tagName.toLowerCase())` equal `"button"` (mirror
`projetos-section.spec.ts:672-675`); (4) the vencida card's own `project-strip-card` carries
`border-destructive` and its meta line carries `text-destructive`, and one of the future-dated
cards carries neither. Add a second `test.describe("DASH-05: empty state")` block (declared BEFORE
the first, mirroring `dashboard.spec.ts`'s own empty-state-before-fixtures ordering) whose single
test calls `sweepLeftovers()` first, then asserts `dash-projetos-empty` is visible with the correct
text and that clicking `dash-projetos-ver-todos` makes `nav-projetos`'s `aria-current` become
`"true"` (mirror `dash-tickets-ver-todos`'s existing assertion in `dashboard.spec.ts`).
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard-kanbans.spec.ts --project=authed --no-deps -g "DASH-05"</automated>
  </verify>
  <done>Zero-etapa projetos never get a strip; strips render fixed-width columns that are provably equal in both width and height regardless of card count; cards cap at 3 plus a correct overflow row; every clickable surface is a real button; vencido styling is correct; the empty state and its Projetos link both work.</done>
</task>

<task type="auto">
  <name>Task 2: Measured &gt; overflow indicator (ResizeObserver) + localStorage collapse persistence + full e2e coverage</name>
  <files>web/src/lib/dashboard/ProjectStrips.svelte, web/e2e/dashboard-kanbans.spec.ts</files>
  <action>
Extend `web/src/lib/dashboard/ProjectStrips.svelte` (do not create a new file; this is new code
inside the same `{#each}` block Task 1 built).

Collapse persistence (spec-ui.md section 3.5, CONTEXT.md's locked key): add two phase-local
helpers, not a shared utility module -- `function collapsedKey(projetoId: string): string { return `apollo.dash.collapsed.${projetoId}`; }`,
`function readCollapsed(projetoId: string): boolean { if (typeof localStorage === "undefined") return false; return localStorage.getItem(collapsedKey(projetoId)) === "true"; }`,
`function writeCollapsed(projetoId: string, value: boolean): void { if (typeof localStorage === "undefined") return; localStorage.setItem(collapsedKey(projetoId), String(value)); }`
(the `typeof localStorage === "undefined"` guard on both matters even though this app is a pure
Vite SPA today -- it is what keeps a future SSR pass or a non-browser test runner from throwing).
Add an `$effect` that seeds `collapsedByProjeto[projeto.id]` from `readCollapsed(projeto.id)` the
first time each projeto id is seen (guard with `if (!(projeto.id in collapsedByProjeto))` so a
later re-render never clobbers a value the user has since toggled in-memory). Change the collapse
button's `onclick` from Task 1's in-memory-only toggle to also call
`writeCollapsed(projeto.id, next)` with the new value on every toggle -- write on every toggle in
both directions (collapse and expand), never call `localStorage.removeItem`.

Measured overflow indicator (spec-ui.md section 3.5, CONTEXT.md's locked "measured, never
inferred" rule; Pattern 2/Pitfall 3 in 22-RESEARCH.md): add
`let rootEl: HTMLDivElement | undefined = $state();` bound via `bind:this={rootEl}` on the
outermost wrapper of the non-empty-state render path (the element wrapping `dash-projetos`). Add
`let overflowingByProjeto = $state<Record<string, boolean>>({});`. Add one shared `$effect` that
reads `emAndamento` and `collapsedByProjeto` (to establish reactive dependencies -- collapsing a
strip unmounts its `project-strip-body`, so the effect must re-run and re-observe whenever either
changes), returns early when `!rootEl`, then `querySelectorAll('[data-testid="project-strip-body"]')`
on `rootEl`; for each matched `stripEl`, find its own `rowEl` via
`stripEl.querySelector('[data-testid="project-strip-row"]')` and its own projeto id via
`stripEl.closest('[data-testid="project-strip"]')?.getAttribute("data-eid")`; skip if either is
missing; otherwise create one `new ResizeObserver(() => { overflowingByProjeto = { ...overflowingByProjeto, [id]: stripEl.scrollWidth > stripEl.clientWidth }; })`,
call `.observe(stripEl)` AND `.observe(rowEl)` on it (both targets matter -- Pattern 2's own
reasoning: the outer element's box only reflects a window/layout resize, the inner row's own
intrinsic width is what actually changes when a column is added/removed), and push it onto a local
array; the effect's cleanup function calls `.disconnect()` on every observer in that array. Always
re-read `stripEl.scrollWidth`/`stripEl.clientWidth` fresh inside the callback -- never read
`entry.contentRect`/`entry.target` from the observer callback's own argument (Pitfall 3). Inside
each strip's `project-strip-body`, as a sibling of `project-strip-row` (not nested inside it),
render `{#if overflowingByProjeto[projeto.id]}` a
`data-testid="project-strip-overflow" data-eid={projeto.id}` span with
`class="pointer-events-none absolute right-0 top-0 bottom-0 flex items-center bg-gradient-to-l from-background px-2"`
containing the literal glyph "&gt;". Never derive this boolean from `etapasOrdenadas.length` or
any other data-shaped count -- only the ResizeObserver callback's own fresh
`scrollWidth`/`clientWidth` comparison may set it.

Extend `web/e2e/dashboard-kanbans.spec.ts` with two more `test.describe` blocks, each with its own
`beforeAll`/`afterAll` fixture set (do not reuse Task 1's fixture across describes):

(1) `"DASH-05: overflow indicator"` -- create one projeto with 6 etapas (`--ordem 1` through
`--ordem 6`, no tarefas needed) and a second, separate projeto with exactly 1 etapa. Before
navigating, `page.setViewportSize({ width: 480, height: 800 })` (narrow enough that 6 columns at
`w-36` plus gaps reliably exceeds `clientWidth`, deterministic regardless of the CI runner's own
screen size). Assert the 6-etapa projeto's `project-strip-overflow` becomes visible (poll via
`expect(...).toBeVisible({ timeout: RESYNC_TIMEOUT })` since the ResizeObserver callback fires
asynchronously after layout) and the 1-etapa projeto's `project-strip-overflow` has count 0.

(2) `"DASH-05: collapse persistence"` -- create one projeto with one etapa. Assert
`project-strip-body` is visible; click that strip's `project-strip-collapse`; assert
`project-strip-body` becomes hidden/count 0; read
`await page.evaluate((key) => localStorage.getItem(key), collapsedKeyLiteral)` (build the exact
same `apollo.dash.collapsed.<id>` string in the test file -- do not import the component's private
helper) and assert it equals `"true"`; assert
`await page.evaluate(() => Object.keys(localStorage).filter((k) => k.startsWith("apollo.dash.collapsed.")).length)`
equals exactly 1 (proving no other collapse key was written); `page.reload()`; assert
`project-strip-body` is still hidden/count 0 (persistence proof); click the collapse toggle again;
assert `project-strip-body` reappears and the same localStorage key now reads `"false"`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard-kanbans.spec.ts --project=authed --no-deps -g "DASH-05"</automated>
  </verify>
  <done>The overflow indicator appears only for a strip whose columns measurably exceed the viewport and never for one that fits; collapse state persists across a full reload via exactly one localStorage key per project, written on every toggle in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser localStorage <-> ProjectStrips.svelte | Client-only collapse-state read/write; never reaches the network |
| Server-scoped query data -> ProjectStrips.svelte | `projetos` prop is already owner-scoped by `instant.perms.ts`'s existing `view` rule (unchanged this phase); this component performs no additional authorization filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | `localStorage['apollo.dash.collapsed.<projetoId>']` | low | accept | Value only ever gates cosmetic collapse UI, never a security/authorization decision; `readCollapsed` treats any value other than the literal string `"true"` as `false` (never `JSON.parse`d, never thrown on malformed input) |
| T-22-02 | Information Disclosure | `filtroEmAndamento`'s client-side etapa-count filter | low | accept | Purely a display filter over data InstantDB's `view` permission rule already scoped server-side to the authenticated owner; hiding a zero-etapa project client-side is not relied on as a security boundary |
| T-22-03 | Denial of Service (self, minor) | Shared `ResizeObserver` effect re-querying the DOM on every `emAndamento`/`collapsedByProjeto` change | low | mitigate | Each observer callback does O(1) work (one boolean write into `$state`); the effect's own cleanup calls `.disconnect()` on every observer before it re-runs or the component unmounts, so observer count never grows unbounded |
</threat_model>

<verification>
Run `cd web && bunx playwright test dashboard-kanbans.spec.ts --project=authed --no-deps` after
Task 2 (both tasks' tests, full file) and confirm all pass. Manually confirm no `if (config.etype === ...)`-style branch was introduced anywhere (none should be -- this plan touches no generic
component) and that no class outside the existing token set (`bg-muted`, `border-destructive`,
`text-destructive`, `text-muted-foreground`, `bg-gradient-to-l from-background`) was added.
</verification>

<success_criteria>
`ProjectStrips.svelte` exists, is mounted inside `Dashboard.svelte`'s unchanged
`dash-placeholder-projetos` slot, and every truth in this plan's `must_haves.truths` is provable by
the Playwright suite created in this plan. DASH-05's ROADMAP success criteria (fixed-width
non-compressing columns, measured overflow indicator, localStorage collapse) all hold against live
data.
</success_criteria>

<output>
Create `.planning/phases/22-dashboard-kanbans-rotinas-heatmap/22-01-SUMMARY.md` when done.
</output>
