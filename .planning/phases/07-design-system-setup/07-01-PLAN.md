---
phase: 07-design-system-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/bun.lock
  - web/vite.config.ts
  - web/src/app.css
  - web/playwright.config.ts
  - web/e2e/design-system.spec.ts
  - web/components.json
  - web/src/lib/utils.ts
  - web/tsconfig.json
  - web/tsconfig.app.json
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Loading the SPA in a fresh, unauthenticated browser context shows Tailwind's Preflight reset applied: the existing unconditional `<h1>Apollo v2</h1>` resolves to the same font-size/weight as body text, and `document.body`'s computed top margin is `0px`."
    - "`getComputedStyle(document.documentElement)` exposes non-empty `--background`, `--foreground`, and `--primary` custom properties, and `web/components.json` is committed at the repo path shadcn-svelte expects, recording `style: \"nova\"` and `iconLibrary: \"lucide\"`."
    - "Emulating `prefers-color-scheme: dark` vs `light` in a Playwright browser context changes the resolved `--background`/`--foreground`/`--primary` values, `document.documentElement` never carries a `dark` class under either scheme, and no toggle control exists anywhere in the DOM."
    - "The app still boots to the login screen (`data-testid=\"login-screen\"`) with zero browser console errors or page errors after the full Tailwind + shadcn-svelte wiring — proven both right after the Tailwind swap and again after the complete phase."
    - "`bun run check` (svelte-check plus the `tsconfig.node.json`/`tsconfig.e2e.json` project references) and `bun run lint` (Biome) both exit 0, with zero new suppressions, on every file this phase touches."
    - "The pre-existing `.env.instantdb`-parsing block and the `@instantdb/svelte` Vite alias in `vite.config.ts` remain byte-identical to before this phase — only additive lines were introduced."
  artifacts:
    - path: "web/src/app.css"
      provides: "Tailwind v4 import, shadcn-svelte design tokens, and automatic prefers-color-scheme dark mode with no class toggle"
      contains: "prefers-color-scheme: dark"
    - path: "web/components.json"
      provides: "shadcn-svelte CLI config — style nova, base color neutral, icon library lucide, $lib-rooted aliases"
      contains: "nova"
    - path: "web/src/lib/utils.ts"
      provides: "cn() className-merge helper (clsx + tailwind-merge) every future shadcn-svelte component imports"
      exports: ["cn"]
    - path: "web/vite.config.ts"
      provides: "Tailwind v4 Vite plugin plus the $lib alias, additive alongside the existing @instantdb/svelte alias and .env.instantdb parsing"
      contains: "tailwindcss()"
    - path: "web/e2e/design-system.spec.ts"
      provides: "Auth-free Playwright proof of all four ROADMAP Phase 7 success criteria"
      contains: "prefers-color-scheme"
    - path: "web/playwright.config.ts"
      provides: "Routes design-system.spec.ts into the auth-free anon project alongside no-leakage.spec.ts, never through the live-auth setup project"
      contains: "design-system"
  key_links:
    - from: "web/vite.config.ts plugins[]"
      to: "web/src/app.css"
      via: "@tailwindcss/vite build-time transform reading the single @import \"tailwindcss\"; line"
      pattern: "tailwindcss\\(\\)"
    - from: "web/components.json aliases"
      to: "web/vite.config.ts resolve.alias / web/tsconfig.app.json paths"
      via: "the identical $lib alias string declared to the CLI and resolved by the bundler and type-checker"
      pattern: "\\$lib"
    - from: "web/e2e/design-system.spec.ts"
      to: "web/playwright.config.ts anon project"
      via: "testMatch regex routing that avoids the authed project's live-auth setup dependency"
      pattern: "design-system"
    - from: "web/src/app.css dark-token block"
      to: "browser prefers-color-scheme media feature"
      via: "a bare @media query wrapping :root, zero JavaScript"
      pattern: "prefers-color-scheme: dark"
  prohibitions:
    - "MUST NOT run an automated codemod (e.g. a Svelte-CLI `add tailwindcss` command) against `vite.config.ts` — its `.env.instantdb`-parsing block mitigates a documented Information Disclosure threat from Phase 1 and must only ever be hand-edited, additively."
    - "MUST NOT leave the class-scoped dark-mode override that shadcn-svelte's init writes by default in place, and MUST NOT install or wire any JS-driven mode-toggling helper or DOM control — SETUP-03 requires the swap to be automatic and OS-driven only, with zero toggle anywhere in the app."
    - "MUST NOT let `design-system.spec.ts` inherit the authed project's live magic-code setup dependency — none of this phase's assertions need an authenticated session, and this project's magic-code retrieval is reserved for the orchestrator (see PROJECT.md Key Decisions), not a dispatched plan-execution run."
    - "MUST NOT emit a checkpoint:human-verify task or a <verify><human-check> block anywhere in this plan — this milestone runs fully unattended (C-12)."
  assumptions_flagged:
    - "A1 (07-RESEARCH.md): `--preset b0` (style nova, base color neutral, icon lucide) is the current CLI's zero-configuration default and is used as the literal present-day equivalent of the locked spec's stale 'default style/base color' wording (PROJECT.md C-11, REQUIREMENTS.md SETUP-02). This substitution was already resolved as non-negotiable before this plan was written and is not re-opened here."
    - "This plan deliberately scopes its own automated verification to the auth-free Playwright specs (design-system.spec.ts + no-leakage.spec.ts) plus the static gates (bun run check, bun run lint), rather than the full `bun run test:e2e` suite. The authed project's live magic-code round trip is an orchestrator-only capability (PROJECT.md Key Decisions) a dispatched plan-execution run cannot perform, and none of Phase 7's 4 ROADMAP success criteria require an authenticated screen; full authenticated-suite regression is Phase 11's explicit job (VERIFY-01)."
---

<objective>
Wire Tailwind CSS v4 and shadcn-svelte into `web/` with the CLI's own zero-configuration default (style `nova`, base color `neutral`, icon library `lucide`), converting shadcn-svelte's default class-based dark mode into an automatic, toggle-free `prefers-color-scheme` media query — so every later v1.1 phase inherits a live component/token foundation without needing to touch build tooling again.

Purpose: This is the phase's tracer slice for the whole v1.1 milestone. Phases 8-10 restyle LoginScreen, Shell, and EntityScreen entirely on top of the tokens, aliases, and dark-mode behavior this plan establishes — if any of it is wrong (a broken alias, a dark mode that never activates, a `.env.instantdb`-parsing regression), every downstream phase inherits the defect silently.
Output: `web/` builds and boots on Tailwind v4 + shadcn-svelte's default tokens, `web/components.json` is committed, dark mode is automatic and toggle-free, and a new auth-free Playwright spec (`web/e2e/design-system.spec.ts`) proves all four ROADMAP Phase 7 success criteria live in Chromium.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-system-setup/07-CONTEXT.md
@.planning/phases/07-design-system-setup/07-RESEARCH.md
@.planning/phases/07-design-system-setup/07-PATTERNS.md
</context>

<environment_facts>
Verified live during planning (2026-08-09) — treat as established, do not re-discover:

- `bun` 1.3.12 and `node` v20.20.2 are present; Playwright's Chromium browser is already installed in the local cache and `@playwright/test` `1.62.1` is already a `web/package.json` devDependency — no framework install step is needed, only a new spec file.
- Registry-verified versions as of the research session: `tailwindcss` `4.3.3`, `@tailwindcss/vite` `4.3.3`, `shadcn-svelte` `1.5.0`. This stack ships new versions every few days; the install/verify commands below pin only the major version (`^4.`) so a routine patch bump between planning and execution does not fail a gate. Re-run `npm view <pkg> version` if execution happens more than ~7 days after 2026-08-09.
- `web/package.json`'s `check` script already runs `svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.e2e.json` — this already covers every file this phase touches (`vite.config.ts` via `tsconfig.node.json`, everything under `src/` via `tsconfig.app.json`, `playwright.config.ts`/`e2e/**` via `tsconfig.e2e.json`). No `check` script edit is needed.
- Root `biome.json`'s `files.includes` already covers `web/src/**/*.ts` (so the new `src/lib/utils.ts` needs no glob edit), `web/vite.config.ts`, `web/e2e/**/*.ts`, and `web/playwright.config.ts`. `components.json` is JSON and outside Biome's scope entirely — it needs no lint pass.
- `web/e2e/.auth/user.json` already exists on disk from a prior real login. This plan's spec never reads it and never depends on it staying valid — every assertion here runs pre-login, in the same `anon`-style empty storageState that `no-leakage.spec.ts` already uses.
- `shadcn-svelte@1.5.0 init`'s `preflightInit()` refuses to run unless `tailwindcss` is already a `package.json` dependency — Task 1 (Tailwind) must land and be committed to disk before Task 2 (`init`) runs; this is a hard tool-ordering constraint, not a planning preference.
- `bunx shadcn-svelte@latest init` fetches its preset/token data from the shadcn-svelte registry over the network at run time — the exact oklch values are generated server-side, not baked into the CLI. This plan's checks assert on custom-property *names* and *value presence/difference*, never on hard-coded color numbers, for exactly this reason.
</environment_facts>

<interfaces>
Contracts this plan establishes, consumed by Phase 8 onward:

From `web/src/lib/utils.ts` (written verbatim by `shadcn-svelte init`, not hand-authored):
- `export function cn(...)` — the `clsx` + `tailwind-merge` className helper every future `shadcn-svelte add <component>` file imports.

From `web/components.json`:
- The alias block (`$lib`, `$lib/components`, `$lib/components/ui`, `$lib/utils`, `$lib/hooks`) that every future `shadcn-svelte add` invocation reads to decide where generated component files land.

From `web/vite.config.ts` / `web/tsconfig.json` / `web/tsconfig.app.json`:
- The `$lib` alias, resolvable both by the Vite dev/build pipeline and by `svelte-check`/`tsc`, that every future `$lib/...` import (including every `$lib/components/ui/*` import Phases 8-10 will add) depends on.

From `web/playwright.config.ts`:
- The `anon` project's broadened `testMatch` — any future spec that needs to assert pre-login DOM/CSS state without triggering the live magic-code `setup` project should route through this same project, not the default `authed` matcher.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Wire Tailwind v4 into the Vite build and prove Preflight end-to-end via an auth-free Playwright spec</name>
  <files>web/package.json, web/bun.lock, web/vite.config.ts, web/src/app.css, web/playwright.config.ts, web/e2e/design-system.spec.ts</files>
  <read_first>
    - web/vite.config.ts, web/src/app.css, web/playwright.config.ts (current contents, already read this session)
    - web/e2e/no-leakage.spec.ts (structural-comment convention, data-testid discipline, no-setup-dependency pattern to imitate)
    - web/e2e/auth.setup.ts (confirms the `login-screen` data-testid convention)
    - .planning/phases/07-design-system-setup/07-RESEARCH.md sections "Pattern 1: Manual Tailwind v4 + Vite wiring", "Code Examples: Tailwind v4 + Vite wiring", "Code Examples: Playwright proof of Tailwind preflight", "Pitfall 4"
    - .planning/phases/07-design-system-setup/07-PATTERNS.md sections "web/vite.config.ts", "web/e2e/design-system.spec.ts"
    - .planning/PROJECT.md constraints C-08, C-11, C-12 and the "Orchestrator (not subagents) performs all magic-code email round trips" Key Decision
  </read_first>
  <action>
Install Tailwind v4 and its official Vite plugin as devDependencies using bun only — never npm/pnpm/yarn (C-08): `cd web && bun add -D tailwindcss @tailwindcss/vite`. Confirm no `package-lock.json` appears in `web/` afterward; delete it and re-run with plain `bunx`/`bun` if one does.

Edit `web/vite.config.ts` additively: import the plugin's default export and append it to the existing `plugins` array so it reads `plugins: [svelte(), tailwindcss()]`. Do not touch the `.env.instantdb`-parsing block above it or the `resolve.alias` entry for `@instantdb/svelte` — both are load-bearing and documented in the file's own comments.

Replace the entire contents of `web/src/app.css` with a single import line for Tailwind. The existing hand-written reset (box-sizing plus a font-family stack) is fully superseded by Tailwind's own Preflight base styles, so remove it rather than keep both — Preflight alone gives an equal-or-stronger reset.

Create `web/e2e/design-system.spec.ts` (new file) with two tests, following `no-leakage.spec.ts`'s structural-comment convention and `data-testid` discipline:
1. A Preflight test whose title contains the word "preflight": navigate to `/`, locate the existing unconditional `<h1>Apollo v2</h1>` (rendered in `App.svelte` above both the signed-in and signed-out branches, so no auth is needed), and assert via `getComputedStyle` that its resolved font size equals the body's resolved font size and its font weight is the un-boosted default (not the browser's bold heading default), then assert `document.body`'s resolved top margin is zero.
2. A clean-boot test whose title contains the word "boots": attach `page.on("console", ...)` and `page.on("pageerror", ...)` listeners collecting error text into an array before navigating, navigate to `/`, assert `page.getByTestId("login-screen")` becomes visible, and assert the collected error array is empty.

This spec must never trigger a real magic-code login, now or when extended by a later task. Edit `web/playwright.config.ts`: broaden the `anon` project's `testMatch` regex so it also matches `design-system.spec.ts` (in addition to the existing `no-leakage.spec.ts`), and broaden the `authed` project's `testIgnore` regex the same way, so the new spec runs under the `anon` project's empty storageState instead of inheriting `authed`'s dependency on the `setup` project's live email round trip. None of this phase's assertions need an authenticated session, and magic-code retrieval in this project is reserved for the orchestrator, not a dispatched plan-execution run.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bun add -D tailwindcss @tailwindcss/vite && test ! -f package-lock.json && grep -qE '"tailwindcss": "\^4\.' package.json && grep -qE '"@tailwindcss/vite": "\^4\.' package.json && grep -q 'tailwindcss()' vite.config.ts && grep -q 'instantdbSveltePath' vite.config.ts && grep -qF '@import "tailwindcss";' src/app.css && bunx playwright test e2e/design-system.spec.ts --project=anon -g "preflight|boots" && echo TASK1_OK</automated>
  </verify>
  <done>`web/` builds on Tailwind v4 via `@tailwindcss/vite`, the pre-existing `.env.instantdb`/`@instantdb/svelte` logic in `vite.config.ts` is untouched, and a real Chromium run proves Preflight is applied and the app still boots to the login screen with zero console/page errors — both in the auth-free `anon` project.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn-svelte with the CLI's own zero-config default, wire the $lib alias, and convert dark mode to a bare prefers-color-scheme media query</name>
  <files>web/components.json, web/src/lib/utils.ts, web/src/app.css, web/vite.config.ts, web/tsconfig.json, web/tsconfig.app.json, web/e2e/design-system.spec.ts, web/package.json, web/bun.lock</files>
  <read_first>
    - .planning/phases/07-design-system-setup/07-RESEARCH.md sections "Pattern 2: Fully non-interactive shadcn-svelte init", "Pattern 3: Automatic prefers-color-scheme dark mode", "Pitfall 1", "Pitfall 2", "Code Examples: Non-interactive shadcn-svelte init", "Code Examples: Playwright proof of shadcn-svelte tokens + dark mode", "Anti-Patterns to Avoid"
    - .planning/phases/07-design-system-setup/07-PATTERNS.md sections "web/components.json", "web/tsconfig.json + web/tsconfig.app.json", "Shared Patterns: Additive-only edits"
    - web/tsconfig.json, web/tsconfig.app.json (current contents, already read this session)
    - PROJECT.md constraint C-11
  </read_first>
  <action>
Run, from `web/`, the fully non-interactive init command exactly as researched (no interactive prompts, safe for unattended execution): `bunx shadcn-svelte@latest init --preset b0 --css src/app.css --lib-alias '$lib' --components-alias '$lib/components' --ui-alias '$lib/components/ui' --utils-alias '$lib/utils' --hooks-alias '$lib/hooks'`. `--preset b0` is the current CLI's zero-configuration default — style `nova`, base color `neutral`, icon library `lucide` — and is this project's resolved, present-day equivalent of the locked spec's stale "default style/base color" wording (PROJECT.md C-11). After it runs, read back `web/components.json` and confirm it records `style: "nova"` and `iconLibrary: "lucide"`. The command writes `web/components.json`, `web/src/lib/utils.ts` (the `cn()` helper combining `clsx` and `tailwind-merge`), and appends a `tw-animate-css` import plus a design-token block on top of the single Tailwind import already in `web/src/app.css`.

Confirm `clsx` and `tailwind-merge` (needed by the generated `utils.ts`) and `tw-animate-css` (needed by the new CSS import) actually landed in `web/package.json` after `init` — this CLI installs its own supporting packages as part of running, but treat that as a fact to confirm, not assume. Separately, confirm `@lucide/svelte` is present in `web/package.json` too: the recorded `iconLibrary: "lucide"` choice in `components.json` is only meaningful once the matching package is actually installed, and REQUIREMENTS.md SETUP-02 names the package explicitly. If `init` did not add it on its own (icon packages are sometimes only pulled in when a component that renders one is later added), install it explicitly with `bun add @lucide/svelte` so this phase leaves a real, resolvable package behind rather than a components.json field with no backing dependency.

Wire the `$lib` alias so the aliases just declared to `init` actually resolve at build time and type-check time. Add a `$lib` entry to `web/vite.config.ts`'s existing `resolve.alias` object (resolved the same way the existing `@instantdb/svelte` entry already is, via `fileURLToPath(new URL(...))`), leaving that existing entry untouched. Add matching path entries for `$lib` and `$lib/*` to both `web/tsconfig.json`'s and `web/tsconfig.app.json`'s `compilerOptions`: `tsconfig.json` currently has no `compilerOptions` at all, so add one (with a `baseUrl` of `.` plus the two path entries) alongside its existing `files`/`references` keys; `tsconfig.app.json` already has a `paths` object holding the `@instantdb/svelte` entry, so add the two new entries into that same object without removing it.

`init`'s own default dark-mode output pairs a class-scoped `dark:` variant with a JS toggle helper this project's C-11 forbids ("no toggle"). Immediately after `init` runs, hand-edit `web/src/app.css`: delete the single custom-variant declaration line that scopes `dark:` utilities to an ancestor class (Tailwind v4's own un-overridden default is already `prefers-color-scheme`-based, so removing that one line is sufficient on its own — no replacement directive is needed), and change the dark-token block's selector from that ancestor class to a `prefers-color-scheme: dark` media query wrapping `:root`, keeping every custom-property name and value `init` wrote otherwise byte-identical. Leave the light-token `:root { ... }` block and the `@theme inline { ... }` token map exactly as `init` wrote them — do not hand-edit any token value. Do not install any package that flips a mode class via JavaScript, and do not add any element to the DOM that could — SETUP-03 requires the swap to stay automatic and OS-driven only.

Extend `web/e2e/design-system.spec.ts` (already routed to the auth-free `anon` project by Task 1) with two more tests: one whose title contains the word "tokens" — emulate `colorScheme: 'light'`, read `--background`/`--foreground`/`--primary` off `getComputedStyle(document.documentElement)` and assert each is non-empty, then emulate `colorScheme: 'dark'` on the same page and assert each of the three values actually changed; and one whose title contains the phrase "dark mode" — assert `document.documentElement.classList.contains("dark")` is `false` under both the light and dark emulated schemes, proving no class-based toggle is ever active.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && test -f components.json && grep -q '"style": "nova"' components.json && grep -q '"iconLibrary": "lucide"' components.json && test -f src/lib/utils.ts && grep -qF '"clsx"' package.json && grep -qF '"tailwind-merge"' package.json && grep -qF '"tw-animate-css"' package.json && grep -qF '"@lucide/svelte"' package.json && grep -qF '$lib' vite.config.ts && grep -qF 'instantdbSveltePath' vite.config.ts && grep -qF '$lib' tsconfig.json && grep -qF '$lib' tsconfig.app.json && grep -qF '@instantdb/svelte' tsconfig.app.json && ! grep -q '@custom-variant dark' src/app.css && grep -qF '@media (prefers-color-scheme: dark)' src/app.css && ! grep -qE '^\.dark[[:space:]]*\{' src/app.css && ! grep -q 'mode-watcher' package.json && bun run check && bunx playwright test e2e/design-system.spec.ts --project=anon -g "tokens|dark mode" && echo TASK2_OK</automated>
  </verify>
  <done>`web/components.json` and `web/src/lib/utils.ts` exist with the CLI's zero-config preset recorded; `$lib` resolves in both the Vite build and the type-checker alongside the untouched `@instantdb/svelte` alias; `web/src/app.css`'s dark tokens live under a bare `prefers-color-scheme` media query with no class-based variant or toggle anywhere; a real Chromium run proves tokens are present, non-empty, and swap under emulated color scheme with `.dark` never applied.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full auth-free regression plus the project's static quality gates, proving all four Phase 7 success criteria hold together</name>
  <files>web/src/app.css, web/src/lib/utils.ts, web/e2e/design-system.spec.ts</files>
  <read_first>
    - web/e2e/no-leakage.spec.ts (already read — the second spec this task re-runs)
    - web/package.json scripts `lint`, `check`, `test:e2e` (already read this session)
    - .planning/phases/07-design-system-setup/07-RESEARCH.md sections "Pitfall 3: Biome lints and formats shadcn-svelte's generated component files", "Pitfall 4", "Validation Architecture"
    - .planning/phases/07-design-system-setup/07-VALIDATION.md
  </read_first>
  <action>
Run the complete auth-free e2e slice plus the project's two static gates as one closing check, proving Phase 7's four ROADMAP success criteria hold together and nothing regressed after the full Tailwind-plus-shadcn-svelte wiring:

1. Run both `design-system.spec.ts` (all four tests: preflight, tokens, dark mode, boots) and `no-leakage.spec.ts` together under the `anon` project — this exercises every assertion this phase adds plus the pre-existing no-leakage guarantee, entirely without a live magic-code round trip. This is a deliberate scope choice, not an oversight: the `authed`/`setup` chain needs the orchestrator's Outlook-COM-bridge access (see PROJECT.md Key Decisions), which a dispatched plan-execution run does not have, and the authenticated-screen regression proof this milestone ultimately needs is Phase 11's explicit job (VERIFY-01/VERIFY-02), not this phase's.
2. Run `bun run check` — svelte-check plus the two `tsc -p` project references, covering every file this phase touched.
3. Run `bun run lint` — Biome over `shared web/src web/vite.config.ts web/e2e web/playwright.config.ts`, confirming zero new suppressions on the newly generated `src/lib/utils.ts` and every hand-edited config file (Biome parity with CLI-generated code is not guaranteed — this is the check that would catch it).
4. Confirm no stray package-manager artifact landed: `web/package-lock.json` must not exist.

If `bun run lint` reports a finding on `src/lib/utils.ts` or any hand-edited file, run the project's autofix script once and re-check; do not add an inline suppression comment to force a pass — a real suppression would pre-emptively break the zero-new-suppressions bar this milestone holds `web/` to overall.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bunx playwright test e2e/design-system.spec.ts e2e/no-leakage.spec.ts --project=anon && bun run check && bun run lint && test ! -f package-lock.json && echo TASK3_OK</automated>
  </verify>
  <done>A single auth-free Playwright run proves all four Phase 7 ROADMAP success criteria together; `bun run check` and `bun run lint` both exit 0 with zero new suppressions on every file this phase touched; no stray lockfile exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local dev machine → npm registry / shadcn-svelte registry | `bun add` and `bunx shadcn-svelte@latest init` fetch package code and (for shadcn-svelte) preset/token data over the network; nothing else in this phase crosses a trust boundary |
| repo working tree → git history | Only `web/components.json`, `web/src/lib/utils.ts`, additive config edits, and the new spec file are committed this phase — no command this phase runs reads or touches `.env.instantdb` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-00 | (scope note, no category) | Whole phase | n/a | accept | Phase 7 is pure frontend build-tooling (Tailwind v4 + shadcn-svelte install/init, CSS token wiring) — it adds zero new auth code, zero new data-handling paths, and zero new network call sites beyond the install/init commands themselves. ASVS V2 (Authentication), V3 (Session Management), V4 (Access Control), V5 (Input Validation), and V6 (Cryptography) are all inapplicable this phase, per 07-RESEARCH.md's Security Domain table. No exploitable surface is introduced by this phase. |
| T-07-01 | Tampering | `bun add -D tailwindcss @tailwindcss/vite` / `bunx shadcn-svelte@latest init` and their transitive `bits-ui`/`clsx`/`tailwind-merge`/`tw-animate-css`/`@lucide/svelte` | high | mitigate | 07-RESEARCH.md's Package Legitimacy Audit already vetted every package by live download count, GitHub-org provenance, and zero `postinstall` scripts, and explicitly documented the "too-new" verdicts on `tailwindcss`/`@tailwindcss/vite`/`shadcn-svelte`/`@lucide/svelte` as false positives given their download counts and recognizable maintainer orgs — approved with no blocking checkpoint, per this milestone's locked "zero human UAT anywhere" constraint. Task 3 re-confirms only the audited package names/versions landed and that no stray lockfile appeared. |
| T-07-02 | Tampering | `web/vite.config.ts`, `web/tsconfig.json`, `web/tsconfig.app.json` | medium | mitigate | Every edit across Tasks 1-2 is manual and line-scoped, never an automated codemod; both tasks' actions explicitly preserve the pre-existing `.env.instantdb`-parsing block and the `@instantdb/svelte` alias/path entry untouched, and each task's `<verify>` positively greps for both after the edit. |
| T-07-03 | Tampering (policy drift) | `web/src/app.css` dark-mode block | medium | mitigate | shadcn-svelte's own `init` output pairs a class-scoped dark variant with a JS toggle helper; Task 2 hand-converts it to a bare `prefers-color-scheme` media query, and Task 2's Playwright check asserts the dark class is never applied to `<html>` under either color scheme — closing the only path a future toggle could exploit. |
| T-07-04 | Information Disclosure | `bunx shadcn-svelte@latest init` network call to the shadcn-svelte registry | low | accept | The only data sent is the chosen preset code (`b0`) and generic CLI metadata — no `.env.instantdb` value, InstantDB credential, or user data is read or transmitted by this command. |
</threat_model>

<verification>
1. `cd web && bunx playwright test e2e/design-system.spec.ts e2e/no-leakage.spec.ts --project=anon` — all six tests pass (four new + two existing), with zero dependency on a live magic-code round trip.
2. `web/components.json` exists, committed, recording `style: "nova"` and `iconLibrary: "lucide"`.
3. `web/src/app.css` contains `@media (prefers-color-scheme: dark)` and contains no class-scoped dark-mode override or `.dark` selector.
4. `web/vite.config.ts`, `web/tsconfig.json`, `web/tsconfig.app.json` each still contain their pre-existing `@instantdb/svelte` reference alongside the new additive `$lib`/Tailwind lines.
5. `cd web && bun run check && bun run lint` — both exit 0, zero new suppressions.
6. `test ! -f web/package-lock.json` — bun remained the sole package-manager executor.
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 satisfied: Tailwind's Preflight reset is provably applied via a Playwright computed-style check against the live SPA (Task 1).
- ROADMAP Success Criterion 2 satisfied: `getComputedStyle(document.documentElement)` exposes non-empty shadcn-svelte custom properties, and `web/components.json` is committed at the expected path (Task 2).
- ROADMAP Success Criterion 3 satisfied: emulating `prefers-color-scheme` changes resolved token values automatically, with zero `.dark` class ever applied and no toggle control anywhere in the DOM (Task 2).
- ROADMAP Success Criterion 4 satisfied: the app still boots to the login screen with zero console errors, proven both right after the Tailwind swap (Task 1) and again after the complete Tailwind-plus-shadcn-svelte wiring (Task 3).
- SETUP-01 satisfied: Tailwind v4 installed and wired via `@tailwindcss/vite`, replacing the plain `app.css` reset.
- SETUP-02 satisfied: `shadcn-svelte` initialized via its own CLI zero-config default, `@lucide/svelte` icons, `components.json` committed.
- SETUP-03 satisfied: dark mode follows `prefers-color-scheme` automatically, with no manual toggle anywhere.
</success_criteria>

<artifacts_this_phase_produces>
Created or modified by this plan:

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `web/components.json` | shadcn-svelte CLI config (style, base color, icon library, aliases) | Every future `shadcn-svelte add <component>` invocation, Phases 8-10 |
| `web/src/lib/utils.ts` | Exports `cn()` | Every future shadcn-svelte component added in Phases 8-10 |
| `web/src/app.css` | Tailwind import + shadcn-svelte design tokens + `prefers-color-scheme` dark mode | Every `.svelte` file's Tailwind utility classes, all future phases |
| `web/vite.config.ts` | `@tailwindcss/vite` plugin + `$lib` alias, additive to the existing InstantDB wiring | Vite dev/build, `svelte-check` |
| `web/tsconfig.json`, `web/tsconfig.app.json` | `$lib`/`$lib/*` path entries, additive to the existing `@instantdb/svelte` path | `svelte-check`, future `$lib/...` imports |
| `web/playwright.config.ts` | `anon` project routing for `design-system.spec.ts`, auth-free | This plan's own `<verify>` commands, and any future auth-free spec |
| `web/e2e/design-system.spec.ts` | 4 Playwright tests: preflight, tokens, dark mode, boots-clean | This plan's own `<verify>` commands, Phase 11's extended suite |

Named symbols introduced: `cn` (`web/src/lib/utils.ts`).
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/07-design-system-setup/07-01-SUMMARY.md`.

Record in the SUMMARY: the exact `tailwindcss`/`@tailwindcss/vite`/`shadcn-svelte` versions that actually resolved at execution time (they may have drifted from the 2026-08-09 research snapshot); the verbatim `components.json` `style`/`baseColor`/`iconLibrary` values read back after `init`; confirmation that `package-lock.json` never appeared; and the deliberate decision to scope Task 3's regression to the auth-free `anon` project rather than the full `bun run test:e2e` suite, so Phase 11's planner knows that authenticated-suite regression is still fully open.
</output>
