---
phase: 16-entity-screen-row-actions-delete-confirmation
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - web/e2e/entities-delete-confirmation.spec.ts
autonomous: true
requirements: [ENTTBL-08, DELCONF-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dedicated Playwright coverage (`entities-delete-confirmation.spec.ts`) proves ENTTBL-08's row-action alignment/spacing — a real, positive, CSS-verified gap between `row-edit`/`row-delete` and a right-aligned actions column — on a full-CRUD entity not already exercised by 16-01's own fundos-based tracer proof."
    - "The same coverage proves DELCONF-01's keyboard paths explicitly: pressing Escape while the AlertDialog is open cancels it (row retained, focus returns to `row-delete`), and pressing Tab then Enter on the focused `AlertDialog.Action` confirms the delete (row removed, focus lands on `entity-create-start`) — both driven by `page.keyboard`, not mouse clicks, and both checked against real `document.activeElement` via `toBeFocused()`."
    - "Dedicated coverage proves the AlertDialog cannot be dismissed (Escape ignored, both actions disabled) while its own `db.transact` delete call is genuinely in flight against the live app — the same class of proof Phase 15 added for the create/edit Dialog's busy state."
    - "Dedicated coverage proves zero native `window.confirm`/`alert`/`prompt` dialog fires anywhere during any row-delete interaction in this file."
    - "Dedicated coverage proves the restricted (`instanciasRotina`) and read-only (`logInferenciaClaude`) capability classes still render zero `row-delete` anywhere, against a genuinely-seeded row (not an empty table) — no new delete affordance was introduced by this phase."
    - "The full pre-existing-plus-new Playwright suite passes with zero failures/skips; `svelte-check`/`tsc` and Biome remain clean; zero raw color literal or duplicate load-bearing testid exists in any file this phase touched."
  artifacts:
    - web/e2e/entities-delete-confirmation.spec.ts
  key_links:
    - "Every test in this file reuses `confirmRowDelete`/`cancelRowDelete` from `web/e2e/helpers/delete-confirmation.ts` (16-01) for its mouse-driven open step, and drives the keyboard-specific paths directly via `page.keyboard.press(...)` on top of that same shared open step — never re-implementing the open/confirm/cancel sequence ad hoc."
    - "`tarefas` (full-CRUD, not exercised by 16-01's own fundos-based tracer proof) is this file's primary subject for Tests A-E, giving genuinely independent coverage rather than re-asserting the same entity/scenario 16-01 already proved."
---

<objective>
Add dedicated, live Playwright coverage proving ENTTBL-08 (row-action alignment/spacing) and DELCONF-01's
keyboard/focus/busy-gating requirements explicitly — on top of Plan 16-01's AlertDialog conversion — then run
the complete pre-existing-plus-new Playwright suite plus the project's quality gates (`svelte-check`/`tsc`,
Biome) as this phase's final regression.

Purpose: Plan 16-01 proved the conversion via `fundos`'s own extended CRUD test (mouse-driven cancel/confirm
+ basic focus checks). This plan closes the remaining gaps ROADMAP Phase 16's success criteria name
explicitly — keyboard-only confirm/cancel, busy-gated dismissal under a live in-flight write, a
CSS-measured alignment proof, and cross-capability-class safety — on a different full-CRUD entity (`tarefas`)
for genuinely independent coverage, then proves the whole milestone-to-date remains green together.
Output: `web/e2e/entities-delete-confirmation.spec.ts` (6 new tests); a full-suite regression run reporting
at least 60 passed, 0 failed, 0 skipped; clean `bun run check` and `bun run lint`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-entity-screen-row-actions-delete-confirmation/16-CONTEXT.md
@.planning/phases/16-entity-screen-row-actions-delete-confirmation/16-01-SUMMARY.md

# EntityScreen.svelte as left by 16-01 — the exact testids/structure this spec asserts against
@web/src/lib/entities/EntityScreen.svelte

# Shared AlertDialog interaction helper to reuse, not reinvent
@web/e2e/helpers/delete-confirmation.ts

# tarefas config — this file's primary test subject (simple CLI seed, no required date field)
@web/src/lib/entities/defs/tarefas.ts

# Existing capability-class-safety + admin-fixture conventions to reuse
@web/e2e/entities-table-restyle.spec.ts
@web/e2e/fixtures/instancia-admin-fixture.ts
@web/e2e/entities-projeto-etapa-tarefa.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedicated Playwright coverage for ENTTBL-08 alignment and DELCONF-01 keyboard/focus/busy-gating</name>

  <precondition>powershell.exe resolves the Outlook Classic COM bridge (`orules.ps1 peek`, per PROJECT.md C-10) from this shell — the `authed` Playwright project this task's &lt;verify&gt; runs depends on the `setup` project, which performs a real live magic-code round trip.</precondition>

  <read_first>
    - `web/src/lib/entities/EntityScreen.svelte` as left by 16-01 — the exact testids this spec asserts against: `row-edit`, `row-delete`, `delete-cancel`, `delete-confirm`, `entity-create-start`, plus the `flex items-center justify-end gap-2` wrapper and `text-right` actions column classes.
    - `web/e2e/helpers/delete-confirmation.ts` — `confirmRowDelete(page, row)`/`cancelRowDelete(page, row)`, reused for every test's mouse-driven open step.
    - `web/e2e/entities-projeto-etapa-tarefa.spec.ts` (top-of-file `apolloCli`/`uniqueName`/`sweepLeftovers`/`beforeEach`/`afterEach` conventions, and its `apolloCli(["tarefa", "criar", "--titulo", ..., "--tipo-prazo", "hard", "--status", "ativo"])` / `apolloCli(["tarefa", "deletar", "--id", ...])` / `apolloCli(["tarefa", "listar"])` call shapes) — the exact CLI fixture pattern this new file's `beforeEach`/`afterEach` sweep mirrors for `tarefas`.
    - `web/e2e/entities-table-restyle.spec.ts` (the `instanciasRotina`/`logInferenciaClaude` tests) — the seed-a-real-row-then-assert-zero-row-delete pattern this task's capability-safety test reuses, via `seedInstance`/`deleteInstance` (from `web/e2e/fixtures/instancia-admin-fixture.ts`) for `instanciasRotina` and the CLI's `log-inferencia registrar`/`deleteAdminRecord` pair for `logInferenciaClaude`.
    - `web/e2e/entities-form-restyle.spec.ts` Test 3 (`ENTFRM-07`) — the live-busy-state observation technique (asserting a disabled/spinner state immediately after a click, without first awaiting the write's resolution) this task's busy-gating test reuses for `delete-confirm`/`delete-cancel`.
  </read_first>

  <behavior>
    - Test A ("ENTTBL-08: row-edit/row-delete render with a real, positive, CSS-verified gap and a right-aligned actions column"): on one seeded `tarefas` row, assert `row-edit`/`row-delete` are both visible; assert their shared wrapper `<div>` (the immediate parent of `row-delete`) has computed `column-gap: 8px` and `justify-content: flex-end`; assert the enclosing `<td>` (nearest `ancestor::td`) has computed `text-align: right`; assert `row-delete`'s bounding-box `x` is strictly greater than `row-edit`'s `x + width` (a real, non-overlapping horizontal gap). Zero writes beyond the CLI seed/cleanup.
    - Test B ("DELCONF-01: row-delete opens a shadcn AlertDialog, not window.confirm — Cancel is focused by default"): register a page-wide `page.on("dialog", ...)` listener that throws if any native dialog ever fires; on a seeded `tarefas` row, click `row-delete`, assert `page.getByRole("alertdialog")` becomes visible, assert `page.getByTestId("delete-cancel")` is `toBeFocused()` (the deliberate `onOpenAutoFocus` override, checked empirically); cancel via `cancelRowDelete` to leave state clean.
    - Test C ("DELCONF-01: keyboard cancel — Escape closes the AlertDialog, row retained, focus returns to row-delete"): open via `row.getByTestId("row-delete").click()`, assert the alertdialog visible, `page.keyboard.press("Escape")`, assert the alertdialog reaches count 0, assert the row is still visible, assert `row.getByTestId("row-delete")` is `toBeFocused()`.
    - Test D ("DELCONF-01: keyboard confirm — Tab then Enter on the destructive Action deletes the record, focus lands on entity-create-start"): open via click, assert `delete-cancel` is focused (default), `page.keyboard.press("Tab")`, assert `delete-confirm` is now focused, `page.keyboard.press("Enter")`, assert the row reaches count 0 (`RESYNC_TIMEOUT`), assert `entity-create-start` is `toBeFocused()`. No CLI cleanup needed — the record is genuinely deleted through the UI.
    - Test E ("DELCONF-01: the AlertDialog cannot be dismissed while the delete is in flight"): open via click, click `delete-confirm`, immediately (without awaiting the write's resolution) assert `delete-confirm` and `delete-cancel` are both `toBeDisabled()`, `page.keyboard.press("Escape")`, assert the alertdialog is still visible (ignored while busy), then assert the row eventually reaches count 0 (`RESYNC_TIMEOUT`) once the write actually resolves. No CLI cleanup needed.
    - Test F ("ENTTBL-08/DELCONF-01 capability safety: restricted and read-only entities gain no new delete affordance"): seed one `instanciasRotina` record (`seedInstance`) and one `logInferenciaClaude` record (CLI `log-inferencia registrar`); on `instanciasRotina`'s screen, assert the seeded row is visible and `row-delete` has count 0 anywhere on the page; on `logInferenciaClaude`'s screen, assert the seeded row is visible and both `row-edit`/`row-delete` have count 0 anywhere on the page. Clean up via `deleteInstance`/`deleteAdminRecord` in a `finally`.
  </behavior>

  <files>web/e2e/entities-delete-confirmation.spec.ts</files>

  <action>
Create `web/e2e/entities-delete-confirmation.spec.ts` (routes automatically under the `authed` project's default `testMatch`, no `playwright.config.ts` edit needed, per every other `entities-*.spec.ts` file's own precedent) with the 6 tests described in `<behavior>`, following `entities-projeto-etapa-tarefa.spec.ts`'s top-of-file comment convention (why `authed`, what this proves), a `RESYNC_TIMEOUT` constant, and a `phase16-e2e-` `PREFIX` constant for `uniqueName`. Import `confirmRowDelete`/`cancelRowDelete` from `./helpers/delete-confirmation.ts`, and `seedInstance`/`deleteInstance`/`deleteAdminRecord` from `./fixtures/instancia-admin-fixture.ts`.

Implement a `beforeEach`/`afterEach` sweep for `tarefas` (list via `apolloCli(["tarefa", "listar"])`, delete any `titulo` starting with `PREFIX` via `apolloCli(["tarefa", "deletar", "--id", ...])`), mirroring `entities-projeto-etapa-tarefa.spec.ts`'s exact sweep-function shape. Each of Tests A/B/C/F seeds its own `tarefas` record via `apolloCli(["tarefa", "criar", "--titulo", titulo, "--tipo-prazo", "hard", "--status", "ativo"])` and, where the record survives the test (A, B, C — not D or F, which either delete it through the UI or use a different entity), deletes it in a `finally` via `apolloCli(["tarefa", "deletar", "--id", id])` in addition to the `afterEach` sweep's own belt-and-braces cleanup.

For Test A's CSS assertions, use Playwright's `toHaveCSS(property, value)` locator assertion (not manual `getComputedStyle` string parsing) for `column-gap`/`justify-content`/`text-align`; locate the wrapper via `row.getByTestId("row-delete").locator("xpath=..")` and the cell via `row.getByTestId("row-delete").locator("xpath=ancestor::td[1]")`.

For Test D, confirm the initial-focus assertion (`delete-cancel` focused) before pressing Tab — this both re-proves Test B's finding independently on a different entity and establishes the correct starting point for the Tab-to-Action keyboard sequence.

For Test E, click `delete-confirm` once via mouse (`await page.getByTestId("delete-confirm").click();`) — this is deliberately a mouse interaction, not `page.keyboard.press("Enter")`, so the immediately-following disabled-state assertions race against the real, unresolved `db.transact` promise exactly as Phase 15's `ENTFRM-07` test did for `entity-submit`, not against a synthetic timing assumption.

For Test F, seed `logInferenciaClaude` via `apolloCli(["log-inferencia", "registrar", "--campo", campo, "--valor-inferido", valorInferido, "--entidade-tipo", "tarefas", "--entidade-id", "phase16-e2e-fixture-entidade"])`, mirroring `entities-table-restyle.spec.ts`'s exact call shape, and clean it up via `deleteAdminRecord("logInferenciaClaude", created.id)` (no CLI `deletar` command exists for this entity).

Register the page-wide native-dialog-throws-on-fire listener (Test B's own requirement) at the top of every test in this file, not just Test B — every test in this file interacts with `row-delete`, so every test is an opportunity to catch a native dialog regression, at negligible extra cost.
  </action>

  <verify>
    <automated>cd web && bunx playwright test e2e/entities-delete-confirmation.spec.ts --project=authed</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && bunx playwright test e2e/entities-delete-confirmation.spec.ts --project=authed` reports `6 passed`, `0 failed`.
    - `grep -c 'page.on("dialog"' web/e2e/entities-delete-confirmation.spec.ts` returns 6 or more (one throw-listener registration per test, per this task's own "every test" instruction).
    - `grep -c 'keyboard.press("Escape")' web/e2e/entities-delete-confirmation.spec.ts` returns 2 or more (Tests C and E).
    - `grep -c 'keyboard.press("Tab")' web/e2e/entities-delete-confirmation.spec.ts` returns 1 or more (Test D).
    - `grep -c 'toBeFocused()' web/e2e/entities-delete-confirmation.spec.ts` returns 5 or more (Tests B/C/D's focus assertions).
    - `grep -c 'confirmRowDelete\|cancelRowDelete' web/e2e/entities-delete-confirmation.spec.ts` returns 2 or more (both helpers imported and used at least once each).
    - `cd web && bun run check` exits 0 (new spec type-checks cleanly under the e2e tsconfig).
  </acceptance_criteria>

  <done>New live Playwright coverage proves, against the real InstantDB app: row-edit/row-delete render with a real, CSS-verified gap and a right-aligned actions column (ENTTBL-08); the AlertDialog's keyboard cancel (Escape) and keyboard confirm (Tab+Enter) paths both work and leave focus in the correct, empirically-checked place; the AlertDialog cannot be dismissed while a real delete transact is in flight; zero native dialog fires anywhere in this file; and the restricted/read-only capability classes still show zero delete affordance against a genuinely-seeded row.</done>
</task>

<task type="auto">
  <name>Task 2: Full pre-existing-plus-new Playwright suite regression and quality gates</name>

  <precondition>powershell.exe resolves the Outlook Classic COM bridge (`orules.ps1 peek`, per PROJECT.md C-10) from this shell — `bun run test:e2e` runs the `setup`/`authed`/`anon` projects together, several of which perform a real live magic-code round trip.</precondition>

  <read_first>
    - `web/package.json`'s `test:e2e`/`check`/`lint` scripts — the exact commands this task runs, unmodified.
    - `.planning/PROJECT.md` C-12 (zero human UAT — every phase's done-criteria must be Playwright-provable) — the standing constraint this task's fully-automated `<verify>` satisfies.
    - `web/src/lib/entities/EntityScreen.svelte` and `web/e2e/entities-delete-confirmation.spec.ts` (final state after Task 1) — the files this task's raw-color-literal grep sweep re-checks.
  </read_first>

  <files>web/ (read-only — full-suite regression and quality gates, no new files this task)</files>

  <action>
Run the complete pre-existing-plus-new Playwright suite as one sequential invocation: `cd web && bun run test:e2e` (all 3 projects: `setup`, `authed`, `anon` — no `--project` filter, no parallel second invocation, per the project's own established rate-limit convention, per 15-01-SUMMARY.md's documented lesson). This milestone verified a 54-test baseline at the end of Phase 15, unchanged by Plan 16-01 (which modified existing tests' delete steps without adding new ones), plus this plan's own 6 new `entities-delete-confirmation.spec.ts` tests — confirm the transcript reports at least 60 passed and zero failed, zero skipped. A lower passed-count or any skip is a scope-narrowing signal, not a pass — investigate and fix (in `EntityScreen.svelte` or the new spec, as appropriate) before treating this task as done, never explain it away.

Then run `cd web && bun run check` (svelte-check + tsc) and `cd web && bun run lint` (Biome, from the repo root per the script's own `cd ..`) — both must exit 0 with zero new suppressions.

Finally, re-run the raw-color-literal grep gate across every file this phase's two plans touched: `EntityScreen.svelte`, `web/e2e/helpers/delete-confirmation.ts`, and `web/e2e/entities-delete-confirmation.spec.ts` — confirm zero matches for `oklch(`/hex/`rgba(` literals outside `app.css`. Also re-confirm every load-bearing testid this phase introduced or preserved resolves to exactly one source occurrence in `EntityScreen.svelte`: `row-edit`, `row-delete`, `delete-cancel`, `delete-confirm`, `entity-create-start`.
  </action>

  <verify>
    <automated>cd web && bun run test:e2e</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && bun run test:e2e` reports at least 60 passed, 0 failed, 0 skipped.
    - `cd web && bun run check` exits 0.
    - `cd web && bun run lint` exits 0.
    - `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}\b|rgba?\(' web/src/lib/entities/EntityScreen.svelte web/e2e/helpers/delete-confirmation.ts web/e2e/entities-delete-confirmation.spec.ts` exits 0 (no raw color literal found).
    - `grep -c 'data-testid="row-edit"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1; `grep -c 'data-testid="row-delete"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1.
    - `grep -c 'data-testid="delete-cancel"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1; `grep -c 'data-testid="delete-confirm"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1.
    - `grep -c 'window.confirm' web/src/lib/entities/EntityScreen.svelte` returns exactly 0.
  </acceptance_criteria>

  <done>A single sequential `bun run test:e2e` run passes with at least 60 tests green and zero failed/skipped, proving ROADMAP Phase 16's 4 success criteria hold together with zero regression to any pre-existing Playwright coverage across the whole v1.2 milestone to date; `svelte-check`/`tsc` and Biome remain clean; zero raw color literal or duplicate testid exists anywhere this phase touched.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `web/e2e/entities-delete-confirmation.spec.ts` (new) — 6 live tests proving ENTTBL-08 (a real,
  CSS-verified row-action gap and right-aligned actions column) and DELCONF-01 (keyboard cancel/confirm
  paths with empirically-checked focus outcomes, busy-gated dismissal during a live in-flight delete,
  zero native dialogs, and cross-capability-class delete-affordance safety), independent of Plan 16-01's
  own fundos-based tracer proof.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test runner (Playwright, Node) → live InstantDB app (real `db.transact` deletes via keyboard/mouse-driven UI interaction) | Reuses the pre-existing, already-authorized delete path Plan 16-01 proved; Tests D/E genuinely delete `tarefas` records through the UI, cleaned up structurally by the delete itself, not by a separate teardown call. |
| Test runner → InstantDB admin API (`seedInstance`/`deleteInstance`/`deleteAdminRecord`, Test F) | Pre-existing admin-fixture escape hatch (04-01/09-01 precedent), used here only to seed/clean up the two capability-class rows this phase's safety test checks — never a production code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Tampering | New Playwright spec content (`entities-delete-confirmation.spec.ts`, keyboard-driven destructive-action interactions) | low | accept | Test-only code under `web/e2e/`, never bundled into the shipped `web/` build; standard Biome/tsc gates (Task 2) apply to it like every other e2e spec. |
| T-16-07 | Elevation of Privilege | Test F explicitly re-verifies restricted/read-only entities gain no delete affordance | low | accept (mitigated by design) | This is itself the mitigation for T-16-04 (16-01's threat register) — an independent, dedicated proof against a genuinely-seeded row, not just an empty-table absence check. |

No package-manager install occurs in this plan (no new dependency, only a new test file) — the Package
Legitimacy Gate does not apply and no `T-16-SC` entry is needed.
</threat_model>

<verification>
Phase-level regression guard, run after both tasks:

`cd web && bun run test:e2e`

This proves, against the live InstantDB app: (1) the new `entities-delete-confirmation.spec.ts` coverage
passes, proving ENTTBL-08's alignment and DELCONF-01's keyboard/focus/busy-gating requirements
independently of Plan 16-01's own tracer proof; (2) every pre-existing spec (login, shell/nav, entity
tables/forms/dialogs including both this phase's plans' AlertDialog migration, routine job, design-system,
no-leakage) remains green, with at least 60 tests passed and zero failed/skipped; (3) `svelte-check`/`tsc`
and Biome are clean. Every load-bearing/new `data-testid` this phase touched resolves to exactly one source
occurrence, and zero raw color literal exists in any touched file.
</verification>

<success_criteria>
Maps 1:1 to ROADMAP.md Phase 16's 4 success criteria (dedicated-proof half; Plan 16-01 delivered the
implementation half):

1. Row action buttons show consistent alignment and spacing across all 9 entities, with no portal-based
   menu introduced — proven here by Test A's CSS-measured gap/alignment check on `tarefas`, independent
   of Plan 16-01's own fundos-based structural proof.
2. Every pre-existing row-scoped Playwright locator still resolves to exactly one element — proven by
   Task 2's full-suite regression and static testid-count grep gates.
3. Clicking delete opens a shadcn AlertDialog that can be confirmed or canceled via keyboard, with correct
   focus handling — proven by Tests B/C/D's keyboard-driven, empirically-focus-checked paths.
4. Dedicated Playwright coverage proves both the confirm path (record deleted) and the cancel path (record
   retained) through the AlertDialog — proven by Tests C (cancel) and D (confirm), both keyboard-driven,
   plus Test E's busy-gating proof and Test F's capability-class safety proof.

All 4 also hold with zero regression to the pre-existing 54-test suite, proven by Task 2's full-suite run.
</success_criteria>

<output>
Create `.planning/phases/16-entity-screen-row-actions-delete-confirmation/16-02-SUMMARY.md` when done
</output>
