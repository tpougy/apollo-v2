---
phase: 10-entity-form-restyle-feedback
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - web/src/lib/components/ui/select/*
  - web/src/lib/components/ui/separator/*
  - web/src/lib/entities/EntityScreen.svelte
  - web/e2e/entities-form-restyle.spec.ts
  - web/e2e/helpers/form-controls.ts
  - web/package.json
  - web/bun.lock
autonomous: true
requirements: [ENTFRM-01, ENTFRM-03, ENTFRM-04]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Static-option select fields (tipoPrazo/tipoGeracao) and plain relationship link fields render as a shadcn Select (role=combobox trigger, role=listbox/option content), same component for both option sources"
    - "subtarefas' xorLink two-step chooser (parent-type + dynamic target picker) still enforces exactly-one-of-tarefa/ticket after the Select swap, including the unlink-on-parent-switch behavior"
    - "Submitting a form with a missing required field shows the error via a shadcn Alert (not window.alert), blocks submission, and fires zero native browser dialogs"
  artifacts:
    - web/src/lib/components/ui/select/
    - web/src/lib/entities/EntityScreen.svelte (select/links/xorLink branches converted, formError as Alert)
    - web/e2e/entities-form-restyle.spec.ts (templatesRotina smoke test, subtarefas xorLink test, validation-error test)
    - web/e2e/helpers/form-controls.ts (selectByText, openAndReadSelectOptions added)
  key_links:
    - "selectedLinks[link.label] / xorParentType / xorParentId ($state, unchanged) <-> Select.Root value / onValueChange <-> handleSubmit's linkPayload/unlinkPayload construction (unchanged)"
    - "formError ($state, unchanged) <-> Alert[variant=destructive] AlertDescription data-testid=entity-error (testid unchanged from the pre-existing <p>)"
---

<objective>
Convert `EntityScreen.svelte`'s remaining native `<select>` usages — the static-option `select` field kind, plain relationship `links`, and the `subtarefas`-only `xorLink` two-step chooser — to shadcn `Select`, and convert the `formError` inline error from a bare `<p>` to a shadcn `Alert`. Builds directly on 10-01's Dialog wrap and field-kind conversions (same file, same wave-2 dependency).

Purpose: Complete ENTFRM-01 (every field kind now shadcn-driven) and ENTFRM-03 (relationship fields, no Combobox registry entry exists per 10-RESEARCH.md Pitfall 4 — Select is the correct, only choice), and deliver ENTFRM-04's validation-error requirement (inline Alert, zero native dialogs).
Output: Full field-kind coverage in `EntityScreen.svelte`'s create/edit Dialog; `entities-form-restyle.spec.ts` extended with a `templatesRotina` Select smoke test and a `subtarefas` xorLink test; a dedicated validation-error test proving ENTFRM-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-entity-form-restyle-feedback/10-RESEARCH.md
@.planning/phases/10-entity-form-restyle-feedback/10-PATTERNS.md
@.planning/phases/10-entity-form-restyle-feedback/10-01-SUMMARY.md
@web/src/lib/entities/EntityScreen.svelte
@web/src/lib/auth/LoginScreen.svelte
@web/e2e/entities-form-restyle.spec.ts
@web/e2e/entities-ticket-subtarefa.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install select; convert the static-option select branch and plain relationship links to shadcn Select</name>
  <files>web/src/lib/components/ui/select/*, web/src/lib/components/ui/separator/*, web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts, web/e2e/helpers/form-controls.ts, web/package.json, web/bun.lock</files>
  <action>
    From `web/`, run `bunx shadcn-svelte@latest add select --no-deps-install -y` (zero new npm dependency; transitively vendors a `separator` component — expected, not an error, per 10-RESEARCH.md).

    Convert the `f.kind === "select"` branch (static-option fields: `tarefas.tipoPrazo`, `tickets.tipoPrazo`, `templatesRotina.tipoGeracao`) to `Select.Root type="single"` per 10-RESEARCH.md Pattern 3: `value={formValues[f.name] as string}`, `onValueChange={(v) => { formValues[f.name] = v; }}`; `Select.Trigger` carries `id={\`field-${f.name}\`}`/`data-testid={\`field-${f.name}\`}` and shows the current value or `"selecione..."`; `Select.Content` renders one `Select.Item value={opt} label={opt}` per `f.options` entry, text identical to the option's value.

    Convert the `config.links ?? []` rendering (plain relationship fields, e.g. `projetos.fundo`, `etapas.projeto`, `tarefas.etapa`, `templatesRotina.fundo`/`antecessor`, `tickets.fundo`) to the identical `Select.Root` component, option source swapped to `linkOptionsFor(link)` (unchanged function): `Select.Trigger id={\`link-${link.label}\`}`/`data-testid={\`link-${link.label}\`}` showing the currently-selected option's `[link.targetLabelField]` or `"—"`; `Select.Content` renders an empty-value `Select.Item value="" label="—"` when `!link.required` (bits-ui's `hasValue` check is literally `value !== ""`, confirmed safe in 10-RESEARCH.md), then one `Select.Item value={opt.id} label={String(opt[link.targetLabelField] ?? "")}` per option row. `selectedLinks[link.label]` stays the exact same plain-string state — only the widget changes.

    Add two functions to `web/e2e/helpers/form-controls.ts` (created in 10-01): `selectByText(page: Page, testid: string, optionText: string): Promise<void>` — clicks the trigger (`page.getByTestId(testid).click()`) then `page.getByRole("option", { name: optionText, exact: true }).click()` — replaces every `.selectOption(...)` call site across this phase's specs (works identically for static-option enum values and relationship-link labels, since bits-ui's Select renders `role="option"` items either way); `openAndReadSelectOptions(page: Page, testid: string): Promise<string[]>` — opens the trigger, reads `page.getByRole("listbox").getByRole("option").allTextContents()`, presses Escape to close, returns the trimmed texts — replaces every `.locator("option").evaluateAll(...)`/`.allTextContents()` enumeration call site.

    Add ONE lightweight `templatesRotina` smoke test to `entities-form-restyle.spec.ts` (does not duplicate `entities-rotina-log.spec.ts`'s existing full-CRUD/antecessor-self-exclude coverage — that stays exclusively in the pre-existing spec, fixed in this phase's own later plan): create a `fundo` fixture via the CLI in `test.beforeAll`, create a `templatesRotina` with `nome`/`regraCompetencia` filled, `tipoGeracao` selected via `selectByText`, and the `fundo` link selected via `selectByText` on `link-fundo`; assert `openAndReadSelectOptions(page, "field-tipoGeracao")` returns exactly `["du_fixo", "corrido_fixo", "encadeado"]`; assert the created row shows the fundo's `nome`; delete the record and the fundo fixture in `afterAll`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>`field-tipoGeracao`/`field-tipoPrazo` and every `link-${label}` render as a shadcn `Select` (`role="combobox"` trigger, `role="listbox"`/`role="option"` content); the templatesRotina smoke test passes live, proving both the static-option and relationship-link Select paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert subtarefas' xorLink two-step chooser to two Selects; prove the XOR invariant still holds</name>
  <files>web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts</files>
  <action>
    Convert the `config.xorLink` block (only `subtarefas` uses this — choices `tarefa`/`ticket`) to two `Select.Root type="single"` components, same two-step structure preserved exactly: the parent-type chooser keeps `id="xor-parent-type"`/`data-testid="xor-parent-type"`, `value={xorParentType ?? ""}`, `onValueChange={(v) => { xorParentType = v; xorParentId = ""; }}`, with one `Select.Item value={choice.label} label={choice.label}` per `config.xorLink.choices`; the dynamic target picker (rendered only `{#if xorParentType}`) keeps `data-testid={\`link-${xorParentType}\`}`, `value={xorParentId}`, `onValueChange={(v) => { xorParentId = v; }}`, options from `xorOptionsFor(activeXorChoice())` exactly as today, including the empty `"—"` `Select.Item`. Do not introduce any new component for this — same `Select` used everywhere else in this plan.

    Add one `subtarefas` test to `entities-form-restyle.spec.ts`: in `test.beforeAll`, create a `tarefa` and a `ticket` fixture via the CLI (mirroring `entities-ticket-subtarefa.spec.ts`'s own `chainTarefa`/`chainTicket` pattern). Create a subtarefa with `xor-parent-type` set to `"tarefa"` via `selectByText` and `link-tarefa` set to the fixture tarefa's título via `selectByText`; assert the created row shows the tarefa's título and an empty ticket column. Open edit, switch `xor-parent-type` to `"ticket"` via `selectByText` (which resets `xorParentId` per the unchanged handler) and set `link-ticket` to the fixture ticket's título; submit; assert — via the CLI's `apollo subtarefa listar --tarefa-id`/`--ticket-id` (server-side truth, mirroring the pre-existing spec's own verification style) — the record is now linked to the ticket and NOT the tarefa, proving the unlink-on-parent-switch invariant survived the Select conversion. Delete the subtarefa and both fixtures afterward.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>`xor-parent-type` and the dynamic `link-${xorParentType}` picker both render as shadcn Select; switching the parent type on edit correctly unlinks the stale parent (proven via the CLI, not just the DOM) — the pre-existing XOR "exactly one" invariant holds after the restyle (ROADMAP Phase 10 SC3).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert formError to an Alert (ENTFRM-04); add the validation-error / zero-native-dialog test</name>
  <files>web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts</files>
  <action>
    Convert `EntityScreen.svelte`'s `formError` render — currently a bare paragraph inside the form section — to the same `Alert[variant="destructive"]` + `CircleAlert` icon + `AlertDescription` composition `LoginScreen.svelte` already uses for its own `erro` state (import `Alert`/`AlertDescription` from the already-installed `$lib/components/ui/alert`, `CircleAlert` from `@lucide/svelte/icons/circle-alert`). Keep `data-testid="entity-error"` on the `AlertDescription` — unchanged, this testid is shared by both the form-submit error path and the list-load `query.error` render branch; apply the identical Alert treatment to that second branch too for visual consistency (both branches currently render the bare `<p data-testid="entity-error">` pattern). No change to `formError`'s state, assignment sites, or `extractErrorMessage()`.

    Add a validation-error test to `entities-form-restyle.spec.ts`: register `page.on("dialog", (d) => { throw new Error(\`Unexpected native dialog: ${d.message()}\`); })` before interacting, open create on any full-CRUD entity (e.g. `fundos`), submit with the required `field-nome` left blank, and assert: (a) `page.getByTestId("entity-error")` becomes visible with non-empty text inside an element whose ancestor carries `[data-slot="alert"]` with a `destructive` class (mirrors `login-flow.spec.ts`'s own Alert-assertion style), (b) the Dialog stays open (`entity-submit` still present — submission was blocked), (c) the dialog-listener above never fired during the whole interaction (its very presence with no thrown error is the proof — Playwright surfaces a thrown listener error as a test failure automatically).
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>`formError` renders via `Alert[variant=destructive]`, testid `entity-error` unchanged; submitting with a missing required field blocks the transact, shows the Alert, and fires zero native `window.alert`/`confirm` dialogs (ROADMAP Phase 10 SC4) — proven live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (Select-driven relationship/enum fields) → InstantDB `db.transact()` | Unchanged — `selectedLinks`/`xorParentType`/`xorParentId` feed the exact same `linkPayload`/`unlinkPayload` construction in `handleSubmit`, only the widget populating that state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | `Select.Item`/`Select.Trigger` text interpolated from live query rows (`opt[link.targetLabelField]`) and static option arrays | low | mitigate | Svelte's default text interpolation auto-escapes every interpolated value; no `{@html}` introduced anywhere in the Select composition — identical trust boundary to the native `<select>`/`<option>` markup it replaces |
| T-10-04 | Tampering (data integrity) | `subtarefas`' xorLink "exactly one of tarefa/ticket" invariant must survive the Select swap, including unlink-on-parent-switch | medium | mitigate | `xorParentType`/`xorParentId` state and the unlink-on-switch branch inside `handleSubmit` are untouched business logic (phase-wide constraint); Task 2's own test proves the invariant server-side (via the CLI, not just the DOM) after the restyle |
| T-10-05 | Information Disclosure | `formError` moved from a bare `<p>` to a more visually prominent `Alert` | low | mitigate | Renders the exact same already-sanitized `extractErrorMessage()` value; no raw exception object ever reaches the DOM; testid `entity-error` unchanged so no downstream consumer regresses |

</threat_model>

<verification>
`cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed` green at the end of this plan, covering the templatesRotina Select smoke test, the subtarefas xorLink test, and the validation-error/zero-native-dialog test together in one run.
</verification>

<success_criteria>
- Every enum (`select`-kind) and relationship (`links`) field renders via shadcn `Select`, same component for both (ENTFRM-01, ENTFRM-03).
- `subtarefas`' xorLink "exactly one" invariant, including unlink-on-switch, is proven live post-restyle (ROADMAP Phase 10 SC3).
- A missing-required-field submission renders an `Alert`, blocks the transact, and fires zero native browser dialogs (ENTFRM-04, ROADMAP Phase 10 SC4).
</success_criteria>

<output>
Create `.planning/phases/10-entity-form-restyle-feedback/10-02-SUMMARY.md` when done
</output>
