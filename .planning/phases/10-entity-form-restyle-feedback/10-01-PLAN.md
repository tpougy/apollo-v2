---
phase: 10-entity-form-restyle-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/components/ui/dialog/*
  - web/src/lib/components/ui/checkbox/*
  - web/src/lib/components/ui/textarea/*
  - web/src/lib/components/ui/popover/*
  - web/src/lib/components/ui/calendar/*
  - web/src/lib/entities/EntityScreen.svelte
  - web/e2e/entities-form-restyle.spec.ts
  - web/e2e/entities-fundos.spec.ts
  - web/e2e/helpers/form-controls.ts
  - web/package.json
  - web/bun.lock
autonomous: true
requirements: [ENTFRM-01, ENTFRM-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicking 'novo' or the row 'editar' Button on any entity opens a real shadcn Dialog (role=dialog) — the bare <form> block no longer renders outside a Dialog"
    - "text/textarea/number/boolean field kinds render as shadcn Input/Textarea/Input[number]/Checkbox inside the Dialog, with every field-${name} testid preserved exactly"
    - "date field kinds render as a shadcn Popover+Calendar date-picker; picking a day populates the field and the value persists correctly to live InstantDB on save"
    - "entities-fundos.spec.ts (the one pre-existing spec touching only date+checkbox+text fields) is fully green again after the restyle, with its checkbox assertions completely untouched"
  artifacts:
    - web/src/lib/components/ui/dialog/
    - web/src/lib/components/ui/checkbox/
    - web/src/lib/components/ui/textarea/
    - web/src/lib/components/ui/popover/
    - web/src/lib/components/ui/calendar/
    - web/src/lib/entities/EntityScreen.svelte
    - web/e2e/entities-form-restyle.spec.ts
    - web/e2e/helpers/form-controls.ts
  key_links:
    - "mode !== null ($state) <-> Dialog.Root open / onOpenChange <-> cancelForm() (unchanged function body)"
    - "formValues[f.name] YYYY-MM-DD string <-> Popover+Calendar value / onValueChange <-> dateInputValueToIso / isoToDateInputValue (both helpers stay byte-identical)"
---

<objective>
Wrap `EntityScreen.svelte`'s existing create/edit `<form>` block in a shadcn `Dialog`, and convert the `text`/`textarea`/`number`/`boolean`/`date` field-kind branches to shadcn `Input`/`Textarea`/`Input[number]`/`Checkbox`/`Popover+Calendar` — the tracer slice this phase's remaining plans expand from (Select/links/xorLink in 10-02, toast in 10-03, remaining existing-spec fixups in 10-04).

Purpose: Prove the riskiest, most novel piece of this phase — a controlled `Dialog` driven by the pre-existing `mode` state, plus the one structurally-new date-picker composition — end-to-end against the live InstantDB app before building anything else on top of it. Zero business-logic change (ENTFRM-01/02 constraint).
Output: `EntityScreen.svelte`'s Dialog-wrapped form (5 of 6 field-kind branches converted, `select`/`links`/`xorLink` still native — 10-02's job), a new `entities-form-restyle.spec.ts` proving the fundos full-CRUD capability class end-to-end, `entities-fundos.spec.ts` fixed for the one call site the date-picker conversion breaks, and a shared `web/e2e/helpers/form-controls.ts` (`pickDate`) other plans in this phase will extend/reuse.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-entity-form-restyle-feedback/10-CONTEXT.md
@.planning/phases/10-entity-form-restyle-feedback/10-RESEARCH.md
@.planning/phases/10-entity-form-restyle-feedback/10-PATTERNS.md
@web/src/lib/entities/EntityScreen.svelte
@web/src/lib/auth/LoginScreen.svelte
@web/e2e/entities-fundos.spec.ts
@web/e2e/entities-table-restyle.spec.ts
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Install dialog/checkbox; wrap the form in Dialog; convert text/textarea/number/boolean fields</name>
  <files>web/src/lib/components/ui/dialog/*, web/src/lib/components/ui/checkbox/*, web/src/lib/components/ui/textarea/*, web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts, web/package.json, web/bun.lock</files>
  <action>
    From `web/`, run `bunx shadcn-svelte@latest add dialog checkbox textarea --no-deps-install -y` (per 10-RESEARCH.md's Standard Stack: zero new npm dependency — `bits-ui`/`@internationalized/date` already satisfy every registry devDependency these three declare).

    In `EntityScreen.svelte`, wrap the existing `{#if mode !== null}<form onsubmit={handleSubmit}>...</form>{/if}` block (currently lines ~427-565) in `Dialog.Root` bound `open={mode !== null}`, with `onOpenChange` calling `cancelForm()` when closing to false — reuse `cancelForm()` byte-identical, do not touch its body. Put the `<form>` inside `Dialog.Content class="sm:max-w-lg max-h-[85vh] overflow-y-auto"`, with a `Dialog.Header`/`Dialog.Title` showing `{mode === "create" ? "Novo" : "Editar"} — {config.titulo}`. Keep the existing `entity-cancel` Button inside the form as a second explicit close path (Dialog's own close-X already routes through the same `onOpenChange` per 10-RESEARCH.md Pattern 1 — both must keep working). Import `Dialog` per shadcn-svelte's standard namespace-import convention (`import * as Dialog from "$lib/components/ui/dialog"`).

    Convert the `f.kind === "text"`/`"textarea"`/`"number"` branches to the already-installed `Input`/new `Textarea`/`Input type="number"` components, preserving `id={\`field-${f.name}\`}`, `data-testid={\`field-${f.name}\`}`, `required`, and the exact `oninput` value-assignment logic per branch (string passthrough for text/textarea, `e.currentTarget.valueAsNumber` with the `""`-sentinel guard for number) — 1:1 element swap only, per 10-PATTERNS.md's table.

    Convert the `f.kind === "boolean"` branch to shadcn `Checkbox` per 10-RESEARCH.md Pattern 2: `checked={Boolean(formValues[f.name])}`, `onCheckedChange={(v) => { formValues[f.name] = v === true; }}` (collapses bits-ui's `"indeterminate"` union member to `false`, which this app never produces), keeping `id`/`data-testid` on the `Checkbox` itself.

    Swap every `<label for=...>` in the form for the already-installed `Label` component (same idiom as `LoginScreen.svelte`'s `<Label for="login-email">`).

    Create `web/e2e/entities-form-restyle.spec.ts` (new file, runs in the default `authed` project — no `playwright.config.ts` edit needed, same as `entities-table-restyle.spec.ts`'s own precedent). Mirror `entities-fundos.spec.ts`'s CLI-fixture/sweep/cleanup discipline exactly (own `apolloCli` helper, `phase10-e2e-` prefix, `beforeEach`/`afterEach` sweep). Write one test, `"ENTFRM-01: fundos (full-CRUD) — Dialog role, text/checkbox fields render and persist"`: navigate to fundos, click `entity-create-start`, assert `page.getByRole("dialog")` is visible, fill `field-nome`/`field-codigo`, check `field-ativo`, fill `field-createdAt` via a plain `.fill("2026-01-15")` for now (the native date input still exists at this point in the task sequence — Task 2 below converts it and updates this one line), submit, assert the row appears with the right `data-eid`, edit `field-nome`, resubmit, assert the edit persisted, delete, assert the row is gone. This test intentionally does not yet cover the date-picker interaction — Task 2 extends it.
  </action>
  <verify>
    <automated>cd web && git diff --stat package.json bun.lock | grep -c 'package.json\|bun.lock' -- ; bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>`bunx shadcn-svelte@latest add dialog checkbox textarea` ran with zero new npm dependency added; opening create/edit on `fundos` shows a real `role="dialog"` element; `field-nome`/`field-codigo`/`field-ativo` render as Input/Input/Checkbox and the new spec's fundos test passes live against InstantDB.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install popover/calendar; convert the date field kind to a Popover+Calendar date-picker</name>
  <files>web/src/lib/components/ui/popover/*, web/src/lib/components/ui/calendar/*, web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts, web/e2e/helpers/form-controls.ts, web/package.json, web/bun.lock</files>
  <action>
    From `web/`, run `bunx shadcn-svelte@latest add popover calendar --no-deps-install -y` (zero new npm dependency, same rationale as Task 1).

    Convert the `f.kind === "date"` branch in `EntityScreen.svelte` to the exact `Popover.Root` + `Popover.Trigger` (wrapping a `Button` via the `child` snippet) + `Popover.Content` + `Calendar type="single"` composition from 10-RESEARCH.md Pattern 4 — this is the one place in this phase the `child`-snippet/`asChild`-equivalent pattern is genuinely required (omitting it puts `data-testid`/`id` on the wrong, invisible outer button). Import `parseDate`/`DateFormatter`/`getLocalTimeZone`/`type DateValue` from `@internationalized/date` (already installed, zero version bump) and the `calendar` icon from `@lucide/svelte/icons/calendar`. `formValues[f.name]` stays a plain `"YYYY-MM-DD"` string exactly as today — do NOT change `isoToDateInputValue`/`dateInputValueToIso` (lines 21-27), the round trip through `@internationalized/date`'s `parseDate`/`CalendarDate.toString()` produces the identical string shape, confirmed in 10-RESEARCH.md's "Critical ISO-conversion finding". Add a module-level `let datePopoverOpen = $state<Record<string, boolean>>({})` to close the popover after a day is picked, per the Pattern 4 code example.

    Create `web/e2e/helpers/form-controls.ts` (new file, mirrors `web/e2e/helpers/magic-code.ts`'s existing style — plain exported async functions, no class). Add `pickDate(page: Page, testid: string): Promise<string>`: clicks the date-picker trigger Button (`page.getByTestId(testid).click()`), then clicks the visible text "15" inside `page.locator('[data-slot="popover-content"]')` (per 10-RESEARCH.md's "Concrete Playwright locator strategies" — day "15" of whichever month the Calendar currently displays can never collide with an adjacent-month overflow cell in a 6-week grid, per Assumption A4; deliberately does not navigate to a specific past/future month, and deliberately avoids the day cell's locale-dependent `aria-label`), and returns the resulting `"YYYY-MM-DD"` string (current year/month, day `15`) so callers assert against the actual picked value instead of a hardcoded literal.

    Extend the fundos test in `entities-form-restyle.spec.ts` (from Task 1): replace the placeholder `.fill("2026-01-15")` on `field-createdAt` with `const createdAtValue = await pickDate(page, "field-createdAt");`, and after the create submit + row-visible assertion, reload and assert the row's date cell contains `createdAtValue` (mirrors `entities-projeto-etapa-tarefa.spec.ts`'s own date-persistence-check pattern) — proving ENTFRM-02's "the record persists that date correctly to live InstantDB on save".
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>Clicking the `field-createdAt` trigger opens `[data-slot="popover-content"]`; picking a day sets the field and the exact picked date round-trips through a real `db.transact()`/reload cycle; `pickDate` is exported from `web/e2e/helpers/form-controls.ts` for reuse by later plans in this phase.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix entities-fundos.spec.ts's date-fill call site (Pitfall 2); leave checkbox lines untouched (Pitfall 3)</name>
  <files>web/e2e/entities-fundos.spec.ts</files>
  <action>
    In `web/e2e/entities-fundos.spec.ts`, replace line 122's `await page.getByTestId("field-createdAt").fill("2026-01-15");` with `await pickDate(page, "field-createdAt");` (import `pickDate` from `./helpers/form-controls.ts`) — the date field is now a Calendar/Popover trigger Button, not a fillable input, per 10-RESEARCH.md Pitfall 2. Do not modify anything else in this file: the `.isChecked()`/`.check()`/`.uncheck()` calls on `field-ativo` (lines 124, 147) need zero changes per Pitfall 3 — Playwright's checkbox helpers are ARIA-role-aware (`role="checkbox"` + `aria-checked`), not element-type-aware, so the restyled `Checkbox` satisfies them identically to the old native `<input type="checkbox">`. Fundos has no `select`/`links`/`xorLink` fields, so this file needs no other change this phase.
  </action>
  <verify>
    <automated>cd web && grep -c 'isChecked\|\.check()\|\.uncheck()' e2e/entities-fundos.spec.ts && bunx playwright test e2e/entities-fundos.spec.ts e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>`entities-fundos.spec.ts` passes green end-to-end against the restyled Dialog/Checkbox/Calendar markup; its checkbox assertions are byte-identical to before this phase; `bun run test:e2e -- entities-fundos entities-form-restyle` (or the two targeted commands above) reports zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (Svelte SPA form/Dialog) → InstantDB `db.transact()` | Unchanged from pre-existing app — this plan only changes the widgets that populate the payload, never the payload construction or the transact call itself |
| Test runner (Playwright, live) → InstantDB hosted app | New/updated e2e specs perform real `db.transact()`/query round trips against the live backend, same trust boundary every prior-phase spec already crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | Date value flowing from `Calendar`'s `onValueChange` into `formValues[f.name]` and then `dateInputValueToIso()` | low | mitigate | Value stays a plain `"YYYY-MM-DD"` string produced by `CalendarDate.toString()`, rendered only via Svelte's auto-escaped text interpolation — no `{@html}` anywhere in the date-picker composition, so no new injection surface versus the native `<input type="date">` it replaces |
| T-10-02 | Information Disclosure | `entities-form-restyle.spec.ts` executes real live `db.transact()` calls against the production-shaped InstantDB app | low | accept | Identical risk profile to every pre-existing e2e spec in this suite (Phases 4-9 precedent); test data is `phase10-e2e-`-prefixed and swept in `beforeEach`/`afterEach`, same discipline as `entities-fundos.spec.ts` |
| T-10-SC | Tampering (supply chain) | New component source pulled from the shadcn-svelte registry via `bunx shadcn-svelte add dialog checkbox textarea popover calendar` | low | mitigate | Confirmed zero new npm dependency this plan — `bits-ui`/`@internationalized/date` (already installed and legitimacy-audited in Phase 8) satisfy every declared registry devDependency, per 10-RESEARCH.md's Package Legitimacy Audit; no `[ASSUMED]`/`[SUS]` package introduced, no human checkpoint required |

</threat_model>

<verification>
Run `cd web && bun run test:e2e -- --grep "ENTFRM-01|WEB-02"` (or targeted `bunx playwright test e2e/entities-fundos.spec.ts e2e/entities-form-restyle.spec.ts --project=authed`) at the end of this plan's execution — both files green against the live InstantDB app. `bunx shadcn-svelte@latest add dialog checkbox textarea popover calendar --no-deps-install -y` must have produced zero diff to `web/package.json`'s dependency versions beyond the components themselves (confirmed via `git diff web/package.json`).
</verification>

<success_criteria>
- Every 9-entity create/edit flow opens a real shadcn `Dialog` (`role="dialog"`) instead of the old bare `<form>` — proven live for `fundos`.
- `text`/`textarea`/`number`/`boolean`/`date` field kinds render via `Input`/`Textarea`/`Input[number]`/`Checkbox`/`Popover+Calendar`, every `field-${name}` testid preserved exactly.
- A date picked via the Calendar persists the correct value to live InstantDB on save (ENTFRM-02, ROADMAP Phase 10 SC2).
- `entities-fundos.spec.ts` is fully green post-restyle with zero changes to its checkbox assertions.
</success_criteria>

<output>
Create `.planning/phases/10-entity-form-restyle-feedback/10-01-SUMMARY.md` when done
</output>
