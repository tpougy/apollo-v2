---
phase: 24-packaging-installability
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - cli/tests/test_packaging_live.py
  - cli/pyproject.toml
  - cli/README.md
autonomous: true
requirements: [PKG-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`uv build` produces a wheel for `cli/`, and installing it — both via a scratch venv in the automated pytest gate and via a real `uv tool install` — succeeds in an isolated environment with no `apollo-v2` monorepo present."
    - "Running `apollo --version`, `apollo doctor`, and `apollo fundo listar` from a directory outside `apollo-v2`, with no `shared/` and no `.env.instantdb`, all complete without a crash, and `doctor` reports the embedded-default app-id source."
    - "`uv tool uninstall apollo-cli` fully removes the installed tool, confirmed by `uv tool list` reporting no tools installed."
    - "The admin-token confinement structural gates (`test_auth_rejection.py`, `test_instant_client.py`) pass unmodified — the new `_DEFAULT_APP_ID` constant from Plan 24-01 required zero exemption-list changes."
    - "`ruff check`, `ruff format --check`, and `ty check` are all clean across `cli/` and `../shared/scripts`, and the full `uv run pytest` suite passes (skipping only tests that legitimately need a live session that isn't present)."
  artifacts:
    - "cli/tests/test_packaging_live.py — automated live packaging round-trip proof (PKG-05), marked `packaging`"
    - "cli/pyproject.toml — `packaging` pytest marker registered"
    - "cli/README.md — doctor/install sections updated to describe the embedded-default fallback and global `uv tool install`"
    - ".planning/phases/24-packaging-installability/24-02-SUMMARY.md — captured verbatim transcript of the real `uv tool install`/`uninstall` acceptance round trip"
  key_links:
    - "test_packaging_live.py's subprocess chain (`uv build` -> `uv venv`/`uv pip install` -> run outside repo) is the automated, permanent regression proof that PKG-05 does not silently regress in a future phase — it exercises the exact code Plan 24-01 changed."
    - "The real `uv tool install --force`/`uv tool uninstall apollo-cli` sequence is the one live, human-legible acceptance proof this phase's requirement text explicitly demands ('comprovado via instalação real em ambiente isolado, não apenas inspeção estática')."
---

<objective>
Formalize Plan 24-01's live scratch-venv proof into a permanent, automated pytest regression test
(`test_packaging_live.py`, PKG-05), confirm the admin-token confinement gates needed zero changes, run the
full `cli/` quality gate (ruff/ty/pytest), and — as this phase's headline, literally-required acceptance
proof — execute the real `uv tool install --force` / run-outside-the-repo / `uv tool uninstall apollo-cli`
round trip once, for real, capturing its exact output.

Purpose: Plan 24-01 fixed the code and proved it once via a scratch venv; this plan proves it stays proved
(a permanent pytest gate) and delivers the phase's own literal acceptance bar — a real global tool install
outside the monorepo, verified live, not described.
Output: `cli/tests/test_packaging_live.py`, a registered `packaging` pytest marker, an updated
`cli/README.md`, and a `24-02-SUMMARY.md` containing the verbatim transcript of the real install/uninstall
round trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-packaging-installability/24-CONTEXT.md
@.planning/phases/24-packaging-installability/24-RESEARCH.md
@cli/pyproject.toml
@cli/README.md
@cli/tests/test_auth_rejection.py
@cli/tests/test_instant_client.py
@cli/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated live packaging regression test + admin-token gate confirmation</name>
  <files>cli/tests/test_packaging_live.py, cli/pyproject.toml</files>
  <action>
    Register a new `packaging` marker in `cli/pyproject.toml`'s `[tool.pytest.ini_options] markers` list
    (one new string, alongside the existing `live` entry), documented as exercising real subprocess
    `uv build`/`uv venv`/`uv pip install` filesystem/build tooling — not the InstantDB network API (per
    RESEARCH.md Open Question 2's resolution: a distinct marker keeps `live`'s meaning precise and lets
    `uv run pytest -m "not live and not packaging"` stay a fast, fully-offline inner loop).

    Create `cli/tests/test_packaging_live.py` per RESEARCH.md's Code Example (adapt it — do not paste
    verbatim; match this project's convention of a docstring stating intent and reusing `find_repo_root()`
    to locate `cli/`): set `pytestmark = pytest.mark.packaging`. Add
    `test_installed_wheel_runs_outside_repo_with_no_shared_or_env_file(tmp_path)` that: runs
    `uv build --out-dir <tmp_path>/dist` with `cwd=find_repo_root() / "cli"`; locates the produced `.whl`
    via `glob`; runs `uv venv <tmp_path>/venv --python 3.12`; runs
    `uv pip install --python <tmp_path>/venv/bin/python <wheel>`; runs the installed `apollo` binary's
    `--version` and `doctor` subcommands with `cwd=<tmp_path>/outside` (a directory created under
    `tmp_path`, guaranteed outside `apollo-v2`), asserting `returncode == 0` for both and that `doctor`'s
    captured stdout contains `"embedded"` case-insensitively (proves the provenance line reports the
    embedded default); runs `apollo fundo listar` from the same outside directory, asserting
    `returncode == 1` and that `stderr` parses as JSON with `error == "no_session"` (proves no import-time
    crash — matches `test_auth_rejection.py`'s existing `no_session` contract, not a live-data assertion).
    Use `subprocess.run(..., capture_output=True, text=True, check=True)` for `--version`/`doctor` and
    `check=False` for the deliberately-nonzero-exit `fundo listar` call.

    Then, as a confirmation step (not a code edit — RESEARCH.md's Security Domain section flags this
    explicitly), run the two pre-existing admin-token-confinement structural gates —
    `tests/test_auth_rejection.py::test_admin_token_confinement` and
    `tests/test_instant_client.py::test_admin_token_key_confined_to_exempt_modules` — to prove Plan
    24-01's new `_DEFAULT_APP_ID` constant required zero changes to either test's exemption list
    (`_ADMIN_TOKEN_EXEMPT_FILENAMES`/`_EXEMPT_FILENAMES`, both already covering `config.py` for their own,
    unrelated reasons). Do not add `config.py`/`bizdays.py` to any new exemption list — if either gate
    fails, that is a real regression to fix, not a gate to loosen.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run pytest tests/test_packaging_live.py -v -m packaging && uv run pytest tests/test_auth_rejection.py tests/test_instant_client.py -k "admin_token_confinement or admin_token_key_confined_to_exempt_modules" -v</automated>
  </verify>
  <done>
    `cli/tests/test_packaging_live.py` exists, is marked `packaging`, and passes for real — genuine
    `uv build`/`uv venv`/`uv pip install` subprocess calls, no mocking. The `packaging` marker is
    registered in `cli/pyproject.toml` (no `PytestUnknownMarkWarning`). Both admin-token confinement gates
    pass with zero changes made to `test_auth_rejection.py`/`test_instant_client.py` in this plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final quality gate + real `uv tool install` acceptance round trip + README update</name>
  <files>cli/README.md</files>
  <action>
    Run and confirm green, from `cli/`: `uv run ruff check --config pyproject.toml . ../shared/scripts`,
    `uv run ruff format --check --config pyproject.toml . ../shared/scripts`,
    `uv run ty check . ../shared/scripts`, and the full `uv run pytest` (all markers, including
    `live`/`packaging` — tests needing a live session skip cleanly when absent per `conftest.py`'s
    `live_session` fixture; this run must show 0 failures, skips are acceptable).

    Then perform the phase's headline live acceptance proof exactly once, for real: from `cli/`,
    `uv build --out-dir /tmp/apollo24-final-dist`; `uv tool install --force
    /tmp/apollo24-final-dist/apollo_cli-*.whl` (the tool name is `apollo-cli`, the console script is
    `apollo` — do not confuse the two when uninstalling); from a directory outside `apollo-v2` (e.g.
    `/tmp`), run `apollo --version`, `apollo doctor`, and `apollo fundo listar`, capturing output and exit
    codes for all three; then `uv tool uninstall apollo-cli`; then `uv tool list` and confirm it reports no
    tools installed (this `uv` version prints exactly `"No tools installed"`); finally
    `rm -rf /tmp/apollo24-final-dist`. Capture every command's exact stdout/stderr from this real round
    trip verbatim into this plan's `24-02-SUMMARY.md` — per PROJECT.md's live-verification convention, this
    transcript IS the PKG-05 acceptance proof, not a description of it.

    Per CONTEXT.md decisions 7-8: this plan touches only `cli/tests/`, `cli/pyproject.toml` (test-marker
    registration only, not a dependency change), and `cli/README.md` — no `web/`, schema, or
    `shared/anbima-calendar.json` edit, and no new runtime or dev dependency added anywhere.

    Update `cli/README.md`: in the doctor-description paragraph under `## Run`, replace the sentence
    describing `.env.instantdb` as always resolvable with one describing the new fallback (`.env.instantdb`/
    `APOLLO_ENV_FILE` override an embedded public default `app_id`, so `doctor`/every subcommand works even
    with no reachable `.env.instantdb`). Add a short new subsection under `## Install` titled
    `Global install (uv tool install)` documenting the `uv build`/`uv tool install`/`uv tool uninstall
    apollo-cli` sequence above as the supported way to run `apollo` outside the monorepo. Do not alter the
    `## About the admin token` section's substance — only the doctor-description paragraph and the new
    install subsection change.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run ruff check --config pyproject.toml . ../shared/scripts && uv run ruff format --check --config pyproject.toml . ../shared/scripts && uv run ty check . ../shared/scripts && uv run pytest -q && rm -rf /tmp/apollo24-final-dist && uv build --out-dir /tmp/apollo24-final-dist && uv tool install --force /tmp/apollo24-final-dist/apollo_cli-*.whl && ( cd /tmp && apollo --version && apollo doctor && ( apollo fundo listar; echo "listar_exit=$?" ) ) ; uv tool uninstall apollo-cli && uv tool list && rm -rf /tmp/apollo24-final-dist</automated>
  </verify>
  <done>
    `ruff check`/`ruff format --check`/`ty check` all report zero errors across `cli/` and
    `../shared/scripts`. The full `uv run pytest` suite passes (0 failures; live-session-dependent tests
    skip cleanly if no session is persisted). `uv tool install --force`/`apollo --version`/`apollo doctor`/
    `apollo fundo listar`/`uv tool uninstall apollo-cli`/`uv tool list` all ran for real from outside
    `apollo-v2`, with the exact captured output pasted into `24-02-SUMMARY.md`; `uv tool list` afterward
    shows no tools installed. `cli/README.md`'s doctor paragraph and new "Global install" subsection are in
    place and accurate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine's global `uv tool` registry ↔ this plan's verification | The real `uv tool install --force`/`uv tool uninstall` round trip is the only step in this phase that touches state outside a scratch tmp directory — it must leave the machine exactly as it found it. |
| Test suite ↔ admin-token confinement gates | `test_auth_rejection.py`/`test_instant_client.py`'s AST-walk exemption lists are the one place a future edit could silently widen the admin-token blast radius; this plan only reads/runs them, never edits them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | Confirming `test_auth_rejection.py::test_admin_token_confinement` / `test_instant_client.py::test_admin_token_key_confined_to_exempt_modules` still pass unmodified after Plan 24-01's edits | critical | mitigate | Task 1 runs both AST-walk gates explicitly (targeted `-k` selection) and requires green with zero changes to either file's exemption list — proving `_DEFAULT_APP_ID` never required loosening the admin-token confinement gate. |
| T-24-02-02 | Tampering | The real `uv tool install`/`uv tool uninstall` round trip could leave the developer machine's global `uv tool` registry in an unexpected state if a step aborts mid-sequence | low | accept | The sequence always ends with `uv tool uninstall apollo-cli` and a `uv tool list` verification in the same task; `--force` cleanly replaces any prior `apollo-cli` install rather than erroring, and cleanup runs unconditionally per the task's own instructions. |
| T-24-02-03 | Repudiation | Without a captured transcript, a future reader cannot verify PKG-05's live acceptance round trip actually ran, versus being described only | medium | mitigate | Task 2 captures the exact stdout/stderr of every command in the manual round trip verbatim into `24-02-SUMMARY.md`, per PROJECT.md's standing live-verification convention. |

</threat_model>

<verification>
Run Task 1's automated packaging pytest and the two targeted admin-token gate tests. Run Task 2's full
quality-gate command chain and the real `uv tool install`/`uninstall` round trip exactly as scripted — all
are real, live commands, never mocked. Confirm `uv tool list` reports no tools installed after Task 2
completes (leave the developer machine clean). Confirm `git diff --stat` for this plan touches only the
three files in `files_modified` above.
</verification>

<success_criteria>
- `cli/tests/test_packaging_live.py` exists, passes for real, and is marked `packaging`.
- `cli/pyproject.toml` registers the `packaging` marker.
- The admin-token confinement gates pass with zero exemption-list changes anywhere in this plan.
- `ruff`/`ty`/full `pytest` are all clean on `cli/` + `../shared/scripts`.
- A real `uv tool install --force`/run-outside-the-repo/`uv tool uninstall apollo-cli` round trip is
  executed once, its exact output captured verbatim in `24-02-SUMMARY.md`, and `uv tool list` confirms a
  clean machine state afterward.
- `cli/README.md` accurately documents the embedded-default fallback and the global install path.
</success_criteria>

<output>
Create `.planning/phases/24-packaging-installability/24-02-SUMMARY.md` when done
</output>
