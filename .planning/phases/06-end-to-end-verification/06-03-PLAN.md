---
phase: 06-end-to-end-verification
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - .planning/phases/06-end-to-end-verification/verify-phase-06.sh
  - .planning/phases/06-end-to-end-verification/06-03-VERIFICATION-EVIDENCE.md
  - README.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05]
user_setup: []

must_haves:
  truths:
    - "One command re-proves every requirement of Phases 1 through 6 and prints PHASE 06 VERIFIED only when all of them pass."
    - "A record created/edited/deleted via the CLI is reflected in the SPA and vice versa, for at least one entity per Phase 3 category, re-executed (not merely claimed) by that command."
    - "ruff, ruff format and ty run clean across every .py file in cli/ AND shared/scripts/."
    - "The web/ formatter, linter, svelte-check and build run clean across every file in web/."
    - "The VERIFY-04 interrupted-job gate and the VERIFY-05 cross-user isolation gate both run inside that same command and fail it loudly when absent or broken."
    - "No gate can pass by being skipped: a missing prerequisite (CLI session, Playwright storageState, second-user session) fails the run with an actionable message."
  artifacts:
    - path: ".planning/phases/06-end-to-end-verification/verify-phase-06.sh"
      provides: "The single 'is Apollo v2 v1 done' gate composing verify-phase-01..05 plus the new VERIFY-04/VERIFY-05 gates"
      contains: "PHASE 06 VERIFIED"
      min_lines: 150
    - path: ".planning/phases/06-end-to-end-verification/06-03-VERIFICATION-EVIDENCE.md"
      provides: "Recorded output of the full green run, including the --final second-user cleanup pass"
    - path: "README.md"
      provides: "Operator documentation for the v1 done gate"
      contains: "verify-phase-06.sh"
  key_links:
    - from: "verify-phase-06.sh"
      to: "verify-phase-01.sh .. verify-phase-05.sh"
      via: "bash invocation of each prior phase script in numeric order"
      pattern: "verify-phase-0[1-5]\\.sh"
    - from: "verify-phase-06.sh"
      to: "cli/tests/test_interrupted_job.py"
      via: "uv run --project cli pytest ... -m live"
      pattern: "test_interrupted_job"
    - from: "verify-phase-06.sh"
      to: "cli/tests/test_cross_user_isolation.py"
      via: "uv run --project cli pytest ... -m live"
      pattern: "test_cross_user_isolation"
    - from: "verify-phase-06.sh"
      to: "web/e2e cross-channel specs"
      via: "asserting the four VERIFY-01 spec filenames appear in the Playwright run output"
      pattern: "entities-fundos\\.spec\\.ts"
---

<objective>
Produce the single command that answers "is Apollo v2 v1 done?" — composing the five already-proven per-phase verification scripts with the two new gates from plans 06-01 and 06-02, and asserting VERIFY-01/02/03 explicitly rather than trusting that a composed script happened to cover them.

Purpose: VERIFY-01 (cross-channel parity), VERIFY-02 (Python quality gates), VERIFY-03 (web quality gates), plus re-assertion of VERIFY-04 and VERIFY-05 inside the same gate. This is the final plan of the final phase of the v1 milestone.
Output: `verify-phase-06.sh`, a recorded green full run including the `--final` second-user cleanup pass, and operator documentation in `README.md`.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-end-to-end-verification/06-CONTEXT.md
@.planning/phases/06-end-to-end-verification/06-RESEARCH.md
@.planning/phases/06-end-to-end-verification/06-01-PLAN.md
@.planning/phases/06-end-to-end-verification/06-02-PLAN.md

@.planning/phases/05-idempotent-routine-instance-job/verify-phase-05.sh
@.planning/phases/03-cli-auth-crud/verify-phase-03.sh
@README.md
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do NOT go exploring. -->

The five prior verification scripts, all `set -euo pipefail`, all resolving `REPO_ROOT` from `SCRIPT_DIR`, all documented "safe to run twice in a row, from any cwd":
- `.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` — SETUP-01..08; runs `bun run build`, `(cd cli && uv run ruff check .)`, `ruff format --check .`, `ty check`, `bun run lint|format:check|check`, `bun run instant:verify`, `guest-write-check.mjs`
- `.planning/phases/02-shared-anbima-calendar/verify-phase-02.sh` — CAL-01..05; runs `update_calendar.py --check`, plus `ruff check/format --check` and `ty check` over `. ../shared/scripts`
- `.planning/phases/03-cli-auth-crud/verify-phase-03.sh` — CLI-01..11; its "Quality gates" section runs `(cd cli && uv run ruff check --config pyproject.toml . ../shared/scripts)`, `ruff format --check --config pyproject.toml . ../shared/scripts`, `ty check . ../shared/scripts` — this is the VERIFY-02 scope
- `.planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh` — WEB-01..10; runs `(cd web && bun run check|lint|format:check|build|test)` — this is the VERIFY-03 scope; guards on `web/e2e/.auth/user.json`
- `.planning/phases/05-idempotent-routine-instance-job/verify-phase-05.sh` — JOB-01/02; Gates 1-7, including the live Playwright `authed` run filtered `-g "routine job|cross-channel"` and a `phase05-` leftover gate; final line `PHASE 05 VERIFIED`

Conventions to copy from `verify-phase-05.sh` in shape:
- Header docstring stating what the script proves and what it deliberately does NOT prove
- `SCRIPT_DIR`/`REPO_ROOT` resolution then `cd "${REPO_ROOT}"`
- `WITH_MAGIC_CODE="${VERIFY_MAGIC_CODE:-0}"` plus a `for arg in "$@"` flag loop
- Numbered `-- Gate N: ...` echoes and `echo "Gate N: PASS"` after each
- Playwright output captured to a `mktemp` file with `trap ... EXIT` cleanup, `cat`ed, then `grep -qF` asserting each required spec filename appeared (the anti-silent-skip pattern)
- Leftover gates shaped `LEFTOVERS="$(... listar | grep -c 'phaseNN-' || true)"; if [ "${LEFTOVERS}" != "0" ]; then ...; exit 1; fi`

The four e2e specs that constitute the VERIFY-01 evidence (RESEARCH Pitfall 1 — all already exist; do NOT write new ones):
- `web/e2e/entities-fundos.spec.ts` — test `"SC-3: a fundo created by the CLI is visible in the SPA"` (full-CRUD category, CLI→SPA)
- `web/e2e/entities-ticket-subtarefa.spec.ts` — SPA→CLI via `apollo subtarefa listar --tarefa-id/--ticket-id` (full-CRUD category, reverse direction)
- `web/e2e/entities-rotina-log.spec.ts` — the `WEB-09` test (log-only category, CLI→SPA)
- `web/e2e/routine-job-cross-channel.spec.ts` — both directions for the `instanciasRotina` category
Playwright projects: `setup` (real magic-code login, writes `web/e2e/.auth/user.json`), `authed`, `anon`.

New gates introduced by this phase:
- `uv run --project cli pytest cli/tests/test_interrupted_job.py -m live` (VERIFY-04, plan 06-02)
- `uv run --project cli pytest cli/tests/test_cross_user_isolation.py -m live` (VERIFY-05, plan 06-01), reading `cli/.auth/second-user-session`, with opt-in teardown via `APOLLO_VERIFY05_DELETE_SECOND_USER=1`
</interfaces>

<scope_notes>
**Compose, do not reinvent** (06-CONTEXT.md's explicit instruction and RESEARCH's primary recommendation). VERIFY-01, VERIFY-02 and VERIFY-03 need ZERO new tests, lint configs or e2e specs — they need re-execution plus an explicit assertion that the re-execution actually covered them. The one thing this plan adds beyond invocation is **anti-silent-coverage assertions**: proving the four cross-channel specs really ran, and re-asserting the Python and web quality-gate command sets at Phase 6's own level, so a future edit to `verify-phase-03.sh` or `verify-phase-04.sh` cannot quietly narrow VERIFY-02/VERIFY-03's scope without failing this gate.

**Do not pass `--with-magic-code` on the default run.** Each prior script's docstring recommends reusing the persisted CLI session and Playwright `storageState`; a magic-code email expires in 60-90 s (PROJECT.md C-10) and re-sending on every composed run is needless churn. Expose it as an opt-in pass-through flag only.
</scope_notes>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase-06.sh — compose Phases 1-5 and pin VERIFY-01/02/03 explicitly</name>
  <files>.planning/phases/06-end-to-end-verification/verify-phase-06.sh</files>
  <read_first>
    - `.planning/phases/05-idempotent-routine-instance-job/verify-phase-05.sh` in full (the structural template for this script)
    - `.planning/phases/03-cli-auth-crud/verify-phase-03.sh` "Quality gates" section (the exact VERIFY-02 command set, including `--config pyproject.toml` and the `../shared/scripts` argument, and the zero-suppressions check)
    - `.planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh` (the VERIFY-03 command set and the `AUTH_STATE_FILE` guard)
    - `.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` and `.planning/phases/02-shared-anbima-calendar/verify-phase-02.sh` (confirm both are cwd-independent and safe to re-run)
    - `06-RESEARCH.md` "Composing the five prior verify scripts" and Pitfall 1
  </read_first>
  <action>
    Create `.planning/phases/06-end-to-end-verification/verify-phase-06.sh`, executable (`chmod +x`), `set -euo pipefail`, resolving `REPO_ROOT` from `SCRIPT_DIR` and `cd`-ing there. Header docstring in `verify-phase-05.sh`'s style, stating: this is the single "is Apollo v2 v1 done" gate for the whole milestone; it re-proves VERIFY-01 through VERIFY-05 plus every requirement of Phases 1-5 by composition; it reuses persisted sessions and never sends a magic-code email on a normal run; it exits 0 printing `PHASE 06 VERIFIED` as its final line only when every gate passes.

    Flags: `--with-magic-code` / `VERIFY_MAGIC_CODE=1` passed through verbatim to `verify-phase-04.sh` and `verify-phase-05.sh`; `--final` / `VERIFY_FINAL=1` reserved for Task 2's second-user cleanup; `--skip-composed` / `VERIFY_SKIP_COMPOSED=1` which skips Gates 1-5 for fast iteration on the new gates ONLY — in that mode the script must print a loud `WARNING: composed phases skipped -- this run does NOT certify v1` line and must NOT print `PHASE 06 VERIFIED`, so a partial run can never be mistaken for a green milestone.

    Gate 0 — preflight, failing fast with actionable messages before burning minutes: assert `uv`, `bun` and `jq` are on PATH; assert `~/.config/apollo-cli/session` exists and `uv run --project cli apollo auth whoami` exits 0 reporting `tp@rbrasset.com.br`; assert `web/e2e/.auth/user.json` exists (hint: run `bun run test:e2e:auth`); assert `cli/.auth/second-user-session` exists (hint: the bootstrap command from plan 06-01 Task 1). Every one is a hard failure — never a skip.

    Gates 1-5 — run `verify-phase-01.sh` through `verify-phase-05.sh` in numeric order, each path resolved relative to `REPO_ROOT`, failing loudly if a path does not resolve rather than silently skipping a phase. Capture each script's output to a `mktemp` file with a `trap` cleanup, `cat` it, and assert its final line matches the expected `PHASE 0N VERIFIED` marker for the scripts that emit one — do not rely on exit status alone, since a future edit could make a script exit 0 after skipping its own gates.

    Gate 6 — VERIFY-01 cross-channel parity, explicit. Re-run the live cross-channel Playwright specs directly (`cd web && bunx playwright test --project=authed --no-deps` restricted to the four VERIFY-01 spec files), capture the output, `cat` it, and `grep -qF` assert that ALL FOUR filenames appear: `entities-fundos.spec.ts`, `entities-ticket-subtarefa.spec.ts`, `entities-rotina-log.spec.ts`, `routine-job-cross-channel.spec.ts`. A missing one fails the gate with a message naming it and stating it may have been renamed or silently skipped. Echo a one-line mapping of spec → Phase 3 category (full-CRUD CLI→SPA, full-CRUD SPA→CLI, log-only CLI→SPA, instanciasRotina both directions) so VERIFY-01's "at least one entity per Phase 3 category" clause is visible in the transcript rather than merely implied.

    Gate 7 — VERIFY-02, explicit and repo-wide: run `(cd cli && uv run ruff check --config pyproject.toml . ../shared/scripts)`, `(cd cli && uv run ruff format --check --config pyproject.toml . ../shared/scripts)`, `(cd cli && uv run ty check . ../shared/scripts)`. Add an anti-vacuity assertion so "clean" cannot mean "checked nothing": count `.py` files under `cli/` (excluding `.venv/`) and `shared/scripts/` with `find`, assert the count exceeds 15, and echo it. Add a zero-suppressions assertion over that same file set for `# noqa`, `# type: ignore` and `# ty: ignore`, matching the gate `verify-phase-03.sh` already runs — strip comment-only header lines from the grep target so the gate's own explanatory prose cannot self-trigger it.

    Gate 8 — VERIFY-03, explicit and repo-wide: run `(cd web && bun run check)`, `bun run lint`, `bun run format:check`, `bun run build`, `bun run test`. Same anti-vacuity assertion: count `*.ts`/`*.svelte` files under `web/src` and `web/e2e` with `find`, assert the count exceeds 15, and echo it.

    End the script for now with placeholder echoes for Gates 9, 10 and 11 plus the `PHASE 06 VERIFIED` final line; Task 2 fills the placeholders in.
  </action>
  <acceptance_criteria>
    - `test -x .planning/phases/06-end-to-end-verification/verify-phase-06.sh` succeeds.
    - `bash -n .planning/phases/06-end-to-end-verification/verify-phase-06.sh` exits 0.
    - Invoking with an absolute path from `/tmp` and `--skip-composed` resolves every path correctly and exits 0.
    - `grep -v '^#' verify-phase-06.sh | grep -c 'verify-phase-0[1-5]\.sh'` outputs at least `5`.
    - `grep -v '^#' verify-phase-06.sh | grep -cE 'entities-fundos\.spec\.ts|entities-ticket-subtarefa\.spec\.ts|entities-rotina-log\.spec\.ts|routine-job-cross-channel\.spec\.ts'` outputs at least `4`.
    - `grep -v '^#' verify-phase-06.sh | grep -c 'shared/scripts'` outputs at least `3` (VERIFY-02 scope is genuinely repo-wide).
    - Temporarily renaming `web/e2e/.auth/user.json` makes the script exit non-zero at Gate 0 with the `bun run test:e2e:auth` hint; restore afterwards.
    - With `--skip-composed`, the output contains `WARNING: composed phases skipped` and does NOT contain `PHASE 06 VERIFIED`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=.planning/phases/06-end-to-end-verification/verify-phase-06.sh; test -x "$f"; bash -n "$f"; test "$(grep -v "^#" "$f" | grep -c "verify-phase-0[1-5]\.sh")" -ge 5; test "$(grep -v "^#" "$f" | grep -c "shared/scripts")" -ge 3'</automated>
  </verify>
  <done>`verify-phase-06.sh` composes all five prior phase gates with output-marker assertions, and pins VERIFY-01/02/03 with explicit, anti-vacuity, anti-silent-skip checks of its own.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the VERIFY-04/VERIFY-05 gates, the --final cleanup pass, and operator docs</name>
  <files>.planning/phases/06-end-to-end-verification/verify-phase-06.sh, README.md</files>
  <read_first>
    - `.planning/phases/06-end-to-end-verification/06-01-PLAN.md` Tasks 2 and 3 (the `APOLLO_SECOND_SESSION_FILE` default, `APOLLO_VERIFY05_DELETE_SECOND_USER=1`, and the fail-never-skip contract)
    - `.planning/phases/06-end-to-end-verification/06-02-PLAN.md` Task 2 (the live harness invocation and the `phase06-verify04-` leftover convention)
    - `cli/tests/test_cross_user_isolation.py` and `cli/tests/test_interrupted_job.py` as actually written by plans 06-01/06-02 (exact test node names for `-k` filters and parametrization ids)
    - `.planning/phases/05-idempotent-routine-instance-job/verify-phase-05.sh` Gate 7 (leftover-gate shape)
    - `README.md` — the existing `## Re-verifying Phase 1` and `## Re-verifying Phase 5` sections this extends
  </read_first>
  <action>
    Replace Task 1's placeholders in `verify-phase-06.sh`:

    Gate 9 — VERIFY-04: run `uv run --project cli pytest cli/tests/test_interrupted_job.py -m live -v`, capturing to a temp file, `cat`ing it, and asserting the transcript contains BOTH kill-point parametrization ids (`about-to-transact` and `transact-returned`), reports `passed`, and reports zero `skipped` — a run where pytest deselected everything must FAIL this gate, not pass it. Follow with the leftover assertion: `apollo rotina template listar` and `apollo rotina instancia listar` must each yield `0` matches for `phase06-verify04-`.

    Gate 10 — VERIFY-05: run `uv run --project cli pytest cli/tests/test_cross_user_isolation.py -m live -v`, same capture-and-assert treatment, requiring the transcript to show the three denial tests plus the positive-control test as passed with zero skipped, and to contain `permission-denied`. Follow with the leftover assertion for `phase06-verify05-` in `apollo fundo listar`.

    Gate 11 — `--final` only: capture tp@'s `user_id` and `fundos` row count first, then re-run the VERIFY-05 module with `APOLLO_VERIFY05_DELETE_SECOND_USER=1` and the cleanup `-k` filter, then assert tp@ survived — `uv run --project cli apollo auth whoami` exits 0 reporting `tp@rbrasset.com.br` with the same `user_id`, and the `fundos` row count is identical. When `--final` is not passed, print an explicit `Gate 11: SKIPPED (pass --final to also delete the second verification user)` line: this is the ONE deliberately conditional gate in the script and its conditionality must be visible in the transcript, never silent. In `--final` mode, print a closing note that the second-user session is now invalid and must be re-bootstrapped before the next VERIFY-05 run.

    Final line: `echo "PHASE 06 VERIFIED"`, and in `--final` mode additionally `echo "APOLLO V2 v1 MILESTONE GATE: GREEN"`.

    Then add a `## Re-verifying the whole milestone (the v1 done gate)` section to `README.md` after the existing `## Re-verifying Phase 5` section, documenting: the one-command invocation; that it composes `verify-phase-01.sh` through `verify-phase-05.sh` plus the new VERIFY-04/VERIFY-05 gates; all four flags (`--with-magic-code`, `--final`, `--skip-composed`, and their `VERIFY_*` env equivalents) and exactly what each changes; the prerequisites Gate 0 enforces and the fix command for each; the second-user bootstrap command (with `APOLLO_SESSION_FILE` set to `cli/.auth/second-user-session`) plus the warning that `--final` invalidates that session; and an explicit statement that `cli/.auth/` is gitignored and must never be committed. Do NOT mention `APOLLO_TEST_TRANSACT_SENTINEL` anywhere in operator documentation — it is test-only instrumentation (plan 06-02, T-06-11).
  </action>
  <acceptance_criteria>
    - `bash -n .planning/phases/06-end-to-end-verification/verify-phase-06.sh` exits 0.
    - `grep -v '^#' verify-phase-06.sh | grep -cE 'test_interrupted_job|test_cross_user_isolation'` outputs at least `3`.
    - `grep -v '^#' verify-phase-06.sh | grep -c 'APOLLO_VERIFY05_DELETE_SECOND_USER'` outputs at least `1`.
    - `grep -v '^#' verify-phase-06.sh | grep -cE 'phase06-verify04-|phase06-verify05-'` outputs at least `3`.
    - `bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh --skip-composed` runs Gates 0, 9 and 10 to completion, exits 0, and its output contains `Gate 11: SKIPPED`.
    - `grep -c 'verify-phase-06.sh' README.md` outputs at least `1`; `grep -c 'APOLLO_TEST_TRANSACT_SENTINEL' README.md` outputs `0`.
    - `grep -v '^#' README.md | grep -c 'cli/.auth'` outputs at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh --skip-composed</automated>
  </verify>
  <done>`verify-phase-06.sh` runs the VERIFY-04 and VERIFY-05 gates with anti-skip transcript assertions and leftover sweeps, exposes the second-user cleanup behind a visible `--final` gate, and `README.md` documents the whole v1 done gate for an operator.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the full gate end to end and record the milestone evidence</name>
  <files>.planning/phases/06-end-to-end-verification/06-03-VERIFICATION-EVIDENCE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/06-end-to-end-verification/verify-phase-06.sh` as completed in Task 2
    - `.planning/ROADMAP.md` Phase 6 "Success Criteria" (the five statements this evidence must map to one-for-one)
    - `.planning/phases/05-idempotent-routine-instance-job/05-06-SUMMARY.md` (the evidence-recording style prior phases used)
    - `.planning/phases/06-end-to-end-verification/06-01-SECOND-USER-EVIDENCE.md` (the second-user record this evidence cross-references)
  </read_first>
  <action>
    Run the complete gate for real, twice, and record both:

    Run A — the normal certification run: `bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh` with no flags, from the repo root, capturing full stdout+stderr to a temp file. It must exit 0 with `PHASE 06 VERIFIED` as the final line. If any gate fails, FIX THE UNDERLYING CAUSE and re-run — never weaken, comment out, or narrow a gate to make it pass. If a failure turns out to be a genuine defect in Phases 1-5 rather than in this phase's plumbing, stop and report it as a milestone blocker with the failing gate's transcript, since that is exactly the signal this phase exists to produce.

    Run B — the final run: `bash .../verify-phase-06.sh --final`. It must exit 0 with `APOLLO V2 v1 MILESTONE GATE: GREEN` after `PHASE 06 VERIFIED`, having deleted the second verification user under Gate 11's guards. Afterwards, re-bootstrap the second-user session per plan 06-01 Task 1 (two-step magic-code login with `APOLLO_SESSION_FILE` set to `cli/.auth/second-user-session`) so the repo is left in a re-runnable state, and confirm with `APOLLO_SESSION_FILE="$PWD/cli/.auth/second-user-session" uv run --project cli apollo auth whoami`.

    Write `06-03-VERIFICATION-EVIDENCE.md` containing: a table mapping each of ROADMAP Phase 6's five success criteria to the specific gate number and command that proved it, with the observed result; the wall-clock duration and UTC timestamp of Run A and Run B; the final lines of both runs verbatim; the Gate 11 tp@-survival figures (pre/post `fundos` count and `user_id`); the second-user re-bootstrap confirmation; and a short "Known scoping decisions" section restating, in one line each, that VERIFY-04 is CLI-only (RESEARCH Open Question 2), that VERIFY-01 is proven by re-executing four pre-existing specs rather than new ones (RESEARCH Pitfall 1), and that a post-kill partial-write state is structurally unreachable because the SDK issues one atomic POST (RESEARCH Pitfall 2). No refresh token, magic code or admin token may appear anywhere in this file.

    Finally, update `.planning/ROADMAP.md`: mark Phase 6's checkbox `[x]` with the completion date, set its `**Plans**` line to `3 plans in 3 waves`, replace the `- [ ] 06-01: TBD` placeholder with the three real plan entries and their one-line objectives, and update the Progress table row for Phase 6 to `3/3 | Complete | <date>`.
  </action>
  <acceptance_criteria>
    - The captured transcript of Run A ends with the exact line `PHASE 06 VERIFIED` and the run exited 0.
    - The captured transcript of Run B contains `APOLLO V2 v1 MILESTONE GATE: GREEN` and the run exited 0.
    - `06-03-VERIFICATION-EVIDENCE.md` contains a five-row criteria→gate mapping table with one row per ROADMAP Phase 6 success criterion, each naming a gate number.
    - `grep -c 'Known scoping decisions' .planning/phases/06-end-to-end-verification/06-03-VERIFICATION-EVIDENCE.md` outputs `1`.
    - `APOLLO_SESSION_FILE="$PWD/cli/.auth/second-user-session" uv run --project cli apollo auth whoami` exits 0 after Run B's re-bootstrap.
    - `uv run --project cli apollo auth whoami` exits 0 with `tp@rbrasset.com.br` and the same `user_id` recorded before Run B.
    - `grep -c '06-01-PLAN.md' .planning/ROADMAP.md` outputs at least `1`, and the Phase 6 line in the Progress table reads `3/3` and `Complete`.
    - `git status --porcelain cli/.auth` produces no output.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh</automated>
  </verify>
  <done>The full v1 done gate has been executed green twice (normal and `--final`), the five ROADMAP success criteria are mapped one-for-one to the gates that proved them, the second-user session is re-bootstrapped, and ROADMAP.md records Phase 6 as complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification script → live InstantDB app | The composed gate performs real reads and writes (fixtures, e2e specs, the routine job) against the production app; every prior phase's cleanup convention must hold. |
| verification transcript → repository / logs | Composed script output is captured to files and quoted into evidence documents. |
| operator documentation → test-only instrumentation | `README.md` is the operator surface; test hooks must not leak into it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Repudiation | A gate that passes because it silently ran nothing (deselected pytest, renamed spec, narrowed lint scope) | mitigate | Every gate asserts on its own transcript: the four VERIFY-01 spec filenames must appear; pytest gates must show `passed` with zero `skipped`; Gates 7/8 assert a minimum checked-file count via `find`; Gates 1-5 assert each composed script's `PHASE 0N VERIFIED` marker line, not just its exit code. |
| T-06-16 | Tampering | `--skip-composed` being used to claim a green milestone | mitigate | In that mode the script prints a `WARNING: composed phases skipped` line and deliberately does NOT print `PHASE 06 VERIFIED`; Task 3's evidence records only unflagged and `--final` runs. |
| T-06-17 | Information Disclosure | Transcripts or evidence documents containing a refresh token, magic code or admin token | mitigate | Composed scripts already emit no credentials (`session.py`'s docstring guarantees the refresh token is never echoed); the evidence file is explicitly prohibited from containing any, and `cli/.auth/` remains gitignored and asserted clean via `git status --porcelain`. |
| T-06-18 | Elevation of Privilege | The verification harness running with an admin token and thereby bypassing the very perms it certifies | mitigate | Every gate drives the ordinary `apollo` CLI (session-based, `session_client()`) or Playwright with a real user `storageState`; the only admin-token use in the whole phase is plan 06-01's confined magic-code send and `delete_user` teardown. |
| T-06-19 | Denial of Service / destructive action | Gate 11's second-user deletion running unintentionally on a routine run | mitigate | Deletion requires BOTH the `--final`/`VERIFY_FINAL=1` flag AND the `APOLLO_VERIFY05_DELETE_SECOND_USER=1` env var set by the script, on top of plan 06-01's four in-test guards; the skipped state is printed explicitly so its conditionality is never invisible. |
| T-06-20 | Tampering | Live fixture records left behind, polluting the app and future runs | mitigate | Leftover gates for `phase06-verify04-` and `phase06-verify05-` in this script, on top of the `phase05-` gate that `verify-phase-05.sh` already runs. |
</threat_model>

<verification>
1. `bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh` — exits 0, final line `PHASE 06 VERIFIED`.
2. `bash .planning/phases/06-end-to-end-verification/verify-phase-06.sh --final` — exits 0, prints `APOLLO V2 v1 MILESTONE GATE: GREEN`.
3. `uv run --project cli apollo auth whoami` — exit 0, tp@'s original `user_id`, after both runs.
4. `git status --porcelain cli/.auth` — empty.
5. `grep -c 'APOLLO_TEST_TRANSACT_SENTINEL' README.md` — outputs `0`.
</verification>

<success_criteria>
- One command certifies the entire v1 milestone and exits non-zero when anything regresses.
- VERIFY-01/02/03 are proven by re-execution plus explicit coverage assertions, with no new tests invented for work already proven in Phases 4 and 5.
- VERIFY-04 and VERIFY-05 run inside the same gate and cannot pass by being skipped.
- The five ROADMAP Phase 6 success criteria are each mapped to the specific gate that proved them, with recorded evidence.
- The repository is left in a re-runnable state: second-user session valid, zero live fixture leftovers, no credential committed.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Path | Purpose |
|----------|------|---------|
| Milestone verification gate | `.planning/phases/06-end-to-end-verification/verify-phase-06.sh` | The single "is Apollo v2 v1 done" command: composes Phases 1-5 and adds the VERIFY-01/02/03/04/05 assertions |
| Milestone evidence record | `.planning/phases/06-end-to-end-verification/06-03-VERIFICATION-EVIDENCE.md` | Criteria→gate mapping, transcripts of the green normal and `--final` runs, tp@-survival figures, scoping decisions |
| Operator documentation | `README.md` (`## Re-verifying the whole milestone`) | How an operator runs, flags, and troubleshoots the v1 done gate |
| Roadmap closure | `.planning/ROADMAP.md` | Phase 6 marked complete with its three real plan entries |
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/06-end-to-end-verification/06-03-SUMMARY.md`.
</output>
