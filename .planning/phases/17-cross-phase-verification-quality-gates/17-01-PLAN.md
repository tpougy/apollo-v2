---
phase: 17-cross-phase-verification-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-04, VERIFY-04, VERIFY-06, VERIFY-07]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A single sequential `bun run test:e2e` run (all 3 Playwright projects, one invocation, no parallel spawn) passes green with at least 60 tests passed and zero failed/skipped — re-confirming VERIFY-04 for the whole milestone-to-date suite together, not per-phase subsets."
    - "Every login / shell-nav / entity-table-and-states / entity-form-dialog / delete-confirmation surface, crossed with full-CRUD / restricted / read-only capability classes, is named to an exact spec+test proving it live (or explicitly justified N/A), confirmed by re-reading the actual spec files in this run — not assumed from prior phase SUMMARYs. This is the concrete, re-checked-together proof VERIFY-07 requires."
    - "Zero `checkpoint:human-verify` / `checkpoint:human-action` task exists in any of Phase 12-16's PLAN.md files, confirming VERIFY-06 holds across the whole milestone."
    - "Zero raw color literal (an `oklch(`, hex, or `rgb(`/`rgba(` function call outside `web/src/app.css`) exists in any file changed since the v1.1 milestone archive commit — confirming POLISH-01 holds across all five phases' diffs together, not just per-phase."
    - "Every `onclick` handler in a touched `.svelte` file attaches to a real interactive element (a shadcn `Button`/`AlertDialog.Action`/`AlertDialog.Cancel` component, which render a native `<button>`, or a native `<button>`/`<a>` itself) — never a bare `<div>`/`<span>`/`<p>` — and no icon-only button lacking an accessible name (`aria-label` or `sr-only` text) exists anywhere touched this milestone. Confirms POLISH-02."
    - "Every `space-y-*`/`gap-*`/`p-*`/`px-*`/`py-*` utility class used in the app's own composition files (`App.svelte`, `Shell.svelte`, `LoginScreen.svelte`, `EntityScreen.svelte`) is a bounded, default-scale Tailwind token — never an arbitrary bracketed value (`gap-[7px]` etc.) — and the observed value set recurs across at least two of the five milestone phases rather than appearing once. This is the static half of POLISH-04's proof (Plan 17-02 supplies the measured/dynamic half)."
  artifacts:
    - "(none new expected — this plan is audit-first; if a genuine violation is found, the fix lands in the existing file where it was found, and that file's name is recorded in this plan's own SUMMARY)"
  key_links:
    - "`git diff 76e6aee --name-only` (76e6aee = `chore: archive v1.1 milestone`, the last commit before v1.2 work began) -> the definitive, self-updating touched-file set every POLISH-0x grep sweep in this plan runs against, instead of a hand-maintained file list that can silently go stale."
    - "`web/package.json`'s `test:e2e` script -> `web/playwright.config.ts`'s `setup`/`authed`/`anon` projects -> every `web/e2e/*.spec.ts` file -> the live InstantDB app."
---

<objective>
Phases 12-16 each individually restyled and verified one screen (login, shell/nav, entity
header/loading/empty, entity form/dialog, row actions/delete confirmation), landing 21 new
Playwright tests on top of the pre-existing 39 (60 total per 16-02-SUMMARY.md) and touching 48
files since the v1.1 archive commit (`76e6aee`). None of that has ever been re-proven TOGETHER in
one pass, and the cross-cutting hygiene constraints this milestone requires (POLISH-01 no raw
color literals, POLISH-02 accessible interactive elements, POLISH-04 one consistent spacing scale)
have only ever been checked per-file within each phase's own plan — never swept across the whole
milestone's diff at once. This plan is that first cross-phase pass: re-run the full suite as one
sequential invocation, build the explicit screen x capability-class coverage matrix VERIFY-07
requires, and grep-sweep every file touched since `76e6aee` for color-literal, accessibility, and
spacing-scale violations that could have slipped through five separate phase-scoped reviews.

Purpose: A closing verification phase whose own audit conclusions are silently narrower than they
claim (fewer files actually checked, fewer tests actually run) is worse than no audit at all — it
reports false confidence. This plan exists to make that impossible: every check below is grounded
in a live command run against the actual current tree, not a restatement of what five prior
SUMMARYs already claimed.
Output: A live-confirmed all-green `bun run test:e2e` run across the full 60+-test suite, a written
screen x capability-class coverage matrix (recorded in this plan's own SUMMARY.md), and a
confirmed-clean (or freshly-fixed) cross-milestone sweep for color literals, accessible
interactive elements, and spacing-scale bounds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/17-cross-phase-verification-quality-gates/17-CONTEXT.md
@.planning/research/PITFALLS.md
@web/README.md
@web/playwright.config.ts
@web/package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Full-suite live regression + explicit screen x capability-class coverage matrix</name>
  <files>web/e2e/ (all *.spec.ts and auth.setup.ts — read/audit only), web/playwright.config.ts, web/package.json (read-only)</files>
  <read_first>web/e2e/entities-header-states.spec.ts (the existing per-phase coverage-matrix convention this task extends across all five phases), .planning/milestones/v1.1-phases/11-full-verification-quality-gates/11-01-PLAN.md (Task 1 — the exact methodology this task mirrors, scaled from 4 surfaces to 5)</read_first>
  <precondition>The environment running this task can reach the live hosted InstantDB app and the C-10 magic-code channel (Outlook Classic COM bridge) — `bun run test:e2e` triggers `auth.setup.ts`'s real automated round trip as part of the `setup` project. Must be run directly, not by a dispatched subagent, per PROJECT.md's Key Decisions table.</precondition>
  <action>
    Run the complete suite as ONE sequential invocation: `cd web && bun run test:e2e`. Do not add a
    `--project` filter and do not run a second Playwright invocation in parallel — InstantDB's
    magic-code endpoint has rate-limited repeated rapid sends within the same session multiple
    times across Phases 12-16 (see their SUMMARYs' "Issues Encountered" sections); a single clean
    sequential run avoids compounding that. Read the full transcript and confirm it reports at
    least 60 passed (16-02-SUMMARY.md's last known-good count) and zero failed and zero skipped. A
    lower passed-count or any skip is a scope-narrowing signal, not a pass — investigate and fix
    before treating this task as done, never explain it away. If a transient live-network flake
    occurs (documented precedent: magic-code email delivery timing, occasional CLI/InstantDB SSL
    handshake timeouts — see Phase 12/13/15/16 SUMMARYs' "Issues Encountered"), re-run once after a
    short cooldown and confirm a clean run; do not retry-loop indefinitely.

    Then build the explicit coverage matrix this phase exists to make provable rather than assumed:
    cross the 5 restyled surfaces (login, shell/nav, entity table+loading+empty states, entity
    form/dialog, delete confirmation) against the 3 capability classes (full-CRUD, restricted
    create/status-only, read-only). For each of the 15 resulting cells, name the exact spec file +
    test name that proves it live (e.g. "entity form/dialog x restricted -> 15-01's
    entities-form-dialog-composition.spec.ts's 'ENTFRM-08' test, instanciasRotina branch"). Several
    cells are structurally not applicable and must be named N/A with a one-line justification rather
    than skipped silently — confirm each by reading the actual source, not asserting from memory:
    login and shell/nav render identically for every authenticated user regardless of capability
    class (no class-conditional branch exists in `LoginScreen.svelte`/`Shell.svelte` — grep-confirm
    this), entity form/dialog x read-only is N/A because zero-capability entities
    (`logInferenciaClaude`) never render a create/edit `Dialog` at all (confirm via
    `EntityScreen.svelte`'s capability guards), and delete-confirmation x restricted / x read-only
    are N/A because `instanciasRotina`/`logInferenciaClaude` both have `capabilities.delete: false`
    (confirm against `web/src/lib/entities/defs/instanciasRotina.ts` and `logInferenciaClaude.ts`).
    Record this cell-by-cell mapping directly in this task's own summary output — this is VERIFY-07's
    concrete, re-checked-together proof, not a restatement of what Phases 12-16 already individually
    claimed.

    Finally, grep every PLAN.md under `.planning/phases/12-*` through `.planning/phases/16-*` for
    `checkpoint:human-verify` or `checkpoint:human-action` task types. Zero hits confirms VERIFY-06
    (no phase in this milestone depended on human UAT) holds for the whole milestone, not just the
    phases you happen to remember planning that way.

    If every cell is covered and the human-checkpoint grep is empty (expected, given all five prior
    phases' clean SUMMARYs), stop here — do not add redundant coverage; Plan 17-02 adds the new
    cross-phase-specific coverage this task's matrix reveals is still missing (dual-color-scheme on
    login/shell/form-dialog/delete-confirmation, and keyboard/focus-visible smoke tests). If a
    genuine coverage gap or a human-checkpoint task is found, that is a real finding — fix the
    smallest possible thing (add the missing assertion to an existing spec, or flag the checkpoint
    for removal) and re-run `bun run test:e2e` once more to confirm green.
  </action>
  <verify>
    <automated>cd web && bun run test:e2e</automated>
  </verify>
  <done>A single sequential `bun run test:e2e` run passes with at least 60 tests green and zero
  failed/skipped, proving VERIFY-04 holds for the full milestone-to-date suite run together. The
  coverage matrix names, for all 15 surface x capability-class cells (with every structurally N/A
  cell explicitly justified from source, not assumed), the exact spec+test proving it — satisfying
  VERIFY-07's re-checked-together requirement. The human-checkpoint grep across Phases 12-16's
  PLAN.md files returns zero hits, confirming VERIFY-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cross-milestone grep sweeps — color literals, accessible interactive elements, testid duplicates, spacing-scale bounds</name>
  <files>Every file returned by `git diff 76e6aee --name-only -- web/src web/e2e web/playwright.config.ts` (48 files as of this plan's authoring — re-run the command fresh, do not hand-copy this list, since it must reflect the actual current diff) — read/audit; modify only the specific file where a genuine violation is found and confirmed</files>
  <read_first>.planning/research/PITFALLS.md (Pitfalls 2, 4, and the "Looks Done But Isn't" checklist — the exact grep patterns and reasoning this task applies at milestone scope instead of per-file scope)</read_first>
  <action>
    Four sweeps, each run against the file set returned by `git diff 76e6aee --name-only -- web/src
    web/e2e web/playwright.config.ts` (76e6aee is the `chore: archive v1.1 milestone` commit — the
    last commit before any v1.2 work began, so this diff is exactly "every file this milestone
    touched," self-updating rather than a hand-maintained list that can go stale):

    (1) POLISH-01 color literals: grep the full touched-file set for `oklch(`, a 3-8 digit hex
    literal, or `rgb(`/`rgba(` function calls. Zero hits expected (already true as of this plan's
    authoring) — if the whole milestone genuinely introduced zero raw color literals outside
    `app.css`, this sweep simply confirms it at scale; if a hit appears, read the surrounding class
    string, replace it with the nearest existing semantic token (`bg-muted`, `border-border`,
    `text-muted-foreground`, etc. — never a new custom value), and re-run the grep to confirm zero.

    (2) POLISH-02 accessible interactive elements: grep the touched `web/src` files for every
    `onclick=` occurrence, then read each match's enclosing element (Svelte formatting can spread an
    opening tag across several lines, so a single-line regex will under-count — read, don't just
    grep-and-trust) and confirm it is a shadcn `Button`/`AlertDialog.Action`/`AlertDialog.Cancel`
    component (all of which render a native `<button>` under the hood) or a native `<button>`/`<a>`
    — never a bare `<div>`/`<span>`/`<p>`. Separately, for every `Button`/`AlertDialog.Action`/
    `AlertDialog.Cancel` usage in the touched files, confirm its rendered content includes visible
    text (as all of them currently do: "novo", "editar", "excluir", "Sair", "Reenviar código",
    "cancelar", "salvar") — if any icon-only button exists with no visible text, confirm it carries
    either an `aria-label` prop or a `sr-only`-classed text span (the pattern already used by the
    pre-existing, untouched `dialog-content.svelte` close button); fix any gap found by adding the
    missing accessible name, never by adding decorative-only markup.

    (3) Load-bearing testid duplicates: extract every literal (non-template-interpolated)
    `data-testid="value"` occurrence across the touched `web/src` files, group by value, and list
    any value appearing more than once. Three duplicates are pre-known and explained (read the
    source to confirm each, don't just trust this list): `entity-create-start` appears 3 times
    because 2 of those are `document.querySelector('[data-testid="entity-create-start"]')` string
    references inside focus-restoration code, not a second live attribute; `login-submit` appears on
    both the email-step and code-step submit buttons, which are mutually exclusive
    (`{#if step === "email"}...{:else}...`) and never both mounted at once; `entity-error` appears on
    both the `formError` and `query.error` `Alert` branches, also mutually exclusive. Any OTHER
    value appearing more than once, or either of these three appearing in a context where both
    branches could render simultaneously, is a real POLISH/regression risk — investigate by reading
    the surrounding conditional and fix (move the id to the one canonical element) before treating
    this sweep as passed.

    (4) POLISH-04 spacing-scale bounds (static half — Plan 17-02 supplies the live-measured half):
    restrict this sweep to the app's own composition files only (`App.svelte`, `Shell.svelte`,
    `LoginScreen.svelte`, `EntityScreen.svelte`) — NOT the vendored `web/src/lib/components/ui/*`
    primitives, whose internal spacing (e.g. `alert-dialog-header.svelte`'s `gap-1.5`,
    `empty-content.svelte`'s `gap-2.5`) is shadcn's own registry-supplied design system, out of this
    milestone's page-composition scope. Grep those 4 files for any arbitrary bracketed spacing value
    (`gap-[...]`, `space-y-[...]`, `p-[...]`, `px-[...]`, `py-[...]`, etc.) — zero expected. Then
    extract the full set of `gap-*`/`space-y-*`/`p-*`/`px-*`/`py-*` values actually used and confirm
    every value recurs in at least 2 of the 4 files (the established set as of this plan's authoring:
    `gap-2`/`gap-4`, `space-y-1`/`space-y-2`/`space-y-4`/`space-y-6`, `p-4`, `px-4`/`px-6`,
    `py-3`/`py-6`/`py-8` — all standard Tailwind default-scale multiples of 4px, none introduced and
    then abandoned in a single file). Record the confirmed value set in this task's summary. If a
    genuinely one-off value is found (used exactly once, nowhere else in the milestone's
    composition files), that is a POLISH-04 violation — replace it with the nearest already-recurring
    scale value and re-run the extraction to confirm.
  </action>
  <verify>
    <automated>git diff 76e6aee --name-only -- web/src web/e2e web/playwright.config.ts | xargs grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}\b|rgba?\(' ; echo "exit=$? (expect 1 or 123 — zero matches, empty output)"</automated>
    <automated>git diff 76e6aee --name-only -- web/src | xargs grep -rohE 'data-testid="[a-zA-Z0-9_-]+"' | sort | uniq -c | sort -rn | awk '$1>1 && $2!="data-testid=\"entity-create-start\"" && $2!="data-testid=\"login-submit\"" && $2!="data-testid=\"entity-error\""' ; echo "(expect empty output — any printed line is a new, unexplained duplicate)"</automated>
    <automated>git diff 76e6aee --name-only -- web/src/App.svelte web/src/lib/Shell.svelte web/src/lib/auth/LoginScreen.svelte web/src/lib/entities/EntityScreen.svelte | xargs grep -noE '\b(gap|space-y|space-x|p|px|py|pt|pb|pl|pr|m|mx|my|mt|mb)-\[[^]]+\]' ; echo "exit=$? (expect 1 — zero arbitrary-bracket spacing values)"</automated>
    <automated>git diff 76e6aee --name-only -- web/src/App.svelte web/src/lib/Shell.svelte web/src/lib/auth/LoginScreen.svelte web/src/lib/entities/EntityScreen.svelte | xargs grep -ohE '\b(gap|space-y|space-x|p|px|py|pt|pb|pl|pr)-[0-9.]+' | sort -u</automated>
  </verify>
  <done>The color-literal grep across every file touched since `76e6aee` returns zero matches
  (POLISH-01 confirmed at milestone scale). Every `onclick` handler is confirmed (by reading, not
  just grepping) to attach to a real `<button>`/`<a>`-backed element, and every `Button`/
  `AlertDialog.Action`/`AlertDialog.Cancel` usage is confirmed to carry visible text or an
  accessible name (POLISH-02 confirmed). The testid-duplicate grep returns zero unexplained
  duplicates beyond the three pre-known, source-confirmed mutually-exclusive-branch cases. The
  spacing-scale grep across the app's own 4 composition files returns zero arbitrary-bracket values,
  and the extracted value set is the bounded, recurring set recorded in this task's summary (POLISH-04
  static half confirmed). Any genuine violation found in any of the four sweeps has been fixed in
  place and the corresponding grep re-run to confirm zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's own audit conclusions ↔ the milestone's actual done-state | The core risk of any closing/audit plan: its own checks could silently narrow scope (fewer files actually grepped, fewer tests actually run, a "looks fine" skim instead of a genuine read) and still report all-clear |
| Playwright test runner (live) ↔ hosted InstantDB app | Pre-existing boundary, inherited unchanged from Phases 4/7-16 — this plan adds no new write path, no new auth surface, and performs zero destructive InstantDB operations beyond what the pre-existing suite already does |
| `auth.setup.ts`'s console log ↔ local test-run stdout | Pre-existing since Phase 8 — the raw magic code + email are logged to local stdout during the automated auth round trip Task 1 re-exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Repudiation | This plan's own "all green"/"zero violations" conclusions (Task 1's coverage matrix, Task 2's four grep sweeps) | medium | mitigate | Task 1 asserts an explicit test-count floor (>= 60) and requires the coverage matrix to name the exact spec+test per cell (source-confirmed N/A justifications, not assumed); Task 2's `git diff 76e6aee` file-set derivation is self-updating rather than hand-maintained, and every sweep instructs "read to confirm, don't grep-and-trust" — an anti-vacuity pattern mirroring v1.1 Phase 11's own closing-phase methodology |
| T-17-02 | Tampering | Grep-sweep results silently narrowed by a stale or incomplete file-set enumeration | low | mitigate | The file-set command (`git diff 76e6aee --name-only`) is re-run fresh by the task, never hand-copied from this plan's authoring-time snapshot, so a file added or renamed after this plan was written is still caught |
| T-17-03 | Information Disclosure | `auth.setup.ts`'s console log of the raw magic code + email during the live round trip Task 1 re-exercises | low | accept | Local-only stdout, code already expires in 60-90s, pre-existing since Phase 8 — this plan introduces no new attack surface |

No package-manager install tasks exist in this plan (zero new npm/bun dependencies) — the Package
Legitimacy Gate and a `T-17-SC` row are not applicable and are intentionally omitted.
</threat_model>

<verification>
`cd web && bun run test:e2e` — the complete Playwright suite, all 3 projects, run as one sequential
invocation against the live InstantDB app, at least 60 passed and zero failed/skipped. Four grep
sweeps (color literals, accessible interactive elements, testid duplicates, spacing-scale bounds)
against the exact file set `git diff 76e6aee --name-only` returns, each confirmed clean or fixed in
place. A human-checkpoint grep across Phases 12-16's PLAN.md files returns zero hits.
</verification>

<success_criteria>
- ROADMAP Phase 17 SC1 (partial — full-suite proof): every spec in `web/e2e/` passes in one real run
  against the live InstantDB app (Task 1).
- ROADMAP Phase 17 SC2 (partial — representative-checks-together proof): the explicit 15-cell
  screen x capability-class coverage matrix is recorded, naming exact evidence per cell (Task 1).
- ROADMAP Phase 17 SC4: a grep sweep across every file touched this milestone finds zero raw color
  literals and zero unexplained duplicate `data-testid` values (Task 2, sweeps 1 and 3).
- VERIFY-06 is explicitly re-confirmed (zero human-checkpoint tasks across Phases 12-16) rather than
  assumed from the milestone having "felt" autonomous throughout (Task 1).
</success_criteria>

<artifacts_produced>
No new files are expected from this plan. If Task 1 or Task 2 finds a genuine violation, the fix
lands in the specific existing file where it was found (named explicitly in this plan's own
SUMMARY.md), never in a new file. This plan's own SUMMARY.md is the durable record of the 15-cell
coverage matrix and the confirmed spacing-scale value set.
</artifacts_produced>

<output>
Create `.planning/phases/17-cross-phase-verification-quality-gates/17-01-SUMMARY.md` when done
</output>