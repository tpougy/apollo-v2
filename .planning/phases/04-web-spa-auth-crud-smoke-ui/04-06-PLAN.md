---
phase: 04-web-spa-auth-crud-smoke-ui
plan: 06
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - web/src/lib/entities/registry.test.ts
  - .planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh
  - web/README.md
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-05, WEB-06, WEB-07, WEB-08, WEB-09, WEB-10]
user_setup: []

must_haves:
  truths:
    - "Every entity declared in shared/instant.schema.ts has a corresponding screen definition in the SPA — asserted by parsing the schema, not by a hand-maintained list."
    - "A single command re-proves WEB-01 through WEB-10 against the live InstantDB app and exits non-zero if any of them regressed."
    - "The web/ formatter, linter, and svelte-check all run clean across every file in web/."
    - "The built browser bundle contains no InstantDB admin token."
    - "An operator can read one document and know how to run the SPA, run the e2e suite, and re-verify the phase."
  artifacts:
    - path: "web/src/lib/entities/registry.test.ts"
      provides: "Schema-driven coverage gate: every schema entity has a defs/*.ts config with correct capabilities"
      contains: "instant.schema.ts"
    - path: ".planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh"
      provides: "One-command, idempotent, cwd-independent re-proof of WEB-01..WEB-10 plus C-08 gates"
      min_lines: 80
    - path: "web/README.md"
      provides: "Operator documentation: dev server, auth flow, screen registry, e2e suite, verification"
  key_links:
    - from: "web/src/lib/entities/registry.test.ts"
      to: "shared/instant.schema.ts"
      via: "regex extraction of entity names from the schema source (no hand-maintained list)"
      pattern: "instant\\.schema\\.ts"
    - from: ".planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh"
      to: "web/e2e"
      via: "bunx playwright test invocation over all three projects"
      pattern: "playwright test"
  edge_coverage:
    - "Coverage gate / a schema entity with no defs/*.ts module: the test FAILS naming the missing entity."
    - "Coverage gate / schema file reformatted so the entity regex matches zero entities: the test FAILS loudly on the >= 9 match assertion rather than passing vacuously."
    - "Coverage gate / instanciasRotina declared with create:true or delete:true: the test FAILS (C-06 invariant)."
    - "Coverage gate / logInferenciaClaude declared with any capability true: the test FAILS (append-only invariant)."
    - "verify script / run twice in a row: both runs exit 0 and git status stays clean (idempotent)."
    - "verify script / run from a directory other than the repo root: still resolves and exits 0."
    - "verify script / any single gate failing: exits non-zero and does NOT print the final PHASE 04 VERIFIED line."
  prohibitions:
    - "No hand-maintained list of entity names in registry.test.ts — the list is parsed from shared/instant.schema.ts."
    - "No # noqa / biome-ignore / ts-expect-error / eslint-disable suppression added anywhere in web/ to make a gate pass."
    - "The verify script must not create records it does not delete, and must not send a magic-code email on a normal run (it reuses the persisted storageState, like verify-phase-03.sh reuses the CLI session)."
    - "No requirement may be marked verified on the strength of a passing test that was never demonstrated to fail."
---

<objective>
Close Phase 4: prove the SPA covers every schema entity, prove every C-08 quality gate is green
across `web/`, and leave behind one command that re-proves WEB-01 through WEB-10 on demand.

Purpose: this is the phase's regression backstop. Phases 5 and 6 will change `web/` (the generation
job lands in the SPA), and they need a single green/red signal for whether the CRUD surface still works.
Output: a schema-driven coverage test, `verify-phase-04.sh`, and operator documentation.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-01-SUMMARY.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-03-SUMMARY.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-04-SUMMARY.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-05-SUMMARY.md
@shared/instant.schema.ts
@web/src/lib/entities/types.ts
@web/src/lib/entities/registry.ts
@web/package.json

Proven prior art — copy the structure, do not reinvent it:
@.planning/phases/03-cli-auth-crud/verify-phase-03.sh
@cli/tests/test_cli_surface.py
@.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-driven screen-coverage gate</name>
  <read_first>
    - cli/tests/test_cli_surface.py (the formatting-locked entity regex, the `>= 9 matches` self-check, and the EXPECTED_SURFACE mapping pattern — mirror all three in TypeScript)
    - shared/instant.schema.ts (the `    <name>: i.entity(` formatting the regex locks onto)
    - web/src/lib/entities/registry.ts (`entityConfigs`, `configByEtype`)
    - web/src/lib/bizdays.test.ts (the existing `bun:test` style in this repo)
  </read_first>
  <behavior>
    - Reads shared/instant.schema.ts from disk and extracts entity names with a formatting-locked regex; asserts at least 9 were found (a reformat that breaks the regex fails loudly instead of matching zero).
    - For each extracted entity name, asserts `configByEtype(name)` returns a config whose `etype` equals that name.
    - Asserts `instanciasRotina` has `capabilities.create === false` and `capabilities.delete === false` and `updatableFields` equal to `["status"]`.
    - Asserts `logInferenciaClaude` has all three capabilities false.
    - Asserts the other seven entities have all three capabilities true.
    - Asserts every config's `listColumns` are non-empty and every entry either names a declared field, a declared link label, or an xorLink choice label (catches typo'd column names).
    - Asserts every config's `ordem` is unique across the registry (no ambiguous nav order).
    - Asserts no config declares a field whose name contains "dono" (case-insensitive) — the donoId prohibition, enforced structurally rather than by grep.
    - Asserts every `LinkDef.targetEtype` and every `XorLinkDef` choice target resolves to a real schema entity name.
  </behavior>
  <action>
    Create `web/src/lib/entities/registry.test.ts` as a `bun:test` module (runs under the existing
    `bun run test` script — do not add a new script). Read the schema source with `node:fs` relative to
    `import.meta.dir` so the test is cwd-independent. Use a formatting-locked entity regex equivalent to the
    CLI's (`/^ {4}(\w+): i\.entity\(/gm`) and assert `matches.length >= 9` before doing anything else, so a
    schema reformat produces a loud failure rather than a vacuous pass — this is the exact self-check
    `cli/tests/test_cli_surface.py` established in Phase 3.

    Encode the capability expectations as a single `EXPECTED_CAPABILITIES` map from entity name to the
    expected `{ create, update, delete }` triple plus an optional `updatableFields`, mirroring the CLI test's
    `EXPECTED_SURFACE`. Every assertion message must name the offending entity so a failure is diagnosable
    without reading the test source.

    Do not import any Svelte component here — this is a pure-config test and must run headlessly under
    `bun test` with no DOM and no network.
  </action>
  <acceptance_criteria>
    - `cd web && bun test` exits 0 and reports the new registry tests alongside the existing bizdays tests.
    - `cd web && bun run check && bun run lint && bun run format:check` all exit 0.
    - Fail-loud demonstration 1 (record verbatim in the SUMMARY): rename
      `web/src/lib/entities/defs/etapas.ts` to `etapas.ts.bak`, run `bun test`, confirm a FAILURE whose
      message names `etapas`; restore and confirm green.
    - Fail-loud demonstration 2 (record verbatim): temporarily set `instanciasRotina`'s
      `capabilities.create` to true, run `bun test`, confirm a FAILURE naming `instanciasRotina`;
      restore and confirm green.
    - `grep -cE '"fundos"|"projetos"|"tarefas"' web/src/lib/entities/registry.test.ts` — any matches must
      appear ONLY inside the EXPECTED_CAPABILITIES map, never as the source of the entity list itself;
      confirm the list originates from the schema regex by demonstration 1 above.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun test && bun run check && bun run lint && bun run format:check</automated>
  </verify>
  <done>A missing or mis-capability'd screen breaks the test suite loudly, and the entity list comes from the schema rather than from a list a human has to remember to update.</done>
</task>

<task type="auto">
  <name>Task 2: verify-phase-04.sh and operator documentation</name>
  <read_first>
    - .planning/phases/03-cli-auth-crud/verify-phase-03.sh (structure: BASH_SOURCE-based repo-root resolution, per-requirement sections, FAIL messages, the terminal VERIFIED line, `set -euo pipefail`)
    - .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh lines around the admin-token bundle grep (reuse that exact technique)
    - .planning/ROADMAP.md Phase 4 success criteria (the five SC statements this script must map onto)
    - cli/README.md (the operator-doc structure to mirror for web/README.md)
    - All five 04-0X-SUMMARY.md files (the exact commands each plan proved, so the script re-runs real gates rather than inventing new ones)
  </read_first>
  <action>
    Create `.planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh`. Start with
    `set -euo pipefail`, resolve the repo root from `BASH_SOURCE[0]` and `cd` there (cwd-independent, like
    verify-phase-03.sh), and structure it as labelled sections that each print a PASS/FAIL line naming the
    requirement id. Sections:

    - C-08 quality gates: `cd web && bun run check`, `bun run lint`, `bun run format:check`, `bun run build`,
      `bun test`. Any non-zero exit is a FAIL. (Covers ROADMAP SC-5 and pre-stages Phase 6 VERIFY-03.)
    - Suppression gate: assert zero `biome-ignore`, `@ts-expect-error`, `@ts-ignore`, or `eslint-disable`
      markers anywhere under `web/src` and `web/e2e`, using a comment-tolerant grep and excluding the
      verify script itself and this gate's own source from matching its own marker strings (Phase 3 hit
      exactly this self-match trap — build the marker strings by concatenation, as `test_cli_surface.py` does).
    - T-04-02 admin-token confinement: read `INSTANT_APP_ADMIN_TOKEN` from `.env.instantdb` and assert the
      literal value appears in NO tracked file (`git ls-files -z | xargs -0 grep -lF`) and in NO file under
      `web/dist` after the build, reusing verify-phase-01.sh's technique verbatim. Also assert
      `grep -rn "@instantdb/admin" web/src` is empty.
    - T-04-03 donoId confinement: assert `grep -rn "donoId" web/src | grep -v EntityScreen.svelte` is empty.
    - WEB-01 + WEB-10: run `bunx playwright test --project=anon` (no session needed) and
      `bunx playwright test --project=authed` (reuses the persisted storageState). Do NOT run
      `--project=setup` by default — it sends a real magic-code email, and a verification script that spams
      the user's inbox on every run is a defect. Instead gate it behind an opt-in flag
      (`--with-magic-code` / `VERIFY_MAGIC_CODE=1`) and, on a normal run, assert only that
      `web/e2e/.auth/user.json` exists and that the `authed` project passes with it — documenting in both
      the script's header comment and `web/README.md` that a full WEB-01 re-proof requires the flag.
      If `web/e2e/.auth/user.json` is missing or the `authed` project fails with an auth error, FAIL with a
      message telling the operator to run `bun run test:e2e:auth` (the setup project) first.
    - WEB-02..WEB-09: the `authed` project run above covers all entity specs; assert explicitly that all four
      entity spec files were executed (parse the Playwright list output rather than assuming) so a silently
      renamed or skipped spec cannot pass as coverage.
    - Cleanliness: assert `git status --porcelain` is empty at the end, and assert zero `phase04-e2e-`
      records survive across `apollo fundo|projeto|etapa|tarefa|ticket|subtarefa|rotina template|
      rotina instancia listar` and `apollo log-inferencia listar`.

    Print `PHASE 04 VERIFIED` as the final line ONLY when every section passed; every FAIL path must exit
    non-zero before reaching it.

    Create `web/README.md` covering: running the dev server (`bun run dev`), the magic-code login flow as a
    user experiences it, how screens are defined (the `defs/*.ts` + `import.meta.glob` registry — explicitly:
    "to add a screen, add a file; never edit the registry"), the `EntityConfig` contract with a short
    description of each field, the capability restrictions and WHY (`instanciasRotina` create/delete
    forbidden per C-06; `logInferenciaClaude` append-only per CLI-10), the donoId injection rule, how to run
    the e2e suite (all three projects, what each proves, and the C-10 magic-code mechanism the setup project
    depends on), and how to run `verify-phase-04.sh` including the `--with-magic-code` flag.
  </action>
  <acceptance_criteria>
    - `bash .planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh` exits 0 and prints
      `PHASE 04 VERIFIED` as its final line.
    - Running it twice in a row exits 0 both times, and `git status --porcelain` is empty after each run.
    - Running it from `/tmp` (`cd /tmp && bash /home/thomaz/pessoal/apollo-v2/.planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh`) exits 0.
    - Fail-loud demonstration (record verbatim in the SUMMARY): add a `// biome-ignore lint: demo` comment
      to any file under `web/src`, re-run the script, confirm exit non-zero, confirm `PHASE 04 VERIFIED` is
      ABSENT from the output and the FAIL line names the suppression gate; remove the comment and confirm green.
    - Second fail-loud demonstration (record verbatim): temporarily move `web/e2e/.auth/user.json` aside,
      re-run, confirm the script FAILS with the "run bun run test:e2e:auth first" guidance; restore and confirm green.
    - `web/README.md` exists and contains sections for the login flow, the defs/registry pattern, the
      capability restrictions with their C-06/CLI-10 rationale, the e2e suite, and the verification command.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh</automated>
  </verify>
  <done>One command re-proves the whole phase, fails loudly and specifically when anything regresses, leaves the repo and the live app untouched, and an operator has the document that explains all of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo / build output -> public | Anything in a tracked file or in `web/dist` is disclosed; the admin token must be in neither |
| Verification script -> live InstantDB app | The script exercises the real app and must leave it exactly as it found it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | `web/dist` bundle and tracked files | mitigate | `verify-phase-04.sh` greps every tracked file and every built asset for the literal `INSTANT_APP_ADMIN_TOKEN` value (Phase 1 technique) and asserts `@instantdb/admin` is absent from `web/src`; a hit is a hard FAIL |
| T-04-03 | Elevation of Privilege | `donoId` handling across the whole SPA | mitigate | Structural assertion in `registry.test.ts` (no field name containing "dono") plus a repo-wide grep gate in the verify script confining the identifier to `EntityScreen.svelte`'s submit path |
| T-04-13 | Tampering | Capability drift on `instanciasRotina` in a later phase | mitigate | `registry.test.ts` asserts `create:false`/`delete:false`/`updatableFields:["status"]` and runs under `bun test`, which `verify-phase-04.sh` invokes — Phase 5 cannot loosen it silently |
| T-04-06 | Information Disclosure | `web/e2e/.auth/user.json` real refresh token | mitigate | Verify script's final `git status --porcelain` emptiness assertion fails if the ignored auth directory ever becomes tracked |
| T-04-15 | Denial of Service | Verification script spamming the real mailbox with magic codes | mitigate | The `setup` project (which sends a real email) is opt-in behind `--with-magic-code`; normal runs reuse the persisted session, mirroring how `verify-phase-03.sh` reuses the CLI session without re-authenticating |
</threat_model>

<verification>
1. `cd web && bun test && bun run check && bun run lint && bun run format:check && bun run build` — all exit 0.
2. `cd web && bunx playwright test` — all projects green.
3. `bash .planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh` — exits 0, final line `PHASE 04 VERIFIED`, twice in a row, and from `/tmp`.
4. `git status --porcelain` — empty after every run.
</verification>

<success_criteria>
- ROADMAP SC-1 through SC-5 for Phase 4 are all re-provable by one command.
- WEB-01..WEB-10 each map to a named, exit-code-enforced section of `verify-phase-04.sh`.
- Every gate in the script has been demonstrated to FAIL when its invariant is broken — no gate is
  accepted on the strength of a green run alone.
</success_criteria>

## Artifacts this phase produces

| Artifact | Consumed by |
|----------|-------------|
| `web/src/lib/entities/registry.test.ts` (schema-driven coverage + capability invariants) | `bun test` on every run; Phase 5 (blocks silent capability loosening on `instanciasRotina`); Phase 6 VERIFY-03 |
| `.planning/phases/04-web-spa-auth-crud-smoke-ui/verify-phase-04.sh` | Phase 5 regression backstop; Phase 6 VERIFY-01/VERIFY-03 reuse |
| `web/README.md` | Operator/AI reference for adding screens without touching shared components |

<output>
After completion, create `.planning/phases/04-web-spa-auth-crud-smoke-ui/04-06-SUMMARY.md`.
Record verbatim: both `registry.test.ts` fail-loud demonstrations, both `verify-phase-04.sh` fail-loud
demonstrations, the exact one-command invocation an operator runs, the full list of WEB-01..WEB-10 mapped
to the script sections that prove each, and confirmation that the live InstantDB app holds zero
`phase04-e2e-` records after the run.
</output>
