---
phase: 04-web-spa-auth-crud-smoke-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/src/lib/entities/types.ts
  - web/src/lib/entities/registry.ts
  - web/src/lib/entities/defs/fundos.ts
  - web/src/lib/entities/EntityScreen.svelte
  - web/src/lib/Shell.svelte
  - web/src/App.svelte
  - web/e2e/entities-fundos.spec.ts
autonomous: true
requirements: [WEB-02]
user_setup: []

must_haves:
  truths:
    - "An authenticated user sees a navigation list of entity screens and can switch between them without a page reload."
    - "The fundos screen lists every fundo the authenticated user owns, including records created by the Phase 3 CLI."
    - "The user can create a fundo from the browser and it appears in the list without a manual refresh."
    - "The user can edit an existing fundo's fields from the browser and the change persists."
    - "The user can delete a fundo from the browser and it disappears from the list."
    - "The create/edit form never offers an owner-id field."
  artifacts:
    - path: "web/src/lib/entities/types.ts"
      provides: "EntityConfig / FieldDef / LinkDef contract every entity definition module implements"
      exports: ["EntityConfig", "FieldDef", "LinkDef", "XorLinkDef"]
    - path: "web/src/lib/entities/registry.ts"
      provides: "Auto-discovery of every defs/*.ts module via import.meta.glob, ordered by config.ordem"
      contains: "import.meta.glob"
    - path: "web/src/lib/entities/EntityScreen.svelte"
      provides: "Generic config-driven table + form CRUD component (the single rendering/transact engine)"
      min_lines: 80
    - path: "web/src/lib/entities/defs/fundos.ts"
      provides: "fundos EntityConfig — nome, codigo, ativo, createdAt"
    - path: "web/src/lib/Shell.svelte"
      provides: "Authenticated shell: registry-driven nav + active EntityScreen"
    - path: "web/e2e/entities-fundos.spec.ts"
      provides: "Live browser CRUD round trip for fundos plus the CLI-created-record visibility proof"
  key_links:
    - from: "web/src/App.svelte"
      to: "web/src/lib/Shell.svelte"
      via: "SignedIn children snippet"
      pattern: "Shell"
    - from: "web/src/lib/Shell.svelte"
      to: "web/src/lib/entities/registry.ts"
      via: "nav items derived from the registry, not a hand-written list"
      pattern: "registry|entityConfigs"
    - from: "web/src/lib/entities/EntityScreen.svelte"
      to: "InstantDB"
      via: "db.useQuery(() => ({ [etype]: {...} })) and db.transact(db.tx[etype][id]...)"
      pattern: "db\\.(useQuery|transact)"
  edge_coverage:
    - "WEB-02 / list with zero records: an explicit empty-state row renders instead of a blank table body."
    - "WEB-02 / create with a required field left blank: submit is blocked, no transact issued."
    - "WEB-02 / edit an existing record: only the changed fields are written; the record id is unchanged."
    - "WEB-02 / delete: the row disappears from the live query result without a page reload."
    - "WEB-02 / a fundo created earlier by the Phase 3 CLI: renders in the SPA list (SC-3 cross-channel read proof)."
    - "WEB-02 / boolean field (ativo) round-trips false correctly (not coerced to the string \"false\")."
    - "WEB-02 / date field (createdAt) round-trips between the stored ISO string and the YYYY-MM-DD input value."
  prohibitions:
    - "No donoId form field, donoId input binding, or donoId literal anywhere in defs/*.ts, EntityScreen.svelte, or Shell.svelte — it is injected exclusively from db.useAuth().user.id at submit time."
    - "No per-entity duplicated table/form markup — all nine screens must render through EntityScreen.svelte."
    - "No db.useQuery call reachable outside the SignedIn gate."
    - "No hand-maintained entity list in registry.ts — discovery is import.meta.glob over defs/*.ts."
---

<objective>
Build the single generic, config-driven CRUD engine (`EntityScreen.svelte`) plus the registry
and shell that carry it, and prove it end to end on ONE entity — `fundos` — in a real browser.

This is the second tracer step: once `fundos` demonstrably creates, reads, updates, and deletes
live records (and shows records the Phase 3 CLI already wrote), the remaining eight screens are
config files, not new machinery.

Purpose: WEB-02, plus ROADMAP SC-3 (CLI-created records visible in the SPA).
Output: `types.ts` contract, auto-discovering `registry.ts`, `EntityScreen.svelte`, `Shell.svelte`,
the `fundos` definition, and a live browser CRUD spec.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-01-SUMMARY.md
@shared/instant.schema.ts
@web/src/App.svelte
@web/src/lib/db.ts

<interfaces>
<!-- Verified InstaQL/InstaML surface (04-RESEARCH.md Patterns 3 & 4, HIGH confidence). Use directly. -->

```typescript
// Reactive query: pass a FUNCTION so it re-subscribes when $state it reads changes.
const q = db.useQuery(() => ({ fundos: {} }));
// q.isLoading: boolean; q.error: { message: string } | null; q.data: { fundos: Fundo[] } | undefined

// Writes (id() is re-exported from web/src/lib/db.ts):
db.transact(db.tx.fundos[id()].update({ nome, codigo, ativo, donoId, createdAt }));
db.transact(db.tx.projetos[eid].update({ nome }).link({ fundo: fundoId }));
db.transact(db.tx.fundos[eid].delete());
// One-shot read (for link-target existence checks):
await db.queryOnce({ fundos: { $: { where: { id: fundoId } } } });
```

<!-- CONTRACT this plan defines. Plans 04-03/04-04/04-05 implement defs/*.ts against it verbatim. -->
```typescript
// web/src/lib/entities/types.ts
export type FieldDef =
  | { name: string; label: string; required: boolean; kind: "text" }
  | { name: string; label: string; required: boolean; kind: "textarea" }
  | { name: string; label: string; required: boolean; kind: "number" }
  | { name: string; label: string; required: boolean; kind: "boolean" }
  | { name: string; label: string; required: boolean; kind: "date" }
  | { name: string; label: string; required: boolean; kind: "select"; options: readonly string[] };

export interface LinkDef {
  label: string;            // link label on THIS entity, e.g. "fundo", "projeto", "antecessor"
  targetEtype: string;      // e.g. "fundos"
  targetLabelField: string; // field shown in the option text, e.g. "nome"
  required: boolean;
  excludeSelf?: boolean;    // true for templatesRotina.antecessor (self-link)
}

export interface XorLinkDef {
  label: string;            // UI label for the parent-type chooser
  choices: readonly [LinkDef, LinkDef];  // exactly one must be linked
}

export interface EntityConfig {
  etype: string;                        // InstantDB entity name, must match shared/instant.schema.ts
  titulo: string;                       // nav + heading text
  ordem: number;                        // nav sort order
  capabilities: { create: boolean; update: boolean; delete: boolean };
  updatableFields?: readonly string[];  // when set, edit form exposes ONLY these (instanciasRotina => ["status"])
  fields: readonly FieldDef[];
  links?: readonly LinkDef[];
  xorLink?: XorLinkDef;
  listColumns: readonly string[];       // field names (and link labels) rendered as table columns
}
```
</interfaces>

Phase 3 field/validation semantics reference (same schema, different language — mirror the rules, not the code):
@cli/apollo_cli/entities/fundo.py
@cli/apollo_cli/crud_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: EntityConfig contract, auto-discovering registry, and the fundos definition</name>
  <read_first>
    - shared/instant.schema.ts (fundos: nome string, codigo string indexed, ativo boolean, donoId string indexed, createdAt date)
    - cli/apollo_cli/entities/fundo.py (which fields are required on criar vs optional on editar; the owner-id injection convention)
    - The `<interfaces>` block above (the EntityConfig contract is authoritative — write it verbatim)
  </read_first>
  <action>
    Create `web/src/lib/entities/types.ts` containing exactly the `FieldDef` / `LinkDef` / `XorLinkDef` /
    `EntityConfig` declarations from this plan's `<interfaces>` block. This file is the contract plans
    04-03, 04-04, and 04-05 build against — do not deviate from the shapes, and do not add an owner-id
    concept to any of them (per D: donoId is never configurable, never form-fillable).

    Create `web/src/lib/entities/registry.ts` exporting `entityConfigs: EntityConfig[]`, built with Vite's
    `import.meta.glob("./defs/*.ts", { eager: true })` and sorted ascending by `ordem`. This mirrors the
    CLI's proven `apollo_cli/entities/` auto-discovery pattern from Phase 3 — adding an entity is adding a
    file, never editing a list, which is what lets plans 04-03/04-04/04-05 run in parallel without touching
    a shared file. Validate at module load that each glob'd module has a default export with a non-empty
    `etype` and throw a descriptive Error naming the offending path otherwise. Also export
    `configByEtype(etype: string): EntityConfig | undefined`.

    Create `web/src/lib/entities/defs/fundos.ts` default-exporting an `EntityConfig` with `etype: "fundos"`,
    `titulo: "Fundos"`, `ordem: 1`, all three capabilities true, fields
    `nome` (text, required), `codigo` (text, required), `ativo` (boolean, required),
    `createdAt` (date, required — the CLI sets it on create; mirror that), no links, no xorLink, and
    `listColumns: ["nome", "codigo", "ativo", "createdAt"]`.
  </action>
  <acceptance_criteria>
    - `cd web && bun run check` exits 0.
    - `cd web && bun run lint && bun run format:check` exit 0.
    - `grep -rn "donoId" web/src/lib/entities/` returns no matches (exit 1).
    - `grep -c 'import.meta.glob' web/src/lib/entities/registry.ts` reports 1.
    - `grep -vE '^\s*(//|\*|/\*)' web/src/lib/entities/registry.ts | grep -cE '"fundos"|"projetos"|"tarefas"' ` reports 0 (no hand-maintained entity name list in the registry).
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun run check && bun run lint && bun run format:check</automated>
  </verify>
  <done>The contract exists, the registry discovers definitions from the filesystem, and `fundos` is described declaratively with no owner-id anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Generic EntityScreen.svelte + registry-driven Shell wired into the SignedIn gate</name>
  <read_first>
    - web/src/lib/entities/types.ts (from Task 1)
    - web/src/App.svelte (the 04-01 auth gate and its `data-testid="app-shell"` hook)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md § Pattern 3, Pattern 4, Pitfall 4 (date fields), Anti-Patterns
    - cli/apollo_cli/crud_helpers.py (owner-id injection on create; not-found guards; the update-only-supplied-fields rule)
  </read_first>
  <action>
    Create `web/src/lib/entities/EntityScreen.svelte` (`<script lang="ts">`), taking `config: EntityConfig`
    via `$props()`. It is the ONLY place table/form markup exists in this SPA.

    Query: `const q = db.useQuery(() => buildQuery(config))` where `buildQuery` returns
    `{ [config.etype]: { $: {}, ...linkLabelsAsSubqueries } }` — include each `config.links` label (and each
    `xorLink.choices` label) as a nested sub-object so linked records come back for the list columns.
    Render `q.isLoading` and `q.error` states explicitly before touching `q.data`.

    Table: one `<th>` per `config.listColumns` plus an actions column. Booleans render as `sim`/`não`;
    dates render as the `YYYY-MM-DD` slice; link columns render the target's `targetLabelField`. When the
    result array is empty, render a single row with `data-testid="empty-state"` — never a bare `<tbody>`.
    Each row gets `data-testid="row"` and `data-eid={record.id}`; the edit and delete buttons get
    `data-testid="row-edit"` / `data-testid="row-delete"`.

    Form: driven entirely by `config.fields` — `text`→`<input type="text">`, `textarea`→`<textarea>`,
    `number`→`<input type="number">` (bind as a number, never a string), `boolean`→`<input type="checkbox">`,
    `date`→`<input type="date">`, `select`→`<select>` over `options`. Each field gets
    `data-testid={"field-" + name}` and a `<label for>`. Required fields carry the native `required`
    attribute so an empty submit is blocked client-side without a transact. In edit mode, when
    `config.updatableFields` is set, render ONLY those fields (this is how `instanciasRotina` becomes
    status-only in plan 04-05).

    Date handling (04-RESEARCH.md Pitfall 4 — `useDateObjects` is false and must stay false): when loading a
    record into the form, slice the stored ISO string to its first 10 characters for the `<input type="date">`
    value; on submit, re-serialize as a full ISO string via `new Date(value + "T00:00:00.000Z").toISOString()`.
    Centralize both directions in two small exported helpers in this component's script (or a sibling
    `dates.ts`) so plans 04-03/04-04/04-05 inherit identical behaviour without restating it.

    Links: for each `LinkDef`, render a `<select data-testid={"link-" + label}>` whose options come from a
    second `db.useQuery(() => ({ [targetEtype]: {} }))`, showing `targetLabelField` and valuing the record id;
    include an empty option when `required` is false; when `excludeSelf` is true, filter out the record
    currently being edited. For `xorLink`, render a parent-type chooser
    (`data-testid="xor-parent-type"`) plus a single id select scoped to the chosen type, and enforce
    exactly-one-of before submitting — mirroring the CLI's `required and len(given) != 1` rule.

    Writes: create uses `id()` from `web/src/lib/db.ts` and issues ONE `db.transact` chaining
    `.update({...fields, donoId})` and, when links are chosen, `.link({...})`. `donoId` is read from
    `db.useAuth().user.id` at submit time and NEVER from a form field or a literal (threat T-04-03).
    Update issues `.update()` with only the fields present in the form (drop `undefined`, mirroring the
    CLI's `drop_none`) and never rewrites `donoId`. Delete issues `db.tx[etype][eid].delete()` behind a
    `window.confirm` guard. Before any `.link()`, verify the chosen target id resolves with
    `db.queryOnce` and surface a `parent_not_found`-style error instead of transacting — InstantDB does
    not validate link targets (threat T-04-04, mirrors the CLI's `get_entity`-before-link guard).
    Surface every transact rejection (including permission denials) in a `data-testid="entity-error"`
    element; never swallow it.

    Create `web/src/lib/Shell.svelte`: `let ativo = $state(entityConfigs[0].etype)`, a `<nav>` rendering one
    button per registry entry with `data-testid={"nav-" + etype}`, and
    `<EntityScreen config={configByEtype(ativo)!} />` keyed on `ativo` so switching screens remounts the
    component (and its queries) cleanly. Show the authenticated user's email and keep the
    `data-testid="logout"` button from 04-01.

    Modify `web/src/App.svelte` to render `<Shell />` inside the existing `<SignedIn {db}>` gate, replacing
    04-01's placeholder body while KEEPING the `data-testid="app-shell"` hook on the wrapper so 04-01's
    auth and no-leakage specs continue to pass unchanged. Do not move any query outside the gate.
  </action>
  <acceptance_criteria>
    - `cd web && bun run check && bun run lint && bun run format:check` all exit 0.
    - `cd web && bun run build` exits 0.
    - `grep -rn "donoId" web/src/lib/entities/ web/src/lib/Shell.svelte` matches ONLY the submit-time
      injection line in `EntityScreen.svelte`; verify with
      `grep -rn "donoId" web/src | grep -v "EntityScreen.svelte"` returning no matches (exit 1).
    - `grep -c "useAuth" web/src/lib/entities/EntityScreen.svelte` reports >= 1 (donoId sourced from the session).
    - `grep -rn "<table" web/src --include=*.svelte | grep -vc EntityScreen.svelte` reports 0 (no duplicated table markup).
    - `cd web && bunx playwright test --project=setup --project=anon` still exits 0 (04-01's specs unbroken).
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun run check && bun run lint && bun run format:check && bun run build && bunx playwright test --project=anon</automated>
  </verify>
  <done>One generic screen component renders any EntityConfig, the shell navigates the registry, and the whole thing lives strictly inside the SignedIn gate.</done>
</task>

<task type="auto">
  <name>Task 3: Prove fundos CRUD live in Chromium, including a CLI-created record (SC-3)</name>
  <read_first>
    - web/playwright.config.ts (project names, STORAGE_STATE const from 04-01)
    - web/e2e/auth.spec.ts (the authed-project pattern to copy)
    - web/src/lib/entities/EntityScreen.svelte (the data-testid hooks defined in Task 2)
    - cli/README.md § "Superfície de comandos" (exact `apollo fundo criar|listar|deletar` invocation)
  </read_first>
  <action>
    Create `web/e2e/entities-fundos.spec.ts` in the `authed` project. Two tests:

    Test A — "SC-3: a fundo created by the CLI is visible in the SPA". Before navigating, shell out with
    `execFileSync` to the Phase 3 CLI (`uv run --project cli apollo fundo criar --nome "<unique>" --codigo
    "<unique>" --ativo` — confirm the exact flag names from `cli/README.md` first, and parse the returned
    JSON for the new record id). Then `page.goto("/")`, click `nav-fundos`, and assert a row containing that
    unique nome appears. Tear down by shelling out to `apollo fundo deletar` with the captured id, and assert
    the row disappears from the live SPA list after a reload. Use a `phase04-e2e-` prefix on every generated
    nome/codigo so leftovers are greppable and cleanly removable.

    Test B — "WEB-02: full browser CRUD round trip". From the `fundos` screen: (1) fill `field-nome`,
    `field-codigo`, `field-createdAt`, toggle `field-ativo`, submit, and assert a new row appears WITHOUT a
    page reload (proves the reactive query, not just persistence); (2) click `row-edit` on that row, change
    `field-nome`, submit, assert the row text updates and `data-eid` is unchanged (proves update, not
    create-and-delete); (3) uncheck `field-ativo` in edit mode, submit, reload the page, and assert the
    column still reads `não` (proves the boolean false round trip, not a `"false"` string coercion);
    (4) click `row-delete`, accept the confirm dialog via `page.on("dialog")`, assert the row is gone;
    (5) assert `data-testid="empty-state"` behaviour by filtering to a nome that matches nothing, or —
    if no filter input exists — assert the empty state renders on an entity with no records rather than
    forcing one here.

    Assert in both tests that no element with a `donoId`/`dono` label or input exists on the page
    (`page.locator('[data-testid^="field-dono"]')` has count 0) — the WEB/CLI privilege-parity guarantee
    made visible (threat T-04-03).

    Every test must clean up the records it created (delete on success AND in an `afterEach` guard) so
    re-running the suite is idempotent and leaves the live InstantDB app as it found it.
  </action>
  <acceptance_criteria>
    - `cd web && bunx playwright test --project=authed entities-fundos.spec.ts` exits 0.
    - Running that same command twice in a row both exit 0 (idempotent — no leftover state breaks run 2).
    - After the run, `uv run --project cli apollo fundo listar` output contains zero records whose nome
      starts with `phase04-e2e-` (verify with a `grep -c 'phase04-e2e-'` reporting 0).
    - Fail-loud demonstration recorded in the SUMMARY: temporarily change `EntityScreen.svelte`'s create
      handler to omit the `.update()` call, re-run Test B, confirm it FAILS on the "row appears" assertion,
      then restore and confirm green.
    - `cd web && bunx playwright test` (all projects) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bunx playwright test</automated>
  </verify>
  <done>fundos create/read/update/delete is proven live in a real browser against the real InstantDB app, and a record written by the Phase 3 CLI is provably visible in the SPA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input -> `db.transact` | User-supplied field values and link ids cross here |
| Browser -> InstantDB | `instant.perms.ts` `donoId` rules are evaluated server-side on every query and transact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | `EntityScreen.svelte` create handler | mitigate | `donoId` is injected from `db.useAuth().user.id` at submit time only; no `FieldDef` can express an owner id; Task 1/2/3 grep and DOM gates assert no `dono*` field exists anywhere in `web/src` outside that one injection line |
| T-04-04 | Tampering | `.link()` calls in `EntityScreen.svelte` | mitigate | `db.queryOnce` existence check on the chosen target id before transacting, mirroring the CLI's `get_entity`-before-link guard; a non-resolving id surfaces an error instead of writing a dangling link |
| T-04-01 | Information Disclosure | `Shell.svelte` / `EntityScreen.svelte` queries | mitigate | Both mount only inside `<SignedIn {db}>`; 04-01's `no-leakage.spec.ts` is re-run as an acceptance criterion of Task 2 so this plan cannot regress the guarantee |
| T-04-08 | Denial of Service (data integrity) | e2e test records in the live InstantDB app | mitigate | Every generated record uses the `phase04-e2e-` prefix and is deleted in an `afterEach` guard; Task 3 asserts a zero-leftover count against the live app |
| T-04-09 | Repudiation | Permission-denied transacts silently swallowed | mitigate | Every transact rejection renders into `data-testid="entity-error"`; no empty catch blocks (Biome's `noEmptyBlockStatements`/lint gate backs this) |
</threat_model>

<verification>
1. `cd web && bun run check && bun run lint && bun run format:check && bun run build` — all exit 0.
2. `cd web && bunx playwright test` — setup, authed (incl. fundos), and anon projects all green.
3. `grep -rn "donoId" web/src | grep -v EntityScreen.svelte` — no matches.
4. `uv run --project cli apollo fundo listar | grep -c phase04-e2e-` — 0.
</verification>

<success_criteria>
- WEB-02 satisfied: fundos create, view, edit, delete all proven live in Chromium.
- ROADMAP SC-3 satisfied for at least one entity: a CLI-created fundo renders in the SPA.
- The generic engine (`types.ts` + `registry.ts` + `EntityScreen.svelte` + `Shell.svelte`) is proven,
  so plans 04-03/04-04/04-05 add only `defs/*.ts` files and specs.
</success_criteria>

## Artifacts this phase produces

| Artifact | Consumed by |
|----------|-------------|
| `web/src/lib/entities/types.ts` (`EntityConfig` contract) | 04-03, 04-04, 04-05 definition modules; 04-06 coverage test |
| `web/src/lib/entities/registry.ts` (`entityConfigs`, `configByEtype`) | `Shell.svelte`; 04-06 schema-vs-registry coverage test |
| `web/src/lib/entities/EntityScreen.svelte` (generic CRUD engine + date/link/XOR handling) | Every entity screen in 04-03/04-04/04-05 |
| `web/src/lib/Shell.svelte` (registry-driven nav, `data-testid="nav-<etype>"`) | Every downstream entity spec's navigation step |
| `web/e2e/entities-fundos.spec.ts` (CLI-record-visibility + CRUD spec template) | Pattern copied by 04-03/04-04/04-05; Phase 6 VERIFY-01 |

<output>
After completion, create `.planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md`.
Record verbatim: the final `EntityConfig` type as shipped (downstream plans depend on it), the exact
`data-testid` vocabulary `EntityScreen.svelte` exposes, the exact CLI invocation used for the SC-3 proof
and its output, and the fail-loud demonstration for Test B.
</output>
</content>
