---
phase: 23-focus-dialog-system
plan: 07
type: execute
wave: 6
depends_on: ["23-06"]
files_modified:
  - web/e2e/focus-dialog-button-inventory.spec.ts
autonomous: true
requirements: [DLG-02, DLG-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the phase's inventoried clickable surfaces (the original 14-item Exhaustive Inert-Button Inventory plus the 2 additive ProjetosSection list-view targets from the resolved wiring-scope decision, totaling 16) is a real <button>, keyboard-reachable via Tab, and activatable via Enter and Space -- verified in one consolidated e2e sweep, not re-derived from each plan's own scoped tests"
    - "At no point during any single-dialog OR depth-2 interaction does more than one Dialog.Content ever become simultaneously visible"
    - "The full pre-existing Playwright suite (every spec file that existed before this phase, plus every focus-dialog-*.spec.ts file this phase added) passes together in one run, with zero regression attributable to this phase's edits to Dashboard.svelte, WeekCalendar.svelte, TicketQueue.svelte, ProjectStrips.svelte, RoutinesByFundo.svelte, MonthHeatmap.svelte, ProjetosSection.svelte, or dashboardQuery.ts"
  artifacts:
    - web/e2e/focus-dialog-button-inventory.spec.ts
  key_links:
    - "This plan asserts against the SAME testids every prior plan already wired -- it introduces no new component code, only a consolidated completeness/regression proof, matching this phase's own Validation Architecture Sampling Rate ('Phase gate: Full suite green before verify-work')"
---

<objective>
Run one consolidated, cross-cutting audit: every click target this phase's 6 prior plans wired is
confirmed to be a real, keyboard-accessible `<button>` opening the correct dialog kind, the
depth-cap-2 swap-in-place invariant holds everywhere it applies, and the entire pre-existing
Playwright suite plus every `focus-dialog-*.spec.ts` file this phase added passes together in one
run.

Purpose: RESEARCH.md's own conclusion for this phase was "there is no new 'hard' sub-problem...
the risk is entirely completeness (missing a click target) and consistency (depth cap, testids),
not technical difficulty" -- this plan is the dedicated completeness net that closes that exact
risk, independent of and in addition to each individual plan's own narrower e2e coverage. This is
also the phase's final gate before milestone audit -- Phase 23 is the last functional phase of
v1.3.
Output: `web/e2e/focus-dialog-button-inventory.spec.ts`, and a fully green combined Playwright run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-focus-dialog-system/23-CONTEXT.md
@.planning/phases/23-focus-dialog-system/23-RESEARCH.md
@web/e2e/focus-dialog-ticket.spec.ts
@web/e2e/focus-dialog-projetos-kanban.spec.ts
@web/e2e/focus-dialog-dia-rotina.spec.ts
@web/e2e/focus-dialog-fundo.spec.ts
@web/e2e/focus-dialog-projeto.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated button-inventory + keyboard-accessibility + depth-cap-2 sweep</name>
  <files>web/e2e/focus-dialog-button-inventory.spec.ts</files>
  <action>
Create `web/e2e/focus-dialog-button-inventory.spec.ts` (prefix `phase23-e2e-`). Seed one complete
fixture set covering every surface: a fundo, a projeto with 2 etapas (one with 2 tarefas, one with
0), a ticket, and a rotina instance dated this week. Build a single
`INVENTORY: { testid: string; dialogTestidHint: string }[]` table in the test file itself, listing
all 16 targets by testid: `dash-week-day-header`, `dash-week-item`, `dash-weekend-day-header`,
`dash-weekend-popover-item`, `dash-ticket-card`, `project-strip-nome`, `project-strip-fundo-badge`,
`project-strip-column-header`, `project-strip-card`, `rotinas-fundo-titulo`, `rotinas-row`,
`dash-heatmap-cell`, `etapa-kanban-column`, `etapa-kanban-card`, `etapa-row-abrir`,
`etapa-tarefa-row-abrir` -- annotate in a comment which ones require the weekend popover or the
projetos-section kanban toggle to be opened first before they exist in the DOM.

Test A ("every inventoried target is a real button"): for each reachable target (opening the
weekend popover / switching to kanban view as needed first), assert
`el.evaluate((e) => e.tagName.toLowerCase()) === "button"`.

Test B ("keyboard reachability and activation"): for a representative subset covering each of the
5 host files (`dash-ticket-card`, `dash-week-day-header`, `project-strip-nome`,
`rotinas-fundo-titulo` -- skip for the "Sem fundo vinculado" no-op case, use the seeded fundo's
real card instead --, `etapa-kanban-column`): `Tab` to the element (or `.focus()` it directly and
assert `document.activeElement` matches, then dispatch a keyboard `Enter` via
`page.keyboard.press("Enter")`) and confirm the SAME dialog opens as a mouse click would; repeat
with `Space` for at least one target, confirming native `<button>` behavior (both keys activate a
real button with no extra keydown handler needed -- this test exists to catch a regression to a
non-button element, not to test bits-ui/browser button semantics themselves).

Test C ("never two simultaneously visible Dialog.Content, across both depth-2 launch points"):
open the Projeto dialog, click into the Etapa dialog (depth 2), assert `page.locator('[role="dialog"]')`
(bits-ui's Dialog.Content role) has count exactly 1 at every step; click the breadcrumb back,
assert count still exactly 1 (now showing Projeto again); repeat the identical assertion sequence
for Dia -> Tarefa (or Rotina/Ticket, whichever the fixture's seeded weekday item is) via the week
calendar.

Test D ("etapa-kanban-column/etapa-kanban-card are no longer plain non-interactive <div>s" --
regression guard for the original Pitfall 3): assert
`page.locator('div[data-testid="etapa-kanban-column"]')` and
`div[data-testid="etapa-kanban-card"]"` both resolve to ZERO elements (the old `<div>`-typed
selector must find nothing, since both are now `<button>`s carrying the identical testid).

`afterAll` sweeps every `phase23-e2e-` fixture.
  </action>
  <verify>
    <automated>cd web && bunx playwright test focus-dialog-button-inventory.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>All 16 inventoried targets are proven real buttons; keyboard activation works for a representative cross-section; depth-cap-2's swap-in-place invariant holds for both Projeto and Dia; the original etapa-kanban div-based selector now matches zero elements.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression run and fix-forward</name>
  <files></files>
  <action>
Run the complete Playwright suite: `cd web && bunx playwright test --project=authed --no-deps`.
This exercises every spec file in `web/e2e/` including every pre-existing file this phase's 6
prior plans touched indirectly (`dashboard.spec.ts`, `dashboard-kanbans.spec.ts`,
`projetos-section.spec.ts`, `shell-nav.spec.ts`, `shell-chrome.spec.ts`, and every other file that
visits the Dashboard or Projetos section) alongside all 6 new `focus-dialog-*.spec.ts` files and
this plan's own new inventory spec. If any pre-existing test fails: diagnose whether the failure is
(a) a genuine regression this phase's edits introduced (fix the source file, not the test, unless
the test's own assertion was already stale/wrong for a documented reason -- cite the reason in the
SUMMARY if so) or (b) a pre-existing flake already logged in STATE.md (e.g. the documented
`login-flow.spec.ts` magic-code timing flake) -- for (b), re-run in isolation to confirm it is
unrelated to this phase before treating it as non-blocking. Run `cd web && bun run check` and `cd
web && bun test src` as final gates. Do not proceed to closing this plan until the full suite is
green (accounting only for already-documented, unrelated pre-existing flakes).
  </action>
  <verify>
    <automated>cd web && bunx playwright test --project=authed --no-deps && bun run check && bun test src</automated>
  </verify>
  <done>The entire Playwright suite, svelte-check/tsc, and bun unit tests all pass together, with any pre-existing flake explicitly identified and confirmed unrelated to this phase's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan's own fixtures | Read-only assertions against already-established, already-owner-scoped seeded data; no new write/read path is introduced anywhere in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-17 | Denial of Service | A `page.locator('[role="dialog"]')` count assertion flaking under CI timing (dialog animation in-flight) | low | accept | Use Playwright's own auto-retrying `expect(...).toHaveCount(1, { timeout: ... })` rather than a synchronous count read, matching every other timing-sensitive assertion already established throughout this codebase's e2e suite |
| T-23-18 | Repudiation | N/A -- this plan performs no state-changing action of its own | n/a | accept | Pure verification plan |
</threat_model>

<verification>
`cd web && bunx playwright test --project=authed --no-deps` is fully green (or shows only the
already-documented, confirmed-unrelated pre-existing flakes). `cd web && bun run check` and `cd web
&& bun test src` both pass with zero errors.
</verification>

<success_criteria>
Phase 23's own ROADMAP success criteria (1-4: all 7 dialogs at 3 widths with correct chrome; every
Dashboard/section surface named in spec §4 is a real, keyboard-accessible button opening the
correct dialog; no dialog ever opens a third level; Esc/click-outside/×/fechar close except while
busy, destructive actions stay in AlertDialog) are all provable by a single green Playwright run,
closing the last functional phase of milestone v1.3.
</success_criteria>

<output>
Create `.planning/phases/23-focus-dialog-system/23-07-SUMMARY.md` when done.
</output>
