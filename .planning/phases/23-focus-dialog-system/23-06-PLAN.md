---
phase: 23-focus-dialog-system
plan: 06
type: execute
wave: 5
depends_on: ["23-01", "23-03", "23-05"]
files_modified:
  - web/src/lib/dashboard/dialogs/ProjectDialog.svelte
  - web/src/lib/dashboard/Dashboard.svelte
  - web/src/lib/dashboard/dashboardQuery.ts
  - web/src/lib/dashboard/ProjectStrips.svelte
  - web/e2e/focus-dialog-projeto.spec.ts
autonomous: true
requirements: [DLG-01, DLG-02, DLG-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicking a project strip's name opens the Projeto dialog (L width) showing the FULL, unbounded kanban of every etapa/tarefa (no 3-card cap), horizontally scrollable, with a working '+ tarefa' per column"
    - "From inside the Projeto dialog, clicking a column header opens the Etapa dialog and clicking a card opens the Tarefa dialog -- both as depth-2, with a breadcrumb back to the Projeto dialog and no third level reachable from either"
    - "project-strip-column-header and project-strip-card on the Dashboard's own mini-kanban strips (outside any dialog) also open Etapa/Tarefa respectively, completing DLG-02's full button inventory"
    - "DASHBOARD_QUERY's projetos.etapas.tarefas branch now also fetches subtarefas, fixing the pre-existing (documented, Phase-22-deferred) gap where every nested tarefa's completion count silently evaluated to 0/N regardless of reality -- this benefits both the new ProjectDialog kanban and the pre-existing ProjectStrips/RoutinesByFundo-adjacent rendering that reads the same nested path"
  artifacts:
    - web/src/lib/dashboard/dialogs/ProjectDialog.svelte
  key_links:
    - "ProjectDialog.svelte's onOpenEtapa/onOpenTarefa callback props are implemented by Dashboard.svelte as openDialog({kind:\"etapa\"|\"tarefa\", id}) -- pushing a SECOND stack entry, reusing the exact EtapaDialog/TaskDialog components Plan 23-03 already built with zero further edits to either file"
    - "The '+ tarefa por coluna' affordance inside ProjectDialog reuses ProjetosSection.svelte's exact hidden-EntityScreen(tarefasConfig, presetLinks={etapa:<id>})-host pattern (ProjetosSection.svelte's own startCreateTarefa/tarefaHostEl), as a THIRD independent hidden host inside this one dialog component (alongside its own editar host)"
---

<objective>
Build `ProjectDialog.svelte` (L) -- the most structurally complex of the 7 dialogs, since it is one
of only two first-level launch points (with Dia) that can open a second dialog from inside its own
body -- and wire the last two Dashboard mini-kanban surfaces (`project-strip-nome`,
`project-strip-column-header`, `project-strip-card`) that target Projeto/Etapa/Tarefa. Also fix a
small, pre-existing, documented query-completeness gap (`DASHBOARD_QUERY`'s nested tarefas branch
never fetched `subtarefas`) that this plan's own Etapa/Projeto column-header progress display would
otherwise silently perpetuate.

Purpose: this is the plan that actually exercises depth-2 navigation for the first time (Plan
23-01 only built the depth-2-ready mechanism; Plan 23-04's Dia dialog is the OTHER depth-2 launch
point but that work is independent) -- proving the breadcrumb-pop-back-to-1, swap-in-place,
never-two-simultaneous-Dialog.Root invariant end-to-end against real UI, not just against the
mechanism's own unit shape.
Output: `ProjectDialog.svelte`, `Dashboard.svelte`'s "projeto"/"etapa" dialog-stack branches (with
depth-2 breadcrumb wiring), `dashboardQuery.ts`'s one-line completeness fix,
`ProjectStrips.svelte`'s remaining wired buttons, and `web/e2e/focus-dialog-projeto.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-focus-dialog-system/23-CONTEXT.md
@.planning/phases/23-focus-dialog-system/23-RESEARCH.md
@web/src/lib/dashboard/Dashboard.svelte
@web/src/lib/dashboard/ProjectStrips.svelte
@web/src/lib/dashboard/dashboardQuery.ts
@web/src/lib/dashboard/dialogs/TaskDialog.svelte
@web/src/lib/dashboard/dialogs/EtapaDialog.svelte
@web/src/lib/sections/ProjetosSection.svelte
@spec-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboardQuery.ts completeness fix + ProjectDialog.svelte (L, depth-2, unbounded kanban, "+ tarefa")</name>
  <files>web/src/lib/dashboard/dashboardQuery.ts, web/src/lib/dashboard/dialogs/ProjectDialog.svelte</files>
  <action>
In `web/src/lib/dashboard/dashboardQuery.ts`, widen the existing `DASHBOARD_QUERY.projetos`
branch from `{ fundo: {}, etapas: { tarefas: {} } }` to
`{ fundo: {}, etapas: { tarefas: { subtarefas: {} } } }` -- still exactly one query (DASH-07's
"uma query, não sete" is unaffected: this deepens the SAME query object by one nesting level, it
does not add a new `db.useQuery` call site anywhere). This closes the gap STATE.md documents from
Phase 22 ("DASHBOARD_QUERY's projetos.etapas.tarefas branch omits subtarefas... so
tarefaConcluida() on a nested strip card always evaluates false -- documented, not patched"):
`ProjectStrips.svelte`'s own `project-strip-column-header` (already showing `{feitas}/{total}` via
`progressoEtapa`) and this plan's own new `ProjectDialog.svelte` column headers both read through
this exact nested path, so both were -- and without this fix, would remain -- silently wrong.
Document this explicitly and prominently in the SUMMARY as a deliberate, in-scope bugfix riding
along with this plan's own new column-header UI, not a silent, unrelated change.

Create `web/src/lib/dashboard/dialogs/ProjectDialog.svelte`. Declare
`ProjectDialogRow = { id: string; nome: string; fundoNome?: string | null; etapas: { id: string;
nome: string; ordem: number; tarefas: { id: string; titulo: string; tipoPrazo: string;
dataPrevista?: string | null; status: string; subtarefas?: { concluida: boolean }[] }[] }[] }`.
Props: `open: boolean`, `projeto: ProjectDialogRow | undefined`, `onOpenEtapa: (id: string) =>
void`, `onOpenTarefa: (id: string) => void`, `onOpenChange: (open: boolean) => void`. No
`breadcrumb` prop -- like Dia, Projeto is always depth-1, never itself a second-level target (only
Etapa/Tarefa can be opened FROM it).

Content (spec §4 row 4: kanban completo das etapas na ordem, scroll horizontal, "+ tarefa" por
coluna): title = `projeto.nome`; contexto =
`` `${projeto.fundoNome ?? "Sem fundo vinculado"} · ${projeto.etapas.length} etapas · ${projeto.etapas.reduce((s, e) => s + e.tarefas.length, 0)} tarefas` ``.
Body: a `data-testid="project-dialog-kanban"` `overflow-x-auto` flex row (plain scroll, no
measured `›` overflow indicator here -- that measured-`scrollWidth`-vs-`clientWidth` rule is
`ProjectStrips.svelte`'s own §3.5 dashboard-strip-specific discipline, not restated for this
dialog's own kanban in §4's content rules; do not port it here). For each `etapa` in
`[...projeto.etapas].sort((a, b) => a.ordem - b.ordem)`: a fixed-width column `<div>` containing
(1) `<button type="button" data-testid="project-dialog-column-header" data-eid={etapa.id}
class="block w-full text-left" onclick={() => onOpenEtapa(etapa.id)}>` showing `ordem`, `nome`, and
`` `${feitas}/${total}` `` from `progressoEtapa(etapa)` (import from `../derive`); (2) every one of
that etapa's `tarefas` (no 3-cap -- this dialog's whole purpose is the uncapped view), each a
`<button type="button" data-testid="project-dialog-card" data-eid={tarefa.id} onclick={(e) => {
e.stopPropagation(); onOpenTarefa(tarefa.id); }}>` showing `titulo` and `tipoPrazo`+`dataPrevista`
with `text-destructive`/`border-destructive` when `vencido(tarefa.dataPrevista,
tarefaConcluida(tarefa), new Date())`; (3) a `<Button variant="outline" size="sm"
data-testid="project-dialog-add-tarefa" data-eid={etapa.id} onclick={() =>
startAddTarefa(etapa.id)}>+ tarefa</Button>`.

"+ tarefa" host: `let addTarefaHostReady = $state(false); let addTarefaHostEl =
$state<HTMLDivElement | undefined>(undefined); let addTarefaEtapaId = $state<string |
null>(null);` `async function startAddTarefa(etapaId: string): Promise<void> { addTarefaEtapaId =
etapaId; addTarefaHostReady = true; await tick(); /* bounded-poll (5s/50ms) for
[data-testid="entity-create-start"] inside addTarefaHostEl, then .click() */ }` -- mirrors
`ProjetosSection.svelte`'s `startCreateTarefa`/`tarefaHostEl` exactly
(`ProjetosSection.svelte:265-292`). Render `{#if addTarefaHostReady}<div class="hidden"
aria-hidden="true" data-testid="project-dialog-add-tarefa-host" bind:this={addTarefaHostEl}>
<EntityScreen config={tarefasConfig} presetLinks={addTarefaEtapaId ? { etapa: addTarefaEtapaId } :
null} /></div>{/if}` -- `presetLinks` reads the LIVE `addTarefaEtapaId` `$state` every render
(never a snapshotted value), so by the time the bounded poll's `.click()` fires, `EntityScreen`'s
own `startCreate()` reads the already-current preset link, identical to
`ProjetosSection.svelte:285-292`'s own documented reasoning.

"editar" (a SEPARATE, second hidden host, `project-dialog-edit-host`): drives `projetosConfig`'s
`row-edit` on `data-eid={projeto.id}`. "ver na página completa" clicks `[data-testid="nav-projetos"]`
then `onOpenChange(false)`. `size="L"`.
  </action>
  <verify>
    <automated>cd web && bun run check</automated>
  </verify>
  <done>ProjectDialog renders every etapa/tarefa unbounded, "+ tarefa" creates a real tarefa correctly linked to the clicked column's etapa, and the query fix makes every progressoEtapa reading (here and in ProjectStrips) correct instead of always 0/N.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard.svelte "projeto"/"etapa" branches (depth-2 breadcrumb wiring) + ProjectStrips.svelte remaining wiring</name>
  <files>web/src/lib/dashboard/Dashboard.svelte, web/src/lib/dashboard/ProjectStrips.svelte</files>
  <action>
In `Dashboard.svelte`: add `function openProjetoDialog(id: string): void { openDialog({ kind:
"projeto", id }); }`, `function openEtapaDialog(id: string): void { openDialog({ kind: "etapa", id
}); }` (`openTarefaDialog` already exists from Plan 23-04). Add
`const activeProjetoForDialog = $derived.by(() => activeDialogRef?.kind === "projeto" ?
projetoRows().find((p) => p.id === activeDialogRef.id) : undefined);` -- shape it into
`ProjectDialog`'s expected `ProjectDialogRow` inline at the mount point (map `fundo?.nome` to
`fundoNome`, pass `etapas` through as-is, now correctly carrying `subtarefas` per Task 1's query
fix). Add
`const activeEtapaForDialog = $derived.by(() => { if (activeDialogRef?.kind !== "etapa") return
undefined; for (const p of projetoRows()) { const e = p.etapas?.find((x) => x.id ===
activeDialogRef.id); if (e) return { id: e.id, nome: e.nome, ordem: e.ordem, projetoNome: p.nome,
fundoNome: p.fundo?.nome ?? null, tarefas: e.tarefas ?? [] }; } return undefined; });`.

Import `ProjectDialog`. Extend the dialog-stack chain with `{:else if activeDialogRef?.kind ===
"projeto"}<ProjectDialog open={true} projeto={activeProjetoForDialog ? { id:
activeProjetoForDialog.id, nome: activeProjetoForDialog.nome, fundoNome:
activeProjetoForDialog.fundo?.nome ?? null, etapas: activeProjetoForDialog.etapas ?? [] } :
undefined} onOpenEtapa={openEtapaDialog} onOpenTarefa={openTarefaDialog}
onOpenChange={(open) => { if (!open) closeAllDialogs(); }} />{:else if activeDialogRef?.kind ===
"etapa"}<EtapaDialog open={true} etapa={activeEtapaForDialog} breadcrumb={breadcrumbRef ? {
label: breadcrumbLabelFor(breadcrumbRef), onClick: popToFirst } : undefined}
onOpenChange={(open) => { if (!open) closeAllDialogs(); }} />{/if}` (import `EtapaDialog` from
`./dialogs/EtapaDialog.svelte`, already built by Plan 23-03 -- pure reuse). The existing `"tarefa"`
branch (Plan 23-04) already passes `breadcrumb` the identical way -- no change needed there, it
already works for both the depth-1 (WeekCalendar/kanban-strip) and depth-2 (via Projeto) paths
since both push through the SAME `openTarefaDialog`/`openDialog` functions.

In `ProjectStrips.svelte`: add `onOpenProjeto: (id: string) => void`, `onOpenEtapa: (id: string) =>
void`, `onOpenTarefa: (id: string) => void` to `$props()` (alongside the already-added
`onOpenFundo` from Plan 23-05). On `project-strip-nome`, add `onclick={() =>
onOpenProjeto(projeto.id)}`. On `project-strip-column-header`, add `onclick={() =>
onOpenEtapa(etapa.id)}`. On `project-strip-card`, add `onclick={(e) => { e.stopPropagation();
onOpenTarefa(tarefa.id); }}` (defensive `stopPropagation`, consistent with every other
nested-card-inside-column click target in this phase, even though the column header and card are
siblings, not nested, in the current DOM). In `Dashboard.svelte`'s existing `<ProjectStrips .../>`
mount, add `onOpenProjeto={openProjetoDialog}`, `onOpenEtapa={openEtapaDialog}`,
`onOpenTarefa={openTarefaDialog}`.
  </action>
  <verify>
    <automated>cd web && bun run check</automated>
  </verify>
  <done>Dashboard.svelte's dialog-stack chain now covers all 7 kinds. Depth-2 breadcrumb wiring is symmetric for both Etapa and Tarefa reached via Projeto. Every ProjectStrips button (nome/fundo-badge/column-header/card) is wired.</done>
</task>

<task type="auto">
  <name>Task 3: e2e coverage -- unbounded kanban, "+ tarefa", depth-cap-2 proof, ProjectStrips full inventory, progressoEtapa fix</name>
  <files>web/e2e/focus-dialog-projeto.spec.ts</files>
  <action>
Create `web/e2e/focus-dialog-projeto.spec.ts` (prefix `phase23-e2e-`). Seed a projeto with an etapa
holding 5 tarefas, at least one with a subtarefa (all `concluida`) to exercise the query-fix
proof, and one etapa with zero tarefas. Tests:
(a) clicking `project-strip-nome` opens the Projeto dialog at L width; its own kanban shows all 5
`project-dialog-card` elements for that column (no cap, no "+N" text anywhere inside the dialog),
unlike the Dashboard strip's own 3-card + "+2" rendering for the SAME etapa visible simultaneously
on the page beneath the dialog;
(b) the dialog's `project-dialog-column-header` for the etapa with the all-concluida-subtarefas
tarefa shows `1/5` (not `0/5`) -- direct proof of Task 1's query-completeness fix; separately,
assert `ProjectStrips.svelte`'s own (pre-existing, page-level, not-inside-any-dialog)
`project-strip-column-header` ALSO now shows `1/5` for the same etapa, proving the fix benefits
both surfaces from one query change;
(c) `project-dialog-add-tarefa` for a given column creates a real tarefa (fill `field-titulo` etc.,
submit) that is immediately linked to that exact etapa -- verify via the CLI (`apollo tarefa
listar`) that the new tarefa's `etapa` link resolves to the clicked column's etapa id;
(d) clicking `project-dialog-column-header` opens the Etapa dialog AS DEPTH 2: assert exactly one
`Dialog.Content` is visible (never two), assert `focus-dialog-breadcrumb` is visible and reads the
projeto's nome, and clicking it returns to the Projeto dialog (still open, same kanban visible) --
assert NOTHING opens a third level from inside that Etapa dialog (its own task rows are not
buttons);
(e) clicking `project-dialog-card` opens the Tarefa dialog as depth 2 with the identical breadcrumb
behavior;
(f) on the page (outside any dialog), `project-strip-nome`/`project-strip-column-header`/
`project-strip-card` all resolve to `tagName === "button"` and each opens its correct dialog kind
directly (depth 1).
`afterAll` sweeps every `phase23-e2e-` fixture including the tarefa created in (c).
  </action>
  <verify>
    <automated>cd web && bunx playwright test focus-dialog-projeto.spec.ts dashboard-kanbans.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>Projeto dialog's unbounded kanban and "+ tarefa" both work correctly; depth-cap-2 is proven live (never two simultaneous Dialog.Content, breadcrumb pops back to exactly depth 1, no third level exists); the progressoEtapa query fix is proven on both the dialog and the pre-existing strip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `dashboardQuery.ts`'s widened `projetos.etapas.tarefas.subtarefas` branch | Still the SAME single query, still scoped server-side by `instant.perms.ts`'s unchanged `donoId` rule -- widening the nesting depth exposes no new entity type, only a field already exposed identically via the query's own flat `tarefas` branch |
| "+ tarefa" hidden host -> `db.transact` | Delegates entirely to `EntityScreen.svelte`'s existing, unmodified create path; `presetLinks` is a UI convenience, never a server-side authorization input (mirrors `SubtarefasPanel.svelte`'s own documented note) |
| `dialogStack` depth-2 (`[projetoRef, etapaOrTarefaRef]`) | Purely local UI navigation state; the breadcrumb's `popToFirst()` only ever truncates the array to length 1, it never re-fetches or re-authorizes anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-14 | Elevation of Privilege | `addTarefaEtapaId` used as an implicit trust signal for which etapa a new tarefa may link to | low | accept | `presetLinks` only pre-fills the form's own link select, which stays visible and editable (spec §2.1) -- the server-side `db.transact` call still goes through `EntityScreen.svelte`'s unmodified validation/link-target-exists check (`EntityScreen.svelte:340-351`), never bypassing it |
| T-23-15 | Tampering | Two `Dialog.Root`s becoming simultaneously mounted if a future edit to the dialog-stack chain adds a second unconditional render branch | high | mitigate | The chain stays a single `{#if}/{:else if}` sequence (never two independent `{#if}` blocks); Task 3's e2e test (d) explicitly asserts exactly one `Dialog.Content` is visible at every step of the depth-2 flow |
| T-23-16 | Information Disclosure | Widening `DASHBOARD_QUERY`'s nesting depth inadvertently fetching a field beyond `subtarefas.concluida` | low | mitigate | The one-line change adds only `{ subtarefas: {} }` (an empty sub-selection, matching every other link traversal in this same query object) -- InstantDB returns every own scalar of the linked `subtarefas` regardless, identical to the flat `tickets`/`tarefas` branches' own existing behavior; no new entity type is introduced |
</threat_model>

<verification>
Run `cd web && bunx playwright test focus-dialog-projeto.spec.ts dashboard.spec.ts
dashboard-kanbans.spec.ts focus-dialog-fundo.spec.ts focus-dialog-dia-rotina.spec.ts
focus-dialog-ticket.spec.ts focus-dialog-projetos-kanban.spec.ts --project=authed --no-deps` --
every focus-dialog spec plus both pre-existing dashboard specs must pass together, since this plan
touches `Dashboard.svelte`/`ProjectStrips.svelte`/`dashboardQuery.ts` for the last time this phase.
</verification>

<success_criteria>
All 7 dialog kinds exist and are reachable from Dashboard.svelte. Depth-cap-2 is proven live for
both of the phase's two first-level launch points that support it (Projeto here; Dia in Plan
23-04). Every `ProjectStrips.svelte` button from the Exhaustive Inert-Button Inventory is wired.
The pre-existing progressoEtapa display gap is fixed.
</success_criteria>

<output>
Create `.planning/phases/23-focus-dialog-system/23-06-SUMMARY.md` when done.
</output>
