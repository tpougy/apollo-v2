---
phase: 23-focus-dialog-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/dashboard/derive.ts
  - web/src/lib/dashboard/derive.test.ts
autonomous: true
requirements: [DLG-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The Fundo dialog (built in Plan 23-05) can list every rotina instance belonging to a fundo, regardless of which calendar week it falls in -- spec-ui.md §4 row 5's 'não só as da semana' requirement -- via a new, pure, unit-tested derive.ts export, never an inline component-local filter"
  artifacts:
    - web/src/lib/dashboard/derive.ts (rotinasDoFundo export added)
    - web/src/lib/dashboard/derive.test.ts (rotinasDoFundo coverage added)
  key_links:
    - "rotinasDoFundo has the same input shape (InstanciaAgendaLike-compatible: id, template.fundo.id) as the existing rotinasPorFundo, so Plan 23-05's FundoDialog.svelte can pass it the exact same already-fetched instanciasRotina array Dashboard.svelte already holds, zero new query"
---

<objective>
Add one new pure, unit-tested export to `web/src/lib/dashboard/derive.ts` --
`rotinasDoFundo(instancias, fundoId)` -- a week-unbounded filter (by fundo id only, no date-window
check) that the Fundo dialog (Plan 23-05) needs to satisfy spec-ui.md §4 row 5's "todas as rotinas
do fundo (não só as da semana)" content requirement.

Purpose: keep this join/filter logic in `derive.ts`'s existing pure, testable module (resolved
decision: "one new pure function needed in derive.ts... do not inline this logic in a component"),
consistent with every other cross-referencing rule this module already owns (`agendaPorDia`,
`rotinasPorFundo`, `cargaDoMes`). This plan has zero UI/component work and zero dependency on any
other Phase 23 plan, so it can execute in parallel with Plan 23-01.
Output: `derive.ts`'s new `rotinasDoFundo` export, covered by `derive.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-focus-dialog-system/23-CONTEXT.md
@.planning/phases/23-focus-dialog-system/23-RESEARCH.md
@web/src/lib/dashboard/derive.ts
@web/src/lib/dashboard/derive.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: derive.ts -- rotinasDoFundo (week-unbounded, fundo-scoped filter)</name>
  <files>web/src/lib/dashboard/derive.ts, web/src/lib/dashboard/derive.test.ts</files>
  <behavior>
    - Given 3 instancias whose `template.fundo.id` are `"a"`, `"b"`, `"a"` respectively, and one
      whose `template` is `null`, `rotinasDoFundo(instancias, "a")` returns exactly the 2 whose
      `template.fundo.id === "a"`, in their original relative order (stable, no re-sort by date --
      this is a pure filter, unlike `rotinasPorFundo`'s own group-then-sort).
    - `rotinasDoFundo(instancias, "a")` includes an instancia whose `dataPrevista` falls many weeks
      outside `rotinasPorFundo`'s own 7-day window, proving this export is genuinely week-unbounded
      (the one behavior distinguishing it from the existing `rotinasPorFundo`).
    - `rotinasDoFundo(instancias, "nonexistent-id")` returns an empty array, never throws.
    - An instancia whose `template` is `null` (no fundo at all) is never matched by any concrete
      `fundoId` string -- confirms the function never coerces `null`/`undefined` to a truthy match.
  </behavior>
  <action>
Add to `web/src/lib/dashboard/derive.ts`, immediately after the existing `rotinasPorFundo` export
(reuse its own `InstanciaAgendaLike` interface -- do not declare a second, near-duplicate type):
`` export function rotinasDoFundo(instancias: InstanciaAgendaLike[], fundoId: string): InstanciaAgendaLike[] { return instancias.filter((i) => i.template?.fundo?.id === fundoId); } ``
Document it with a JSDoc comment citing spec-ui.md §4 row 5 ("todas as rotinas do fundo, não só as
da semana") and explicitly noting the contrast with `rotinasPorFundo`: this export takes NO
`semana` parameter and applies no date-window filter at all -- it is the deliberate week-unbounded
counterpart the Fundo dialog needs, reusing the exact same `InstanciaAgendaLike` shape so any
caller that already has the array `rotinasPorFundo` accepts can pass it here unchanged.

Add a new `describe("rotinasDoFundo", ...)` block to `web/src/lib/dashboard/derive.test.ts`,
placed immediately after the existing `describe("rotinasPorFundo", ...)` block, covering every
case in `<behavior>` above. Reuse this file's existing fixture-construction style (plain literal
objects matching `InstanciaAgendaLike`, no new test helper needed).
  </action>
  <verify>
    <automated>cd web && bun test src/lib/dashboard/derive.test.ts</automated>
  </verify>
  <done>rotinasDoFundo exists as a pure, exported derive.ts function filtering by fundo id only (no week window), covered by 4 passing unit tests including one instancia dated outside any 7-day window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `rotinasDoFundo`'s input array | Already owner-scoped by `instant.perms.ts` before it ever reaches this pure function -- this module never fetches, it only filters what its caller already fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Information Disclosure | `rotinasDoFundo` receiving the full, unfiltered `instanciasRotina` array (every fundo's instances, not just one) | low | accept | This mirrors every other `derive.ts` export's existing contract (`rotinasPorFundo`/`agendaPorDia` also receive the full array and filter client-side) -- the array itself was already scoped server-side to the current user's own data before this function ever sees it; `rotinasDoFundo` only narrows it further by `fundoId`, it never widens exposure |
</threat_model>

<verification>
Run `cd web && bun test src/lib/dashboard/derive.test.ts` and confirm all tests pass, including the
4 new `rotinasDoFundo` cases. Run `cd web && bun run check` and confirm zero new TypeScript errors.
</verification>

<success_criteria>
`rotinasDoFundo` is a pure, unit-tested `derive.ts` export ready for Plan 23-05's `FundoDialog.svelte`
to import directly, satisfying spec-ui.md §4 row 5's week-unbounded content requirement with zero
new query and zero component-local join logic.
</success_criteria>

<output>
Create `.planning/phases/23-focus-dialog-system/23-02-SUMMARY.md` when done.
</output>
