---
phase: 19-projetos-section-master-detail
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - web/e2e/helpers/gotoNested.ts
  - web/e2e/shell-nav.spec.ts
  - web/e2e/entities-header-states.spec.ts
  - web/e2e/cross-phase-verification.spec.ts
  - web/e2e/entities-projeto-etapa-tarefa.spec.ts
autonomous: true
requirements: [NEST-02, NEST-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every pre-existing Playwright file/loop that assumed every primary-nav entity (including projetos) mounts a plain EntityScreen now either excludes projetos from that generic assumption or asserts the new master-detail markup instead -- with no reduction in what each test actually proves (NEST-02, NEST-03)."
    - "gotoNested(page, \"etapas\")/gotoNested(page, \"tarefas\") both keep their original stable call signature (Phase 18's contract) -- zero edits to any of the 4 files whose gotoNested(\"tarefas\") call sites live outside this phase's own edited files (entities-form-dialog-composition.spec.ts, entities-delete-confirmation.spec.ts, and cross-phase-verification.spec.ts's own two gotoNested(\"tarefas\") call sites)."
    - "The full pre-existing + new Playwright suite (all 3 projects: setup/authed/anon) passes, and web/e2e/ contains zero remaining nav-etapas/nav-tarefas click or reference tied to the old flat projetos screen."
  artifacts:
    - web/e2e/helpers/gotoNested.ts
    - web/e2e/shell-nav.spec.ts
    - web/e2e/entities-header-states.spec.ts
    - web/e2e/cross-phase-verification.spec.ts
    - web/e2e/entities-projeto-etapa-tarefa.spec.ts
  key_links:
    - "gotoNested.ts's updated \"etapas\"/\"tarefas\" bodies -> the exact testids ProjetosSection shipped in Plans 19-01/19-02/19-03 (nav-projetos, project-item, projetos-tab-todas, project-etapas-list) -- this plan is Wave 4 specifically because it depends on those testids already existing and stable."
---

<objective>
Fix every pre-existing Playwright assertion that Plans 19-01 through 19-03's Projetos redesign
breaks, discovered via two sources: RESEARCH.md's own catalogued Pitfalls 1-4
(`entities-projeto-etapa-tarefa.spec.ts`'s WEB-03/WEB-04/dangling-link tests, `gotoNested.ts`'s
"etapas"/"tarefas" bodies, `shell-nav.spec.ts`'s NAV-02 loop), and this plan's own grep audit,
which surfaced two additional generic per-primary-entity loops RESEARCH.md's Pitfalls section did
not enumerate: `entities-header-states.spec.ts`'s `ENTTBL-07` test and
`cross-phase-verification.spec.ts`'s `VERIFY-07/POLISH-04` walkthrough test, both of which iterate
every primary nav button (including `nav-projetos`) and assert `entity-header`/`entity-table-frame`
-- markup `ProjetosSection` no longer renders at its top level by default.

Purpose: this is the phase's regression-proof wave, mirroring Phase 18's own Plan 18-03 in
structure and rigor. Every one of these tests, once fixed, must prove EXACTLY what it proved
before (never less), just against the new master-detail markup where the old flat-table markup
no longer applies.
Output: `gotoNested.ts`'s "etapas"/"tarefas" bodies repointed at the new UI (zero call-site edits
outside this plan's own 5 files); `shell-nav.spec.ts`, `entities-header-states.spec.ts`,
`cross-phase-verification.spec.ts` each excluding/redirecting their `nav-projetos` iteration;
`entities-projeto-etapa-tarefa.spec.ts`'s `WEB-03` (projetos CRUD) and `WEB-04`/`T-04-04`
(etapas create-only + dangling-link) rewritten against the new UI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-projetos-section-master-detail/19-CONTEXT.md
@.planning/phases/19-projetos-section-master-detail/19-RESEARCH.md
@.planning/phases/19-projetos-section-master-detail/19-03-SUMMARY.md

@web/e2e/helpers/gotoNested.ts
@web/e2e/shell-nav.spec.ts
@web/e2e/entities-header-states.spec.ts
@web/e2e/cross-phase-verification.spec.ts
@web/e2e/entities-projeto-etapa-tarefa.spec.ts
@web/src/lib/sections/ProjetosSection.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint gotoNested's etapas/tarefas bodies; fix the 3 generic per-primary-entity loops that now break on nav-projetos</name>
  <files>web/e2e/helpers/gotoNested.ts, web/e2e/shell-nav.spec.ts, web/e2e/entities-header-states.spec.ts, web/e2e/cross-phase-verification.spec.ts</files>
  <action>
**`gotoNested.ts`**: replace the current single-path body (the `nested-goto` Select click) with a
three-branch function, `await page.goto("/")` first as today, then: when `etype === "etapas"`,
click `nav-projetos` then click the FIRST `project-item` and return -- add a code comment
explaining etapas no longer has an unscoped-table destination since Phase 19 moved it inside a
selected projeto's own detail column; when `etype === "tarefas"`, click `nav-projetos` then click
`projetos-tab-todas` and return -- add a code comment explaining "Todas as tarefas" is the
Projetos detail column's second tab per spec-ui.md §2.2, reproducing the exact unscoped
`EntityScreen(tarefasConfig)` markup every other `gotoNested("tarefas")` call site already depends
on, with zero call-site changes required; for every other `etype`, fall through unchanged to the
existing `nested-goto` click followed by `` `nested-goto-${etype}` ``. Update the file's own
docstring: the "PUBLIC SIGNATURE IS STABLE ACROSS PHASES" contract holds (only this function's
body changed), but note the "etapas" case now depends on at least one projeto existing in the live
app at call time -- callers relying on this case must guarantee that themselves (as
`shell-nav.spec.ts`'s fix below does).

**`shell-nav.spec.ts`**: the `"each nav Button renders its corresponding EntityScreen"` test
(lines 27-41) and the `"NAV-01/NAV-03..."` test (lines 43-66) need NO edit -- `ProjetosSection`
renders exactly one `<h2>Projetos</h2>` by design (Plan 19-01) and no extra `<h2>` in the default,
no-selection landing state (Plan 19-03's Tabs-mounting guarantee), so
`EXPECTED_H2_BY_TESTID["nav-projetos"] = "Projetos"` still resolves correctly. Only the
`"NAV-02..."` test (lines 68-84) needs a rewrite: add an `import { execFileSync } from
"node:child_process";` at the top of the file. Inside the test, immediately after the existing
negative-existence assertion (the `toHaveCount(0)` check for the four removed nav testids),
guarantee at least one projeto exists (the shared live app is not guaranteed to have one
otherwise) -- compute a `REPO_ROOT` constant the same way `entities-projeto-etapa-tarefa.spec.ts`
does (`new URL("../..", import.meta.url).pathname`), build a unique name via something like
`` `phase19-e2e-nav02-${Date.now()}` ``, and create a throwaway projeto with `execFileSync("uv",
["run", "--project", "cli", "apollo", "projeto", "criar", "--nome", nome, "--status", "ativo"], {
cwd: REPO_ROOT, encoding: "utf-8" })`, `JSON.parse`-ing the stdout for its `id`. Wrap the rest of
the test body in a `try`/`finally`: inside `try`, keep the existing loop but reduce it to
`["tarefas", "templatesRotina", "subtarefas"]` (asserting `entity-table-frame` visible after each
`gotoNested` call, exactly as today), then separately call `gotoNested(page, "etapas")` and
assert `project-etapas-list` is visible (the new marker, replacing the old `entity-table-frame`
assertion for this one etype only); inside `finally`, delete the throwaway projeto via the
equivalent `apollo projeto deletar --id <id>` CLI invocation.

**`entities-header-states.spec.ts`**: this file's own regression, found via this plan's grep audit
(not in RESEARCH.md's Pitfalls list) -- `ENTTBL-07` (lines 186-206) loops all 5 primary nav
buttons (dashboard already excluded) and asserts `entity-table-frame` for each, INCLUDING
`nav-projetos`, which no longer renders that testid by default. Change line 191's locator to also
exclude projetos (append `:not([data-testid="nav-projetos"])`), change `expect(count).toBe(5)`
(line 193) to `toBe(4)`, and rename the test title to say "4 entities" instead of "5 entities".
Immediately after this test, add a new dedicated test (e.g. named `"ENTTBL-07: Projetos renders
its own master-detail markup, not the generic entity-table-frame"`) that navigates to `/`, clicks
`nav-projetos`, asserts `page.locator("h2")` has the text `"Projetos"`, asserts `project-search`
is visible, and asserts `entity-table-frame` has a count of `0`.

**`cross-phase-verification.spec.ts`**: same category of regression, also found via this plan's
grep audit. The `"VERIFY-07/POLISH-04: cross-phase walkthrough..."` test's Shell leg (lines
189-200) loops `[data-testid^="nav-"]:not([data-testid="nav-dashboard"])` (5 entities) asserting
`entity-header`/`entity-table-frame` plus a `columnGap` measurement for each, including
`nav-projetos`. Change line 189's locator to also exclude projetos and line 191's
`expect(navCount).toBe(5)` to `toBe(4)`. Immediately after the loop (before the "---- fundos leg
----" comment, still inside the same test), add three statements: click `nav-projetos`; assert
`page.locator("h2")` has the text `"Projetos"`; assert `entity-header` has a count of `0`. This
test already imports everything this change needs. Do not touch this file's other tests
(`POLISH-04`, the color-scheme tests, the keyboard/focus-visible smoke tests) -- none of them
reference `nav-projetos` (grep-verified during planning) and must stay byte-identical.
  </action>
  <verify>
    <automated>cd web && bunx playwright test shell-nav.spec.ts entities-header-states.spec.ts cross-phase-verification.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
All 3 files pass. `grep -n 'nav-etapas\|nav-tarefas' web/e2e/shell-nav.spec.ts` (the only literal
testid references left, both inside the NAV-02 negative-existence check, not click targets)
returns only the pre-existing negative-assertion line. `gotoNested.ts`'s exported function
signature is unchanged (`(page: Page, etype: string): Promise<void>`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WEB-03 (projetos CRUD) against the new master-detail UI</name>
  <files>web/e2e/entities-projeto-etapa-tarefa.spec.ts</files>
  <action>
Replace the `"WEB-03: projetos full browser CRUD round trip..."` test (lines 155-220) with a
version driving `ProjetosSection`'s testids instead of the old flat table. Keep the `WEB-03`
prefix in the new test's name (traceability), and reuse the existing `uniqueName`/`submitForm`/
`waitForCreateSettle`/`pickDate`/`selectByText` helpers, `chainFundoNome`, and `RESYNC_TIMEOUT`
unchanged (already imported/defined earlier in this file). New test body, in order: (1) navigate
to `/` and click `nav-projetos`; click `project-create-start`, fill `field-nome` with a fresh
`uniqueName("projeto-a")` and `field-status` with `"ativo"`, submit via `submitForm(page)` -- no
fundo link selected. Locate the resulting `project-item` filtered by that name, assert it is
visible within `RESYNC_TIMEOUT`, capture its `data-eid`, and assert it (truthy). Walk up to its
enclosing `project-group` via an XPath ancestor lookup and assert that group's
`project-group-heading` reads exactly `"Sem fundo vinculado"`. (2) Click the item to select it;
assert `project-header` contains the text `"Sem fundo vinculado"`, `"0 etapas"`, and `"0
tarefas"`. (3) After `waitForCreateSettle(page)`, click `project-edit-start`, use `pickDate(page,
"field-dataInicioPrevista")` to set a date and capture its returned string, submit, assert
`entity-submit` has a count of `0`, wait 1500ms, reload the page, click `nav-projetos` again,
re-locate the same projeto's `project-item` by name and assert its `data-eid` still matches the
captured one (within `RESYNC_TIMEOUT`), click it, and assert `project-header` contains the
captured date string (within `RESYNC_TIMEOUT`) -- proves update-in-place across a reload. (4)
Click `project-create-start` again, fill `field-nome` with a fresh `uniqueName("projeto-b")` and
`field-status` with `"ativo"`, this time also using `selectByText(page, "link-fundo",
chainFundoNome)` before submitting; locate the resulting `project-item`, assert it is visible, and
assert its enclosing `project-group`'s `project-group-heading` reads exactly `chainFundoNome`
(not `"Sem fundo vinculado"`). No UI-driven delete step remains (`ProjetosSection` has no delete
affordance -- spec-ui.md §2.2 lists only "editar projeto"/"+ etapa" as header actions; this
mirrors the same create-only-per-spec scoping already applied to etapas). Cleanup of the two
throwaway projetos is unaffected: this file's existing `sweepLeftovers()` (CLI-driven,
`beforeAll`/`afterAll`, unchanged) already deletes every lingering `phase04-e2e-`-prefixed
projeto regardless of how it was created, so test hygiene is preserved without a UI delete step.
  </action>
  <verify>
    <automated>cd web && bunx playwright test entities-projeto-etapa-tarefa.spec.ts --project=authed --no-deps -g "WEB-03"</automated>
  </verify>
  <done>
`WEB-03` passes against the new master-detail UI: create (with/without fundo), correct grouping,
selection, edit-in-place all proven with real assertions, zero regression in what the test
verifies.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite WEB-04 (etapa create-only) and the T-04-04 dangling-link test against the new UI</name>
  <files>web/e2e/entities-projeto-etapa-tarefa.spec.ts</files>
  <action>
Replace the `"WEB-04: etapas full browser CRUD round trip..."` test (lines 222-271) with a
create-only version, per 19-CONTEXT.md's Open Question 1 resolution (no etapa edit/delete
affordance exists in this UI -- spec-ui.md §2.2 describes "+ etapa" only; deferred to Phase 23's
Etapa dialog). New body, in order: navigate to `/`, click `nav-projetos`, locate and click the
`project-item` filtered by `chainProjetoNome`, and assert `project-header` contains
`chainProjetoNome` (within `RESYNC_TIMEOUT`). Click `project-add-etapa-start` (a code comment
should note this opens the hidden `EntityScreen(etapasConfig)` create dialog with `presetLinks`
pre-filling `projeto` to the currently selected projeto -- still visible/editable per spec-ui.md
§2.1, left as-is here since this etapa genuinely belongs to the selected chain projeto). Fill
`field-nome` with a fresh `uniqueName("etapa")`, `field-ordem` with `"30"`, `field-status` with
`"ativo"`; assert `link-projeto` already contains `chainProjetoNome`; submit. Locate the resulting
`etapa-row` filtered by the new nome, assert it is visible (within `RESYNC_TIMEOUT`), capture and
assert its `data-eid` is truthy, and assert the row contains the text `"30"`.

Replace `"WEB-04 threat T-04-04: a dangling projeto link is blocked..."` (lines 330-371) with a
version that overrides "+ etapa"'s preset link (proving the link `<select>` stays genuinely
editable, spec-ui.md §2.1) instead of the old flow's free-standing link choice. New body, in
order: create a throwaway "doomed" projeto via `apolloCli` (this file's existing helper) with a
fresh `uniqueName("projeto-doomed")`, capturing its `id`; navigate to `/`, click `nav-projetos`,
select the `project-item` filtered by `chainProjetoNome`, click `project-add-etapa-start`, fill
`field-nome` with a fresh `uniqueName("etapa-dangling")`, `field-ordem` with `"1"`, `field-status`
with `"ativo"`; then use `selectByText(page, "link-projeto", nomeProjetoVitima)` to override the
preset link to the doomed projeto instead of the pre-filled chain projeto; delete the doomed
projeto via `apolloCli` BEFORE submitting (reproducing the exact dangling-parent race
`EntityScreen.svelte`'s submit-time `queryOnce` guard, T-04-04, exists to catch); click
`entity-submit`; assert `entity-error` becomes visible (within `RESYNC_TIMEOUT`) and contains
`"parent_not_found"`; assert no `etapa-row` filtered by the dangling etapa's nome exists; click
`entity-cancel`; and finally re-list etapas via `apolloCli(["etapa", "listar"])` and assert none
of them has the dangling etapa's nome, confirming no server-side leftover either.

`WEB-05` (lines 273-328, `gotoNested(page, "tarefas")`-based) needs NO edit -- it already lands on
the unscoped "Todas as tarefas" tab via Task 1's `gotoNested.ts` fix, byte-identical markup. Diff
this file against its pre-plan state and confirm `WEB-05` has zero changes.
  </action>
  <verify>
    <automated>cd web && bunx playwright test entities-projeto-etapa-tarefa.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
`WEB-04` proves create-only etapa creation via "+ etapa" with a correctly pre-filled, still-
editable `link-projeto`; the dangling-link test proves the same submit-time parent-existence
guard still blocks a deleted-out-from-under-the-form parent, now via the override-the-preset-link
flow. `WEB-05` is byte-identical to before this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new -- this plan edits Playwright spec files and the `gotoNested` e2e helper only; no
production runtime code is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04-01 | Tampering | Rewritten spec assertions could silently narrow what they prove | high | mitigate | Every rewritten test is diffed against its own pre-plan assertions in this plan's per-task done criteria (same security-relevant paths: dangling-parent guard, update-in-place, grouping correctness) -- narrower coverage is a plan failure, not an acceptable trade. |
| T-19-04-02 | Repudiation | n/a | n/a | accept | Test-only code; no write-path or audit-log code touched. |

</threat_model>

<verification>
After Task 3, run the full suite once as the phase-level gate: `cd web && bun run test:e2e` (all
3 projects) and `cd web && bun test src` (unit, including `projetosDerive.test.ts` from Plan
19-02 and `registry.test.ts`). Also `cd web && bun run check` and `cd web && bun run lint`.
Finally confirm `grep -rn 'nav-etapas"' web/e2e --include=*.spec.ts | grep -v "toHaveCount(0)"`
returns zero matches (every remaining `nav-etapas` reference is inside a negative-existence
assertion, never a click target) and `grep -c 'if (config.etype\|if (cfg.etype'
web/src/lib/entities/EntityScreen.svelte web/src/lib/entities/registry.ts` returns `0` for both
files (untouched by the whole phase).
</verification>

<success_criteria>
- `gotoNested.ts`'s public signature is unchanged; its "etapas"/"tarefas" bodies work against the
  new UI with zero edits to the 3 other files that call `gotoNested(page, "tarefas")`.
- `shell-nav.spec.ts`, `entities-header-states.spec.ts`, `cross-phase-verification.spec.ts` each
  correctly exclude or redirect their `nav-projetos` iteration; each gained a small dedicated
  assertion proving Projetos' own new markup instead of silently dropping coverage.
- `entities-projeto-etapa-tarefa.spec.ts`'s `WEB-03`/`WEB-04`/dangling-link tests pass against the
  new UI with no reduction in what they prove; `WEB-05` is untouched.
- Full `bun run test:e2e` (all 3 projects) and `bun test src` both pass -- the phase's overall
  regression gate.
</success_criteria>

<output>
Create `.planning/phases/19-projetos-section-master-detail/19-04-SUMMARY.md` when done.
</output>
