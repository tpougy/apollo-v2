---
phase: 19-projetos-section-master-detail
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/src/lib/sections/ProjetosSection.svelte
  - web/src/lib/sections/projetosDerive.ts
  - web/src/lib/sections/projetosDerive.test.ts
  - web/e2e/projetos-section.spec.ts
autonomous: true
requirements: [NEST-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Selecting a projeto renders its etapas as collapsible rows ordered by the row-level etapas.ordem field ascending (never etapasConfig.ordem's constant nav-sort value), one open at a time (accordion single), each showing a thin progress bar and an N/M counter derived from progressoEtapa's subtarefa-based rule -- never a tarefas.status string comparison (NEST-02)."
    - "Opening an etapa lists that etapa's own tarefas inline: a disabled completion checkbox reflecting tarefaConcluida (>=1 subtarefa AND all subtarefas.concluida true), titulo, prazo styled destructive only when vencido() is true, and a passive (non-clickable) subtarefa N/M chip (NEST-02)."
    - "\"+ etapa\" and \"+ tarefa nesta etapa\" each open EntityScreen's real create dialog via a hidden instance, pre-linked (presetLinks) to the live selected projeto/open etapa, with the link <select> still visible and editable (NEST-02)."
  artifacts:
    - web/src/lib/sections/projetosDerive.ts
    - web/src/lib/sections/projetosDerive.test.ts
  key_links:
    - "projetosDerive.ts's tarefaConcluida/progressoEtapa/vencido are pure (no db import, hoje passed as a parameter) and consumed IDENTICALLY by the accordion header's progress bar/counter and each inline task row's checkbox/prazo styling -- one source of truth, never a second ad-hoc completion or overdue check anywhere in ProjetosSection.svelte."
    - "etapa-host/tarefa-host presetLinks are read from the LIVE selectedProjetoId/openEtapaId $state at the moment their dialog opens, never a memoized snapshot from an earlier selection."
---

<objective>
Build out the etapas accordion and its inline tarefas list inside `ProjetosSection.svelte`'s
detail column (spec-ui.md §2.2's core aninhamento requirement): etapas ordered by their own
`ordem` field, single-open, with a progress bar/counter and, once opened, that etapa's tarefas
with completion/overdue styling. Introduce the phase-local pure derivation module
`projetosDerive.ts` so "done" and "overdue" are computed by exactly one function each, matching
REQUIREMENTS.md §5.3/§5.4's locked rules verbatim.

Purpose: this is NEST-02's remaining half after Plan 19-01's projeto-level browsing -- the actual
"etapas of a project, with tasks inside" aninhamento the phase exists to ship. `projetosDerive.ts`
is phase-local (not `web/src/lib/dashboard/derive.ts`) per CONTEXT.md's explicit allowance,
leaving Phase 21 free to create its own canonical `derive.ts` and consolidate/dedupe later without
having to reconcile a partially-built file it does not own yet.
Output: the etapas accordion (list view only -- kanban is Plan 19-03), its inline tarefas list,
"+ etapa"/"+ tarefa nesta etapa" hidden-instance create flows, and
`web/src/lib/sections/projetosDerive.ts` + `.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-projetos-section-master-detail/19-CONTEXT.md
@.planning/phases/19-projetos-section-master-detail/19-RESEARCH.md
@.planning/REQUIREMENTS.md
@spec-ui.md

@web/src/lib/sections/ProjetosSection.svelte
@web/src/lib/entities/defs/etapas.ts
@web/src/lib/entities/defs/tarefas.ts
@web/src/lib/entities/defs/subtarefas.ts
@web/src/lib/bizdays.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: projetosDerive.ts (tarefaConcluida/progressoEtapa/vencido) + etapas accordion with progress bar, ordered by row-level ordem</name>
  <files>web/src/lib/sections/projetosDerive.ts, web/src/lib/sections/projetosDerive.test.ts, web/src/lib/sections/ProjetosSection.svelte, web/e2e/projetos-section.spec.ts</files>
  <behavior>
    - `tarefaConcluida({ subtarefas: [] })` -> `false` (no subtarefas => never counts as done,
      REQUIREMENTS.md §5.3).
    - `tarefaConcluida({ subtarefas: [{concluida:true}] })` -> `true`;
      `tarefaConcluida({ subtarefas: [{concluida:true},{concluida:false}] })` -> `false` (ALL
      must be true).
    - `progressoEtapa({ tarefas: [3 tarefas, 1 done per the rule above, 2 not] })` -> `{ feitas: 1,
      total: 3 }`.
    - `progressoEtapa({ tarefas: [] })` -> `{ feitas: 0, total: 0 }`.
    - `vencido("2020-01-01T00:00:00.000Z", false, new Date("2026-01-01"))` -> `true`;
      `vencido("2020-01-01T00:00:00.000Z", true, new Date("2026-01-01"))` -> `false` (already
      concluded is never vencido); `vencido(undefined, false, new Date())` -> `false`
      (REQUIREMENTS.md §5.4's exact three-part rule, `hoje` always a parameter -- never an
      implicit `Date.now()`/`new Date()` default inside the function).
  </behavior>
  <action>
Create `web/src/lib/sections/projetosDerive.ts`, a pure module (no `db` import, mirroring
`bizdays.ts`'s module-level doc-comment style) exporting: an interface `SubtarefaLike` with a
`concluida: boolean` field; an interface `TarefaLike` with an optional `dataPrevista: string |
null` and an optional `subtarefas: SubtarefaLike[]`; an interface `EtapaLike` with an optional
`tarefas: TarefaLike[]`; a function `tarefaConcluida(tarefa: TarefaLike): boolean` that reads
`tarefa.subtarefas ?? []` and returns `true` only when that array's length is greater than zero
AND every entry's `concluida` is `true`; a function `progressoEtapa(etapa: EtapaLike): { feitas:
number; total: number }` that reads `etapa.tarefas ?? []` and returns `feitas` as the count of
entries for which `tarefaConcluida` is `true` and `total` as the array's length; and a function
`vencido(dataPrevista: string | null | undefined, concluido: boolean, hoje: Date): boolean` that
returns `false` immediately when `dataPrevista` is falsy, otherwise returns `new
Date(dataPrevista) < hoje && !concluido`. Add a doc comment noting this is Phase 19's phase-local
copy of the rule REQUIREMENTS.md §5.3/§5.4
locks; Phase 21's `web/src/lib/dashboard/derive.ts` is the eventual canonical owner and may
import or re-derive this logic, but does not exist yet and this module does not depend on it.
Add `web/src/lib/sections/projetosDerive.test.ts` using `bun:test` (`import { describe, expect,
test } from "bun:test";`), fixture-driven exactly like `bizdays.test.ts`, covering every behavior
case listed above plus one more: `progressoEtapa` with a mix of tarefas that DO and DO NOT have
subtarefas, asserting tarefas with zero subtarefas land in `total` but never `feitas`.

In `ProjetosSection.svelte`, resolve `etapasConfig` via `requireConfig("etapas")` (Plan 19-01's
helper) and import `progressoEtapa` from `./projetosDerive`. Add an `openEtapaId` `$state<string |
null>` local, default `null`. Inside the `project-detail` block (below the header, replacing the
Task 1-of-19-01 placeholder comment), compute `etapasOrdenadas` as a shallow copy of
`selectedProjeto.etapas ?? []` sorted by comparing each pair's `ordem` field ascending, reading
the **row-level** `etapas.ordem` field fetched by the bespoke query -- never
`etapasConfig.ordem` (the unrelated nav-sort constant `10`, not even present on a query result
row; RESEARCH.md's Pattern 3 / `etapas.ts:9-13`'s own disambiguating comment). Wrap in
`data-testid="project-etapas-list"` (always rendered, even when `etapasOrdenadas.length === 0` --
render an inline "Nenhuma etapa cadastrada." message in that case, never an empty unlabeled div,
since Plan 19-04's `shell-nav.spec.ts` fix depends on this testid always existing once a projeto
is selected). Use the installed `Accordion.Root type="single" collapsible bind:value={openEtapaId}`
(read the exact prop names from the barrel `index.ts` Task 1 of 19-01 had you inspect -- adjust if
the installed API differs, e.g. `onValueChange` instead of `bind:value`). One `Accordion.Item
value={etapa.id}` per etapa: `Accordion.Trigger` row `data-testid="etapa-row" data-eid={etapa.id}`
containing: `<span class="font-mono w-8">{etapa.ordem}</span>`, `<span>{etapa.nome}</span>`,
`{@const { feitas, total } = progressoEtapa(etapa)}` a thin progress bar (`<div class="h-1 w-full
rounded-full bg-muted"><div class="h-1 rounded-full bg-foreground" style={`width: ${total > 0 ?
(feitas / total) * 100 : 0}%`}></div></div>`, only existing tokens per spec §0.1/§0.2), and a
counter `<span>{feitas}/{total}</span>`.

Add the "+ etapa" hidden host (mirrors Plan 19-01 Task 1's `projeto-host` pattern exactly, a
second independent instance): declare `etapaHostReady` (`$state(false)`) and `etapaHostEl`
(`HTMLDivElement | undefined`, `bind:this`), plus an async `openEtapaDialog(selector: string)`
that sets `etapaHostReady = true`, `await`s `tick()`, then calls
`etapaHostEl?.querySelector<HTMLButtonElement>(selector)?.click()` -- identical shape to Plan
19-01 Task 1's `openProjetoDialog`, a second independent instance, not a shared one. Render the
host conditionally on `etapaHostReady`: a `div` with `class="hidden"`, `aria-hidden="true"`,
`data-testid="etapa-host"`, `bind:this={etapaHostEl}`, containing `<EntityScreen
config={etapasConfig} presetLinks={...} />` where `presetLinks` is the plain object literal `{
projeto: selectedProjetoId }` when `selectedProjetoId` is set, else `null` -- computed inline from
the live `selectedProjetoId` `$state`, re-evaluated on every render, never memoized into a
separate variable that could go stale (RESEARCH.md's threat pattern 2). Add the
`project-add-etapa-start` Button ("+ etapa") to `project-header`'s actions area, next to "editar
projeto", with `onclick` calling `openEtapaDialog` with the selector string
`[data-testid="entity-create-start"]`.

Extend `web/e2e/projetos-section.spec.ts`: create (via CLI, `phase19-e2e-` prefix) a chain
fundo -> projeto -> two etapas with `ordem` 20 and 10 (deliberately out of creation order) and a
handful of tarefas under each (some with subtarefas all `concluida`, some with subtarefas mixed,
some with none), navigate to Projetos, select that projeto, and assert: `etapa-row` elements
render in `ordem` ascending (10 before 20) regardless of creation order; each row's counter text
matches the expected `feitas/total` computed from the fixture data; clicking one `etapa-row`
expands it and clicking the other closes the first (single-open); "+ etapa" creates a new etapa
via CLI-free UI flow (fill `field-nome`/`field-ordem`/`field-status`, submit) that appears in the
correct sorted position without needing to re-select `link-projeto` (presetLinks pre-fills it,
but assert the select IS still visible/editable per spec §2.1).
  </action>
  <verify>
    <automated>cd web && bun test src/lib/sections/projetosDerive.test.ts && bunx playwright test projetos-section.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
`projetosDerive.test.ts` passes every behavior case listed above; the etapas accordion renders
ordered by row-level `ordem` ascending, single-open, with a progress bar/counter matching
`progressoEtapa`; "+ etapa" creates via the hidden-instance pattern with `presetLinks` correctly
pre-filling (but not locking) the `projeto` link.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline tarefas list inside an open etapa (checkbox/prazo/subtarefa chip) + "+ tarefa nesta etapa"</name>
  <files>web/src/lib/sections/ProjetosSection.svelte, web/e2e/projetos-section.spec.ts</files>
  <behavior>
    - A tarefa whose `tarefaConcluida(...)` is `true` renders its `etapa-tarefa-concluida`
      checkbox checked and disabled; `false` renders it unchecked and disabled (never an
      interactive toggle -- there is no writable "conclusão" field on `tarefas` in the schema,
      per REQUIREMENTS.md §5.3; toggling completion is out of scope for this phase).
    - A tarefa with `dataPrevista` in the past and NOT `tarefaConcluida` renders its prazo text
      with `text-destructive`; a tarefa with `dataPrevista` in the past but `tarefaConcluida ===
      true` does NOT render `text-destructive` (matches `vencido()`'s `!concluido` term).
    - The subtarefa chip always shows `{subtarefas.filter(concluida).length}/{subtarefas.length}`
      and is rendered as a non-interactive `Badge`, never a `<button>` (Phase 20 wires the click).
  </behavior>
  <action>
Inside each `Accordion.Item`'s `Accordion.Content`, render `data-testid="etapa-tarefas-list"`
wrapping `{#each [...(etapa.tarefas ?? [])] as tarefa (tarefa.id)}` rows
`data-testid="etapa-tarefa-row" data-eid={tarefa.id}`: a `Checkbox` (`data-testid=
"etapa-tarefa-concluida"`, `checked={tarefaConcluida(tarefa)}`, `disabled`), the `tarefa.titulo`,
a prazo `<span data-testid="etapa-tarefa-prazo" class={vencido(tarefa.dataPrevista,
tarefaConcluida(tarefa), new Date()) ? "text-destructive" : ""}>{tarefa.dataPrevista ?
tarefa.dataPrevista.slice(0, 10) : "—"}</span>` (import `vencido`/`tarefaConcluida` from
`./projetosDerive`, added in Task 1), and a subtarefa chip `{@const subs = tarefa.subtarefas ??
[]}<Badge data-testid="etapa-tarefa-subtarefas-chip" variant="outline">{subs.filter((s) =>
s.concluida).length}/{subs.length}</Badge>` (import `Badge` from `$lib/components/ui/badge`) --
a plain `Badge`, never wrapped in a `<button>`, with a code comment noting the chip is
intentionally inert pending Phase 20's `SubtarefasPanel` (NEST-05) per CONTEXT.md's explicit
deferral.

Add the "+ tarefa nesta etapa" hidden host, a THIRD independent hidden instance (do not reuse
`etapaHostEl`): declare `tarefaHostReady` (`$state(false)`) and `tarefaHostEl` (`HTMLDivElement |
undefined`, `bind:this`), plus an async `openTarefaDialog(selector: string)` with the same shape
as the other two hosts' open functions, querying and clicking within `tarefaHostEl`. Render the
host conditionally on `tarefaHostReady`: a `div` with `class="hidden"`, `aria-hidden="true"`,
`data-testid="tarefa-host"`, `bind:this={tarefaHostEl}`, containing `<EntityScreen
config={tarefasConfig} presetLinks={...} />` where `presetLinks` is `{ etapa: openEtapaId }` when
`openEtapaId` is set, else `null` (resolve `tarefasConfig` via `requireConfig("tarefas")`
alongside `etapasConfig`). Add an `etapa-add-tarefa-start` Button ("+ tarefa nesta etapa") inside
`Accordion.Content`, below the tarefas list, with `onclick` calling `openTarefaDialog` with the
selector string `[data-testid="entity-create-start"]` -- `presetLinks` here reads the live
`openEtapaId`, so it always targets whichever etapa is actually expanded at click time, not a
stale snapshot.

Extend `web/e2e/projetos-section.spec.ts`: using the same fixture chain from Task 1, assert a
tarefa with all-`concluida` subtarefas renders its checkbox `checked`, a tarefa with mixed/no
subtarefas renders it unchecked; assert a past-`dataPrevista`, not-yet-done tarefa's prazo span
has `text-destructive` in its computed class list (via `getComputedStyle(el).color` matching the
app's `--destructive` token, mirroring existing dual-color-scheme assertions' `getComputedStyle`
technique) while a past-`dataPrevista` DONE tarefa's prazo span does not; assert each
`etapa-tarefa-subtarefas-chip`'s text matches the fixture's actual subtarefa counts; click "+
tarefa nesta etapa", fill `field-titulo`/`field-tipoPrazo`/`field-status`, submit, and assert the
new `etapa-tarefa-row` appears inside the currently-open etapa's list without needing to
re-select `link-etapa`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
Inline tarefas render a disabled completion checkbox matching `tarefaConcluida`, prazo styled
`text-destructive` only per `vencido()`'s exact rule, and a passive subtarefa chip; "+ tarefa
nesta etapa" creates via the hidden-instance pattern pre-linked to the live open etapa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new beyond Plan 19-01's -- this plan adds two more hidden `EntityScreen` instances
(`etapasConfig`, `tarefasConfig`) using the identical, already-accepted pattern.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02-01 | Tampering | `presetLinks: { projeto: <id> }` / `{ etapa: <id> }` race with a mid-dialog selection change | low | mitigate | Both values are plain object literals computed inline from the live `selectedProjetoId`/`openEtapaId` `$state` at render time (Svelte 5 prop reactivity), never memoized snapshots; `EntityScreen.svelte`'s own unmodified submit-time `queryOnce` parent-existence guard is the real backstop regardless. |
| T-19-02-02 | Elevation of Privilege | `etapa-host`/`tarefa-host` lifecycle | low | mitigate | Same mitigation as Plan 19-01's T-19-01-01 -- both hosts live only inside `ProjetosSection`'s component tree, torn down on route change. |
| T-19-02-03 | Information Disclosure | Disabled completion checkbox / subtarefa chip derived client-side | low | accept | Read-only derived display over already-fetched, already-authorized rows from the single bespoke query; no new data is exposed. |

</threat_model>

<verification>
`cd web && bun test src/lib/sections/projetosDerive.test.ts` passes. `cd web && bunx playwright
test projetos-section.spec.ts --project=authed --no-deps` passes. `cd web && bun run check` and
`cd web && bun run lint` are clean.
</verification>

<success_criteria>
- `projetosDerive.ts`'s three pure functions match REQUIREMENTS.md §5.3/§5.4 exactly, unit-tested.
- Etapas accordion: row-level `ordem` ascending, single-open, progress bar/counter from
  `progressoEtapa`.
- Inline tarefas: disabled checkbox from `tarefaConcluida`, `text-destructive` prazo from
  `vencido()`, passive subtarefa chip.
- "+ etapa"/"+ tarefa nesta etapa" both work via the hidden-instance pattern with live
  `presetLinks`.
</success_criteria>

<output>
Create `.planning/phases/19-projetos-section-master-detail/19-02-SUMMARY.md` when done.
</output>
