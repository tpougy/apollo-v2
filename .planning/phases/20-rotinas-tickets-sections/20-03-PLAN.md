---
phase: 20-rotinas-tickets-sections
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - web/src/lib/sections/ProjetosSection.svelte
  - web/e2e/projetos-section.spec.ts
autonomous: true
requirements: [NEST-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicking a tarefa's subtarefa chip inside a project's open etapa opens the same SubtarefasPanel, scoped to that tarefa"
    - "The chip is now a real <button>, keyboard-activatable, keeping its existing data-testid so text-only e2e assertions from Phase 19 keep passing"
    - "An orphaned tarefa (no etapa, reachable only via the \"Todas as tarefas\" tab) can also open the panel — the tab that exists specifically because tarefa.etapa is optional is not a dead end for subtarefas"
  artifacts:
    - web/src/lib/sections/ProjetosSection.svelte (edited)
    - web/e2e/projetos-section.spec.ts (edited)
  key_links:
    - "ProjetosSection imports the SubtarefasPanel built in Plan 20-01 — zero new prop or branch added to SubtarefasPanel itself"
    - "The \"Todas as tarefas\" tab's tarefa rows use the same click-delegation technique TicketsSection already established in Plan 20-01"
---

<objective>
Reuse the `SubtarefasPanel.svelte` built in Plan 20-01 for the tarefa-parented half of NEST-05:
wire `ProjetosSection.svelte`'s etapa-detail subtarefa count chip into a real trigger, AND give
the "Todas as tarefas" tab's rows the same trigger — the latter because an orphaned tarefa
(`tarefas.etapa` is `required: false`, per spec §2.2) is only ever reachable through that tab, and
once this phase retires the interim `subtarefas` nav route, that tab becomes its ONLY path to a
subtarefas panel.

Purpose: `19-CONTEXT.md` explicitly deferred wiring the chip to Phase 20; `20-RESEARCH.md`'s
Code Example marks the exact hand-off point (`ProjetosSection.svelte:498-501`'s "Intentionally
inert" comment). ROADMAP Phase 20 Success Criterion #3 requires the shared panel to open
"wherever tasks are shown, including inside Projetos' etapa detail" — "including" implies etapa
detail is one instance of a broader requirement, and the "Todas as tarefas" tab is the only other
place tasks are shown in this codebase.
Output: chip-to-button conversion, orphan-tarefa reachability, updated e2e coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-rotinas-tickets-sections/20-CONTEXT.md
@.planning/phases/20-rotinas-tickets-sections/20-RESEARCH.md
@web/src/lib/sections/ProjetosSection.svelte
@web/src/lib/sections/SubtarefasPanel.svelte
@web/src/lib/entities/defs/tarefas.ts
@web/e2e/projetos-section.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the etapa-detail subtarefa chip into a real button opening SubtarefasPanel scoped to that tarefa</name>
  <files>web/src/lib/sections/ProjetosSection.svelte</files>
  <action>
Import `SubtarefasPanel` and add `let activeSubtarefaTarefaId = $state&lt;string | null&gt;(null);`.
At `ProjetosSection.svelte:498-507`, remove the "Intentionally inert" comment block and wrap the
existing `&lt;Badge data-testid="etapa-tarefa-subtarefas-chip" variant="outline"&gt;` in a
`&lt;button type="button"&gt;`, keeping `data-testid="etapa-tarefa-subtarefas-chip"` on the OUTER
`&lt;button&gt;` element (not the inner `Badge`) — `projetos-section.spec.ts:656,661,666,671`
locate this testid and assert only its text, never its tag, so this rename-free wrap keeps those
four assertions passing unedited. `onclick` toggles:
`activeSubtarefaTarefaId = activeSubtarefaTarefaId === tarefa.id ? null : tarefa.id`.

Directly below each tarefa row's own markup inside the `{#each ... as tarefa}` loop (still nested
inside the `{#if openEtapaId === etapa.id}` guard, before the row's `{/each}` closes but after that
one row's own closing `&lt;/div&gt;`), add:
`{#if activeSubtarefaTarefaId === tarefa.id}{#key tarefa.id}&lt;SubtarefasPanel parentType="tarefa" parentId={tarefa.id} parentLabel={tarefa.titulo} onClose={() => (activeSubtarefaTarefaId = null)} /&gt;{/key}{/if}`.
This reuses `SubtarefasPanel` with zero new prop and zero `if (etype === ...)` branch inside it —
the component is already parent-type-agnostic from Plan 20-01. Do not add a second `db.useQuery`:
`SubtarefasPanel` issues its own `scopeWhere`-filtered query, independent of `ProjetosSection`'s
existing bespoke nested query (`ProjetosSection.svelte:67-72`), whose `subs` data continues to
serve only the chip's `N/M` count text — the two data-fetch boundaries stay separate, per
`20-RESEARCH.md`'s explicit warning against feeding one into the other.
  </action>
  <verify>
    <automated>cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps -g "subtarefa chip"</automated>
  </verify>
  <done>The etapa-detail subtarefa chip is a real, keyboard-activatable button; clicking it toggles a SubtarefasPanel scoped to that tarefa, inline below the row.</done>
</task>

<task type="auto">
  <name>Task 2: Give the "Todas as tarefas" tab's rows the same panel-opening affordance (orphan-tarefa reachability)</name>
  <files>web/src/lib/sections/ProjetosSection.svelte</files>
  <action>
Add `let activeOrphanSubtarefaId = $state&lt;string | null&gt;(null);` and
`let activeOrphanSubtarefaTitulo = $state("");`. At `ProjetosSection.svelte:614-617` (the "Todas
as tarefas" tab's `EntityScreen(tarefasConfig)` mount), wrap it in a
`data-testid="todas-tarefas-table"` `&lt;div&gt;` whose `onclick` reads
`(event.target as HTMLElement).closest('[data-testid="row"]')`; when found, read `data-eid` into
`activeOrphanSubtarefaId` (toggle semantics identical to Task 1) and the row's first `&lt;td&gt;`
text (titulo — per `tarefas.ts`'s `listColumns: ["titulo", ...]` ordering) into
`activeOrphanSubtarefaTitulo` — this is the exact same click-delegation technique
`TicketsSection.svelte` (Plan 20-01) already established for the identical reason: `EntityScreen`'s
`&lt;TableRow&gt;` has no click handler of its own, and `EntityScreen.svelte` is never touched to add
one. Below the wrapper, inside the same `{#if detailTab === "todas"}` guard:
`{#if activeOrphanSubtarefaId}{#key activeOrphanSubtarefaId}&lt;SubtarefasPanel parentType="tarefa" parentId={activeOrphanSubtarefaId} parentLabel={activeOrphanSubtarefaTitulo} onClose={() => (activeOrphanSubtarefaId = null)} /&gt;{/key}{/if}`.
This applies uniformly to every row in this tab (orphan or not) — no special-casing by whether the
tarefa has an etapa, keeping the logic branch-free.
  </action>
  <verify>
    <automated>cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps -g "Todas as tarefas"</automated>
  </verify>
  <done>Clicking any row in the "Todas as tarefas" tab (including an orphaned tarefa with no etapa) opens SubtarefasPanel scoped to that tarefa.</done>
</task>

<task type="auto">
  <name>Task 3: Update projetos-section.spec.ts for the now-interactive chip and the new orphan-reachability path</name>
  <files>web/e2e/projetos-section.spec.ts</files>
  <action>
Update the existing "NEST-02: subtarefa chip shows the exact concluida/total count from the
fixture" test (`projetos-section.spec.ts:639-674`): remove the trailing assertion block whose
comment documents this chip as "a passive Badge, never an interactive" element pending Phase 20 —
that hand-off comment is now stale — and replace the assertion itself so it confirms the chip's
outer element's tag now equals a real button element, matching spec §0's rule that every clickable
surface is a genuine, keyboard-reachable button.

Add a new test reusing the existing `tarefaDoneId`/`tarefaDoneTitulo` fixture (already linked to
exactly one completed subtarefa, set up in this file's own `beforeAll`): open the project, expand
`etapaBaixaNome`, click `tarefaDoneTitulo`'s row's subtarefa chip, assert `subtarefas-panel`
becomes visible showing exactly that fixture's one subtarefa (and not any subtarefa belonging to a
different tarefa); click the panel's "+ subtarefa", fill `field-titulo`/`field-ordem`, submit,
assert the new row appears in the panel's scoped list; delete it via the panel's own
`row-delete`/`delete-confirm`, assert the count badge reverts to its original `1/1`; click the chip
again (toggle) and assert `subtarefas-panel` disappears.

Add a second new test for the "Todas as tarefas" path: create an orphan tarefa via the CLI (no
`--etapa-id`, same fixture-prefix convention as this file's existing `uniqueName` helper), navigate
to `projetos-tab-todas`, click that tarefa's row, assert `subtarefas-panel` opens scoped to it,
create one subtarefa from the panel, assert it appears, then clean up (delete subtarefa via panel,
delete tarefa via CLI in the test's own teardown).
  </action>
  <verify>
    <automated>cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>The stale "never a button" assertion is replaced with a positive button-tag assertion; both new tests (etapa-detail chip and orphan-tarefa "Todas as tarefas" row) pass, proving the tarefa-parented path works identically to the ticket-parented path from Plan 20-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Svelte SPA) → InstantDB | Unchanged — `ProjetosSection` continues to issue its own bespoke nested query for etapa/tarefa/subtarefa counts; `SubtarefasPanel`'s own scoped query is independent, same client-side-filter-only boundary documented in Plan 20-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Tampering | ProjetosSection's two new click-delegation wrappers (etapa-chip toggle, "Todas as tarefas" row click) | low | accept | Both wrappers only set local `$state` (`activeSubtarefaTarefaId`/`activeOrphanSubtarefaId`) read back into `SubtarefasPanel`'s `parentId` prop — no write path is affected, and `SubtarefasPanel` itself re-validates the parent server-side on submit (unchanged `EntityScreen.svelte` xorLink guard). A stale or mismatched `parentLabel` (display-only) could show a wrong title in the panel header, never a wrong link. |

</threat_model>

<verification>
Run `cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps` (full
file). Confirm `git diff --stat` touches only the 2 files in `files_modified`, and that
`web/src/lib/entities/EntityScreen.svelte` is unchanged.
</verification>

<success_criteria>
- The etapa-detail subtarefa chip is a real button opening a tarefa-scoped SubtarefasPanel.
- An orphaned tarefa (no etapa) can reach its subtarefas panel via "Todas as tarefas" — no dead
  end is introduced by retiring the interim nav route later in this phase.
- `projetos-section.spec.ts` has zero stale assertions contradicting the new interactive chip.
</success_criteria>

<output>
Create `.planning/phases/20-rotinas-tickets-sections/20-03-SUMMARY.md` when done
</output>
