---
phase: 20-rotinas-tickets-sections
plan: 05
type: execute
wave: 4
depends_on: ["20-02", "20-04"]
files_modified:
  - web/src/lib/Shell.svelte
  - web/e2e/helpers/gotoNested.ts
  - web/e2e/shell-nav.spec.ts
autonomous: true
requirements: [NEST-04, NEST-05]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The interim, transitional secondary-access dropdown from Phase 18 no longer exists — RotinasSection and TicketsSection/SubtarefasPanel are now the only paths to templatesRotina/subtarefas"
    - "NAV-02's e2e coverage still proves no first-level nav path exists for etapas/tarefas/templatesRotina/subtarefas, now via each entity's real parent-hosted route"
  artifacts: []
  key_links:
    - "shell-nav.spec.ts's NAV-02 test uses openSubtarefasPanelForTicket (Plan 20-04's helper) to prove subtarefas reachability instead of iterating it through gotoNested"
---

<!-- planner-discipline-allow: nested-goto -->
<!-- planner-discipline-allow: HANDLED_BY_SECTION -->

<objective>
Delete the interim `nestedGroups`/`HANDLED_BY_SECTION`/"Acesso direto (temporário)" dropdown from
`Shell.svelte` now that `RotinasSection` (Plan 20-02) and `TicketsSection`/`SubtarefasPanel`
(Plans 20-01/20-03) give `templatesRotina` and `subtarefas` real, parent-hosted homes — closing out
the transitional bridge `18-CONTEXT.md`/`19-CONTEXT.md` explicitly deferred to this phase. Harden
`gotoNested.ts`'s now-unreachable generic fallback, and fix `shell-nav.spec.ts`'s NAV-02 test so it
proves `subtarefas` reachability through the real panel instead of the retired dropdown.

Purpose: `20-RESEARCH.md`'s Code Example marks this exact removal ("delete the whole block...
rather than leaving a permanently-empty dropdown mounted"). This must run AFTER Plan 20-04
migrates every remaining `subtarefas` call site off `gotoNested` — deleting the dropdown first
would break those tests mid-phase.
Output: `Shell.svelte` with a clean 6-branch router and no interim dropdown, a defensive
`gotoNested.ts` fallback, and a corrected NAV-02 regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-rotinas-tickets-sections/20-CONTEXT.md
@.planning/phases/20-rotinas-tickets-sections/20-RESEARCH.md
@web/src/lib/Shell.svelte
@web/e2e/helpers/gotoNested.ts
@web/e2e/shell-nav.spec.ts
@web/e2e/helpers/subtarefasPanel.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Shell.svelte's interim dropdown; harden gotoNested.ts's dead fallback</name>
  <files>web/src/lib/Shell.svelte, web/e2e/helpers/gotoNested.ts</files>
  <action>
In `Shell.svelte`, delete the `HANDLED_BY_SECTION` `Set`, the `nestedGroups` computed array, and
the entire "Acesso direto (temporário)" wrapper `&lt;div&gt;` containing the `Select.Root`
(`data-testid` starting with `nested-goto`) — locate these by their own doc comments referencing
"interim"/"temporário"/Phase 20, since exact line numbers have shifted after Plans 20-01/20-02's
routing additions. By the time this task runs, `templatesRotina` and `subtarefas` are BOTH served
by real routes (`RotinasSection`'s Templates tab, `TicketsSection`/`ProjetosSection`'s
`SubtarefasPanel`), so the dropdown's own `nested` filter (over `entityConfigs` where
`nav === "nested"`) would already compute to an empty array — remove the whole block rather than
leave a permanently-empty dropdown mounted. Do not remove or alter the router's `{#if rota.section === "dashboard"}...{:else if rota.etype === "tickets"}...{:else if rota.etype === "instanciasRotina"}...{:else if rota.etype === "projetos"}...{:else}...{/if}`
chain itself — only the standalone interim-dropdown block above it goes.

In `gotoNested.ts`, replace the final two lines (the generic `Select`-driven fallback) with a
thrown `Error` naming the unhandled `etype` — every one of the 4 `nav: "nested"` entities now has
an explicit branch (`etapas`/`tarefas` since Phase 19, `templatesRotina` since Plan 20-02;
`subtarefas` was migrated OFF this helper entirely in Plan 20-04, so no branch for it is added
here either), so the old generic fallback is dead code once the dropdown it drove no longer
exists — fail loud instead of silently trying to click a testid that no longer renders.
  </action>
  <verify>
    <automated>cd web && bunx playwright test entities-rotina-log.spec.ts entities-projeto-etapa-tarefa.spec.ts entities-form-dialog-composition.spec.ts entities-delete-confirmation.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>Shell.svelte has no interim dropdown; gotoNested.ts's remaining etapas/tarefas/templatesRotina branches keep every other spec file's pre-existing gotoNested call sites passing unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Fix shell-nav.spec.ts's NAV-02 test — subtarefas reachability proven via the real panel, not the retired dropdown</name>
  <files>web/e2e/shell-nav.spec.ts</files>
  <action>
In the NAV-02 test, remove `"subtarefas"` from the loop that currently iterates
`["tarefas", "templatesRotina", "subtarefas"]` calling `gotoNested` and asserting
`entity-table-frame` visible — `subtarefas` no longer lands on a raw, unscoped table, so this
assertion shape no longer applies to it. Keep `tarefas`/`templatesRotina` in that loop unchanged.
Add a separate, dedicated assertion for `subtarefas` immediately after: create a throwaway ticket
via the CLI, call `openSubtarefasPanelForTicket(page, ticket.id)` (import from
`./helpers/subtarefasPanel.ts`), and assert `subtarefas-panel` becomes visible — proving
`subtarefas` remains reachable without any first-level nav entry, just nested inside its parent,
matching this test's own name ("no first-level nav path for etapas/tarefas/templatesRotina/
subtarefas, but each remains reachable"). Also strengthen the test's existing assertion (which
already checks the 4 removed `nav-*` testids have zero count) with an additional check that no
element with a `data-testid` beginning with the dropdown's old prefix exists anywhere on the page
— proving the interim access path from Phase 18 is fully gone, not merely redundant. Clean up the
throwaway ticket via the CLI in the test's own teardown.
  </action>
  <verify>
    <automated>cd web && bunx playwright test shell-nav.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>NAV-02 test passes: tarefas/templatesRotina still reachable via gotoNested, subtarefas reachable via the real SubtarefasPanel, and the interim dropdown's own testids are gone entirely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Svelte SPA) router | `Shell.svelte`'s route branches are a client-side `$state` switch, never an authorization boundary — removing the interim dropdown changes discoverability only, not access control (server-side `instant.perms.ts` scoping is unchanged and was never dependent on which nav path a user took). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-07 | Denial of Service (regression) | `gotoNested.ts`'s hardened fallback | low | mitigate | Throwing on an unhandled `etype` surfaces a clear e2e failure immediately if any future test call site is added for an etype with no branch, rather than the old silent behavior of clicking a testid that may no longer exist and producing a confusing Playwright timeout instead. |

</threat_model>

<verification>
Run the full e2e suite: `cd web && bun run test:e2e`. This is the phase-gate run — every test
touched across all 5 plans of this phase must be green, including Fundos/Log (NEST-06, untouched)
and every pre-existing spec that referenced the retired interim dropdown.
</verification>

<success_criteria>
- `Shell.svelte` has exactly the 6-section router chain with no interim dropdown.
- `gotoNested.ts` fails loud (never silently) on any unhandled etype.
- NAV-02 proves subtarefas remains reachable through its real parent-hosted panel.
- Full `bun run test:e2e` suite passes — phase-gate green per this project's zero-human-UAT
  convention.
</success_criteria>

<output>
Create `.planning/phases/20-rotinas-tickets-sections/20-05-SUMMARY.md` when done
</output>
