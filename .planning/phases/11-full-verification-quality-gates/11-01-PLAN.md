---
phase: 11-full-verification-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/README.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, QUAL-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single sequential `bun run test:e2e` run (all 3 Playwright projects, no parallel auth-dependent spawn) passes green across every spec in web/e2e/ together — not per-phase subsets — with at least 39 tests passed and zero failed/skipped (VERIFY-01)."
    - "Every restyled screen (login, shell/nav, entity table, entity form) crossed with every capability class (full-CRUD, create-only/status-only, read-only) is named to an exact spec+test proving it live, confirmed by explicit audit rather than assumed (VERIFY-02)."
    - "`web/README.md` explicitly names `bun run test:e2e` as the single, zero-manual-step, clean-checkout command that reproduces the whole suite — the documented proof VERIFY-03 requires (ROADMAP Phase 11 SC3)."
    - "`bun run check` and `bun run lint` both exit 0 on `web/`, each showing only its one already-acknowledged pre-existing finding (EntityScreen.svelte configProp warning; calendar-caption.svelte useParseIntRadix info) and nothing new (QUAL-01)."
  artifacts:
    - web/README.md (updated — VERIFY-03 callout added to the existing "Running the e2e suite" section)
    - "(conditional, not anticipated) a new/extended web/e2e/*.spec.ts, only if Task 1's audit finds a genuine uncovered screen x capability-class cell"
  key_links:
    - "web/package.json's test:e2e script -> web/playwright.config.ts's setup/authed/anon projects -> every web/e2e/*.spec.ts file -> the live InstantDB app"
    - "web/README.md's documented command -> the literal `bun run test:e2e` npm script -> auth.setup.ts's automated (non-human) magic-code round trip (C-10)"
---

<objective>
Phase 11 is the closing/audit phase of the v1.1 milestone: Phases 7-10 already restyled and individually
verified every screen (login, shell/nav, entity table, entity form+feedback), each passing its own clean
`bun run test:e2e` / `bun run check` / `bun run lint` run in isolation. This phase re-proves all of it
together in one sequential run, explicitly audits that every restyled screen x capability-class combination
is actually covered by a named spec (not assumed from four separate reports), re-confirms the quality gates
hold with zero new suppressions, and documents the one command that reproduces the whole proof unattended.

Purpose: Individual phase-level VERIFICATION.md reports are trustworthy in isolation but have never been
required to hold simultaneously in one clean run since Phase 10 closed — state may have drifted (rate
limits, flakiness, a stray uncommitted change). This phase is the single point where that composite claim
is actually tested, mirroring the value v1.0's Phase 6 got from composing its own per-phase gates
(.planning/RETROSPECTIVE.md: "Composing all per-phase verification scripts into a single end-to-end gate
is a high-value final phase — it is often the first time all scripts are actually run together").
Output: A live-confirmed all-green `bun run test:e2e` run across the full suite, a written coverage audit
naming the spec+test proving each screen x capability-class cell, quality gates re-confirmed clean with
zero new suppressions, and `web/README.md` updated to explicitly document the single VERIFY-03 command.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-full-verification-quality-gates/11-CONTEXT.md
@.planning/phases/07-design-system-setup/07-VERIFICATION.md
@.planning/phases/08-auth-shell-restyle/08-VERIFICATION.md
@.planning/phases/09-entity-table-restyle/09-VERIFICATION.md
@.planning/phases/10-entity-form-restyle-feedback/10-VERIFICATION.md
@web/README.md
@web/playwright.config.ts
@web/package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Full-suite live re-confirmation and explicit screen x capability-class coverage audit</name>
  <files>web/e2e/ (all *.spec.ts, read/audit only: auth.setup.ts, auth.spec.ts, design-system.spec.ts, login-flow.spec.ts, shell-nav.spec.ts, entities-table-restyle.spec.ts, entities-form-restyle.spec.ts, entities-fundos.spec.ts, entities-projeto-etapa-tarefa.spec.ts, entities-rotina-log.spec.ts, entities-ticket-subtarefa.spec.ts, routine-job.spec.ts, routine-job-cross-channel.spec.ts, no-leakage.spec.ts), web/playwright.config.ts, web/package.json</files>
  <precondition>The environment running this task can reach the C-10 magic-code channel (Outlook Classic COM bridge via `powershell.exe`, per PROJECT.md) — `auth.setup.ts`'s automated round trip depends on it and must be performed directly (not by a dispatched subagent, per PROJECT.md's Key Decisions table).</precondition>
  <action>
    Run the complete suite as ONE sequential invocation: `cd web && bun run test:e2e`. Do not add a
    `--project` filter and do not run any second Playwright invocation in parallel with it — 11-CONTEXT.md's
    rate-limit constraint (InstantDB 429'd the test inbox during Phase 10's parallel execution) applies
    here too. Read the full transcript and confirm it reports at least 39 passed (the last known-good count
    from 10-VERIFICATION.md) and zero failed and zero skipped. A lower passed-count or any skip is a
    scope-narrowing signal, not a pass — investigate and fix before treating this task as done, never
    explain it away.

    Then perform the coverage audit this phase exists to make explicit rather than assumed: cross the 4
    restyled screens (login, shell/nav, entity table, entity form) against the 3 capability classes
    (full-CRUD, create-only/status-only, read-only). For each of the resulting cells, name the exact spec
    file + test name that proves it live (e.g. "entity table x read-only -> entities-table-restyle.spec.ts's
    'ENTTBL: logInferenciaClaude' test"). The read-only x entity-form cell is structurally not applicable
    (zero-capability entities render no create/edit Dialog at all, by design — confirm this by reading
    `web/src/lib/entities/EntityScreen.svelte`'s capability guards, don't just assert it) — name it N/A with
    that one-line justification rather than skipping it silently. Record this cell-by-cell mapping directly
    in this task's own summary output.

    If every cell is covered (expected, given all four prior phases' clean VERIFICATION.md reports), stop
    here — do not add redundant coverage. If a genuine cell is uncovered by any existing spec, add the
    smallest possible new `web/e2e/*.spec.ts` (or extend an existing one, following the file's own
    `data-testid` and helper conventions from `web/e2e/helpers/`) closing exactly that gap, then re-run
    `bun run test:e2e` once more to confirm the addition is green alongside everything else that already
    passed — scope the addition to the one missing cell, nothing speculative beyond it.
  </action>
  <verify>
    <automated>cd web && bun run test:e2e</automated>
  </verify>
  <done>A single sequential `bun run test:e2e` run passes with at least 39 tests green and zero
  failed/skipped, proving VERIFY-01 holds for the full suite run together (not per-phase subsets). The
  coverage audit names, for every restyled-screen x capability-class cell (with the one structurally N/A
  cell explicitly justified), the exact spec+test proving it, satisfying VERIFY-02. Any genuine gap found
  is closed with the smallest possible spec addition and re-proven green in the same run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Quality-gate re-confirmation with zero-new-suppression proof (QUAL-01)</name>
  <files>web/src/lib/entities/EntityScreen.svelte, web/src/lib/components/ui/calendar/calendar-caption.svelte (read-only — re-confirming the two acknowledged pre-existing findings' locations, not modifying either file)</files>
  <action>
    Run `cd web && bun run check` and, separately, `cd web && bun run lint` (run them independently, not
    chained, so both transcripts are captured in full even if one reports a finding). `bun run check` must
    exit 0 with exactly one finding: the `state_referenced_locally` warning on `EntityScreen.svelte`'s
    `configProp`, already independently traced via `git blame` to a Phase 4 (v1.0) commit in
    07-VERIFICATION.md and re-confirmed pre-existing in every subsequent phase's VERIFICATION.md through
    Phase 10 — do not attempt to fix it, per 11-CONTEXT.md's explicit instruction that this predates the
    v1.1 milestone and is not new scope. `bun run lint` must exit 0 with exactly one info-level finding: the
    `lint/correctness/useParseIntRadix` finding on `calendar-caption.svelte` (CLI-generated shadcn-svelte
    boilerplate, acknowledged in 10-REVIEW.md's IN-01), and no error- or warning-level finding anywhere
    else — do not attempt to fix it either.

    If either command reports any error, any warning beyond the one named above, or any info-level finding
    beyond the one named above, that is a real QUAL-01 violation (a new suppression/regression versus the
    v1.0 baseline) and must be fixed with a real code change before this task is done — never silenced with
    a suppression comment, which would itself be a new suppression.
  </action>
  <verify>
    <automated>cd web && bun run check; cd web && bun run lint</automated>
  </verify>
  <done>`bun run check` and `bun run lint` both exit 0 on the current `web/` tree, each showing only its one
  already-acknowledged pre-existing finding and nothing new — QUAL-01 (zero new suppressions vs the v1.0
  baseline) confirmed to hold.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the single, zero-manual-step full-suite command (VERIFY-03)</name>
  <files>web/README.md</files>
  <action>
    In `web/README.md`'s existing "Running the e2e suite" section (the table describing the `setup`/
    `authed`/`anon` projects plus the bash block showing `bun run test:e2e:auth` / `bunx playwright test
    --project=authed --no-deps` / `bunx playwright test --project=anon` / `bunx playwright test`), append one
    short paragraph directly after the existing bash block. State explicitly that the plain `bunx playwright
    test` invocation (aliased as `bun run test:e2e` in `package.json`) is the single command that reproduces
    the ENTIRE suite from a clean checkout with zero manual/human step anywhere in the sequence — including
    the real magic-code round trip, which `auth.setup.ts` + `web/e2e/helpers/magic-code.ts` perform
    automatically via the C-10 Outlook COM bridge, not by a human relaying an email. Cite this paragraph as
    the concrete documented proof of VERIFY-03 for the v1.1 milestone, and reference this same phase's Task
    1 live run as the most recent confirmation that the command ends in an all-green summary.

    Do not duplicate the existing table or bash block, do not create a new top-level section, and do not
    touch the unrelated "Verifying Phase 5" section or the root `README.md`'s existing quality-gate
    documentation — QUAL-01's Biome/svelte-check documentation already lives there and is out of this task's
    scope; this task only adds the one VERIFY-03 paragraph to `web/README.md`'s existing e2e section.
  </action>
  <verify>
    <automated>grep -c "VERIFY-03" web/README.md; grep -c "bun run test:e2e" web/README.md; grep -c "^## Running the e2e suite" web/README.md</automated>
  </verify>
  <done>`web/README.md`'s existing "Running the e2e suite" section explicitly names `bun run test:e2e` as
  VERIFY-03's single, zero-manual-step, clean-checkout proof command, with no duplicated or relocated
  content and exactly one "Running the e2e suite" heading remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This phase's own audit conclusions ↔ the milestone's actual done-state | The real risk of a closing/audit phase: its own checks could silently narrow scope (fewer specs actually run, fewer files actually linted) and still report "all green" |
| Playwright test runner (live) ↔ hosted InstantDB app | Pre-existing boundary, inherited unchanged from Phases 4/7-10 — this phase adds no new write path and no new auth surface |
| `auth.setup.ts`'s console log ↔ local test-run stdout | Pre-existing since Phase 8 — the raw magic code + email are logged to local stdout during the automated auth round trip this phase's Task 1 re-exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Repudiation | This phase's own "all green" audit conclusion (VERIFY-01/02, Task 1) | medium | mitigate | Task 1 asserts an explicit test-count floor (>= 39, the last known-good count) and requires the coverage audit to name the exact spec+test per cell rather than trust a bare exit code — an anti-vacuity check preventing a silently-narrowed suite from reporting false-green, mirroring v1.0 Phase 6's own anti-vacuity gates |
| T-11-02 | Tampering | `bun run check`/`bun run lint` output diffed against the 2 named pre-existing findings (Task 2) | low | mitigate | Task 2 fails the phase (requires a real fix, never a suppression comment) if any error/warning/info finding appears beyond the two explicitly named, pre-traced items — the literal QUAL-01 requirement |
| T-11-03 | Information Disclosure | `auth.setup.ts`'s console log of the raw magic code + email during the live round trip re-exercised by Task 1 | low | accept | Local-only stdout, code already expires in 60-90s, pre-existing since Phase 8 — this phase introduces no new attack surface, consistent with 11-CONTEXT.md's own scoping |

No package-manager install tasks exist in this phase (zero new npm/pip/cargo dependencies) — the Package
Legitimacy Gate and a `T-11-SC` row are not applicable and are intentionally omitted.
</threat_model>

<verification>
`cd web && bun run test:e2e` — the complete Playwright suite, all 3 projects (`setup`/`authed`/`anon`), run
as one sequential invocation against the live InstantDB app, at least 39 passed and zero failed/skipped.
`cd web && bun run check` and `cd web && bun run lint` — both exit 0 with only the two named pre-existing
findings and nothing new. `web/README.md` documents the single VERIFY-03 command.
</verification>

<success_criteria>
- ROADMAP Phase 11 SC1: every pre-existing spec in `web/e2e/` passes against the fully restyled markup in
  one real run against the live InstantDB app (Task 1).
- ROADMAP Phase 11 SC2: the restyled login, shell/nav, and at least one full CRUD cycle per capability class
  (full-CRUD, create-only/status-only, read-only) are all green in that same single suite run, with the
  coverage explicitly named cell-by-cell rather than assumed (Task 1).
- ROADMAP Phase 11 SC3: a single documented command (`bun run test:e2e`) re-runs the complete suite from a
  clean checkout with no manual/human step, ending in an all-green summary, documented in `web/README.md`
  (Task 3).
- ROADMAP Phase 11 SC4: `bun run check` and `bun run lint` both exit clean on `web/` with zero new
  suppressions versus the v1.0 baseline (Task 2).
</success_criteria>

<output>
Create `.planning/phases/11-full-verification-quality-gates/11-01-SUMMARY.md` when done
</output>
