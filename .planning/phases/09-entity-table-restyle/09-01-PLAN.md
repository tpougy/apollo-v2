---
phase: 09-entity-table-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/components/ui/table/**
  - web/src/lib/components/ui/badge/**
  - web/src/lib/entities/EntityScreen.svelte
  - web/e2e/entities-table-restyle.spec.ts
autonomous: true
requirements: [ENTTBL-01, ENTTBL-02, ENTTBL-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Navigating to each of the 9 entity screens renders rows inside shadcn Table markup (native `<table>`, role=table) populated from a live InstantDB query, with row count matching the query result — proven live for fundos (full-CRUD), instanciasRotina (restricted), and logInferenciaClaude (read-only)."
    - "status/tipoGeracao/tipoPrazo columns and any kind:\"boolean\" column render as shadcn Badge (`data-slot=\"badge\"`) instead of a plain text node, with at least one entity (fundos) showing 2+ distinct Badge values simultaneously visible."
    - "Row-level edit/delete render as shadcn Button and, on fundos (full-CRUD), still open the edit path and perform a live create/edit/delete round trip against InstantDB exactly as before the restyle."
    - "instanciasRotina shows zero create action and only a status-narrowed edit action (zero delete); logInferenciaClaude shows zero row actions of any kind — both proven live post-restyle, matching each entity's pre-restyle capability exactly."
    - "No new npm dependency is introduced by this phase — Table and Badge are locally generated files only (`git diff web/package.json web/bun.lock` is empty after `bunx shadcn-svelte add table badge -y`)."
    - "Every pre-existing web/e2e/entities-*.spec.ts file (fundos, projeto-etapa-tarefa, rotina-log, ticket-subtarefa) passes unmodified against the restyled markup, and `bun run check`/`bun run lint` remain clean with zero new suppressions."
  artifacts:
    - web/src/lib/components/ui/table/
    - web/src/lib/components/ui/badge/
    - web/src/lib/entities/EntityScreen.svelte
    - web/e2e/entities-table-restyle.spec.ts
  key_links:
    - "EntityScreen.svelte's `{#each config.listColumns as column}` header/body loops and every `{#if config.capabilities.*}` guard stay wired identically to the new Table/Badge/Button markup — guards preserved verbatim, unchanged position, around the new Button elements."
    - "isBadgeColumn()/badgeVariantFor() implement Pattern 2's column-name allowlist (`status`,`tipoGeracao`,`tipoPrazo`) plus any `field.kind===\"boolean\"`, applied identically across all 9 entities — never per-entity special-cased."
    - "data-testid values `entity-error`/`row`/`row-edit`/`row-delete`/`empty-state`/`entity-create-start` stay on the exact same row-level/element-level nodes so every existing entities-*.spec.ts Playwright locator keeps resolving unchanged."
---

<objective>
Restyle `EntityScreen.svelte`'s list-view (`<table>`/rows/row-actions/empty-state) for all 9 domain
entities onto the shadcn-svelte `Table` primitive with `Badge`-rendered status/enum/boolean columns
and `Button`-wrapped row actions, per ENTTBL-01/02/03 (REQUIREMENTS.md) and ROADMAP Phase 9's 4
success criteria, while leaving the create/edit `<form>` (lines 391-529, Phase 10's scope), the
script block, and every capability-gating guard byte-identical.

Purpose: This is the single generic renderer every one of the 9 entity nav buttons mounts
(`Shell.svelte`'s `{#key ativo}`) — a pure visual pass-through over an already-correct,
already-generic data/capability model. The entire risk surface is "did every testid and capability
guard survive the markup swap," proven live against InstantDB, never mocked (C-12).

Output: `Table`/`Badge` component groups installed under `web/src/lib/components/ui/` (no new npm
dependency), `EntityScreen.svelte`'s list-view section restyled in place, and a new
`entities-table-restyle.spec.ts` proving all 4 ROADMAP success criteria live across one entity per
capability class.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-entity-table-restyle/09-CONTEXT.md
@.planning/phases/09-entity-table-restyle/09-RESEARCH.md
@.planning/phases/09-entity-table-restyle/09-PATTERNS.md

# Current file to restyle in place — list-view section only (lines ~331-390); script block
# (1-329) and the create/edit <form> (391-529) stay byte-identical, out of scope this phase
@web/src/lib/entities/EntityScreen.svelte

# Already-installed shadcn primitive (Phase 8) — the concrete import/usage convention every
# Button conversion in this plan follows
@web/src/lib/components/ui/button/index.ts

# Read-only reference for exact capabilities/listColumns/fields per entity — do NOT edit any
# defs/*.ts file this phase; 09-PATTERNS.md already tabulates every entity's shape
@web/src/lib/entities/defs/instanciasRotina.ts
@web/src/lib/entities/defs/logInferenciaClaude.ts
@web/src/lib/entities/defs/fundos.ts

# Existing test contracts this plan must keep passing / extend, not reinvent
@web/e2e/entities-fundos.spec.ts
@web/e2e/entities-rotina-log.spec.ts
@web/e2e/fixtures/instancia-admin-fixture.ts
@web/components.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Install shadcn Table/Badge and restyle EntityScreen.svelte's list-view end-to-end</name>
  <files>web/src/lib/components/ui/table/**, web/src/lib/components/ui/badge/**, web/src/lib/entities/EntityScreen.svelte</files>
  <action>
Per C-08 (bun/bunx only), from `web/` run `bunx shadcn-svelte@latest add table badge -y`. This
generates `web/src/lib/components/ui/table/{table,table-header,table-body,table-row,table-head,table-cell,table-caption,table-footer}.svelte`
plus `index.ts` (exporting the flat named forms `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell`/`TableCaption`/`TableFooter`),
and `web/src/lib/components/ui/badge/{badge.svelte,index.ts}` (exporting `Badge` with a `variant`
prop: `default`/`secondary`/`destructive`/`outline`/`ghost`/`link`). Per 09-RESEARCH.md's Package
Legitimacy Audit, neither component declares a new dependency (`table.json` has none; `badge.json`'s
sole devDependency `tailwind-variants@^3.3.0` is already satisfied at `^3.3.1` since Phase 8) —
confirm `git diff web/package.json web/bun.lock` is empty after the install; if it is not, stop and
report which package changed before proceeding (this must never touch `package.json` this phase).

Then restyle `web/src/lib/entities/EntityScreen.svelte`'s list-view markup only (current lines
~343-389, the `<table>`/`<thead>`/`<tbody>`/`entity-create-start` block) — the script block (lines
1-329) and the create/edit `<form>` (lines 391-529) stay byte-identical; do not touch either. Add
three imports at the top of the script block: `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell`
from `$lib/components/ui/table`, `Badge` from `$lib/components/ui/badge`, and `Button` from
`$lib/components/ui/button` (the file currently imports none of these — Phase 8's `Button` convention
is a plain named import, `import { Button } from "$lib/components/ui/button";`).

Add two small helper functions in the script block, implementing 09-RESEARCH.md's Pattern 2
(column-name allowlist, value-blind, never per-entity special-cased): `isBadgeColumn(columnName:
string): boolean` returns true when `config.fields.find((f) => f.name === columnName)?.kind ===
"boolean"`, else returns `["status", "tipoGeracao", "tipoPrazo"].includes(columnName)` (a literal
`Set` or array is fine, either way the allowlist is exactly these three names). And
`badgeVariantFor(columnName: string, row: Row): "secondary" | "outline"` reads the same field lookup:
for a boolean field, return `"secondary"` when `row[field.name]` is truthy else `"outline"`; for
`columnName === "status"` return `"secondary"`; for every other allowlisted name (`tipoGeracao`,
`tipoPrazo`) return `"outline"` — read the raw row value directly (never the already-stringified
`columnValue()` "sim"/"não" output) to keep the boolean branch value-typed. Do not introduce any
per-value color logic (e.g. keyword-matching `status` strings like "concluído"/"atrasado") — every
`status` badge uses the single `"secondary"` variant regardless of its free-text value, per
09-RESEARCH.md's explicit anti-pattern warning (C-11's "não precisa inventar moda" intent).

Swap the tag structure 1:1: `<table>`→`Table`, `<thead><tr>`→`TableHeader`+`TableRow`,
`<th>`→`TableHead`, `<tbody>`→`TableBody`, `<td>`→`TableCell`, `<tr>`→`TableRow` — keeping every
existing `{#each config.listColumns as column}`/`{#if rowsOf().length === 0}`/`{#each rowsOf() as row
(row.id)}` loop structure and every `data-testid`/`data-eid`/`colspan` attribute byte-identical to
the current file: `data-testid="empty-state"` on the empty-state `TableRow` (not on its inner
`TableCell`), `data-testid="row"` plus `data-eid={row.id}` on each populated `TableRow` (not on any
`TableCell` — existing specs call `row.getByTestId("row-edit")` and `row.getAttribute("data-eid")` on
the row-level locator itself). Inside each column's `TableCell`, wrap the resolved
`columnValue(row, column)` string in `<Badge variant={badgeVariantFor(column, row)}>` guarded by
`{#if isBadgeColumn(column)} ... {:else} ... {/if}` — plain text (unchanged) for every other column.

Replace the three plain `<button type="button">` elements with `Button`, preserving every
`data-testid`, every `onclick` handler, and every surrounding `{#if config.capabilities.*}` guard
verbatim, unchanged position: the `row-edit` button becomes `<Button variant="outline" size="sm"
data-testid="row-edit" onclick={() => startEdit(row)}>editar</Button>` inside the existing `{#if
config.capabilities.update}`; the `row-delete` button becomes `<Button variant="destructive"
size="sm" data-testid="row-delete" onclick={() => handleDelete(row)}>excluir</Button>` inside the
existing `{#if config.capabilities.delete}`; the `entity-create-start` button (below the table,
unchanged position) becomes `<Button type="button" data-testid="entity-create-start"
onclick={startCreate}>novo</Button>` inside the existing `{#if mode === null &&
config.capabilities.create}`. `Button`'s default `type` is already `"button"` (Phase 8 precedent),
so only the create button needs the explicit prop for parity with the current markup's explicit
`type="button"`. Do not touch the `<h2>{config.titulo}</h2>` heading, the `entity-error`/loading/error
`<p>` elements above the table, or anything at or below line 391 (the `<form>` block).
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-fundos.spec.ts --project=authed</automated>
  </verify>
  <done>`bunx shadcn-svelte add table badge -y` introduced zero `package.json`/`bun.lock` changes; the fundos screen renders inside shadcn Table markup with its `ativo` boolean column as a Badge; row-edit/row-delete/entity-create-start render as shadcn Button; the existing, unmodified `entities-fundos.spec.ts` (create → edit → boolean round trip → delete → empty-state) passes live against InstantDB, proving the tracer slice end-to-end for a full-CRUD entity.</done>
</task>

<task type="auto">
  <name>Task 2: New Wave-0 spec proving ENTTBL-01/02/03 across all three capability classes</name>
  <files>web/e2e/entities-table-restyle.spec.ts</files>
  <precondition>`.env.instantdb`'s `INSTANT_APP_ADMIN_TOKEN` (established Phase 1, already consumed by `web/e2e/fixtures/instancia-admin-fixture.ts` since Phase 4/5) is present at the repo root — required for this task's `seedInstance`/`deleteInstance`/`deleteAdminRecord` reuse; no new setup needed, this only confirms an existing precondition.</precondition>
  <action>
Create `web/e2e/entities-table-restyle.spec.ts` (runs in the `authed` project via the existing
`testMatch: /.*\.spec\.ts/`, no `playwright.config.ts` edit needed). Follow the exact structure and
cleanup discipline of `entities-fundos.spec.ts`/`entities-rotina-log.spec.ts`: a `phase09-e2e-` name
prefix, a `beforeEach`/`afterEach` leftover sweep, and per-test `try`/`finally` teardown. Import
`seedInstance`, `deleteInstance`, and `deleteAdminRecord` from `./fixtures/instancia-admin-fixture.ts`
(unchanged, test-only file — do not modify it) exactly as `entities-rotina-log.spec.ts` does.

Write three tests, one per capability class, each asserting `page.getByRole("table")` is visible
(proving native `<table>`'s implicit ARIA role survives the restyle with zero markup change needed)
in addition to the entity-specific checks below.

Test 1 (`fundos`, full-CRUD): create two fundos via the CLI (`apollo fundo criar`) — one with
`--ativo`, one without — using distinct `phase09-e2e-`-prefixed `nome`/`codigo` values. Navigate to
`nav-fundos`, wait for both rows (`getByTestId("row").filter({ hasText: ... })`) to become visible
with a generous timeout for the live subscription, then assert the ativo row's
`[data-slot="badge"]` locator has text `"sim"` and the inativo row's has text `"não"` — two distinct
Badge values simultaneously visible in the same table, satisfying ENTTBL-02's "at least one entity
with multiple distinct status values visible at once." Also assert `page.getByTestId("row")`'s total
count is at least 2 (row-count-matches-query, ENTTBL-01). Delete both fundos via the CLI in a
`finally` block.

Test 2 (`instanciasRotina`, restricted): seed exactly one instance via `seedInstance` (mirroring
`entities-rotina-log.spec.ts`'s WEB-07 fixture usage — `dedupeKey`/`competencia` unique per run,
`tipoPrazo: "hard"`, `status: "pendente"`). Navigate to `nav-instanciasRotina`, assert the seeded
row is visible and contains at least one `[data-slot="badge"]` element (its `status` column), then
assert `entity-create-start` has count 0 anywhere on the screen, `row-delete` has count 0 anywhere on
the screen, and the seeded row's own `row-edit` has count exactly 1 — proving ENTTBL-03's "no create
action and only a status-changing row action" post-restyle. Delete the seeded instance via
`deleteInstance` in a `finally` block.

Test 3 (`logInferenciaClaude`, read-only): create one log entry via the CLI (`apollo log-inferencia
registrar`) with a `phase09-e2e-`-prefixed `campo`. Navigate to `nav-logInferenciaClaude`, assert the
row is visible, then assert zero `[data-slot="badge"]` elements exist anywhere on the screen (none of
its `listColumns` — `createdAt`/`entidadeTipo`/`campo`/`valorInferido` — match the Badge allowlist or
a boolean kind, per 09-PATTERNS.md), and assert `entity-create-start`/`row-edit`/`row-delete` all have
count 0 — proving ENTTBL-03's "zero row actions of any kind" post-restyle. Delete the log entry via
`deleteAdminRecord("logInferenciaClaude", created.id)` in a `finally` block (no CLI `deletar` command
exists for this append-only entity, matching `entities-rotina-log.spec.ts`'s WEB-09 precedent).
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-table-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>All three new tests pass live against InstantDB: fundos shows `role="table"` markup with two simultaneously-visible, distinct Badge values and a row count matching the query; instanciasRotina shows a Badge on its status column with zero create/delete affordances and exactly one status-only edit affordance; logInferenciaClaude shows zero Badges and zero row actions of any kind — covering ENTTBL-01/02/03 across all three capability classes.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression and quality-gate proof</name>
  <files>none — verification-only task, no files modified</files>
  <action>
Run the complete Playwright suite and both quality gates to confirm the restyle introduced zero
regressions anywhere in the app, not only in the entities this plan's own new spec exercises. This is
the phase-gate proof that `entities-projeto-etapa-tarefa.spec.ts` (`etapas`/`tarefas`/`projetos` —
free-text `status` Badges, `tarefas.tipoPrazo`'s true `select` enum Badge), `entities-rotina-log.spec.ts`
(WEB-06's `templatesRotina` full CRUD including its `tipoGeracao`/`ativo` Badges and the self-referential
`antecessor` link column, WEB-07's `instanciasRotina` capability proof, WEB-09's `logInferenciaClaude`
read-only proof), `entities-ticket-subtarefa.spec.ts` (`tickets.tipoPrazo` enum Badge, `subtarefas.concluida`
boolean Badge), and every other existing spec (`auth.spec.ts`, `login-flow.spec.ts`, `shell-nav.spec.ts`,
`design-system.spec.ts`, `no-leakage.spec.ts`, `routine-job*.spec.ts`) still pass completely unmodified
against the restyled markup — none of their `data-testid`-based locators needed to change, since Task 1
preserved every testid byte-for-byte. Also confirm `bun run check` (svelte-check + tsc, including the two
new component groups' generated `.svelte`/`.ts` files) and `bun run lint` (Biome, covering `web/src` and
`web/e2e`) both exit clean with zero new suppressions compared to the pre-Phase-9 baseline — the two new
helper functions (`isBadgeColumn`/`badgeVariantFor`) and the `Table`/`Badge`/`Button`-based markup must
type-check and lint clean without any `// biome-ignore` or `@ts-expect-error` addition.
  </action>
  <verify>
    <automated>cd web && bun run test:e2e && bun run check && bun run lint</automated>
  </verify>
  <done>The entire pre-existing `web/e2e/` suite plus this plan's new `entities-table-restyle.spec.ts` all pass in one green run against the live InstantDB app, and `bun run check`/`bun run lint` both exit 0 with zero new suppressions — confirming the restyle is a clean, capability-preserving, testid-preserving swap across all 9 entities, not only the ones exercised by this plan's own new spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (client) → `EntityScreen.svelte` list-view markup | Client-rendered `Table`/`Badge`/`Button` over already-fetched, already-authorized InstantDB rows (`db.useQuery()`); no new network/query path introduced by this restyle |
| `EntityScreen.svelte` capability gating (client, presentational) → `instant.perms.ts` (server, enforcement) | `{#if config.capabilities.*}` guards are UI-only affordance toggles; the real object-level access-control boundary is InstantDB's `donoId`-scoped rules, entirely untouched by this phase |
| Local dev tooling → shadcn-svelte registry (`bunx shadcn-svelte add table badge`) | Build/dev-time fetch of component source from `shadcn-svelte.com`; no runtime network call, no new npm dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | `EntityScreen.svelte` row-action/create-button capability guards | medium | mitigate | Every `{#if config.capabilities.update}`/`{#if config.capabilities.delete}`/`{#if mode === null && config.capabilities.create}` guard is copied verbatim, unchanged position, around the new `Button` elements (Task 1) — never re-derived or "cleaned up." Task 2's new spec plus Task 3's full-suite run (re-running `entities-rotina-log.spec.ts`'s WEB-07/WEB-09) explicitly assert zero create/delete affordances on `instanciasRotina` and zero row actions of any kind on `logInferenciaClaude` post-restyle. Real enforcement remains `instant.perms.ts` (untouched this phase), so a regression here would be a functional/visual bug rather than an actual privilege escalation — but ENTTBL-03 requires it caught regardless of severity of real-world impact. |
| T-09-02 | Tampering | `columnValue()` string output interpolated into `Badge`/`TableCell` children | low | accept | Svelte's default `{}` text interpolation auto-escapes; no `{@html}` is introduced anywhere by this restyle; unchanged from pre-restyle behavior (the current plain `<td>{columnValue(...)}</td>` already relies on the same auto-escaping). |
| T-09-SC | Tampering | `bunx shadcn-svelte add table badge -y` (registry fetch / potential dependency introduction) | high | mitigate | 09-RESEARCH.md's Package Legitimacy Audit confirms this command introduces NO new npm package: `table.json` declares zero dependencies, `badge.json`'s sole devDependency (`tailwind-variants@^3.3.0`) is already satisfied at the installed `^3.3.1` (Phase 8). The two touched pre-existing packages (`shadcn-svelte`, `tailwind-variants`) are already approved/installed with their `[SUS]` flags documented as false positives (the legitimacy checker's "too-new" signal reads latest-published-version timestamp, not first-release date; both have high weekly download counts and verified GitHub source repos). Task 1's concrete gate is `git diff web/package.json web/bun.lock` being empty post-install; no blocking human-verify checkpoint is warranted since this is not a new install. |
</threat_model>

<verification>
1. `cd web && bunx playwright test e2e/entities-fundos.spec.ts --project=authed` (Task 1) — full CRUD + boolean Badge round trip on the richest full-CRUD entity, live.
2. `cd web && bunx playwright test e2e/entities-table-restyle.spec.ts --project=authed` (Task 2) — Table role + Badge rendering + row-count-matches-query across one entity per capability class.
3. `cd web && bun run test:e2e && bun run check && bun run lint` (Task 3) — every pre-existing spec unmodified-green, zero new type/lint suppressions.
4. `git diff web/package.json web/bun.lock` — empty (no new npm dependency introduced by `shadcn-svelte add table badge`).
</verification>

<success_criteria>
Maps 1:1 to ROADMAP.md Phase 9's 4 success criteria:

1. Navigating to each of the 9 entity screens renders its rows inside shadcn `Table` markup populated from a live InstantDB query, with row count matching the query result for at least one entity per capability class — proven by Task 1 (fundos) and Task 2 (instanciasRotina, logInferenciaClaude).
2. Enum/status-like fields (`status`, `tipoGeracao`, `tipoPrazo`, and boolean-kind equivalents) render as shadcn `Badge` elements rather than plain text nodes, verified across representative entities including at least one (fundos) with multiple distinct status values visible at once — proven by Task 1 and Task 2.
3. Row-level edit/delete actions render as shadcn `Button`s and, on a full-CRUD entity, still open the edit path and perform a live delete against InstantDB exactly as before the restyle — proven by Task 1's unmodified `entities-fundos.spec.ts` pass.
4. `instanciasRotina`'s table shows no create action and only a status-changing row action (no full edit/delete), and `logInferenciaClaude`'s table shows zero row actions of any kind — both proven live post-restyle by Task 2, matching each entity's pre-restyle capability exactly.
</success_criteria>

<output>
Create `.planning/phases/09-entity-table-restyle/09-01-SUMMARY.md` when done
</output>
