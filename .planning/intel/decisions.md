# Decisions (from ADRs)

No ADR-classified documents were present in this ingest batch.

Only one source document was classified: `docs/superpowers/specs/2026-08-09-migracao-instantdb-design.md` (type: SPEC). It contains several architecture-level decisions embedded in prose (e.g. InstantDB as sole backend, monorepo structure, vendored ANBIMA calendar, magic-code auth for both channels), but since the document was classified as SPEC — not ADR — those decisions are recorded as constraints in `constraints.md` rather than as locked ADR decisions here.

If any of these decisions should be formalized and locked, author a dedicated ADR and re-run ingestion.
