---
milestone: v1.3
audited: 2026-08-12
status: passed
scores:
  requirements: 21/21
  phases: 6/6
  integration: 21/21
  flows: 6/6
gaps: {}
tech_debt:
  - phase: 18-navigation-foundation-entityscreen-extension
    items:
      - "WINDOWS.md carries 9 stale ledger entries from this phase's era still marked 'open' for files that demonstrably pass today — bookkeeping gap, not a code defect (noted in 19-VERIFICATION.md)."
  - phase: 19-projetos-section-master-detail
    items:
      - "DASHBOARD_QUERY's projetos.etapas.tarefas branch initially omitted subtarefas, making progressoEtapa always read 0/N on ProjectStrips until Phase 23 (23-06) widened the query — now fixed, left here only as a historical note."
  - phase: 23-focus-dialog-system
    items:
      - "ProjetosSection.svelte's etapa-kanban-column/etapa-kanban-card are literal nested <button> elements — HTML content-model-invalid in the abstract, but explicitly verified live (keyboard Tab-order, independent Enter-activation, correct stopPropagation) to work correctly in this SSR-free Vite SPA. Logged as WINDOWS.md entry #14 for revisit only if SSR is ever adopted."
      - "A handful of known-environmental Playwright flakes recur across phases (magic-code email timing since before Phase 10; occasional InstantDB admin-API SSL handshake timeouts under full-suite load) — every occurrence was reproduced passing in isolation across all 6 phases' verifications; none is a functional regression."
---

# Milestone v1.3 Audit — Navegação reorganizada + Dashboard de acompanhamento

**Audited:** 2026-08-12
**Status:** ✓ PASSED

## Summary

All 6 phases (18-23) shipped, all individually verified `passed`, and the cross-phase integration
check found zero blockers and zero warnings across all 21 requirements. This milestone reorganized
Apollo v2's navigation into a 6-section topbar (hiding `etapas`/`templatesRotina`/`subtarefas`/
`tarefas` behind their nested parents) and built the Dashboard as the new landing screen: a
5-weekday calendar, a hard-deadline-first ticket queue, fundo-grouped routines with a monthly
workload heatmap, per-project non-compressing mini-kanban strips, and a 7-dialog focus system
wiring every clickable surface to a consistent read-first drill-down.

## Requirements Coverage

21/21 requirements satisfied. Zero orphans (every REQ-ID in REQUIREMENTS.md's traceability table
has at least one phase VERIFICATION.md entry and at least one SUMMARY.md `requirements-completed`
match). Zero unsatisfied.

| Category | Requirements | Phase | Status |
|----------|--------------|-------|--------|
| NAV (nav topbar) | NAV-01..05 | 18 | ✓ Complete |
| NEST-01, NEST-06 | EntityScreen extension, Fundos/Log unchanged | 18 | ✓ Complete |
| NEST-02, NEST-03 | Projetos master-detail | 19 | ✓ Complete |
| NEST-04, NEST-05 | Rotinas tabs, Tickets + SubtarefasPanel | 20 | ✓ Complete |
| DASH-01, 02, 03, 06, 07 | Dashboard shell, week calendar, ticket queue, derive.ts, dashboardQuery.ts | 21 | ✓ Complete |
| DASH-04, DASH-05 | Rotinas-by-fundo + heatmap, mini-kanbans | 22 | ✓ Complete |
| DLG-01, 02, 03 | 7 focus dialogs | 23 | ✓ Complete |

## Phase Verification Summary

| Phase | Name | Plans | Verification | Notes |
|-------|------|-------|---------------|-------|
| 18 | Navigation Foundation & EntityScreen Extension | 3/3 | passed | 71/71 e2e green |
| 19 | Projetos Section (Master-Detail) | 4/4 | passed | 87/87 e2e green (incl. $isNull "Sem etapa" live-verified) |
| 20 | Rotinas & Tickets Sections | 5/5 | passed | DEF-01 (stale Tab-count regression) resolved inline |
| 21 | Dashboard Data Layer, Shell, Week Calendar & Ticket Queue | 3/3 | passed | 104/104 e2e green; consolidated Phase 19's provisional derive module |
| 22 | Dashboard Kanbans, Rotinas & Heatmap | 2/2 | passed | Central risk (non-compressing kanban columns) proven via bounding-box live assertions |
| 23 | Focus Dialog System | 7/7 | passed | 16-button inventory audited twice (plan-checker + consolidated sweep); depth-cap-2 proven live at both launch points |

**24 plans total across 6 phases.**

## Cross-Phase Integration

Spawned `gsd-integration-checker`, which live-ran 91 tests across cross-phase specs
(`cross-phase-verification.spec.ts`, `dashboard.spec.ts`, `dashboard-kanbans.spec.ts`,
`shell-nav.spec.ts`, `focus-dialog-button-inventory.spec.ts`, `focus-dialog-projeto.spec.ts`,
`focus-dialog-ticket.spec.ts`) — all passing. Confirmed:

- **Single-query contract holds**: `dashboardQuery.ts`'s `DASHBOARD_QUERY` is the sole
  `db.useQuery` in the entire Dashboard feature; every consumer (`WeekCalendar`, `TicketQueue`,
  `ProjectStrips`, `RoutinesByFundo`, `MonthHeatmap`, all 7 dialogs) reads via props only.
- **EntityScreen's one additive extension held the line**: `scopeWhere`/`presetLinks` (Phase 18)
  reused identically by every downstream consumer (Phases 19/20/23), zero second prop pair, zero
  `if (etype === ...)` branch anywhere.
- **Nested-entity hiding + reachability, end-to-end**: `etapas`/`tarefas`/`subtarefas`/
  `templatesRotina` are genuinely unreachable at first level and genuinely reachable through their
  correct nested paths, verified live.
- **Full user journey, live**: login → Dashboard default route → all 6 topbar sections → every
  dialog-target surface → correct dialog → "editar" (real `EntityScreen` form, no duplicate
  markup) / "ver na página completa →" (correct section nav) → depth never exceeds 2.
- **Design discipline held across all 6 phases**: zero raw hex, zero non-token color utility,
  correct dark-mode heatmap ramp inversion, one spacing scale throughout.

No BLOCKER or WARNING integration findings.

## Tech Debt (non-blocking, see frontmatter)

Three minor, already-documented items carried forward (see `tech_debt` above) — none blocks
milestone completion. All were caught and either fixed inline (the `dashboardQuery.ts`
subtarefas-fetch gap, DEF-01's stale Tab-count) or explicitly verified safe (the nested-button
pattern, confirmed keyboard-accessible via live Playwright Tab-traversal in a browser with no
SSR).

## Out-of-Scope Boundary Held

Confirmed no phase touched `shared/instant.schema.ts`, `instant.perms.ts`, `cli/`, or
`routineJob.ts`; no router/URL was added; no drag-and-drop; no inline status edit outside the
Rotina dialog; `instanciasRotina.capabilities.create/delete` remain `false` throughout.

## Verdict

**PASSED.** Proceed to `/gsd-complete-milestone v1.3`.
