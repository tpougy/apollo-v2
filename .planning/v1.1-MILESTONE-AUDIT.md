---
milestone: v1.1
name: UI bonita com Tailwind + shadcn-svelte
status: tech_debt
audited: 2026-08-10
phases: 5
requirements_total: 19
requirements_mapped: 19
requirements_unmapped: 0
---

# Milestone v1.1 Audit — UI bonita com Tailwind + shadcn-svelte

## Scope

Phases 7-11: Design System Setup, Auth & Shell Restyle, Entity Table Restyle, Entity Form Restyle & Feedback, Full Verification & Quality Gates. Pure visual/component restyle of the existing Svelte 5 SPA on Tailwind v4 + shadcn-svelte (CLI default preset `b0` = nova/neutral/lucide), no new domain functionality, zero human UAT throughout (PROJECT.md C-12).

## Phase Verification Summary

| Phase | Status | Score |
|-------|--------|-------|
| 7 — Design System Setup | passed | 4/4 |
| 8 — Auth & Shell Restyle | passed | 5/5 |
| 9 — Entity Table Restyle | passed | 4/4 |
| 10 — Entity Form Restyle & Feedback | passed | 5/5 |
| 11 — Full Verification & Quality Gates | passed | 4/4 |

All 5 phases individually verified `passed` with live evidence (real Playwright runs against the live InstantDB app, including real magic-code email round trips) — no phase deferred to human judgment anywhere.

## Requirements Coverage

19/19 v1.1 requirements mapped to a phase and marked Complete in `REQUIREMENTS.md`'s traceability table. Zero unmapped.

## Integration Check

Cross-phase wiring verified live (see `gsd-integration-checker` run, 2026-08-10): Phase 7's Tailwind/shadcn tokens and dark-mode media query are consumed by every component added in Phases 8-10; every installed shadcn component group is actively rendered (no orphaned installs); Sonner toasts fire on every write path; capability guards for `instanciasRotina`/`logInferenciaClaude` remain byte-identical pre/post-restyle. End-to-end flow (login → shell → table → form → toast) re-run live: 38/39 passing (see Known Issues).

## Known Issues (tech debt, non-blocking)

1. **Delete confirmation still uses native `window.confirm()`** (`web/src/lib/entities/EntityScreen.svelte:368`) — pre-existing since v1.0 Phase 4, explicitly out of scope for this milestone (see `10-CONTEXT.md`'s Deferred Ideas: ENTFRM-04 only covers form-submission validation errors, not the delete-confirm dialog; no REQUIREMENTS.md item mandates converting it). Genuinely a minor visual inconsistency against the milestone's "styled with shadcn-svelte defaults" spirit — worth a small follow-up in a future milestone (convert to shadcn `AlertDialog`) but does not block this milestone's own stated scope.
2. **`login-flow.spec.ts`'s wrong-code test is occasionally flaky** due to live InstantDB magic-code email delivery timing/rate-limiting (documented in PROJECT.md C-10, hit repeatedly across Phases 8/10/11, always resolved by a retry, never a code defect). VERIFY-03's "single command, all-green" claim is technically fragile against this external dependency's timing variance, not against anything this milestone built. Acceptable given the alternative (a human relaying the magic code) is explicitly out of scope per C-12/C-10.

Neither item blocks milestone completion — both are pre-existing, explicitly scoped-out, or environmental, not gaps in what v1.1 committed to deliver.

## Verdict

**tech_debt** — proceed to `/gsd-complete-milestone v1.1`. Both known issues are non-blocking and explicitly logged for future consideration; requirements coverage is 19/19 complete with live evidence at every phase.
