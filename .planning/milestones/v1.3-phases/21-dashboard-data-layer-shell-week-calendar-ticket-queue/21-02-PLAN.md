---
phase: 21-dashboard-data-layer-shell-week-calendar-ticket-queue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/dashboard/dashboardQuery.ts
  - web/src/lib/dashboard/Dashboard.svelte
  - web/src/lib/dashboard/TicketQueue.svelte
  - web/e2e/dashboard.spec.ts
  - web/e2e/fixtures/instancia-admin-fixture.ts
autonomous: true
requirements: [DASH-07, DASH-01, DASH-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dashboard.svelte issues exactly one db.useQuery for the whole feature, via useDashboardQuery() in dashboardQuery.ts; TicketQueue receives its rows as a prop and never calls db.useQuery itself"
    - "The ticket queue lists every pending ticket ordered hard-deadline-first then by date, shows Empty.Root when there are zero tickets, and its \"ver todos\" control navigates to the real Tickets section without any change to Shell.svelte"
    - "The Dashboard's root grid is 3 columns at lg: (13rem | flexible center | 16rem) and a single column below lg, in the order semana, tickets, rotinas, projetos"
    - "dashboardQuery.ts's exact 5-key query shape, including the instanciasRotina.template.fundo two-hop path, resolves against the live InstantDB app without adding template/fundo to defs/instanciasRotina.ts"
  artifacts:
    - web/src/lib/dashboard/dashboardQuery.ts
    - web/src/lib/dashboard/Dashboard.svelte
    - web/src/lib/dashboard/TicketQueue.svelte
    - web/e2e/dashboard.spec.ts
  key_links:
    - "TicketQueue's \"ver todos\" button forwards a real .click() to Shell.svelte's already-rendered nav-tickets Button (document.querySelector), the same plain-onclick .click() idiom already proven at ProjetosSection.svelte:207/254/278 and documented at SubtarefasPanel.svelte:81-86 — chosen because Shell.svelte itself receives zero changes this phase"
    - "Dashboard.svelte's grid slot wrappers (dash-week-slot, dash-tickets-slot, dash-placeholder-rotinas, dash-placeholder-projetos) keep stable testids/classes across Plan 21-03's later swap of the week slot's inner content"
---

<objective>
Build `dashboardQuery.ts` (DASH-07's single, bespoke `db.useQuery`), rewrite `Dashboard.svelte`
into its real 3-column/1-column grid shell (DASH-01), and ship `TicketQueue.svelte` (DASH-02) as
this phase's first fully real, end-to-end Dashboard capability — proven live against the hosted
InstantDB app.

Purpose: this is the phase's tracer slice — the thinnest path that touches every layer the
Dashboard feature needs (query -> grid shell -> a real rendered, real-data column), wired
end-to-end with a real Playwright verification, before the Week Calendar expands the same shell
in Plan 21-03. The `instanciasRotina.template.fundo` two-hop path (the one part of `dashboardQuery.ts`
CONTEXT.md/RESEARCH.md flagged as unproven in this exact shape) is proven directly against the
live app in this plan, not merely inferred from the schema.
Output: `dashboardQuery.ts`, `Dashboard.svelte`, `TicketQueue.svelte`, `web/e2e/dashboard.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dashboard-data-layer-shell-week-calendar-ticket-queue/21-CONTEXT.md
@.planning/phases/21-dashboard-data-layer-shell-week-calendar-ticket-queue/21-RESEARCH.md
@web/src/lib/dashboard/Dashboard.svelte
@web/src/lib/Shell.svelte
@web/src/lib/db.ts
@web/src/lib/sections/ProjetosSection.svelte
@web/src/lib/entities/EntityScreen.svelte
@web/src/lib/entities/defs/tickets.ts
@web/src/lib/components/ui/empty/index.ts
@web/e2e/fixtures/instancia-admin-fixture.ts
@shared/instant.schema.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: dashboardQuery.ts + Dashboard.svelte grid shell + TicketQueue.svelte — real end-to-end ticket queue slice</name>
  <files>web/src/lib/dashboard/dashboardQuery.ts, web/src/lib/dashboard/Dashboard.svelte, web/src/lib/dashboard/TicketQueue.svelte, web/e2e/dashboard.spec.ts</files>
  <action>
Create `web/src/lib/dashboard/dashboardQuery.ts` exporting a `DASHBOARD_QUERY` constant matching
spec-ui.md §5.1's exact locked shape: `projetos: { fundo: {}, etapas: { tarefas: {} } }`,
`tarefas: { etapa: { projeto: {} }, subtarefas: {} }`, `instanciasRotina: { template: { fundo: {} } }`,
`tickets: { fundo: {}, subtarefas: {} }`, `fundos: {}`. Export `useDashboardQuery()` that calls
`db.useQuery(() => DASHBOARD_QUERY as never)` — the same cast-at-the-InstaQL-boundary idiom
`EntityScreen.svelte:82` and `ProjetosSection.svelte:68-73` already use for a bespoke multi-entity
query shape the generated schema union cannot express literally. This is the ONLY `db.useQuery`
call site for the entire Dashboard feature (DASH-07: "uma query, não sete") — every other
Dashboard component (this plan's `TicketQueue`, Plan 21-03's `WeekCalendar`) must receive its rows
as props from `Dashboard.svelte`, never call `db.useQuery`/`useDashboardQuery` itself. Do not add a
`donoId` where-clause — `instant.perms.ts`'s `view` rule already scopes every row server-side, and
every existing bespoke query in this codebase (cited above) omits one too. Never add `template`
or `fundo` as a `LinkDef` to `web/src/lib/entities/defs/instanciasRotina.ts` — that omission is
deliberate (it prevents `EntityScreen` from rendering a re-parenting `<select>` that would desync
an instance's `dedupeKey`); this query bypasses `EntityConfig`/`buildQuery` entirely.

Rewrite `web/src/lib/dashboard/Dashboard.svelte` (currently a 3-line placeholder). Call
`useDashboardQuery()` once at the top. Define local row types beside the query, mirroring
`ProjetosSection.svelte`'s own `ProjetoRow`/`TarefaRow` convention (one level of nesting per
`DASHBOARD_QUERY`'s shape) for `projetos`, `tarefas`, `instanciasRotina`, `tickets`, `fundos`.
While `query.isLoading` is true, render nothing but a plain "carregando..." text node (no
skeleton component needed for a first paint this brief). Render the root grid:
`data-testid="dash-grid"` with class `grid grid-cols-1 gap-4 lg:grid-cols-[13rem_minmax(0,1fr)_16rem] lg:grid-rows-[auto_1fr]`.
Inside it, four slot wrappers with STABLE testids/classes (Plan 21-03 will swap only the first
slot's inner content, never its wrapper):
- `data-testid="dash-week-slot"` class `"order-1 lg:order-none lg:col-start-2 lg:row-start-1"` —
  this task's content is a single line of text, "carregando semana..." (no fetch, no fabricated
  data; Plan 21-03 replaces this text with the real `WeekCalendar`).
- `data-testid="dash-tickets-slot"` class `"order-2 lg:order-none lg:col-start-1 lg:row-start-1 lg:row-span-2"` —
  mounts `<TicketQueue tickets={ticketRows} onVerTodos={goToTickets} />` (real, this task).
- `data-testid="dash-placeholder-rotinas"` class `"order-3 lg:order-none lg:col-start-3 lg:row-start-1 lg:row-span-2"` —
  a grid cell containing only the static text "Em breve: rotinas da semana e carga do mês". This
  task fetches no rotina/heatmap data for this cell and fabricates none; Phase 22 owns its content.
- `data-testid="dash-placeholder-projetos"` class `"order-4 lg:order-none lg:col-start-2 lg:row-start-2"` —
  a grid cell containing only the static text "Em breve: projetos em andamento". Same rule: no
  fetch, no fabricated data; Phase 22 owns its content.
The base-viewport `order-1..4` classes give the correct single-column stacking order below `lg`
(semana, tickets, rotinas, projetos, per spec-ui.md §3.1's literal text); the `lg:col-start-*`/
`lg:row-start-*` classes give the 3-column layout at `lg:` (`lg:order-none` cancels the mobile
order so explicit grid placement wins). Define `goToTickets` as
`() => document.querySelector<HTMLButtonElement>('[data-testid="nav-tickets"]')?.click()` — a
real `.click()` on Shell.svelte's own already-rendered, unmodified `nav-tickets` Button, the same
idiom already proven in this codebase for a plain shadcn `Button` with a standard `onclick`
(`ProjetosSection.svelte:207,254,278`; documented explicitly at `SubtarefasPanel.svelte:81-86` —
`.click()` works because that Button wires a normal `onclick`, unlike bits-ui's pointer-event-only
Select). This is the only way `Dashboard.svelte` can move `Shell.svelte`'s `rota` `$state` to the
Tickets section, since `Shell.svelte` itself receives zero changes this phase (it already mounts
`<Dashboard />` unconditionally since Phase 18).

Create `web/src/lib/dashboard/TicketQueue.svelte`. Props via `$props()`:
`tickets: TicketRow[]` (each `{ id, titulo, tipoPrazo, dataPrevista, dataRecebimento, fundo }`),
`onVerTodos: () => void`. Sort a local derived copy: `tipoPrazo === "hard"` first, then by
`(dataPrevista ?? dataRecebimento)` ascending, then by `id` for stability — do not filter by any
completion signal (REQUIREMENTS.md §5.3: `tickets` has no non-string completion field, so the
queue lists every ticket). Render `data-testid="dash-tickets"`; when the sorted list is empty,
render `Empty.Root`/`Empty.Header`/`Empty.Media`/`Empty.Title` "Nenhum ticket pendente" (mirror
`EntityScreen.svelte:492-511`'s structure, `data-testid="dash-tickets-empty"`, no create button —
tickets are not created from this screen). Otherwise render one `<button type="button" data-testid="dash-ticket-card" data-eid={ticket.id}>` per ticket — a real button with no `onclick` handler
wired (Phase 23 owns opening its dialog; this task adds no click behavior beyond focus/keyboard
reachability, which a native `<button>` already provides) — containing a 2-line-clamped title
(`class="line-clamp-2 text-sm"`) and a meta line
`{ticket.fundo?.nome ?? "Sem fundo"} · {ticket.tipoPrazo.toUpperCase()} · {(ticket.dataPrevista ?? ticket.dataRecebimento).slice(0, 10)}`.
Footer: `<button type="button" data-testid="dash-tickets-ver-todos" onclick={onVerTodos}>ver todos os tickets →</button>`.

Create `web/e2e/dashboard.spec.ts` (mirror `tickets-section.spec.ts`'s CLI-fixture /
`phase21-e2e-` prefix / `sweepLeftovers`-in-`beforeAll`/`afterAll` style). Write one test:
`beforeAll` creates a fundo (`apollo fundo criar --nome ... --codigo ...`), a hard ticket with an
earlier `--data-prevista` and a soft ticket with a later `--data-prevista` (both via
`apollo ticket criar --titulo ... --corpo ... --remetente ... --data-recebimento ... --tipo-prazo <hard|soft> --status pendente --data-prevista ... --fundo-id <fundoId>`). The test navigates to
`/`, asserts both tickets render as `dash-ticket-card`, asserts the hard ticket's card appears
before the soft ticket's in DOM order (`page.getByTestId("dash-ticket-card").first()` has
`data-eid` equal to the hard ticket's id), asserts the meta line text contains the fundo name and
"HARD"/"SOFT", then clicks `dash-tickets-ver-todos` and asserts `nav-tickets` gets
`aria-current="true"` (mirrors `shell-nav.spec.ts`'s existing assertion style) — proving navigation
to the real Tickets section works with zero `Shell.svelte` changes.
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>Dashboard.svelte mounts the full 4-slot grid shell; TicketQueue renders every ticket ordered hard-then-date with a working "ver todos" link to the real Tickets section; the one Playwright test in dashboard.spec.ts passes against the live app.</done>
</task>

<task type="auto">
  <name>Task 2: Extend dashboard.spec.ts — empty state, responsive grid order, and live DASH-07 two-hop query proof</name>
  <files>web/e2e/dashboard.spec.ts, web/e2e/fixtures/instancia-admin-fixture.ts</files>
  <action>
Extend `web/e2e/fixtures/instancia-admin-fixture.ts` additively (never remove or change the
meaning of an existing export — `entities-rotina-log.spec.ts`'s calls must keep working
unmodified): add an optional third parameter `templateId?: string` to `seedInstance`. When
supplied, chain `.link({ template: templateId })` onto the same `adminDb.tx.instanciasRotina[newId].update(...)`
call before passing it to `adminDb.transact(...)` — the exact chainable `.update().link()` shape
already proven at `routineJob.ts:653-664`. Add one new exported function,
`adminQuery<T>(query: Record<string, unknown>): Promise<T>`, a thin generic pass-through to
`adminDb.query(query as never)` — a TEST-ONLY escape hatch (mirrors this file's existing
`deleteAdminRecord`'s "generic by etype" precedent) letting a spec run an arbitrary InstaQL shape
through the admin API without adding a new named function per shape.

Add three more tests to `web/e2e/dashboard.spec.ts`, reusing Task 1's fixtures where possible:
(1) Empty state — in an isolated `test.step`/separate test that first sweeps every
`phase21-e2e-` ticket via the CLI, assert `dash-tickets-empty` is visible with "Nenhum ticket
pendente" before Task 1's `beforeAll` fixtures exist (order the file so this runs first, or
create its own throwaway fixtures and delete them immediately, whichever keeps test order
independent — do not rely on Task 1's test having already run). (2) Responsive grid order — at
a desktop viewport (`page.setViewportSize({ width: 1280, height: 900 })`), read
`getComputedStyle` on `dash-grid` and assert `gridTemplateColumns` has exactly 3 space-separated
tracks with the first ≈208px (13rem) and the last ≈256px (16rem); at a mobile viewport
(`{ width: 375, height: 800 }`), read the bounding boxes of `dash-week-slot`, `dash-tickets-slot`,
`dash-placeholder-rotinas`, `dash-placeholder-projetos` and assert their `y` coordinates are
strictly ascending in that order (semana, tickets, rotinas, projetos — spec-ui.md §3.1's literal
mobile stacking order). (3) DASH-07 live two-hop proof — create a fundo and a
`templatesRotina` linked to it via the CLI (`apollo rotina template criar --nome ... --tipo-geracao du_fixo --regra-competencia ... --fundo-id <fundoId>`), seed one `instanciasRotina` via the
extended `seedInstance(fields, OWNER_EMAIL, templateId)`, then call
`adminQuery(DASHBOARD_QUERY)` (import `DASHBOARD_QUERY` from `../src/lib/dashboard/dashboardQuery.ts`
— the exact same object the app's own `useDashboardQuery()` runs, so there is zero drift between
what this test proves and what ships) and assert the returned `instanciasRotina` array contains an
entry with the seeded id whose `template.fundo.id` and `template.fundo.nome` equal the fundo just
created. Clean up every fixture (CLI `deletar` for the fundo/template, `deleteInstance` for the
seeded instance) in a `finally`/`afterAll` block.
  </action>
  <verify>
    <automated>cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>All four tests in dashboard.spec.ts pass: hard-first ticket ordering + navigation, empty state, responsive 3-col/1-col grid order, and the live admin-API proof that instanciasRotina.template.fundo resolves through dashboardQuery.ts's exact query object against the real InstantDB app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Dashboard.svelte) -> InstantDB | `dashboardQuery.ts`'s aggregate query spans 5 entity types in one call; the real authorization boundary is `instant.perms.ts`'s unchanged `donoId` `view` rule, not any client-side filter this query could add. |
| `web/e2e/fixtures/instancia-admin-fixture.ts` -> InstantDB (admin API) | Test-only, never imported by `web/src`; holds `INSTANT_APP_ADMIN_TOKEN` from `.env.instantdb`, same as the pre-existing Phase 4/5 usage of this file. |
| Same-origin DOM (`goToTickets`'s `.click()`) -> Shell.svelte's own rendered button | Not a security boundary — app code driving its own already-rendered, same-origin DOM; no privilege change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Information Disclosure | `dashboardQuery.ts`'s 5-key aggregate query | medium | mitigate | No client-side `donoId` where-clause is added as a substitute for server-side scoping — rely exclusively on `instant.perms.ts`'s existing `view: "auth.id != null && auth.id == data.donoId"` rule, identical to every other bespoke query in this codebase (`ProjetosSection.svelte`, `EntityScreen.svelte`). |
| T-21-04 | Tampering | `goToTickets`'s testid-selector `.click()` forwarding | low | accept | If `nav-tickets`'s testid ever changes, "ver todos" silently stops navigating (no compile-time link between the two files) — acceptable because `Shell.svelte`'s nav testids are a stable, spec-locked contract (spec-ui.md §1.4) unrelated to this phase's own scope, and `dashboard.spec.ts`'s own test would immediately catch the regression. |
| T-21-05 | Elevation of Privilege | `adminQuery`/extended `seedInstance` in `instancia-admin-fixture.ts` | low | mitigate | Both stay TEST-ONLY (never imported by `web/src`, admin token sourced only from `.env.instantdb`, same guarantee this file's header comment already documents and Phase 4's own build-bundle grep verified) — this plan only extends existing exports, it does not weaken the file's TEST-ONLY boundary. |

</threat_model>

<verification>
Run `cd web && bunx playwright test dashboard.spec.ts --project=authed --no-deps` — all four tests
green. Confirm `git diff --stat` touches only the 5 files in `files_modified` above (in
particular, zero lines changed in `web/src/lib/Shell.svelte`, `web/src/lib/entities/EntityScreen.svelte`,
`web/src/lib/entities/registry.ts`, and `web/src/lib/entities/defs/instanciasRotina.ts`).
</verification>

<success_criteria>
- `dashboardQuery.ts` is the only `db.useQuery` call site for the Dashboard feature.
- `Dashboard.svelte` renders the correct 3-column/1-column responsive grid with 4 stable slots.
- `TicketQueue.svelte` is fully real: correct ordering, empty state, working navigation.
- The `instanciasRotina.template.fundo` two-hop query path is proven live against the hosted
  InstantDB app, using the exact same query object the shipped app runs.
</success_criteria>

<output>
Create `.planning/phases/21-dashboard-data-layer-shell-week-calendar-ticket-queue/21-02-SUMMARY.md` when done
</output>
