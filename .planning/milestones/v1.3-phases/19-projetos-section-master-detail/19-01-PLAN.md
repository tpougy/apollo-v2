---
phase: 19-projetos-section-master-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/sections/ProjetosSection.svelte
  - web/src/lib/Shell.svelte
  - web/e2e/projetos-section.spec.ts
  - web/src/lib/components/ui/accordion
  - web/src/lib/components/ui/tabs
  - web/src/lib/components/ui/scroll-area
autonomous: true
requirements: [NEST-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Clicking nav-projetos shows a two-column master-detail screen (not the old flat table): a w-56 master column listing every projeto grouped by fundo (\"Sem fundo vinculado\" group always last), with a working client-side name search and a display-only \"agrupar: fundo | nenhum | status\" control (NEST-02)."
    - "\"+ novo projeto\" opens EntityScreen's own real create dialog (unchanged validation/donoId-injection/link logic) via a hidden EntityScreen(projetosConfig) instance, and the newly created projeto appears in the master list, correctly grouped, with no page reload (NEST-02)."
    - "Selecting a project-item highlights it (variant=\"secondary\") and shows that projeto's own breadcrumb, name, fundo, and etapa/tarefa counts in the detail column, reading those counts from the SAME bespoke query -- no second fetch (NEST-02)."
  artifacts:
    - web/src/lib/sections/ProjetosSection.svelte
    - web/e2e/projetos-section.spec.ts
  key_links:
    - "Shell.svelte's routing branch: {#if rota.etype === \"projetos\"}<ProjetosSection />{:else if active}<EntityScreen config={active} />{/if} -- mounts the new master-detail screen for exactly this one etype; every other primary entity keeps mounting the generic EntityScreen unmodified."
    - "ProjetosSection's single db.useQuery({projetos:{fundo:{},etapas:{tarefas:{subtarefas:{}}}}}) -> feeds BOTH the master column's grouping and the detail column's \"N etapas · N tarefas\" counts -- the same query object, read twice, never fetched twice."
---

<objective>
Stand up `ProjetosSection.svelte` as the new master-detail screen for the Projetos primary nav
item, replacing the generic `EntityScreen` mount for `etype === "projetos"` only (spec-ui.md
§2.2, §0.6's Shell-is-a-router exception). This plan delivers the first vertical slice: browse
every projeto grouped by fundo with a working search, create a new projeto through the existing
generic create-dialog engine (reused via a hidden instance, never a second form), and select a
projeto to see its own breadcrumb/header with real etapa/tarefa counts pulled from one bespoke
nested query.

Purpose: this is the tracer for the whole phase -- it proves the hardest architectural facts
RESEARCH.md flagged (bespoke multi-level `db.useQuery`, reusing `EntityScreen`'s create dialog
via a hidden mounted instance + real button click, Shell as the one permitted per-etype routing
branch) end-to-end, on a real Playwright-verified path, before Plan 19-02/19-03 build the etapas
accordion, kanban toggle, and "Todas as tarefas" tab on top of the same query and the same
hidden-instance pattern.
Output: `web/src/lib/sections/ProjetosSection.svelte` (new), `Shell.svelte`'s Projetos mount point
and interim `nestedGroups` pruned, `accordion`/`tabs`/`scroll-area` added to
`web/src/lib/components/ui/` (installed here for Plan 19-02/19-03 to consume), and
`web/e2e/projetos-section.spec.ts` (new) proving grouping, search, create, and selection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-projetos-section-master-detail/19-CONTEXT.md
@.planning/phases/19-projetos-section-master-detail/19-RESEARCH.md
@spec-ui.md

@web/src/lib/Shell.svelte
@web/src/lib/entities/EntityScreen.svelte
@web/src/lib/entities/registry.ts
@web/src/lib/entities/types.ts
@web/src/lib/entities/defs/projetos.ts
@web/src/lib/entities/defs/fundos.ts
@web/src/lib/db.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Install accordion/tabs/scroll-area, create ProjetosSection's master column + bespoke query, mount it in Shell</name>
  <files>web/src/lib/sections/ProjetosSection.svelte, web/src/lib/Shell.svelte, web/e2e/projetos-section.spec.ts, web/src/lib/components/ui/accordion, web/src/lib/components/ui/tabs, web/src/lib/components/ui/scroll-area</files>
  <action>
1. From `web/`, run `bunx shadcn-svelte@latest add accordion tabs scroll-area`. Per 19-RESEARCH.md's
Package Legitimacy Audit this copies source files into `web/src/lib/components/ui/{accordion,tabs,scroll-area}/`
and touches neither `package.json` nor `bun.lock` (registry source-copy, not a new npm
dependency) -- confirm with `git status web/package.json` showing no change before continuing.
Read each new directory's `index.ts` barrel export once to confirm the exact exported component
names before using them in step 2 (expect `Accordion.Root/Item/Trigger/Content`,
`Tabs.Root/List/Trigger/Content`, and a scroll-area root/viewport pair) -- do not guess the API
from memory.

2. Create `web/src/lib/sections/ProjetosSection.svelte`. Import `configByEtype` from
`../entities/registry` (never import a `defs/*.ts` module directly -- that bypasses the
registry's own validation) and resolve configs via a small local helper function
`requireConfig(etype: string): EntityConfig` that calls `configByEtype(etype)` and throws a
descriptive `Error` naming the missing etype if the result is `undefined`; call it once as
`const projetosConfig = requireConfig("projetos")`. (`etapasConfig`/`tarefasConfig` are resolved
the same way in Plan 19-02/19-03 when first needed -- do not resolve them here if unused.)

Define four local row types mirroring the bespoke query's exact shape: `SubtarefaRow` (`id:
string`, `titulo: string`, `concluida: boolean`); `TarefaRow` (`id`, `titulo`, `status`,
`tipoPrazo: string`, optional `dataPrevista: string`, optional `subtarefas: SubtarefaRow[]`);
`EtapaRow` (`id`, `nome: string`, `ordem: number`, `status: string`, optional `tarefas:
TarefaRow[]`); `ProjetoRow` (`id`, `nome: string`, `status: string`, optional
`dataInicioPrevista`/`dataFimPrevista: string`, optional `fundo: { id: string; nome: string }`,
optional `etapas: EtapaRow[]`). Issue the ONE bespoke query per RESEARCH.md's Pattern 1 (mirrors
`EntityScreen.svelte:82`'s own `db.useQuery(() => buildQuery(config) as never)` cast-at-the-
InstaQL-boundary precedent): call `db.useQuery` with an arrow function returning the exact
literal object `{ projetos: { fundo: {}, etapas: { tarefas: { subtarefas: {} } } } }`, cast `as
never` at the call boundary the same way `EntityScreen.svelte` does -- this precise nesting depth,
no more and no fewer levels. Store the result in a `query` constant. Add a `rowsOf(): ProjetoRow[]`
helper that reads `query.data` (cast to `Record<string, ProjetoRow[]> | undefined`) and returns
`data?.projetos ?? []`, mirroring `EntityScreen.svelte`'s own `rowsOf()` cast style exactly. Add a
`totalTarefas(projeto: ProjetoRow): number` helper that sums `(e.tarefas ?? []).length` across
`projeto.etapas ?? []` via `reduce`. This is the ONLY `db.useQuery` this component ever issues
directly (Plan 19-02/19-03 read from this same `query`/`rowsOf()` -- never add a second top-level
fetch). Handle `query.isLoading`/`query.error` the same way `EntityScreen.svelte` does (Skeleton /
destructive Alert), reusing `$lib/components/ui/skeleton` and `$lib/components/ui/alert`.

3. Master column state and grouping (spec-ui.md §2.2, RESEARCH.md's `groupByFundo` pattern
extended to 3 modes). Declare a `GroupBy` union type of `"fundo" | "nenhum" | "status"`, and three
`$state` locals: `searchTerm` (string, default `""`), `groupBy` (`GroupBy`, default `"fundo"`),
`selectedProjetoId` (`string | null`, default `null`). Write a `groupProjetos(rows: ProjetoRow[],
mode: GroupBy): { label: string | null; rows: ProjetoRow[] }[]` function that: sorts a shallow
copy of `rows` by `nome` via `localeCompare`; if `mode === "nenhum"` returns a single entry `{
label: null, rows: sorted }`; otherwise buckets rows into a `Map<string, ProjetoRow[]>` keyed by
`row.fundo?.nome ?? "Sem fundo vinculado"` (when `mode === "fundo"`) or `row.status` (when `mode
=== "status"`); converts the map to an array of `[label, rows]` entries and sorts them
alphabetically by label, EXCEPT in `"fundo"` mode where the literal key `"Sem fundo vinculado"`
is always sorted last regardless of alphabetical order; and finally maps each entry to `{ label,
rows }`. Compute two `$derived` values: `filtered` (`rowsOf()` filtered by
`r.nome.toLowerCase().includes(searchTerm.trim().toLowerCase())`, per spec §2.2's "filtro
client-side sobre as linhas já carregadas" -- never re-query on input) and `groups`
(`groupProjetos(filtered, groupBy)`).

4. Master column markup, `w-56 border-r shrink-0 space-y-4` per spec §2.2's fixed-width column:
an `Input` (`data-testid="project-search"`, placeholder "Buscar por nome...", bound to
`searchTerm` via `oninput`); a `Select.Root` (`data-testid="project-groupby"` on the
`Select.Trigger`) with items `fundo`/`nenhum`/`status` bound to `groupBy` -- display-only, no
persistence, matches every other `Select.Root` usage already in `EntityScreen.svelte`; a
`project-create-start` Button ("+ novo projeto"); a `project-list` wrapper iterating `groups()`
(keyed by `group.label ?? "__flat__"`) that renders a `project-group` div per entry containing a
`project-group-heading` paragraph (`class="text-xs uppercase text-muted-foreground"`, text
`group.label`) ONLY when `group.label` is non-null, followed by one `Button` per row in
`group.rows` (`variant` `"secondary"` when `row.id === selectedProjetoId` else `"ghost"`, `class="w-full
justify-start"`, `data-testid="project-item"`, `data-eid={row.id}`, `onclick` setting
`selectedProjetoId = row.id`) showing `row.nome`.

5. Hidden host for "+ novo projeto" (RESEARCH.md Pattern 2 -- bits-ui's Dialog Portal renders to
`document.body` regardless of a hidden ancestor). This is the ONLY spec-compliant way to reuse
`EntityScreen`'s create dialog from outside its own mount: `EntityScreen.svelte` gets NEITHER a
third prop NOR a "mode"/etype branch (spec §0.6 permits exactly one additive extension, already
spent on `scopeWhere`/`presetLinks` in Phase 18) -- every "+ X" affordance in this phase (this
one, plus Plan 19-02's "+ etapa"/"+ tarefa nesta etapa") drives a real, unmodified `EntityScreen`
instance's own DOM button instead. Declare `projetoHostReady` (`$state(false)`) and
`projetoHostEl` (`HTMLDivElement | undefined`, bound via `bind:this`), plus an async function
`openProjetoDialog(selector: string)` that sets `projetoHostReady = true`, `await`s `tick()`
(imported from `"svelte"`), then calls `projetoHostEl?.querySelector<HTMLButtonElement>(selector)?.click()`.
Render the host conditionally: when `projetoHostReady` is true, a `div` with `class="hidden"`,
`aria-hidden="true"`, `data-testid="projeto-host"`, `bind:this={projetoHostEl}`, containing
`<EntityScreen config={projetosConfig} />`. Wire "+ novo projeto"'s `onclick` to call
`openProjetoDialog` with the selector string `[data-testid="entity-create-start"]`. The lazy
`{#if projetoHostReady}` gate is deliberate: `EntityScreen.svelte` unconditionally renders its own
`<h2>{config.titulo}</h2>` (line 456), and this component must render exactly one `<h2>` on the
page (Task 2's h2) -- mounting the hidden host only on first "+ novo projeto"/"editar projeto"
click keeps `shell-nav.spec.ts`'s existing single-`<h2>` assertion green for every user flow that
never opens a create/edit dialog. Every query into the hidden host is scoped to `projetoHostEl`
(never a bare global `document.querySelector`) so a second hidden host added in Plan 19-02 can
never be mis-targeted.

6. Detail column, `flex-1` (spec §2.2's breadcrumb/header, NOT yet the etapas accordion --
Plan 19-02's job): when `selectedProjetoId` is set, look up `selectedProjeto` via
`rowsOf().find((r) => r.id === selectedProjetoId)`; when found, render a `project-detail` wrapper
containing a `project-breadcrumb` paragraph (`class="text-xs text-muted-foreground"`, text
`PROJETOS › {selectedProjeto.nome}`) and a `project-header` div holding an `<h3 class="text-lg
font-semibold">{selectedProjeto.nome}</h3>` plus a summary paragraph (`class="text-sm
text-muted-foreground"`) reading `{fundo-or-"Sem fundo vinculado"} · {etapas.length} etapas ·
{totalTarefas(selectedProjeto)} tarefas`. Use `<h3>`, never a second `<h2>` -- the section's
single `<h2>Projetos</h2>` (top of the template, `class="text-xl font-semibold tracking-tight"`
matching `EntityScreen.svelte:456`'s own h2 styling) must stay the only one. When no
`selectedProjeto` is found (or `selectedProjetoId` is null), render a `project-empty` div with the
text "Selecione um projeto para ver seus detalhes." Add a code comment noting that "editar
projeto" (Task 2 of this plan) and "+ etapa" (Plan 19-02) header actions are added afterward --
this task adds neither button.

7. Wrap the whole component in a root `<section class="space-y-6">` containing, in order: the
single `<h2 class="text-xl font-semibold tracking-tight">Projetos</h2>`, a `<div class="flex
gap-6">` holding the master column then the detail column, and the hidden host(s) last (mirrors
`EntityScreen.svelte:453-464`'s own root-section spacing scale, one nesting level under Shell's
own `space-y-6` `<main>` -- same depth every existing entity screen already uses).

8. Edit `Shell.svelte`: import `ProjetosSection` from `./sections/ProjetosSection.svelte`. Change
the routing branch (currently `{#key rota.etype}{#if active}<EntityScreen config={active}
/>{/if}{/key}`) so that when `rota.etype === "projetos"` it renders `<ProjetosSection />` instead,
falling through to the existing `{:else if active}<EntityScreen config={active} />` branch for
every other etype -- this is the one permitted per-route mount-point branch in `Shell.svelte` (a
router deciding which top-level component to mount is not the same thing as the etype-branch
anti-pattern spec §0.6 bans inside a generic engine; `EntityScreen.svelte`/`registry.ts` are
untouched by this line).
Also prune the interim `nestedGroups` Select (lines 26-41) to exclude the two etypes
`ProjetosSection` now owns: declare a `HANDLED_BY_SECTION` `Set` containing `"etapas"` and
`"tarefas"`, and change the `nested` filter to also exclude any config whose `etype` is in that
set -- `templatesRotina`/`subtarefas` stay in the dropdown per CONTEXT.md's Open Question 2
resolution (Phase 20 retires them). Add a one-line comment above `HANDLED_BY_SECTION` explaining
it is a router-level allowlist of etypes that now have a real first-class home, not a
generic-engine etype branch.

9. Create `web/e2e/projetos-section.spec.ts` with an authed-project spec covering: (a) creating a
projeto via CLI with a fundo link and one without, navigating to `nav-projetos`, and asserting
both appear as `project-item` rows, the fundo-linked one under a `project-group-heading` matching
that fundo's nome, the fundo-less one under `"Sem fundo vinculado"` sorted last among the group
headings; (b) typing a substring of one projeto's nome into `project-search` and asserting the
other disappears from the DOM; (c) clicking `project-create-start`, filling `field-nome`/
`field-status` (mirroring `entities-projeto-etapa-tarefa.spec.ts`'s `submitForm`/`waitForTimeout`
patterns for the live-backend timing already documented there), asserting the new `project-item`
appears with no reload; (d) clicking a `project-item` and asserting `project-breadcrumb`/
`project-header` show the correct nome/fundo/counts, and that the clicked item now has the
`secondary` variant (assert via `getComputedStyle` background-color difference from an unselected
sibling, mirroring `shell-nav.spec.ts`'s own active-state color-diff assertion). Use the
`phase19-e2e-` prefix and the same CLI-fixture/sweep-leftovers pattern already established in
`entities-projeto-etapa-tarefa.spec.ts` (execFileSync against `uv run --project cli apollo ...`).
  </action>
  <verify>
    <automated>cd web && ls src/lib/components/ui/accordion src/lib/components/ui/tabs src/lib/components/ui/scroll-area >/dev/null && bunx playwright test projetos-section.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
`accordion`/`tabs`/`scroll-area` exist under `web/src/lib/components/ui/` with zero
`package.json`/`bun.lock` diff; clicking `nav-projetos` shows the two-column master-detail layout
with a single `<h2>Projetos</h2>` on the page; grouping (fundo last-sorted "Sem fundo
vinculado"), search, create, and selection all pass in the new
`web/e2e/projetos-section.spec.ts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "editar projeto" via the same hidden EntityScreen(projetosConfig) instance</name>
  <files>web/src/lib/sections/ProjetosSection.svelte, web/e2e/projetos-section.spec.ts</files>
  <behavior>
    - Clicking `project-edit-start` with a projeto selected opens the hidden `EntityScreen`
      instance's real edit form, pre-filled with that projeto's current field values (proven by
      asserting `field-nome`'s value equals the selected projeto's nome).
    - Submitting the edit form updates the projeto in place (same `data-eid`, changed `nome`
      visible in the master list's `project-item` and the detail header) -- no delete+recreate.
    - Clicking `project-edit-start` with NO projeto selected is impossible (the button only
      renders inside the `project-detail` block, never in `project-empty`).
  </behavior>
  <action>
Add a `project-edit-start` Button ("editar projeto", `variant="outline"`) to the `project-header`
actions area added in Task 1, calling `openProjetoDialog` (from Task 1) with a selector scoped to
the currently selected row inside the SAME hidden host: `` openProjetoDialog(`[data-testid="row"][data-eid="${selectedProjetoId}"]
[data-testid="row-edit"]`) `` -- reuses the exact same `projetoHostReady`/`projetoHostEl`
lazy-mount from Task 1 (do NOT create a second hidden `projetosConfig` instance; the hidden
host's own unscoped query already returns every projeto, including the selected one, with a
matching `data-eid`). Guard the click handler so it is a no-op when `selectedProjetoId` is null
(the button is never rendered in that state anyway, per Task 1's `{#if selectedProjeto}` gate,
but keep the guard for defense-in-depth since `openProjetoDialog` takes a raw selector string).

Extend `web/e2e/projetos-section.spec.ts` with a test that: creates a projeto via CLI, navigates
to Projetos, selects it, clicks `project-edit-start`, asserts `field-nome` is pre-filled with the
current nome, changes it, submits, and asserts the master list's matching `project-item` and the
detail header's `<h3>` both show the new nome with the SAME `data-eid` (proves update-in-place,
mirroring `entities-projeto-etapa-tarefa.spec.ts` WEB-05's `data-eid`-unchanged assertion style).
  </action>
  <verify>
    <automated>cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
"editar projeto" opens the pre-filled real edit form via the Task 1 hidden host; submitting
updates the projeto in place, visible in both the master list and the detail header with an
unchanged `data-eid`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `ProjetosSection.svelte` -> InstantDB (bespoke `db.useQuery`) | Unscoped-by-owner nested query; InstantDB's `instant.perms.ts` (untouched) still scopes every returned row to the authenticated `donoId` server-side, regardless of what this component asks for. |
| Hidden `EntityScreen(projetosConfig)` instance -> its own create/edit dialog | Reuses the exact same generic write path (`donoId` injection, submit-time `queryOnce` parent-existence guard) every other entity screen already uses -- no new validation logic is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01-01 | Elevation of Privilege | Hidden `projeto-host` lifecycle | low | mitigate | The hidden host lives only inside `ProjetosSection`'s own component tree and is torn down by `Shell.svelte`'s existing `{#key rota.etype}` remount the moment the user navigates away from Projetos -- never lifted to a Shell-level always-mounted singleton. |
| T-19-01-02 | Tampering | Bespoke query has no `where` filter at all | low | accept | Same owner-scoped `instant.perms.ts` rule InstantDB already enforces server-side for every query in this app; the bespoke shape changes which LINKS are fetched, never which rows are authorized. |
| T-19-01-03 | Tampering | Regression risk in the 5 pre-existing e2e files/loops that assume every primary nav entity mounts a plain `EntityScreen` | high | mitigate | This plan's own new spec file is additive-only; it does not edit any pre-existing file. Plan 19-04 is the dedicated regression-fix wave and runs the full suite as its own gate. |

</threat_model>

<verification>
`cd web && bunx playwright test projetos-section.spec.ts --project=authed --no-deps` passes. `cd
web && bun run check` and `cd web && bun run lint` are clean on the new/edited files. `grep -c 'if
(config.etype\|if (cfg.etype' web/src/lib/entities/EntityScreen.svelte` still returns 0 (untouched
by this plan).
</verification>

<success_criteria>
- `nav-projetos` mounts `ProjetosSection`, a two-column master-detail layout with exactly one
  `<h2>Projetos</h2>`.
- Master column: grouped-by-fundo list ("Sem fundo vinculado" last), working search, "+ novo
  projeto" via the hidden-instance pattern.
- Detail column: breadcrumb + header (name/fundo/etapa-count/tarefa-count) on selection, "editar
  projeto" via the same hidden instance.
- `accordion`/`tabs`/`scroll-area` installed with zero `package.json`/`bun.lock` diff.
- New `web/e2e/projetos-section.spec.ts` passes; zero pre-existing spec file edited.
</success_criteria>

<output>
Create `.planning/phases/19-projetos-section-master-detail/19-01-SUMMARY.md` when done.
</output>
