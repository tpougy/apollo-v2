---
phase: 18-navigation-foundation-entityscreen-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/entities/EntityScreen.svelte
autonomous: true
requirements: [NEST-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "With scopeWhere and presetLinks both null (the only values used anywhere in Phase 18), EntityScreen behaves byte-identically to before this phase -- proven by the full pre-existing Playwright suite passing with zero edits to any spec file (NEST-01)."
    - "Zero if (config.etype === ...) (or cfg.etype) branch exists anywhere in EntityScreen.svelte (NEST-01)."
  artifacts:
    - web/src/lib/entities/EntityScreen.svelte
  key_links:
    - "buildQuery's `$: scopeWhere ? { where: scopeWhere } : {}` -> db.useQuery(() => buildQuery(config)) -- must degrade to today's literal `$: {}` when scopeWhere is null."
    - "startCreate's `selectedLinks = presetLinks ? { ...links, ...presetLinks } : links` -> the existing submit-time parent-existence re-validation (db.queryOnce) -- presetLinks pre-fills but never bypasses that check."
---

<objective>
Give `EntityScreen.svelte` the two additive, optional props (`scopeWhere`, `presetLinks`) that
spec-ui.md §2.1 and NEST-01 require, wired into the exact two existing expressions
(`buildQuery`'s returned `$` key, `startCreate`'s `selectedLinks` assignment) so that with both
props `null` -- the only state Phase 18 itself ever produces, since `Shell.svelte` (Plan 18-01)
never passes them -- the component's behavior is provably byte-identical to today.

Purpose: this is the generic-engine extension point every Phase 19/20 nested section
(Projetos' etapa/task lists, Rotinas' tabs, the shared subtarefas panel) will build on. It must
ship with zero regression and zero etype-specific branching, or every later phase inherits a
broken foundation.
Output: `EntityScreen.svelte` declares `scopeWhere?: Record<string, unknown> | null` and
`presetLinks?: Record<string, string> | null`, both defaulting to `null`, feeding `buildQuery`
and `startCreate` exactly as specified below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-navigation-foundation-entityscreen-extension/18-CONTEXT.md
@.planning/phases/18-navigation-foundation-entityscreen-extension/18-RESEARCH.md
@spec-ui.md

@web/src/lib/entities/EntityScreen.svelte
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive scopeWhere/presetLinks props to EntityScreen, wired to buildQuery and startCreate</name>
  <files>web/src/lib/entities/EntityScreen.svelte</files>
  <behavior>
    - `buildQuery(cfg)` with the outer `scopeWhere` closed-over as `null` returns
      `{ [cfg.etype]: { $: {}, ...sub } }` -- byte-identical to today's return value (regression
      proof: the full existing e2e suite passes with zero spec-file edits).
    - `buildQuery(cfg)` with `scopeWhere` set to e.g. `{ "ticket.id": "x" }` returns
      `{ [cfg.etype]: { $: { where: { "ticket.id": "x" } }, ...sub } }`.
    - `startCreate()` with `presetLinks` `null` leaves `selectedLinks` identical to today: every
      `config.links` label mapped to `""`.
    - `startCreate()` with `presetLinks` set to e.g. `{ "etapa": "y" }` leaves
      `selectedLinks = { ...links, etapa: "y" }`, and the corresponding `link-etapa` `<select>`
      remains visible and editable (no UI removed or hidden).
    - Zero `if (config.etype === ...)`/`if (cfg.etype === ...)` branch anywhere in the diff.
  </behavior>
  <action>
Per spec-ui.md §2.1 and RESEARCH.md's Pattern 1 (verbatim-verified integration points), change
the props declaration at line 31 from `let { config: configProp }: { config: EntityConfig } =
$props();` to also destructure `scopeWhere = null` and `presetLinks = null`, typed
`scopeWhere?: Record<string, unknown> | null` and `presetLinks?: Record<string, string> | null`
on the inline `$props()` type annotation. Do NOT alias or snapshot these two new names the way
`configProp`/`config` are aliased -- reference `scopeWhere`/`presetLinks` directly by their
destructured names everywhere below (RESEARCH.md Pitfall 3: re-wrapping them in `$state`
reintroduces the "state referenced locally" warning; plain destructured `$props()` locals do not
need that treatment).

In `buildQuery` (lines 61-68), change only the `$: {}` sub-expression on the function's `return`
statement to `$: scopeWhere ? { where: scopeWhere } : {}` -- every other line in the function
(the `sub` object construction, the `cfg.links`/`cfg.xorLink` loops) is untouched. `buildQuery`'s
own parameter list stays `(cfg: EntityConfig)` -- do not add a second parameter; the function
closes over the component-level `scopeWhere` local instead.

In `startCreate` (lines 195-211), change only the line `selectedLinks = links;` to
`selectedLinks = presetLinks ? { ...links, ...presetLinks } : links;` -- every other line in the
function (the `links` object construction from `config.links`, the `xorParentType`/`xorParentId`
resets) is untouched.

No other line in `EntityScreen.svelte` changes. This is the entire diff -- resist any temptation
to add a "mode" prop, a `scopeWhere`-driven conditional render, or any etype-aware branch; those
are explicitly forbidden by spec-ui.md §2.1/§0.6 and CONTEXT.md.
  </action>
  <verify>
    <automated>cd web && bun run test:e2e</automated>
  </verify>
  <done>
`EntityScreen.svelte` declares `scopeWhere?`/`presetLinks?` with `null` defaults; `buildQuery`
and `startCreate` both merge them additively per the two expressions above; the full
pre-existing Playwright suite (all 3 projects: `setup`, `authed`, `anon`) passes with zero edits
to any spec file -- this run happens against the CURRENT (pre-Plan-18-01/18-03) topbar, which is
exactly the point: this plan's own proof is independent of the nav restructuring landing first.
`grep -c 'if (config.etype\|if (cfg.etype' web/src/lib/entities/EntityScreen.svelte` returns 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (`EntityScreen.svelte`) -> InstantDB (InstaQL `where`) | `scopeWhere` is merged into the query client-side; InstantDB re-applies `instant.perms.ts`'s owner-scoped rules server-side regardless of the `where` clause sent by the client |
| User input (create form) -> `selectedLinks` (via `presetLinks` pre-fill) | The pre-filled value is still subject to the existing submit-time parent-existence re-validation (`db.queryOnce` + `rows.length === 0` guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02-01 | Tampering | `scopeWhere` merge in `buildQuery` | medium | accept | A caller-supplied `scopeWhere` can at most narrow/misdirect which rows of the SAME already-authorized owner's data are displayed or created against -- InstantDB's server-side `donoId`-scoped perms rule (`instant.perms.ts`, untouched) is the real access-control boundary, not this client-side convenience filter. No caller passes a non-null `scopeWhere` anywhere in Phase 18 (`Shell.svelte` omits both props); this is a forward-looking acceptance for Phase 19/20's future parent-scoped callers. |
| T-18-02-02 | Elevation of Privilege | `presetLinks` pre-fill in `startCreate` | low | accept | Only changes the INITIAL value of an already-editable `<select>`; the existing submit-time parent-existence check re-validates against the live database regardless of how `selectedLinks` was seeded, so a pre-filled value can never bypass authorization. |
| T-18-02-03 | Tampering | Regression of the null/null default path | high | mitigate | The entire existing Playwright suite passing with ZERO edits to any spec file is this plan's own hard verification gate -- any accidental behavior change on the default path fails a pre-existing, already-green test immediately, not silently. |

</threat_model>

<verification>
`cd web && bun run test:e2e` (all 3 projects) passes with zero spec-file edits. `cd web && bun
run check` and `cd web && bun run lint` are clean on `EntityScreen.svelte`.
</verification>

<success_criteria>
- `scopeWhere`/`presetLinks` are additive optional props, both defaulting to `null`.
- `buildQuery`/`startCreate` degrade to today's exact literal output when both are `null`.
- Zero etype-specific branch, zero "mode" prop, in the diff.
- Full pre-existing e2e suite green without a single spec-file edit.
</success_criteria>

<output>
Create `.planning/phases/18-navigation-foundation-entityscreen-extension/18-02-SUMMARY.md` when done.
</output>
