---
phase: 18-navigation-foundation-entityscreen-extension
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/e2e/shell-chrome.spec.ts
  - web/e2e/entities-header-states.spec.ts
  - web/e2e/cross-phase-verification.spec.ts
  - web/e2e/entities-ticket-subtarefa.spec.ts
  - web/e2e/entities-delete-confirmation.spec.ts
  - web/e2e/entities-projeto-etapa-tarefa.spec.ts
  - web/e2e/entities-form-dialog-composition.spec.ts
  - web/e2e/entities-form-restyle.spec.ts
  - web/e2e/entities-rotina-log.spec.ts
autonomous: true
requirements: [NAV-05, NEST-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 27 catalogued e2e call sites that used to click a nav-<nested-etype> button now calls gotoNested(page, etype) instead, with zero remaining reference to nav-etapas/nav-templatesRotina/nav-subtarefas/nav-tarefas anywhere in web/e2e/ (NAV-05)."
    - "Every wildcard [data-testid^=\"nav-\"] count assertion reflects the new 6-item topbar (or the 5-entity subset when Dashboard is deliberately excluded), across all 4 affected files (NAV-05)."
    - "entities-fundos.spec.ts required zero edits, and the Log-specific assertions in entities-rotina-log.spec.ts are untouched (NEST-06)."
  artifacts:
    - web/e2e/shell-chrome.spec.ts
    - web/e2e/entities-header-states.spec.ts
    - web/e2e/cross-phase-verification.spec.ts
    - web/e2e/entities-ticket-subtarefa.spec.ts
    - web/e2e/entities-delete-confirmation.spec.ts
    - web/e2e/entities-projeto-etapa-tarefa.spec.ts
    - web/e2e/entities-form-dialog-composition.spec.ts
    - web/e2e/entities-form-restyle.spec.ts
    - web/e2e/entities-rotina-log.spec.ts
  key_links:
    - "gotoNested.ts (created in Plan 18-01) -> every migrated call site -> nested-goto/nested-goto-<etype> testids rendered by Shell.svelte's interim affordance (also Plan 18-01) -- this plan is Wave 2 specifically because it depends on both of those existing first."
---

<objective>
Fix every pre-existing Playwright assertion that the Phase 18 topbar restructuring (Plan 18-01)
breaks: 4 files with a hardcoded 9-item `[data-testid^="nav-"]` wildcard count now that the
topbar has 6 items (or 5 entities, once Dashboard is correctly excluded from entity-specific
loops), and 7 files with 27 total direct `nav-etapas`/`nav-templatesRotina`/`nav-subtarefas`/
`nav-tarefas` click call sites that must migrate to the new `gotoNested(page, etype)` helper.

Purpose: this is NAV-05's full regression-proof requirement and the second half of NEST-06's
proof (Fundos/Log render unchanged; only `entities-rotina-log.spec.ts`'s `templatesRotina`
navigation lines need editing, not its Log-specific assertions). This plan is Wave 2 because it
depends on Plan 18-01's 6-item topbar and `gotoNested.ts` helper already existing.
Output: `shell-chrome.spec.ts`, `entities-header-states.spec.ts`, and
`cross-phase-verification.spec.ts` have their wildcard-count assertions corrected;
`cross-phase-verification.spec.ts` plus the other 6 spec files have zero remaining
`nav-etapas`/`nav-templatesRotina`/`nav-subtarefas`/`nav-tarefas` references.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-navigation-foundation-entityscreen-extension/18-CONTEXT.md
@.planning/phases/18-navigation-foundation-entityscreen-extension/18-RESEARCH.md

@web/e2e/shell-chrome.spec.ts
@web/e2e/entities-header-states.spec.ts
@web/e2e/cross-phase-verification.spec.ts
@web/e2e/entities-ticket-subtarefa.spec.ts
@web/e2e/entities-delete-confirmation.spec.ts
@web/e2e/entities-projeto-etapa-tarefa.spec.ts
@web/e2e/entities-form-dialog-composition.spec.ts
@web/e2e/entities-form-restyle.spec.ts
@web/e2e/entities-rotina-log.spec.ts
@web/e2e/helpers/gotoNested.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix wildcard-count assertions broken by the 9-to-6 topbar change</name>
  <files>web/e2e/shell-chrome.spec.ts, web/e2e/entities-header-states.spec.ts</files>
  <action>
`shell-chrome.spec.ts`'s test `"single content-frame consistency across entities"` (line 63):
change `expect(count).toBe(9);` to `expect(count).toBe(6);`. No other change needed in this
file -- this test only reads `shell-content-frame`'s bounding box/maxWidth, not entity-specific
content, and index 0 (now `nav-dashboard`) is harmless since `Dashboard.svelte` still mounts
inside the same `shell-content-frame` `<main>` element.

`entities-header-states.spec.ts`'s test `"ENTTBL-07: every one of the 9 entities' content
renders inside entity-table-frame"` (lines 183-203): change the locator on line 188 from
`page.locator('[data-testid^="nav-"]')` to
`page.locator('[data-testid^="nav-"]:not([data-testid="nav-dashboard"])')`, and change
`expect(count).toBe(9);` (line 190) to `expect(count).toBe(5);`. Rename the test's title to drop
the stale "9 entities" phrasing (use "5 entities" instead) since Dashboard is explicitly not an
entity screen and renders no `entity-table-frame` -- the per-button loop (lines 192-202) already
correctly asserts `entity-table-frame`/table-or-empty-state for each remaining button once
Dashboard is excluded from the locator.
  </action>
  <verify>
    <automated>cd web && bunx playwright test shell-chrome.spec.ts entities-header-states.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
Both files pass; neither file contains a stale `toBe(9)`/`toHaveCount(9)` assertion; neither
file's entity-specific loop includes the Dashboard button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate cross-phase-verification.spec.ts + entities-ticket-subtarefa.spec.ts + entities-delete-confirmation.spec.ts to gotoNested</name>
  <files>web/e2e/cross-phase-verification.spec.ts, web/e2e/entities-ticket-subtarefa.spec.ts, web/e2e/entities-delete-confirmation.spec.ts</files>
  <action>
Add `import { gotoNested } from "./helpers/gotoNested.ts";` to all 3 files.

`cross-phase-verification.spec.ts` needs 4 distinct fixes, all in this one file: (1) test
`"VERIFY-07/POLISH-04: cross-phase walkthrough..."`, lines 188-199 -- the `navButtons`/`navCount`
loop asserts `entity-header`/`entity-table-frame` for every button, which Dashboard does not
have; change the locator on line 188 to
`page.locator('[data-testid^="nav-"]:not([data-testid="nav-dashboard"])')` and `expect
(navCount).toBe(9);` (line 190) to `expect(navCount).toBe(5);`. (2) test
`"VERIFY-05/POLISH-03: Shell header and all 9 nav buttons legible in both color schemes"`, lines
325-347 -- this is a pure visibility check with no entity-specific assertion, so Dashboard stays
included: change `await expect(navButtons).toHaveCount(9);` (line 335) to `toHaveCount(6);` and
`for (let i = 0; i < 9; i++)` (line 336) to `for (let i = 0; i < 6; i++)`; also rename the test
title's "all 9 nav buttons" to "all 6 nav buttons". (3) & (4) lines 355-356 and 379-380, both
inside the `"VERIFY-05/POLISH-03: tarefas create Dialog legible..."` test's per-color-scheme
loop -- replace `await page.goto("/"); await page.getByTestId("nav-tarefas").click();` with
`await gotoNested(page, "tarefas");` at both occurrences (the `page.emulateMedia({ colorScheme
})` call immediately before each stays untouched).

`entities-ticket-subtarefa.spec.ts`: replace all 9 occurrences of the `nav-subtarefas` 2-line
click pattern with `await gotoNested(page, "subtarefas");` -- 5 are preceded by `await page.goto
("/");` (lines 225-226, 262-263, 295-296, 322-323, 413-415, using the goto immediately above
each), and the remaining are preceded by `await page.reload();` (lines 393-394, 438-439,
449-450) or bracketed by a preceding `waitForSettle`/`reload` pair -- in every case, delete the
`goto`/`reload` line together with the `nav-subtarefas` click line and replace both with the
single `gotoNested` call (a fresh `page.goto("/")` inside `gotoNested` is behaviorally
equivalent to a same-URL `page.reload()` here: neither call site's surrounding assertions depend
on reload semantics specifically, only on landing back on a fresh, authenticated `subtarefas`
screen).

`entities-delete-confirmation.spec.ts`: replace all 5 occurrences of the `nav-tarefas` 2-line
click pattern (lines 101-102, 153-154, 191-192, 222-223, 261-262, each preceded by `await
page.goto("/");`) with `await gotoNested(page, "tarefas");`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test cross-phase-verification.spec.ts entities-ticket-subtarefa.spec.ts entities-delete-confirmation.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
All 3 files pass; `grep -n 'nav-tarefas\|nav-subtarefas' web/e2e/cross-phase-verification.spec.ts
web/e2e/entities-ticket-subtarefa.spec.ts web/e2e/entities-delete-confirmation.spec.ts` returns
0 matches; both wildcard-count assertions in `cross-phase-verification.spec.ts` reflect the new
topbar (5 when Dashboard is excluded, 6 when it is included).
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining 4 files to gotoNested</name>
  <files>web/e2e/entities-projeto-etapa-tarefa.spec.ts, web/e2e/entities-form-dialog-composition.spec.ts, web/e2e/entities-form-restyle.spec.ts, web/e2e/entities-rotina-log.spec.ts</files>
  <action>
Add `import { gotoNested } from "./helpers/gotoNested.ts";` to all 4 files.

`entities-projeto-etapa-tarefa.spec.ts`: 4 occurrences -- lines 228-229 (`nav-etapas` after
`page.goto("/")`), 257-258 (`nav-etapas` after `page.waitForTimeout(1500); await page.reload
();`), 283-284 (`nav-tarefas` after `page.goto("/")`), 345-346 (`nav-etapas` after `page.goto
("/")`). Replace each 2-line pattern with `await gotoNested(page, "etapas");` or `await
gotoNested(page, "tarefas");` as appropriate (delete the `waitForTimeout`/`reload` pair at
257-258 along with the click, same reasoning as Task 2: a fresh navigation is behaviorally
equivalent here).

`entities-form-dialog-composition.spec.ts`: 3 occurrences, all `nav-tarefas` after `page.goto
("/")` (lines 41-42, 99-100, 177-178). Replace each with `await gotoNested(page, "tarefas");`.

`entities-form-restyle.spec.ts`: 2 occurrences -- lines 291-292 (`nav-templatesRotina` after
`page.goto("/")`), 424-425 (`nav-subtarefas` after `page.goto("/")`). Replace with `await
gotoNested(page, "templatesRotina");` and `await gotoNested(page, "subtarefas");` respectively.

`entities-rotina-log.spec.ts`: 2 occurrences, both `nav-templatesRotina` -- line 118-119 (after
`page.goto("/")`) and line 171-172 (after `await waitForSettle(page); await page.reload();`).
Replace both with `await gotoNested(page, "templatesRotina");` (deleting the `waitForSettle`/
`reload` pair at the second occurrence). Leave every Log/`instanciasRotina`-specific assertion
in this file completely untouched -- this file's Log-related tests are NEST-06's regression
proof and must not be edited beyond these 2 navigation lines.
  </action>
  <verify>
    <automated>cd web && bunx playwright test entities-projeto-etapa-tarefa.spec.ts entities-form-dialog-composition.spec.ts entities-form-restyle.spec.ts entities-rotina-log.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>
All 4 files pass; `grep -rn 'nav-etapas\|nav-templatesRotina\|nav-subtarefas\|nav-tarefas'
web/e2e` (repo-wide) returns 0 matches -- confirming all 27 catalogued call sites across all 7
files are migrated; `entities-rotina-log.spec.ts`'s Log/`instanciasRotina` assertions are
byte-identical to before this task (diff shows only the 2 navigation-line replacements).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new -- this plan edits Playwright spec files and consumes the Plan 18-01 e2e helper only;
no production runtime code is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03-01 | Tampering | Migrated spec assertions / wildcard-count fixes | low | accept | Test-only code; a bug here can only produce a false pass/fail in CI, never a production security effect. Verified by literally running every migrated spec file and asserting real pass/fail per task. |
| T-18-03-02 | Repudiation | n/a | n/a | accept | No write-path or audit-log code is touched by this plan. |

</threat_model>

<verification>
After Task 3, run the full suite once as the phase-level gate: `cd web && bun run test:e2e`
(all 3 projects) and `cd web && bun test src` (unit, `registry.test.ts`). Also run `cd web &&
bun run check` and `cd web && bun run lint`. Finally confirm `grep -rn
'nav-etapas\|nav-templatesRotina\|nav-subtarefas\|nav-tarefas' web/e2e` and `grep -n 'if
(config.etype\|if (cfg.etype' web/src/lib/entities/EntityScreen.svelte` both return zero
matches.
</verification>

<success_criteria>
- `shell-chrome.spec.ts`, `entities-header-states.spec.ts`, `cross-phase-verification.spec.ts`
  no longer assert a stale 9-item wildcard count.
- Zero remaining `nav-etapas`/`nav-templatesRotina`/`nav-subtarefas`/`nav-tarefas` reference
  anywhere in `web/e2e/`.
- `entities-fundos.spec.ts` required zero edits; `entities-rotina-log.spec.ts`'s Log-specific
  assertions are untouched.
- Full `bun run test:e2e` suite (all 3 projects) passes -- the phase's overall regression gate.
</success_criteria>

<output>
Create `.planning/phases/18-navigation-foundation-entityscreen-extension/18-03-SUMMARY.md` when done.
</output>
