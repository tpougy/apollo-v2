---
phase: 20-rotinas-tickets-sections
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-03"]
files_modified:
  - web/e2e/helpers/subtarefasPanel.ts
  - web/e2e/entities-ticket-subtarefa.spec.ts
  - web/e2e/entities-form-restyle.spec.ts
autonomous: true
requirements: [NEST-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every pre-existing e2e test that used to open the unscoped subtarefas table via gotoNested(page, \"subtarefas\") now reaches an equivalent, correctly-scoped SubtarefasPanel instead"
    - "The still-editable xor-parent-type selector's manual-override capability (spec §2.4's own \"apenas pré-resolvido\" wording) remains exercised by at least one migrated test"
  artifacts:
    - web/e2e/helpers/subtarefasPanel.ts
  key_links:
    - "openSubtarefasPanelForTicket/openSubtarefasPanelForTarefa target the exact TicketsSection (Plan 20-01) and ProjetosSection \"Todas as tarefas\" (Plan 20-03) row-click affordances — no new app-side testid is introduced by this plan"
---

<!-- planner-discipline-allow: gotoNested(page, "subtarefas") -->

<objective>
Retire every remaining `gotoNested(page, "subtarefas")` call site (9 occurrences across 2 files,
covering 7 distinct tests) in favor of two new, parent-id-aware e2e helpers that open the real
`SubtarefasPanel` built in Plans 20-01/20-03. This is the prerequisite for Plan 20-05's removal of
the interim `nested-goto` dropdown — that dropdown cannot be deleted while any test still depends
on it for `subtarefas`.

Purpose: `20-RESEARCH.md` Pitfall 3 — `SubtarefasPanel` is, by design, always scoped to ONE
concrete parent; a single generic `gotoNested(page, "subtarefas")` call cannot serve tests that
need to freely pick or switch between both xor choices. `entities-ticket-subtarefa.spec.ts`'s
6 tests and `entities-form-restyle.spec.ts`'s 1 xor-Select test all need individual,
parent-id-aware rewrites, not a `gotoNested.ts` body extension.
Output: `web/e2e/helpers/subtarefasPanel.ts`, migrated call sites in both spec files, one
documented, justified test retirement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-rotinas-tickets-sections/20-CONTEXT.md
@.planning/phases/20-rotinas-tickets-sections/20-RESEARCH.md
@web/e2e/entities-ticket-subtarefa.spec.ts
@web/e2e/entities-form-restyle.spec.ts
@web/e2e/helpers/gotoNested.ts
@web/e2e/helpers/form-controls.ts
@web/e2e/helpers/delete-confirmation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: New helper file — openSubtarefasPanelForTicket / openSubtarefasPanelForTarefa</name>
  <files>web/e2e/helpers/subtarefasPanel.ts</files>
  <action>
Create `web/e2e/helpers/subtarefasPanel.ts`, mirroring `gotoNested.ts`'s plain-exported-async-
function style (no class). `openSubtarefasPanelForTicket(page: Page, ticketId: string)`:
`await page.goto("/")`, click `nav-tickets`, then `page.locator('[data-testid="row"][data-eid="${ticketId}"]').click()`
(this row lives inside `TicketsSection`'s click-delegation wrapper from Plan 20-01 — clicking it
sets `selectedTicketId` and opens the scoped panel). `openSubtarefasPanelForTarefa(page: Page, tarefaId: string)`:
`await page.goto("/")`, click `nav-projetos`, click `projetos-tab-todas`, then
`page.locator('[data-testid="row"][data-eid="${tarefaId}"]').click()` (this reaches
`ProjetosSection`'s "Todas as tarefas" wiring from Plan 20-03, which works for any tarefa
regardless of whether it has an etapa — exactly why this helper does not need a projeto/etapa
parameter). Both functions return `Promise&lt;void&gt;`; callers assert `subtarefas-panel` visibility
themselves, matching `gotoNested.ts`'s own convention of not asserting inside the helper.
  </action>
  <verify>
    <automated>cd web && bunx tsc --noEmit -p e2e/tsconfig.json 2>/dev/null || bunx tsc --noEmit e2e/helpers/subtarefasPanel.ts</automated>
  </verify>
  <done>web/e2e/helpers/subtarefasPanel.ts exports both functions with the exact signatures above, type-checks cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 7 remaining tests off gotoNested(page, "subtarefas")</name>
  <files>web/e2e/entities-ticket-subtarefa.spec.ts, web/e2e/entities-form-restyle.spec.ts</files>
  <action>
Import `openSubtarefasPanelForTicket`/`openSubtarefasPanelForTarefa` from
`./helpers/subtarefasPanel.ts` in both files, replacing the now-unused `gotoNested` import where it
was ONLY used for `subtarefas` (keep it if a file still needs it for another etype — none of these
two files do, per the earlier repo-wide grep for this etype).

In `entities-ticket-subtarefa.spec.ts`, migrate each of the 6 tests that currently call
`gotoNested(page, "subtarefas")`:

- "subtarefa created with a tarefa parent...": replace the `gotoNested` + manual
  `entity-create-start`/`xor-parent-type`/`link-tarefa` sequence with
  `openSubtarefasPanelForTarefa(page, chainTarefaId)` then click `subtarefa-add-start` (the parent
  is now pre-resolved automatically), fill `field-titulo`/`field-ordem`, submit. Keep every
  existing assertion (column values, CLI-side `listSubtarefasByTarefa`/`listSubtarefasByTicket`
  checks, delete via `confirmRowDelete`) — only the opening sequence changes.
- "subtarefa created with a ticket parent...": same shape via `openSubtarefasPanelForTicket(page, chainTicketId)`.
- "T-04-04: subtarefa submitted with no parent selected is blocked, nothing written": RETIRE this
  test with a code comment explaining why: its premise (a parent type is pre-selected by the
  generic engine's own default, but no id is chosen) required a raw, unscoped create form — the
  ONLY UI path that ever exposed that specific state. `SubtarefasPanel`'s visible instance disables
  its own native create button (`capabilities.create: false`, Plan 20-01) specifically because that
  default-xor state is unsafe, and its "+ subtarefa" trigger always drives BOTH the parent type and
  the parent id to a resolved value before the dialog is ever shown to a human or a test — there is
  no remaining UI entry point that reaches "type known, id blank" deterministically. The underlying
  validation guard this test exercised lives in `EntityScreen.svelte` (unchanged this phase, still
  in effect as a defense against a driven-click race) and is not being removed — only this
  particular UI-reachability regression test is, since its reachability assumption no longer holds
  post-Phase-20.
- "T-04-11: switching parent type before submit links only the final choice": migrate the opening
  to `openSubtarefasPanelForTarefa(page, chainTarefaId)` + click `subtarefa-add-start` (now
  pre-resolved to "tarefa"/`chainTarefaId`), THEN keep the existing manual override — call
  `selectByText(page, "xor-parent-type", "ticket")` then `selectByText(page, "link-ticket", chainTicketTitulo)`
  exactly as before — proving the selector genuinely remains present and editable per spec §2.4's
  "apenas pré-resolvido" wording, even though the normal flow never requires touching it. Keep the
  rest of the test (submit, CLI-side assertions, delete) unchanged.
- "T-04-11: editing a subtarefa's parent type unlinks the old parent...": migrate the opening to
  `openSubtarefasPanelForTarefa(page, chainTarefaId)` + driven create (resolved to tarefa), keep the
  server-side pre-edit assertions, keep `row-edit` + the manual `selectByText(page, "xor-parent-type", "ticket")`/
  `selectByText(page, "link-ticket", chainTicketTitulo)` override on edit (this is the SAME
  still-available override path, exercised on edit instead of create), keep the unlink assertions.
  Replace the final `gotoNested(page, "subtarefas")` reload-check with
  `openSubtarefasPanelForTicket(page, chainTicketId)` (the row is now parented to the ticket, so it
  belongs in the ticket-scoped panel, not a re-opened tarefa-scoped one) and assert it appears there
  with the correct columns.
- "subtarefa concluida boolean round-trips...": migrate the opening to
  `openSubtarefasPanelForTarefa(page, chainTarefaId)` + driven create, and replace both
  reload-recheck `gotoNested(page, "subtarefas")` calls with `openSubtarefasPanelForTarefa(page, chainTarefaId)`
  again (same parent, re-opened after reload) — keep the boolean-toggle/reload/assert sequence
  otherwise unchanged.

In `entities-form-restyle.spec.ts`'s "ROADMAP Phase 10 SC3" test (its own separate
`chainTarefaId`/`chainTicketId` fixtures, line ~424), apply the identical migration shape to the
"T-04-11: editing...unlinks" case above: `openSubtarefasPanelForTarefa` + driven create, keep the
manual xor-override on `row-edit`, keep the server-side unlink assertions, keep the final delete.
  </action>
  <verify>
    <automated>cd web && bunx playwright test entities-ticket-subtarefa.spec.ts entities-form-restyle.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>Both spec files contain zero remaining calls to gotoNested(page, "subtarefas"); every migrated test passes; the one retired test's removal is documented inline with its rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only helper (`subtarefasPanel.ts`) → app UI | No new production code; this plan touches only `web/e2e/`, never `web/src/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Repudiation (test coverage gap) | Retired "T-04-04: no parent selected" test | low | accept | The retirement is documented inline in the spec file with its rationale (state is provably unreachable through the shipped UI); the underlying `EntityScreen.svelte` validation guard it exercised is unmodified this phase and remains covered as a defensive check, just without a dedicated UI-reachability regression test. |

</threat_model>

<verification>
Run `cd web && bunx playwright test entities-ticket-subtarefa.spec.ts entities-form-restyle.spec.ts --project=authed --no-deps`.
Confirm `grep -rn 'gotoNested(page, "subtarefas")' web/e2e/` returns no matches.
</verification>

<success_criteria>
- Zero remaining `gotoNested(page, "subtarefas")` call sites anywhere in `web/e2e/`.
- The still-editable xor selector's manual-override path stays covered by at least the two
  migrated "switching"/"editing" tests.
- The one retired test's removal is documented, not silent.
</success_criteria>

<output>
Create `.planning/phases/20-rotinas-tickets-sections/20-04-SUMMARY.md` when done
</output>
