---
phase: 23-focus-dialog-system
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-03"]
files_modified:
  - web/src/lib/dashboard/dialogs/DayDialog.svelte
  - web/src/lib/dashboard/dialogs/RotinaDialog.svelte
  - web/src/lib/dashboard/Dashboard.svelte
  - web/src/lib/dashboard/WeekCalendar.svelte
  - web/src/lib/dashboard/MonthHeatmap.svelte
  - web/e2e/focus-dialog-dia-rotina.spec.ts
autonomous: true
requirements: [DLG-01, DLG-02, DLG-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every WeekCalendar day-header, week-calendar item (tarefa/rotina/ticket), weekend-popover item, and month-heatmap cell is a real, wired <button> opening the correct dialog -- dash-week-day-header/dash-heatmap-cell open Dia; dash-week-item/dash-weekend-popover-item dispatch to Tarefa, Rotina, or Ticket by item.tipo"
    - "The weekend Popover now has a distinct per-day clickable header (sábado / domingo) that opens the Dia dialog for that specific date, separate from clicking an item inside it"
    - "A rotina item's displayed label is always the linked template's nome, never the raw instancia UUID, in both the weekday cards and the weekend popover -- the pre-existing Pitfall-1 display bug is fixed as part of wiring this exact surface"
    - "The Dia dialog shows that day's FULL, uncapped agenda (no 3-item slice) and offers only 'ir para esta semana' + fechar in its footer -- no editar, no ver na página completa (Dia has no underlying single entity)"
    - "The Rotina dialog's editar affordance is restricted to the status field only, inherited automatically from EntityScreen's existing editableFields() narrowing for instanciasRotinaConfig, with zero new restriction logic written in this plan"
  artifacts:
    - web/src/lib/dashboard/dialogs/DayDialog.svelte
    - web/src/lib/dashboard/dialogs/RotinaDialog.svelte
  key_links:
    - "Dashboard.svelte's dialogStack render chain (started in Plan 23-01) gains \"dia\", \"rotina\", and \"tarefa\" branches; the \"tarefa\" case MUST resolve from the top-level flat query.data.tarefas array (which carries subtarefas), never from projetoRows()'s nested etapas[].tarefas[] (which the 22-01-documented query gap leaves without subtarefas)"
    - "WeekCalendar.svelte and DayDialog.svelte both receive a rotinaNomeById: Map<string,string> prop (the same map Dashboard.svelte already computes for RoutinesByFundo since Phase 22) and use it to resolve any tipo===\"rotina\" item's display label instead of item.titulo"
---

<objective>
Build `DayDialog.svelte` (M, the one dialog with no underlying entity -- footer offers only "ir
para esta semana") and `RotinaDialog.svelte` (S, status-only editable), and fully wire
`WeekCalendar.svelte` (day headers, item dispatch across all 3 item types, the weekend popover's
missing per-day click target, and the pre-existing rotina-label display bug) and
`MonthHeatmap.svelte`'s single heatmap-cell click target -- completing every remaining inert button
in the Dashboard's calendar family.

Purpose: close items #1-4 and #12 of the Exhaustive Inert-Button Inventory in one coherent plan,
since a single `WeekCalendar` item-click handler must dispatch across 3 dialog kinds
simultaneously (Tarefa, already built in Plan 23-03; Ticket, already built in Plan 23-01; Rotina,
built here) -- splitting that one dispatch across more plans would leave the handler provably
incomplete at every intermediate commit. This plan is also the one place this phase fixes a
pre-existing, unrelated display bug (the rotina-title UUID placeholder) because it lives on the
exact element this plan is already rewriting.
Output: `DayDialog.svelte`, `RotinaDialog.svelte`, `Dashboard.svelte`'s "dia"/"rotina"/"tarefa"
dialog-stack branches, `WeekCalendar.svelte`'s complete wiring, `MonthHeatmap.svelte`'s wiring, and
`web/e2e/focus-dialog-dia-rotina.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-focus-dialog-system/23-CONTEXT.md
@.planning/phases/23-focus-dialog-system/23-RESEARCH.md
@web/src/lib/dashboard/Dashboard.svelte
@web/src/lib/dashboard/WeekCalendar.svelte
@web/src/lib/dashboard/MonthHeatmap.svelte
@web/src/lib/dashboard/derive.ts
@web/src/lib/dashboard/dialogs/FocusDialog.svelte
@web/src/lib/dashboard/dialogs/TicketDialog.svelte
@web/src/lib/dashboard/dialogs/TaskDialog.svelte
@web/src/lib/entities/defs/instanciasRotina.ts
@spec-ui.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DayDialog.svelte (M, no editar/ver-pagina) and RotinaDialog.svelte (S, status-only editar)</name>
  <files>web/src/lib/dashboard/dialogs/DayDialog.svelte, web/src/lib/dashboard/dialogs/RotinaDialog.svelte</files>
  <action>
Create `web/src/lib/dashboard/dialogs/DayDialog.svelte` -- the one dialog whose footer deliberately
omits "editar"/"ver na página completa" (resolved decision: Dia dialog footer -- no underlying
entity exists to edit or navigate to; `FocusDialog`'s `onEditar`/`onVerPagina` are simply left
`undefined`, using its already-built `footerExtra` slot for "ir para esta semana" instead -- no
`FocusDialog` change needed). Props: `open: boolean`, `iso: string`, `items: Item[]` (import
`Item`/`ItemTipo` types from `../derive`), `rotinaNomeById: Map<string, string>`, `onOpenItem: (tipo:
ItemTipo, id: string) => void`, `onIrParaSemana: (iso: string) => void`, `onOpenChange: (open:
boolean) => void`, `breadcrumb?: { label: string; onClick: () => void }` (present for contract
symmetry; in practice never populated, since nothing else in this phase ever opens Dia as a
second-level dialog -- only Dia and Projeto are first-level launch points). `items` is passed
through verbatim from `agenda.get(iso)` -- the FULL per-day array `agendaPorDia` already builds
uncapped (spec §4 row 2: "agenda integral do dia... sem corte"); this component must never
`.slice()` it.

Local `WEEKDAY_FULL = ["domingo","segunda-feira","terça-feira","quarta-feira","quinta-feira","sexta-feira","sábado"]`
indexed by `new Date(`${iso}T00:00:00.000Z`).getUTCDay()`. Title =
`` `${cap(WEEKDAY_FULL[dow])}, ${iso.slice(8,10)}/${iso.slice(5,7)}` `` (a tiny local `cap()`
helper capitalizing the first letter). Contexto =
`` `${items.length} afazeres · ${items.filter((i) => i.vencido).length} atrasados` ``. Duplicate
the same tiny `borderClassFor(tipo)` 3-line helper `WeekCalendar.svelte` already has (tarefa ->
`border-foreground`, rotina -> `border-muted-foreground`, ticket -> `border-destructive`) -- this
is a deliberate small duplication, not worth extracting into a shared module for 3 lines used by 2
components. Body: a `data-testid="day-dialog-items"` list; for each `item` in `items` (already
pre-sorted hard-first by `agendaPorDia`, render in given order, no re-sort): a
`<button type="button" data-testid="day-dialog-item" data-eid={item.id} data-tipo={item.tipo}
class="block w-full text-left border-l-[3px] {borderClassFor(item.tipo)} px-2 py-1 text-sm"
onclick={() => onOpenItem(item.tipo, item.id)}>` showing
`item.tipo === "rotina" ? (rotinaNomeById.get(item.id) ?? "Rotina") : item.titulo` -- the SAME
rotina-label fix this plan's Task 3 applies to `WeekCalendar.svelte`, applied here too since
`agenda.get(iso)`'s rotina items carry the identical `titulo: instancia.id` placeholder (per
`derive.ts:252-258`'s own documented note). When `items.length === 0`, render "Nenhum item neste
dia." instead. Render `<FocusDialog {open} {onOpenChange} size="M" {title} contexto={...}
{breadcrumb}>{#snippet children()}...{/snippet}{#snippet footerExtra()}<Button type="button"
variant="outline" data-testid="day-dialog-ir-semana" onclick={() =>
onIrParaSemana(iso)}>ir para esta semana</Button>{/snippet}</FocusDialog>` -- `onEditar`/`onVerPagina`
both omitted (left `undefined`).

Create `web/src/lib/dashboard/dialogs/RotinaDialog.svelte`, self-contained per the established
pattern. Declare
`RotinaDialogRow = { id: string; dedupeKey: string; dataPrevista: string; dataPrevistaEstimada?: string | null; competencia: string; tipoPrazo: string; status: string; templateNome?: string | null; fundoNome?: string | null }`.
Props: `open: boolean`, `rotina: RotinaDialogRow | undefined`, `onOpenChange`, `breadcrumb?` (used
when Dia opens a rotina item as depth-2). Content (spec §4 row 7): title =
`rotina.templateNome ?? "Rotina"`; contexto = `` `${rotina.fundoNome ?? "Sem fundo"} · ${rotina.competencia}` ``.
Body: `dataPrevista` (formatted DD/MM/YYYY), `dataPrevistaEstimada` (if present), `dedupeKey`,
`tipoPrazo`, `status`, and a line "gerada pelo template: {templateNome ?? "—"}". "editar" drives
`instanciasRotinaConfig`'s own hidden host (`rotina-dialog-edit-host`, `row-edit` bounded poll,
identical pattern) -- write NO extra restriction logic: `EntityScreen.svelte`'s existing
`editableFields()` (`EntityScreen.svelte:123-128`) already narrows the resulting form to `status`
only the instant it drives `instanciasRotinaConfig`, because that config's `updatableFields` is
already `["status"]` (resolved decision: Rotina dialog special case -- "só status editável" is
inherited for free). "ver na página completa" clicks `[data-testid="nav-instanciasRotina"]` then
`onOpenChange(false)`. `size="S"`.
  </action>
  <verify>
    <automated>cd web && bun run check</automated>
  </verify>
  <done>DayDialog renders every item in a day's full, uncapped agenda with correct rotina labels and offers only ir-para-semana + fechar; RotinaDialog's editar form exposes only the status field, proven by reusing EntityScreen's existing narrowing with zero new code.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard.svelte -- "dia"/"rotina"/"tarefa" dialogStack branches, type widening, tarefa-fundo join</name>
  <files>web/src/lib/dashboard/Dashboard.svelte</files>
  <action>
Widen the existing top-level `TarefaRow` type (this is the type used for `data?.tarefas`, the FLAT
query branch that carries `subtarefas` -- NOT the type nested under `EtapaRow.tarefas`, which the
same declaration is reused for but which the query never populates with `subtarefas`/`etapa` at
that nesting; both usages stay type-compatible since every new field is optional): add
`descricao?: string | null; dataPrevistaEstimada?: string | null; competencia?: string | null;
status: string;` and widen the existing `etapa?: { id: string; projeto?: ProjetoRow }` field to
`etapa?: { id: string; nome: string; projeto?: ProjetoRow }` (the query's `etapa: { projeto: {} }`
traversal already returns `nome` as an own scalar of `etapas`, it was simply undeclared). Widen
`InstanciaRotinaRow` to add `dedupeKey: string; dataPrevistaEstimada?: string | null;`.

Add `function openDiaDialog(iso: string): void { openDialog({ kind: "dia", id: iso }); }`,
`function openRotinaDialog(id: string): void { openDialog({ kind: "rotina", id }); }`,
`function openTarefaDialog(id: string): void { openDialog({ kind: "tarefa", id }); }`, and
`function irParaEstaSemana(iso: string): void { semanaBase = semanaUtil(iso).dias[0]; closeAllDialogs(); }`
(reuses the existing `semanaUtil`/`semanaBase` already driving week navigation -- spec §4 row 2's
"ir para esta semana" button).

Add `const fundoByProjetoId = $derived.by(() => { const map = new Map<string, FundoRow | null>();
for (const p of projetoRows()) map.set(p.id, p.fundo ?? null); return map; });` (a Dashboard-local
lookup, not a new `derive.ts` export -- this is the exact componentry-level join
23-RESEARCH.md's Q1 recommends, distinct from the one genuinely new pure function, `rotinasDoFundo`,
already added in Plan 23-02). Add:
`const activeDiaItems = $derived.by(() => activeDialogRef?.kind === "dia" ? (agenda.get(activeDialogRef.id) ?? []) : []);`
`const activeRotina = $derived.by(() => { if (activeDialogRef?.kind !== "rotina") return undefined; const raw = (query.data as DashboardData | undefined)?.instanciasRotina?.find((i) => i.id === activeDialogRef.id); if (!raw) return undefined; return { id: raw.id, dedupeKey: raw.dedupeKey, dataPrevista: raw.dataPrevista, dataPrevistaEstimada: raw.dataPrevistaEstimada, competencia: undefined, tipoPrazo: raw.tipoPrazo, status: raw.status, templateNome: raw.template?.nome ?? null, fundoNome: raw.template?.fundo?.nome ?? null }; });`
-- NOTE: `InstanciaRotinaRow` as widened does not yet declare `competencia` even though
`defs/instanciasRotina.ts:55` has it as a real field; add `competencia: string;` to the widened
`InstanciaRotinaRow` type too (this was missed above -- add it there, not as a second patch) and
read it as `raw.competencia` here, never `undefined`.
`const activeTarefaForDialog = $derived.by(() => { if (activeDialogRef?.kind !== "tarefa") return undefined; const data = query.data as DashboardData | undefined; const raw = (data?.tarefas ?? []).find((t) => t.id === activeDialogRef.id); if (!raw) return undefined; const projetoId = raw.etapa?.projeto?.id ?? null; const fundo = projetoId ? (fundoByProjetoId.get(projetoId) ?? null) : null; return { id: raw.id, titulo: raw.titulo, descricao: raw.descricao, tipoPrazo: raw.tipoPrazo, dataPrevista: raw.dataPrevista, dataPrevistaEstimada: raw.dataPrevistaEstimada, competencia: raw.competencia, status: raw.status, subtarefas: raw.subtarefas, etapaNome: raw.etapa?.nome ?? null, projetoNome: raw.etapa?.projeto?.nome ?? null, fundoNome: fundo?.nome ?? null }; });`
-- **critical**: this reads from `data?.tarefas` (the flat, top-level branch, which carries
`subtarefas`), never from `projetoRows()`'s nested `etapas[].tarefas[]` (STATE.md's documented
22-01 gap: that nested branch never fetched `subtarefas`, so any lookup through it would silently
show every tarefa as having zero subtarefas regardless of reality).

Refine `breadcrumbLabelFor`'s `"dia"` branch (Plan 23-01 left it returning the raw ISO as a
placeholder) to
`` if (ref.kind === "dia") { const dow = new Date(`${ref.id}T00:00:00.000Z`).getUTCDay(); const names = ["dom","seg","ter","qua","qui","sex","sáb"]; return `${names[dow]} ${ref.id.slice(8,10)}/${ref.id.slice(5,7)}`; } ``.

Import `DayDialog`/`RotinaDialog`. Extend the existing `{#if activeDialogRef?.kind === "ticket"}...{/if}`
chain (Plan 23-01) into an `{:else if activeDialogRef?.kind === "dia"}<DayDialog open={true}
iso={activeDialogRef.id} items={activeDiaItems} rotinaNomeById={rotinaNomeById}
onOpenItem={(tipo, id) => openDialog({ kind: tipo, id })} onIrParaSemana={irParaEstaSemana}
onOpenChange={(open) => { if (!open) closeAllDialogs(); }} />{:else if activeDialogRef?.kind ===
"rotina"}<RotinaDialog open={true} rotina={activeRotina} breadcrumb={breadcrumbRef ? { label:
breadcrumbLabelFor(breadcrumbRef), onClick: popToFirst } : undefined} onOpenChange={(open) => { if
(!open) closeAllDialogs(); }} />{:else if activeDialogRef?.kind === "tarefa"}<TaskDialog
open={true} tarefa={activeTarefaForDialog} breadcrumb={breadcrumbRef ? { label:
breadcrumbLabelFor(breadcrumbRef), onClick: popToFirst } : undefined} onOpenChange={(open) => { if
(!open) closeAllDialogs(); }} />{/if}` (import `TaskDialog` from `./dialogs/TaskDialog.svelte`,
already built by Plan 23-03 -- this is a pure reuse, zero edits to that file).
  </action>
  <verify>
    <automated>cd web && bun run check</automated>
  </verify>
  <done>Dashboard.svelte's dialog-stack chain now covers 4 of 7 kinds (ticket, dia, rotina, tarefa); the tarefa lookup provably uses the flat, subtarefa-carrying query.data.tarefas array; type widening compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: WeekCalendar.svelte full wiring (+ rotina-label bugfix, weekend restructure) and MonthHeatmap.svelte wiring, with e2e coverage</name>
  <files>web/src/lib/dashboard/WeekCalendar.svelte, web/src/lib/dashboard/MonthHeatmap.svelte, web/e2e/focus-dialog-dia-rotina.spec.ts</files>
  <action>
In `web/src/lib/dashboard/WeekCalendar.svelte`: add three props --
`rotinaNomeById: Map<string, string>`, `onOpenDia: (iso: string) => void`, `onOpenItem: (tipo:
ItemTipo, id: string) => void`. Add `function labelFor(item: Item): string { return item.tipo ===
"rotina" ? (rotinaNomeById.get(item.id) ?? "Rotina") : item.titulo; }` (Pitfall 1 fix) and use
`{labelFor(item)}` in place of the bare `{item.titulo}` interpolation in BOTH the weekday-card
item loop (currently `WeekCalendar.svelte:66`) and the weekend-popover item loop.

Add `onclick={() => onOpenDia(dia)}` to the existing `dash-week-day-header` button (item #1 of the
inventory, currently inert). Add `onclick={() => onOpenItem(item.tipo, item.id)}` to the existing
`dash-week-item` button (item #2 -- dispatches to Tarefa/Rotina/Ticket by `item.tipo`, all 3 dialog
kinds now exist as of this plan).

Restructure the weekend section (item #3 -- convert to a real button -- and item #4 -- the missing
per-day header, per resolved decision on the weekend popover per-day button): replace the single
combined `weekendItems` derived value with two: `const sabadoItems = $derived(agenda.get(sabado) ??
[]);` and `const domingoItems = $derived(agenda.get(domingo) ?? []);` (keep `weekendCount` as the
sum of both lengths, unchanged). Inside `Popover.Content`, render two sections in order: for
sábado, a `<button type="button" data-testid="dash-weekend-day-header" data-eid={sabado}
class="w-full px-1 py-1 text-left text-xs font-medium" onclick={() => onOpenDia(sabado)}>sábado
{Number(sabado.slice(8,10))}</button>` followed by `sabadoItems`, each rendered as `<button
type="button" data-testid="dash-weekend-popover-item" data-eid={item.id} data-tipo={item.tipo}
class="block w-full text-left border-l-[3px] {borderClassFor(item.tipo)} px-1 text-xs"
onclick={() => onOpenItem(item.tipo, item.id)}>{labelFor(item)}</button>` (converted from a plain
`<div>` -- item #3); then an identical section for domingo. This keeps the existing
`dash-weekend-popover-item` testid and text-content contract byte-identical for
`dashboard.spec.ts`'s pre-existing weekend-chip test, which only asserts on that testid's content
inside the popover, never on the flat-vs-grouped structure.

In `web/src/lib/dashboard/MonthHeatmap.svelte`: add `onOpenDia: (iso: string) => void` prop and
`onclick={() => onOpenDia(iso)}` to the existing `dash-heatmap-cell` button (item #12).

In `Dashboard.svelte`'s existing `<WeekCalendar .../>` and `<MonthHeatmap .../>` mount points, pass
`rotinaNomeById={rotinaNomeById}` (already computed since Phase 22, just never threaded to
`WeekCalendar` until now), `onOpenDia={openDiaDialog}`, and
`onOpenItem={(tipo, id) => openDialog({ kind: tipo, id })}` (WeekCalendar), plus
`onOpenDia={openDiaDialog}` (MonthHeatmap).

Create `web/e2e/focus-dialog-dia-rotina.spec.ts` (prefix `phase23-e2e-`). Seed a fundo, a rotina
template + instance dated this week (via the admin `seedInstance` fixture, mirroring
`dashboard.spec.ts`'s own pattern), a hard tarefa dated this week, and a hard ticket dated on this
week's Saturday. Tests: (a) the rotina item in the week band shows the template's `nome`, never the
instancia id, in BOTH the weekday card (if it lands on a weekday) and, for a weekend-dated one, the
weekend popover; (b) clicking a `dash-week-day-header` opens the Dia dialog showing that day's full
item list including any item beyond a 3-item cap (seed a 4th tarefa on the same day and assert all
4 titles appear, none omitted, no "+N" text anywhere in the dialog); (c) clicking a `dash-week-item`
of each of the 3 `data-tipo` values opens the matching dialog kind (assert each dialog's own
distinguishing testid, e.g. `focus-dialog-editar` targeting the right `EntityConfig`, or absence of
`focus-dialog-editar` entirely for a `rotina`-vs-`tarefa` mixup check is unnecessary here -- assert
title text match instead); (d) inside the weekend popover, `dash-weekend-day-header` for sábado
opens the Dia dialog for that exact Saturday's ISO, distinct from clicking a
`dash-weekend-popover-item`, which opens that item's own dialog instead; (e) `dash-heatmap-cell`
opens the Dia dialog for that exact date; (f) the Dia dialog's footer contains
`day-dialog-ir-semana` and `focus-dialog-fechar` but NEVER `focus-dialog-editar` or
`focus-dialog-ver-pagina`; clicking `day-dialog-ir-semana` for a date in a different week than
today changes `dash-header-title`'s displayed week range; (g) the Rotina dialog's `focus-dialog-editar`
reveals a form containing `field-status` but NOT `field-dedupeKey`/`field-competencia` (status-only,
per `updatableFields`). `afterAll` sweeps every `phase23-e2e-` fixture and deletes the seeded
instance via the admin fixture's `deleteInstance`.
  </action>
  <verify>
    <automated>cd web && bunx playwright test focus-dialog-dia-rotina.spec.ts dashboard.spec.ts --project=authed --no-deps</automated>
  </verify>
  <done>Every calendar-family inert button (day-header, item x3 types, weekend day-header, weekend item, heatmap cell) is wired and opens the correct dialog; the rotina UUID-label bug is fixed everywhere it previously appeared; zero regression to dashboard.spec.ts's existing DASH-03/04 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `agenda.get(iso)` (already-derived, already owner-scoped Item[]) -> DayDialog's body | Read-only render of already-fetched, already-scoped data; no new fetch |
| `onIrParaSemana` -> `semanaBase` `$state` mutation | Purely local UI navigation state, never persisted (T-21-06 precedent already established) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08 | Information Disclosure | `activeTarefaForDialog`'s lookup silently falling back to the nested, subtarefa-less `projetos[].etapas[].tarefas[]` path instead of the flat `data.tarefas` array | medium | mitigate | Task 2's action explicitly names the flat array as the only correct source and cites the documented 22-01 query gap; a code comment in `Dashboard.svelte` repeats this warning at the lookup site itself |
| T-23-09 | Tampering | `onIrParaSemana` receiving an arbitrary/malformed `iso` string | low | accept | `iso` always originates from `dialogStack`'s own `id`, which itself always originates from a real `agenda`/`carga` Map key computed by already-tested `derive.ts` exports -- never user-typed input |
| T-23-10 | Repudiation | N/A -- no state-changing action in this plan | n/a | accept | This plan is entirely read-only navigation; the only write path (Rotina's status-only editar) is `EntityScreen.svelte`'s own unmodified, already-audited `handleSubmit` |
</threat_model>

<verification>
Run `cd web && bunx playwright test focus-dialog-dia-rotina.spec.ts dashboard.spec.ts
focus-dialog-ticket.spec.ts focus-dialog-projetos-kanban.spec.ts --project=authed --no-deps` and
confirm all four files pass together -- this is the first cross-plan regression checkpoint since
`Dashboard.svelte`, `WeekCalendar.svelte`, and `MonthHeatmap.svelte` are all touched here.
</verification>

<success_criteria>
Every remaining Dashboard calendar-family inert button opens its correct dialog; the rotina label
bug is fixed at its source (WeekCalendar) and in the new DayDialog that shares the same data. Dia's
footer exception (no editar/ver-pagina) holds. Rotina's status-only editar is proven, not merely
asserted.
</success_criteria>

<output>
Create `.planning/phases/23-focus-dialog-system/23-04-SUMMARY.md` when done.
</output>
