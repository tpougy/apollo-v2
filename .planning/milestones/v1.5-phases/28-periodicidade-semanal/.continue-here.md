---
context: phase
phase: 28-periodicidade-semanal
task: null
total_tasks: null
status: in_progress
last_updated: 2026-09-22T18:28:02.005Z
---

## Critical Anti-Patterns

_None — this is a proactive context-budget pause (harness warned at 65% usage), not a failure._

<current_state>
Milestone v1.5 (`Correções descobertas no onboarding real do calendário de rotinas`)
is running fully autonomously (`/gsd-autonomous --from 26 --to 31`, explicit user
directive from the session that created this milestone: no `AskUserQuestion`,
resolve grey areas yourself).

Progress:
- **Phase 26 (Validação na escrita)** — ✅ fully complete.
- **Phase 27 (Robustez do job)** — ✅ fully complete. Code review found and fixed
  a genuine TS/Python parity bug across 2 fix iterations; a narrower residual risk
  was consciously accepted and documented.
- **Phase 28 (Periodicidade semanal)** — ⏸ **PAUSED HERE, right before execution.**
  `28-CONTEXT.md`, `28-RESEARCH.md`, `28-PATTERNS.md` all exist and are committed.
  **3 plans exist and are plan-checker-approved**: `28-01-PLAN.md` (Wave 1,
  BLOCKING — schema push), `28-02-PLAN.md` (Wave 2 — CLI/compute, depends on
  28-01), `28-03-PLAN.md` (Wave 2 — SPA parity, depends on 28-01). **None have
  been executed yet.** This is a clean plan→execute boundary, not a mid-execution
  interruption.
- **Phases 29, 30, 31** — not started (no directories yet; scope is fully
  specified in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, nothing
  to re-derive).
- Milestone lifecycle (audit → complete → cleanup) — not started.

Between the prior pause (2026-08-14) and this one (2026-09-22), the user ran
`/gsd-update` (GSD 1.10.0 → 1.14.0) and I completed it mid-session — this is
unrelated to the milestone and needs no follow-up. One interface change was hit
and worked around: `gsd-tools.cjs query init.plan-phase` no longer accepts a
`--skip-research` flag (harmless — research was already cached from the prior
session, so the flag was simply omitted).
</current_state>

<completed_work>

Phase 26: ✅ complete.
Phase 27: ✅ complete.
Phase 28:
- Context, research, pattern-mapping: ✅ done.
- Planning (`28-01`/`28-02`/`28-03`): ✅ done, committed `250f084`.
- Plan-checker: ✅ `VERIFICATION PASSED` — independently re-verified the schema
  decision, recomputed all 7 Friday dates, confirmed 3 pre-existing tests that
  would break and are already repaired in-plan, confirmed the new dispatch
  function is additive (not a modification of existing `du_fixo`/`corrido_fixo`
  logic).
- Planning gates (requirements coverage, roadmap annotate, gap-analysis): ✅ done,
  committed `c8868e0`.
- **Execution: NOT started.**
</completed_work>

<remaining_work>

1. Execute `28-01-PLAN.md` (Wave 1, `[BLOCKING]`) — adds
   `templatesRotina.diaSemana` to `shared/instant.schema.ts`, runs the REAL
   `bun run instant:push` + verify against the LIVE production InstantDB app
   (which already has 18 fundos/84 templatesRotina/168 instanciasRotina).
   **Wait for this to fully land before starting Wave 2** — 28-02 and 28-03
   both `depends_on: ["28-01"]`.
2. Execute `28-02-PLAN.md` — `--dia-semana` CLI flag,
   `_compute_semanal_instances`/`computeSemanalInstances` in both runtimes,
   live proof of the real "Atualiz Calc RF" case (7 Fridays,
   `2026-08-14`..`2026-09-25`, via an explicit `--data-base 2026-08-09` flag —
   **not** wall-clock-dependent, confirmed safe to run regardless of today's
   actual date), shared fixture update, repairs
   `cli/tests/test_crud_rotina_template.py:151`.
3. Execute `28-03-PLAN.md` (can run after/alongside 28-02, both only depend on
   28-01; `workflow.use_worktrees=false` in this project so run executors
   sequentially, one `Agent()` at a time, not in parallel) — SPA parity in
   `web/src/lib/entities/defs/templatesRotina.ts`, repairs
   `web/e2e/entities-rotina-log.spec.ts:125` and
   `web/e2e/entities-form-restyle.spec.ts:302`.
4. Spot-check (git log + `Self-Check: PASSED` in both SUMMARY.md files).
5. `Skill(gsd-code-review, args="28")` → commit REVIEW.md → if findings, dispatch
   `gsd-code-fixer` (fix_scope critical_warning) → re-review → repeat up to 3
   passes. **Do not skip this** — it found genuine bugs in both Phase 26 and
   Phase 27.
6. Spawn `gsd-verifier` for Phase 28 goal achievement → `VERIFICATION.md`.
7. `gsd_run query phase.complete "28"` → commit → evolve `PROJECT.md`'s
   Validated section (mirror the exact style of the existing Phase 26/27
   entries) → commit.
8. Repeat the full cycle (write `CONTEXT.md` myself, decide research y/n,
   pattern-map, plan, plan-check, execute, code-review+fix, verify, complete)
   for **Phase 29** (Controle de geração, RANGE-01), **Phase 30** (Ciclo de
   vida e higiene, LIFE-01/02/03), **Phase 31** (Cadastro em lote, BATCH-01).
   None of their `CONTEXT.md` files exist yet — their scope is already fully
   specified in `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`, nothing
   to re-derive or ask about.
9. After all 6 phases (26-31) complete: `Skill(gsd-audit-milestone)` → if
   passed, `Skill(gsd-complete-milestone, args="v1.5")` → `Skill(gsd-cleanup)`.
</remaining_work>

<decisions_made>

See `.planning/HANDOFF.json`'s `decisions` array — includes the GSD-version-gap
note, the re-confirmed schema decision, the date-safety confirmation for the
Friday-dates live test, and the rationale for this proactive pause.
</decisions_made>

<blockers>
None. This is a proactive context-budget pause (harness reported 65% usage),
taken at a clean plan→execute boundary — not a technical or design blocker.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state + decisions (read this first)
2. `.planning/phases/28-periodicidade-semanal/28-01-PLAN.md`,
   `28-02-PLAN.md`, `28-03-PLAN.md` — ready to execute, already plan-checked
3. `.planning/phases/28-periodicidade-semanal/28-CONTEXT.md` +
   `28-RESEARCH.md` + `28-PATTERNS.md` — background, if a plan detail needs
   clarifying during execution
4. `.planning/REQUIREMENTS.md` — full v1.5 requirement set, for Phases 29-31
   scope once Phase 28 is done
5. `.planning/ROADMAP.md` — Phase 29-31 goals/success criteria/dependencies

## Infrastructure State
- `workflow.use_worktrees=false` — all execution sequential on the main
  working tree, no branching.
- `.planning/state.json` is a new untracked file (appeared after the GSD
  1.10→1.14 update) — inspect on resume before deciding whether to gitignore
  or commit it; do not delete it blindly.
- Real InstantDB app in production already has 18 fundos/84 templatesRotina/168
  instanciasRotina — Phase 28's schema push targets this SAME live app.

<context>
Long-running fully-autonomous milestone execution, hand-driving each GSD
workflow step (the underlying skills are markdown instruction sets, not a
single opaque command). Stay silent between phases per standing user
preference — report only at genuine completion or a genuine blocker. This
pause itself was reported to the user as a heads-up (context budget), which is
appropriate — it is not a per-phase progress narration.
</context>

<next_action>
Start with: spawn `gsd-executor` for `28-01-PLAN.md` (`Agent(subagent_type="gsd-executor", ...)`,
sequential mode, same prompt shape used for every executor spawn in this session
so far — see the 26-01/27-01/27-02 executor prompts earlier in this
conversation for the exact template). Wait for it to fully complete (including
the live schema push) before spawning 28-02 and 28-03. Then continue exactly as
described in `<remaining_work>` above.
</next_action>
