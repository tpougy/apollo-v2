---
phase: 31-cadastro-em-lote
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - cli/tests/test_batch_import.py
autonomous: true
requirements: [BATCH-01]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A batch shaped like the real onboarding (18 fundos + 84 templatesRotina, including 2+ encadeado chains) completes in exactly ONE `apollo import` invocation, not 102 separate CLI calls — live-proven against the real InstantDB app (ROADMAP Success Criterion 3)."
    - "Re-running that same full-scale file reports every record as `existing`, live-proven at the same 18+84 scale with unchanged live row counts (ROADMAP Success Criterion 2, at literal onboarding scale)."
    - "A batch where roughly a third of its natural keys already exist in the database (simulating a prior partial run having already landed those) converges to the correct final state on one full-file re-run — no natural-key duplicate is ever created (ROADMAP Success Criterion 2's resumability guarantee, D-05/D-06/D-07)."
    - "Every quality gate the phase's own code touches (ruff, ty, the full offline suite, test_cli_surface.py) stays green across the whole cli/ package, not just this phase's own files."
  artifacts:
    - path: "cli/tests/test_batch_import.py"
      provides: "the full 18+84-scale Success Criterion 3 proof, its re-run idempotency proof, and the partial-batch-already-landed resume-without-duplication proof"
      contains: "success_criterion_3"
  key_links:
    - from: "cli/tests/test_batch_import.py's full-scale fixture builder"
      to: "cli/apollo_cli/batch_import.py's run_batch_import (Plan 31-01)"
      via: "run_cli([\"import\", \"--from-json\", str(path)])"
      pattern: "\"import\", \"--from-json\""
---

<objective>
Prove `apollo import` at the phase's own literal acceptance scale — an 18-fundo/84-template batch
(the real onboarding shape ROADMAP Success Criterion 3 names explicitly) completing in exactly ONE
invocation, not 102 — and prove the resumability guarantee (Success Criterion 2) both by re-running
the identical full-scale file and by simulating a genuinely partial prior landing (a third of the
batch's natural keys already present before the full file is run once) and confirming zero
duplication on convergence. This plan adds no new production code — it extends Plan 31-01's
`test_batch_import.py` with the scale/resume proofs the tracer plan deliberately deferred, and closes
with a whole-`cli/`-package gate sweep.

Purpose: REQUIREMENTS.md BATCH-01 / ROADMAP Phase 31 Success Criteria 2 and 3 — the concrete,
named acceptance bar ("18 fundos + 84 templates... uma única invocação, não 102"; "uma falha no meio
de um lote não deixa o cadastro num estado que duplique registros ao ser retomado").
Output: two new live tests proving SC2/SC3 at literal scale, plus a final whole-package gate
confirmation — no `batch_import.py`/`cli.py`/`routine_job.py`/`entities/rotina.py` changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-cadastro-em-lote/31-CONTEXT.md
@.planning/phases/31-cadastro-em-lote/31-01-SUMMARY.md

@cli/apollo_cli/batch_import.py
@cli/apollo_cli/routine_job.py
@cli/tests/test_batch_import.py
@cli/tests/conftest.py

## D-NN references (same mapping as 31-01-PLAN.md's `<context>` — not re-derived here)

D-05 (natural-key idempotency), D-06 (id pre-assignment before transact), D-07 (single atomic
transact + ambiguous-failure recovery) are the three decisions this plan's tests exercise at scale;
D-10 (never creates `instanciasRotina`) is reconfirmed by Task 2's structural check. See
31-01-PLAN.md's `<context>` for the full D-01..D-10 mapping against 31-CONTEXT.md's plain-numbered
`<decisions>` block.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full onboarding-scale proof — 18 fundos + 84 templatesRotina in ONE invocation, live (ROADMAP Success Criterion 3), plus same-scale re-run idempotency (Success Criterion 2)</name>
  <files>cli/tests/test_batch_import.py</files>
  <precondition>A persisted `apollo auth login` session exists for the real InstantDB production
  account (existing project setup, not created by this plan); this task's live proof requires it to
  actually run, not skip.</precondition>
  <read_first>
    - .planning/REQUIREMENTS.md BATCH-01 and .planning/ROADMAP.md's Phase 31 Success Criterion 3 (the
      literal "18 fundos + 84 templates... uma única invocação, não 102" acceptance bar this task
      proves directly, not approximated)
    - .planning/phases/31-cadastro-em-lote/31-01-SUMMARY.md (Plan 31-01's final `batch_import.py`
      function names/report shape and validation reason-code vocabulary — read the SUMMARY, not the
      source file again, since Plan 31-01 already established and live-tested these shapes unchanged)
    - cli/tests/test_batch_import.py (Plan 31-01's own fixture-building helpers/idioms — this task's
      full-scale fixture reuses the same record-shape conventions at 18x/84x the record count, built
      programmatically, not hand-typed)
  </read_first>
  <behavior>
    - A single generated batch file describing 18 fundo records and 84 templatesRotina records
      (mixing all 4 `tipoGeracao` values, cycling `regraCompetencia` through every supported value,
      `fundoId` distributed unevenly across the 18 fundo local_ids including a handful with none, and
      at least 2 disjoint `encadeado` chains of 2-3 templates each chained via same-batch
      `$antecessorId`) completes via exactly ONE `apollo import --from-json` call — not 102 separate
      `apollo fundo criar`/`apollo rotina template criar` calls.
    - Re-running the identical 18+84 file reports 100% `existing` for both entities, zero `created`,
      and the live row counts by natural key are unchanged from the first run.
  </behavior>
  <action>
    Add a helper in `cli/tests/test_batch_import.py` that programmatically builds an 18-fundo/
    84-template batch dict in Python (looped construction — a `for` loop generating each record from
    an index, never 84 hand-typed literal dicts): `_local_id`s `f1..f18`/`t1..t84`, every `nome`/
    `codigo` suffixed with one shared `unique_suffix()` call for the whole fixture, template
    `tipoGeracao` cycling through all 4 promoted choice-set values, `regraCompetencia` cycling through
    `REGRAS_COMPETENCIA_SUPORTADAS`, `fundoId` distributed round-robin across the 18 fundo local_ids
    (leaving a handful of templates with no `fundoId` at all), and at least 2 disjoint `encadeado`
    chains of 2-3 templates each, chained via `$`-`antecessorId` within the 84 (reuse Plan 31-01's own
    chain-construction shape, scaled up). Write it to one `tmp_path` JSON file.

    Add `test_full_onboarding_scale_single_invocation_success_criterion_3`: one
    `run_cli(["import", "--from-json", str(path)])` call; assert exit 0; assert the report's
    `fundos.created` has length 18 and `templatesRotina.created` has length 84; append every created
    id to `cleanup_records`; re-query live and confirm exactly 18 fundo rows and 84 template rows
    exist matching this batch's `codigo`/`nome` set, with both encadeado chains' `antecessor` links
    resolving to the correct real sibling ids.

    Add `test_full_onboarding_scale_rerun_is_fully_existing_success_criterion_2`: immediately
    re-run the SAME file (no mutation) via a second `run_cli(["import", "--from-json", str(path)])`
    call; assert exit 0; assert BOTH `created` lists are empty and BOTH `existing` lists have length
    18/84 respectively; re-query and assert the live row counts by natural key are unchanged (still
    exactly 18/84 — no duplicates).
  </action>
  <acceptance_criteria>
    - `uv run --project cli pytest cli/tests/test_batch_import.py -m live -k "onboarding_scale" -v`
      exits 0.
    - `uv run --project cli ruff check cli/tests/test_batch_import.py && uv run --project cli ruff
      format --check cli/tests/test_batch_import.py` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run ruff check tests/test_batch_import.py && uv run ruff format --check tests/test_batch_import.py && uv run pytest tests/test_batch_import.py -m live -k "onboarding_scale" -v && echo TASK1_OK</automated>
  </verify>
  <done>An 18-fundo/84-template batch, including 2+ encadeado chains, completes in exactly one
  `apollo import` invocation (not 102 calls) and an identical re-run reports 100% existing with zero
  duplicate rows — ROADMAP Success Criteria 2 and 3 both live-proven at the literal onboarding
  scale.</done>
</task>

<task type="auto">
  <name>Task 2: Partial-batch-already-landed resume proof + whole-cli/-package gate sweep (D-05/D-06/D-07, D-10 reconfirmed)</name>
  <files>cli/tests/test_batch_import.py</files>
  <read_first>
    - .planning/phases/31-cadastro-em-lote/31-CONTEXT.md decisions 5/6/7 (natural-key idempotency +
      id pre-assignment + single-atomic-transact-with-recovery — the exact mechanism this task's
      resume scenario exercises, not a new one)
    - cli/apollo_cli/routine_job.py's module docstring atomicity framing (the "0 or ALL, never
      partial" reachable-states rule for a SINGLE transact call — this task's simulated
      -partial-landing scenario deliberately engineers "a prior invocation already committed some
      records" by seeding directly via `live_client` BEFORE the real `apollo import` call runs, not
      by attempting to force a torn write inside one transact, which RESEARCH.md Q3's live-confirmed
      atomicity already rules out as unreachable)
  </read_first>
  <behavior>
    - A batch file where roughly a third of its fundo/template natural keys already exist in the
      database (seeded directly beforehand, simulating "a prior partial run already landed these")
      converges correctly on one full-file `apollo import` call: the pre-existing third is reported
      `existing`, the rest `created`, and re-querying confirms not one natural key has more than one
      matching row.
    - `ruff`, `ty`, the full offline suite, and `test_cli_surface.py` stay green across the whole
      `cli/` package.
  </behavior>
  <action>
    Add `test_partial_batch_already_landed_resumes_without_duplication`: build a fresh ~6-fundo/
    ~15-template batch (reuse Task 1's construction helper with a smaller record count). BEFORE
    calling `apollo import` at all, directly seed roughly a third of the batch's fundo/template
    natural keys into the database via `live_client.transact(...)` calls (bypassing the CLI entirely
    — the closest honest simulation of "a prior partial run already committed these," since a
    genuinely torn `apollo import` mid-run is unreachable to engineer directly per RESEARCH.md Q3).
    Then run the FULL, unmodified batch file via ONE `run_cli(["import", "--from-json", str(path)])`
    call; assert exit 0; assert the pre-seeded local_ids' natural keys appear in `existing` (not
    `created`) and the rest appear in `created`; re-query every natural key in the batch directly and
    assert exactly one row exists per key (zero duplicates) — the natural-key-matching complement to
    Task 1's whole-file-re-run proof. Append every id (pre-seeded and newly-created) to
    `cleanup_records`.

    Add `test_batch_import_module_defines_no_instance_entity_reference`: a second, independent
    structural check of D-10/C-06 beyond Plan 31-01's own grep gate — import `apollo_cli.batch_import`
    directly in Python and scan its module attributes (`dir(...)`/`vars(...)`) confirming none of them
    equal or contain the instance-entity's schema name; this is a runtime check on the loaded module
    object, not a text grep, closing the same claim from a second angle.

    This task's `<verify>` (below, not an inline test assertion) runs the full `ruff check`/
    `ruff format --check`/`ty check` across `cli/apollo_cli` + `cli/tests` and
    `pytest cli/tests -m "not live"` to confirm zero regression anywhere in the package from this
    phase's two plans combined, not just the files they touched directly.
  </action>
  <acceptance_criteria>
    - `uv run --project cli pytest cli/tests/test_batch_import.py -m live -v` exits 0 (the full file,
      every test across both plans, in one combined run).
    - `uv run --project cli pytest cli/tests -m "not live" -v` exits 0 (whole-package offline sweep).
    - `uv run --project cli ruff check cli/apollo_cli cli/tests && uv run --project cli ruff format
      --check cli/apollo_cli cli/tests && uv run --project cli ty check cli/apollo_cli` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run ruff check apollo_cli tests && uv run ruff format --check apollo_cli tests && uv run ty check apollo_cli && uv run pytest tests -m "not live" -v && uv run pytest tests/test_batch_import.py -m live -v && echo TASK2_OK</automated>
  </verify>
  <done>A batch with a third of its natural keys already landed (simulating a partial prior run)
  converges to the correct final state on one full re-run with zero duplication; a second,
  independent structural check confirms the module can never create an `instanciasRotina` row; every
  quality gate across the whole `cli/` package stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same boundary as 31-01-PLAN.md (this plan adds no new production code, only tests exercising Plan
31-01's already-mitigated boundary at scale — see that plan's `<threat_model>` for the full register,
T-31-01..T-31-07).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 (reconfirmed at scale) | Repudiation | run_batch_import's post-transact-failure recovery | high | mitigate | Re-exercised implicitly by this plan's 18+84-scale run and its re-run — a larger chunk count over the same recovery path, no new code, live-proven at the scale that matters most for a real ambiguous-network-failure risk. |
| T-31-05 (reconfirmed, second angle) | Tampering | batch_import.py's structural inability to create instanciasRotina | critical | mitigate | Task 2 adds a second, independent proof (a runtime module-attribute scan) beyond Plan 31-01's own text grep, closing the same claim from two angles. |
| T-31-08 | Repudiation | test-fixture data (pre-seeded + batch-created) at 18+84 and partial-resume scale surviving beyond the test run | low | mitigate | Every created id (pre-seeded and batch-created) is appended to `cleanup_records`, deleted in teardown exactly like every other live test in this suite — no new residue-producing pattern introduced. |

</threat_model>

<verification>
- `uv run --project cli pytest cli/tests/test_batch_import.py -m live -v` — full file, both plans'
  tests, including the 18+84-scale Success Criterion 3 proof and the partial-resume convergence
  proof, in one combined run.
- `uv run --project cli pytest cli/tests -m "not live" -v` — whole-package offline sweep.
- `uv run --project cli ruff check cli && uv run --project cli ruff format --check cli && uv run
  --project cli ty check cli` — CLI gates green across the whole package.
</verification>

<success_criteria>
- A batch equivalent to the real onboarding shape (18 fundos + 84 templates) completes in ONE
  `apollo import` invocation, not 102 (ROADMAP Success Criterion 3, live-proven at literal scale).
- Re-running that same file, and separately, resuming after a simulated partial prior landing, both
  converge without duplicating any record by natural key (ROADMAP Success Criterion 2).
- Every quality gate across the whole `cli/` package — not just this phase's own files — stays green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (contributed by this plan)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| 18+84-scale Success Criterion 3 live test | regression/acceptance test | phase verification, future regressions to this scale |
| Partial-batch-already-landed resume test | regression/acceptance test | phase verification, D-05/D-06/D-07's resumability guarantee |

</artifacts>

<output>
Create `.planning/phases/31-cadastro-em-lote/31-02-SUMMARY.md` when done.
Record: confirmation the 18+84 batch completed in one invocation with exact counts; confirmation the
re-run and partial-resume scenarios both converged with zero duplicate rows; confirmation the
whole-`cli/`-package gate sweep (ruff/ty/offline suite) passed with both plans' changes combined.
</output>
