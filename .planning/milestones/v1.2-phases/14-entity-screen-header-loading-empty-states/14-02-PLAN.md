---
phase: 14-entity-screen-header-loading-empty-states
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - web/e2e/entities-header-states.spec.ts
autonomous: true
requirements: [ENTTBL-04, ENTTBL-05, ENTTBL-06, ENTTBL-07]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dedicated Playwright coverage (`entities-header-states.spec.ts`) proves ENTTBL-04 for one entity per capability class — fundos (full-CRUD), instanciasRotina (restricted), logInferenciaClaude (read-only): the page-header's title+description render for all three, and the primary create action renders only when `config.capabilities.create` is true."
    - "The same coverage proves ENTTBL-05 (a Skeleton is visible before data resolves, with zero occurrence of the old plain-text loading indicator), ENTTBL-06 (the Empty composition is a sibling of `<Table>` — zero `<table>` element exists while it is shown — and its CTA opens the same create Dialog `entity-create-start` opens), and ENTTBL-07 (every one of the 9 entities' table/empty content renders inside the same bounded `entity-table-frame` Card)."
    - "The full pre-existing Playwright suite (44 tests, verified this session) plus the 6 new tests in `entities-header-states.spec.ts` (50 total) passes with zero failures/skips against the live InstantDB app."
    - "`svelte-check`/`tsc` (`bun run check`) and Biome (`bun run lint`) remain clean on `web/`, and no raw color literal was introduced in any file touched across this phase's two plans."
  artifacts:
    - web/e2e/entities-header-states.spec.ts
  key_links:
    - "`entities-header-states.spec.ts`'s dual-color-scheme assertion (`page.emulateMedia`) reuses the exact pattern already proven in `design-system.spec.ts`, applied to the new header/Card/Empty surfaces Plan 14-01 introduced."
    - "The Skeleton test's CDP `Network.emulateNetworkConditions` throttle (applied before `page.goto`) is the mechanism that reliably surfaces `query.isLoading`'s transient branch for assertion — a fresh Playwright browser context has no pre-populated InstantDB IndexedDB cache, so the throttled first load genuinely blocks on the network long enough to observe it."
---

<objective>
Add dedicated, live Playwright coverage proving ENTTBL-04/05/06/07 explicitly — across all 3
capability classes, in both color schemes where relevant — on top of Plan 14-01's restructuring, then
run the complete pre-existing-plus-new Playwright suite plus the project's quality gates
(`svelte-check`/`tsc`, Biome) as the phase's final regression.

Purpose: Plan 14-01 proved the restructuring via existing specs (which assert CRUD/capability
behavior, not the new header/loading/empty/Card structure itself). This plan closes that gap with
purpose-built assertions on the new surfaces, then proves the whole milestone-to-date remains green
together — the downstream verification step ROADMAP Phase 14 requires before Phase 15 can build on
top of this screen.
Output: `web/e2e/entities-header-states.spec.ts` (6 new tests); a full-suite regression run reporting
at least 50 passed, 0 failed, 0 skipped; clean `bun run check` and `bun run lint`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-entity-screen-header-loading-empty-states/14-CONTEXT.md
@.planning/phases/14-entity-screen-header-loading-empty-states/14-01-SUMMARY.md

# EntityScreen.svelte as restructured by 14-01 — the exact testids/structure this spec asserts against
@web/src/lib/entities/EntityScreen.svelte

# Dual-color-scheme assertion pattern to reuse, not reinvent
@web/e2e/design-system.spec.ts

# Existing capability-class/nav conventions to reuse
@web/e2e/entities-table-restyle.spec.ts
@web/e2e/shell-nav.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedicated Playwright coverage for ENTTBL-04/05/06/07 across all 3 capability classes</name>

  <precondition>powershell.exe resolves the Outlook Classic COM bridge (`orules.ps1 peek`, per PROJECT.md C-10) from this shell — the `authed` Playwright project this task's &lt;verify&gt; runs depends on the `setup` project, which performs a real live magic-code round trip.</precondition>

  <read_first>
    - `web/src/lib/entities/EntityScreen.svelte` as left by 14-01 — the exact testids this spec asserts against: `entity-header`, `entity-description`, `entity-create-start`, `entity-loading`, `entity-table-frame`, `empty-state`, `empty-state-create`.
    - `web/e2e/design-system.spec.ts` (the tokens test) — the `page.emulateMedia({ colorScheme })` pattern this task's dual-color-scheme assertion reuses.
    - `web/e2e/entities-table-restyle.spec.ts` — the one-test-per-capability-class convention (fundos/instanciasRotina/logInferenciaClaude), `nav-${etype}` click pattern, and `RESYNC_TIMEOUT` idiom this new spec follows.
    - `web/e2e/shell-nav.spec.ts` — confirms `[data-testid^="nav-"]` resolves to exactly 9 elements, reused for the all-9-entities Card-bounding test.
    - Card's confirmed `data-slot="card"` (`web/src/lib/components/ui/card/card.svelte`) and Skeleton's confirmed `animate-pulse` default class — corroborating, not load-bearing, since this spec's own additive testids (`entity-loading`, `entity-table-frame`) are the primary hooks.
  </read_first>

  <behavior>
    - Test 1 (fundos, full-CRUD, light+dark): `entity-header` is visible with computed `justify-content: space-between`; it contains exactly one `<h2>` with text "Fundos" and a non-empty `entity-description`; `entity-create-start` has count 1; `entity-table-frame` is visible and its bounding-box `y` is greater than `entity-header`'s (i.e. positioned below it, confirming header-above-content order). Repeated once under `colorScheme: "light"` and once under `"dark"`.
    - Test 2 (instanciasRotina, restricted): `entity-header` visible with non-empty `entity-description`; `entity-create-start` has count 0 (capability-gated); `entity-table-frame` visible.
    - Test 3 (logInferenciaClaude, read-only): same shape as Test 2 — header+description render, `entity-create-start` count 0, `entity-table-frame` visible.
    - Test 4 (fundos, empty state): with fundos at its baseline zero-record state, `empty-state` is visible, `page.getByRole("table")` has count 0 (Empty is a sibling replacement, not nested table content), `empty-state`'s `empty-state-create` is visible; clicking it opens the same create Dialog (`field-nome` becomes visible) that `entity-create-start` opens, then cancel via `entity-cancel` with zero record created.
    - Test 5 (fundos, loading): with the browser context's network throttled via CDP (`Network.emulateNetworkConditions`, applied before `page.goto`, so the fresh context's InstantDB client cannot serve from cache), `entity-loading` is visible with 1+ `.animate-pulse` children before the throttle is lifted; after lifting the throttle, `entity-loading` disappears and `entity-table-frame` becomes visible.
    - Test 6 (all 9 entities): clicking through every `[data-testid^="nav-"]` button in turn, `entity-table-frame` is visible each time and contains either a `role="table"` element or the `empty-state` testid (never neither) — proving ENTTBL-07 across every entity, not just the 3 capability-class representatives.
  </behavior>

  <files>web/e2e/entities-header-states.spec.ts</files>

  <action>
Create `web/e2e/entities-header-states.spec.ts` (routes automatically under the `authed` project's default `testMatch`, no `playwright.config.ts` edit needed, per `shell-nav.spec.ts`'s own precedent) with the 6 tests described in `<behavior>`, following `entities-table-restyle.spec.ts`'s top-of-file comment convention (why `authed`, what this proves) and its `RESYNC_TIMEOUT` constant.

For Test 1's dual-color-scheme loop, call `page.emulateMedia({ colorScheme })` before `page.goto("/")` for each of `"light"`/`"dark"` in turn (matching `design-system.spec.ts`'s exact call shape), re-navigating and re-asserting the full Test 1 body inside the loop rather than writing two near-duplicate test blocks.

For Test 4, do not seed or delete any fundos record via the CLI — this live app's `fundos` baseline is empty (per PROJECT.md: no real production data exists yet, and every other spec touching fundos cleans up after itself), matching the same assumption `entities-fundos.spec.ts`'s WEB-02 test already relies on for its own final `empty-state`-visible assertion. Clicking `empty-state-create` then `entity-cancel` opens and closes the create Dialog without ever calling `db.transact`, so this test creates zero side effects and needs no cleanup.

For Test 5, obtain a CDP session via `context.newCDPSession(page)`, call `Network.enable` then `Network.emulateNetworkConditions` with a high `latency` (several seconds) and low `downloadThroughput`/`uploadThroughput` before `page.goto("/")` — fundos is the default active entity on mount (`ordem: 1`, confirmed by `shell-nav.spec.ts`'s own comment), so no nav click is needed to observe its loading state. After asserting `entity-loading` is visible, immediately reset the CDP network conditions to unthrottled (`latency: 0`, `downloadThroughput: -1`, `uploadThroughput: -1`) and assert the screen settles into `entity-table-frame`. Also assert the old plain-text loading indicator this plan's predecessor removed (a case-insensitive substring match on the legacy copy) never appears anywhere on the page during this test — a page-wide negative check.

For Test 6, reuse `shell-nav.spec.ts`'s `[data-testid^="nav-"]` selector and its confirmed count of 9; for each nav button, click it, wait for `entity-table-frame` to become visible (`RESYNC_TIMEOUT`), then assert the sum of `role="table"` elements and `empty-state`-testid elements found within `entity-table-frame` is at least 1.

Do not trigger any InstantDB write in Tests 1, 2, 3, 5, or 6 — only Test 4 opens (and immediately cancels) the create Dialog, with zero `db.transact` call anywhere in this spec.
  </action>

  <verify>
    <automated>cd web && bunx playwright test e2e/entities-header-states.spec.ts --project=authed</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && bunx playwright test e2e/entities-header-states.spec.ts --project=authed` reports `6 passed`, `0 failed`.
    - `grep -c 'emulateMedia' web/e2e/entities-header-states.spec.ts` returns 1 or more (dual-color-scheme assertion present).
    - `grep -c 'newCDPSession' web/e2e/entities-header-states.spec.ts` returns exactly 1 (the loading-state throttle mechanism present, scoped to one test).
    - `grep -c 'db.transact\|apolloCli' web/e2e/entities-header-states.spec.ts` returns exactly 0 (this spec performs zero live writes — read-only structural/behavioral assertions plus one open/cancel dialog interaction).
    - `cd web && bun run check` exits 0 (new spec type-checks cleanly under the e2e tsconfig).
  </acceptance_criteria>

  <done>New live Playwright coverage proves, against the real InstantDB app: the page-header (title+description+capability-gated create action) renders correctly for one entity per capability class, in both light and dark color schemes for fundos; the Empty composition is a true sibling of `<Table>` (zero `<table>` while shown) with a working CTA reusing the create flow; the Skeleton loading placeholder is genuinely observable before data resolves and the old plain-text indicator never appears; and every one of the 9 entities' content is Card-bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Full pre-existing-plus-new Playwright suite regression and quality gates</name>

  <precondition>powershell.exe resolves the Outlook Classic COM bridge (`orules.ps1 peek`, per PROJECT.md C-10) from this shell — `bun run test:e2e` runs the `setup`/`authed`/`anon` projects together, several of which perform a real live magic-code round trip.</precondition>

  <read_first>
    - `web/package.json`'s `test:e2e`/`check`/`lint` scripts — the exact commands this task runs, unmodified.
    - `.planning/PROJECT.md` C-12 (zero human UAT — every phase's done-criteria must be Playwright-provable) — the standing constraint this task's fully-automated `<verify>` satisfies.
    - `web/src/lib/entities/EntityScreen.svelte`, all 9 `web/src/lib/entities/defs/*.ts`, and `web/src/lib/entities/types.ts` (final state after 14-01) — the files this task's raw-color-literal grep sweep re-checks.
  </read_first>

  <files>web/ (read-only — full-suite regression and quality gates, no new files this task)</files>

  <action>
Run the complete pre-existing-plus-new Playwright suite as one sequential invocation: `cd web && bun run test:e2e` (all 3 projects: `setup`, `authed`, `anon` — no `--project` filter, no parallel second invocation, per the project's own established rate-limit convention). This session verified a 44-test baseline (43 across all `*.spec.ts` files plus 1 `setup` project test) before this phase, plus this plan's own 6 new `entities-header-states.spec.ts` tests — confirm the transcript reports at least 50 passed and zero failed, zero skipped. A lower passed-count or any skip is a scope-narrowing signal, not a pass — investigate and fix (in `EntityScreen.svelte`, a `defs/*.ts` file, or the new spec, as appropriate) before treating this task as done, never explain it away.

Then run `cd web && bun run check` (svelte-check + tsc across `web/src`, `web/vite.config.ts`, and the e2e tsconfig) and `cd web && bun run lint` (Biome, from the repo root per the script's own `cd ..`) — both must exit 0 with zero new suppressions.

Finally, re-run the raw-color-literal grep gate (Pitfall 4) across every file this phase's two plans touched together: `EntityScreen.svelte`, all 9 `defs/*.ts` files, `types.ts`, and `entities-header-states.spec.ts` — confirm zero matches for `oklch(`/hex/`rgba(` literals outside `app.css`. This is the phase-level close-out of the standing constraint each plan already checked locally.
  </action>

  <verify>
    <automated>cd web && bun run test:e2e</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && bun run test:e2e` reports at least 50 passed, 0 failed, 0 skipped.
    - `cd web && bun run check` exits 0.
    - `cd web && bun run lint` exits 0.
    - `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}\b|rgba?\(' web/src/lib/entities/EntityScreen.svelte web/src/lib/entities/defs/*.ts web/src/lib/entities/types.ts web/e2e/entities-header-states.spec.ts` exits 0 (no raw color literal found).
    - `grep -c 'data-testid="entity-create-start"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1.
    - `grep -c 'data-testid="empty-state"' web/src/lib/entities/EntityScreen.svelte` returns exactly 1.
  </acceptance_criteria>

  <done>A single sequential `bun run test:e2e` run passes with at least 50 tests green and zero failed/skipped, proving ROADMAP Phase 14's 4 success criteria hold together with zero regression to any pre-existing Playwright coverage; `svelte-check`/`tsc` and Biome remain clean; zero raw color literal or duplicate testid exists anywhere this phase touched.</done>
</task>

</tasks>

## Artifacts This Phase Produces

- `web/e2e/entities-header-states.spec.ts` (new) — 6 live tests proving ENTTBL-04 (page-header
  title+description+capability-gated action) across all 3 capability classes with a dual-color-scheme
  check on fundos, ENTTBL-05 (genuinely observable Skeleton loading state via CDP network throttling),
  ENTTBL-06 (Empty as a true sibling of `<Table>`, CTA reusing the create flow), and ENTTBL-07
  (Card-bounded content across all 9 entities).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test runner (Playwright, Node) → Chrome DevTools Protocol (`Network.emulateNetworkConditions`) | Test-only, dev-time network throttle used solely to make an already-real transient UI state (`query.isLoading`) reliably observable; never shipped, no production code path. |
| Browser (client) → InstantDB hosted backend (read-only assertions plus one benign create-dialog open/cancel, zero `db.transact` calls) | Reuses pre-existing, already-authorized query/read paths; the one new interaction (`empty-state-create` click) opens and cancels the create Dialog without submitting, so no new write occurs anywhere in this plan's new spec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | New Playwright spec content (`entities-header-states.spec.ts`, assertions + one dialog open/cancel) | low | accept | Test-only code under `web/e2e/`, never bundled into the shipped `web/` build; standard Biome/tsc gates (Task 2) apply to it like every other e2e spec. |
| T-14-06 | Denial of Service | CDP network throttle in Test 5 | low | accept | Scoped to that single test's own `Page`/`BrowserContext`; explicitly reset to unthrottled immediately after the Skeleton assertion; never touches any other test or the shipped app. |

No package-manager install occurs in this plan (no new dependency, only a new test file) — the
Package Legitimacy Gate does not apply and no `T-14-SC` entry is needed.
</threat_model>

<verification>
Phase-level regression guard, run after both tasks:

`cd web && bun run test:e2e`

This proves, against the live InstantDB app: (1) the new `entities-header-states.spec.ts` coverage
passes across all 3 capability classes plus both color schemes on fundos; (2) every pre-existing spec
(login, shell/nav, entity tables/forms including Plan 14-01's restructuring, routine job,
design-system, no-leakage) remains green, with at least 50 tests passed and zero failed/skipped; (3)
`svelte-check`/`tsc` (`bun run check`) and Biome (`bun run lint`) are clean. Every load-bearing/new
`data-testid` this phase touched resolves to exactly one source occurrence, and zero raw color
literal exists in any touched file.
</verification>

<success_criteria>
Maps 1:1 to ROADMAP.md Phase 14's 4 success criteria (dedicated-proof half; Plan 14-01 delivered the
implementation half):

1. Each entity screen shows a page-header row (title + short description + right-aligned primary
   "novo" action) above the table — proven by Test 1/2/3's `entity-header`/`entity-description`
   assertions across all 3 capability classes.
2. While loading, the screen shows a `Skeleton` shaped like the real table rows, not plain text —
   proven by Test 5's CDP-throttled observation of `entity-loading` and the absence of the old
   placeholder text.
3. Zero-record entities show an icon/title/description/CTA `Empty` composition reusing the create
   action — proven by Test 4's sibling-to-`<Table>` structural check and its CTA-opens-the-same-Dialog
   behavioral check.
4. The table renders inside a bounded card/border container across all 9 entities — proven by Test
   6's full nav-loop `entity-table-frame` check.

All 4 also hold with zero regression to the pre-existing 44-test suite, proven by Task 2's full-suite
run.
</success_criteria>

<output>
Create `.planning/phases/14-entity-screen-header-loading-empty-states/14-02-SUMMARY.md` when done
</output>
