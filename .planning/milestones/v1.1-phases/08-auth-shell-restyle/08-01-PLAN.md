---
phase: 08-auth-shell-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/auth/LoginScreen.svelte
  - web/src/lib/Shell.svelte
  - web/src/lib/components/ui/button/**
  - web/src/lib/components/ui/input/**
  - web/src/lib/components/ui/label/**
  - web/src/lib/components/ui/card/**
  - web/src/lib/components/ui/alert/**
  - web/package.json
  - web/bun.lock
  - web/e2e/login-flow.spec.ts
  - web/e2e/shell-nav.spec.ts
  - web/playwright.config.ts
autonomous: true
requirements: [AUTHUI-01, AUTHUI-02, SHELLUI-01, SHELLUI-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A real magic-code login (email step, then code step) completes end-to-end driving shadcn Input/Label/Button/Card markup at each step, against the live InstantDB app."
    - "The login flow's loading, error, and success states are each visually distinguishable via distinct shadcn primitives (disabled+spinner Button, destructive Alert, app-shell reveal) — no bespoke CSS."
    - "After login, Shell renders the 9-entity nav using shadcn Button components in a flex layout (no dashboard/panel), and clicking each entry navigates to the corresponding EntityScreen."
    - "Exactly one nav entry shows an active-state indicator (DOM aria-current + shadcn variant) matching the current selection at all times."
    - "Clicking Logout (a shadcn Button) ends the live session and returns to the restyled LoginScreen."
  artifacts:
    - web/src/lib/components/ui/button/
    - web/src/lib/components/ui/input/
    - web/src/lib/components/ui/label/
    - web/src/lib/components/ui/card/
    - web/src/lib/components/ui/alert/
    - web/src/lib/auth/LoginScreen.svelte
    - web/src/lib/Shell.svelte
    - web/e2e/login-flow.spec.ts
    - web/e2e/shell-nav.spec.ts
  key_links:
    - "Both submit Buttons in LoginScreen.svelte carry an explicit type=\"submit\" (shadcn-svelte Button.svelte defaults type to \"button\") or the form's onsubmit never fires."
    - "data-testid=\"login-submit\" is reused identically on both the email-step and code-step Button — auth.setup.ts clicks it twice expecting the same testid."
    - "Shell.svelte's nav Button variant (secondary/ghost) stays wired to the ativo $state and aria-current, driven by the same onclick handler as before."
    - "data-testid=\"app-shell\" stays in App.svelte's wrapper div — untouched, out of this plan's file scope."
---

<objective>
Rebuild `LoginScreen.svelte` (magic-code email + code entry) and `Shell.svelte` (entity nav + logout)
on shadcn-svelte primitives (`Button`, `Input`, `Label`, `Card`, `Alert`), with the existing two-step
auth flow and nav/logout behavior functionally unchanged, per AUTHUI-01, AUTHUI-02, SHELLUI-01,
SHELLUI-02 (REQUIREMENTS.md) and PROJECT.md C-11 (shadcn-svelte defaults only)/C-12 (zero human
UAT, every `<verify>` automated)/C-08 (`bun`/`bunx` only).

Purpose: These are the two authenticated-shell-adjacent screens every user sees before touching any
entity — the visual restyle must be provably indistinguishable in behavior from the current
plain-CSS version, proven live against the real InstantDB app (real magic-code round trip), not
mocked.

Output: 5 shadcn-svelte component groups installed under `web/src/lib/components/ui/`, both target
screens restyled in place, and two new Playwright specs (`login-flow.spec.ts`, `shell-nav.spec.ts`)
proving every ROADMAP Phase 8 success criterion live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auth-shell-restyle/08-RESEARCH.md
@.planning/phases/08-auth-shell-restyle/08-PATTERNS.md

# Current files to restyle in place — read before editing
@web/src/lib/auth/LoginScreen.svelte
@web/src/lib/Shell.svelte

# Read-only reference — do NOT modify; app-shell testid lives here, out of scope
@web/src/App.svelte

# Existing test contracts this plan must keep passing / extend, not reinvent
@web/e2e/auth.setup.ts
@web/e2e/helpers/magic-code.ts
@web/playwright.config.ts
@web/components.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Install shadcn primitives and restyle LoginScreen.svelte end-to-end</name>
  <precondition>powershell.exe resolves the Outlook Classic COM bridge (`orules.ps1 peek`, the C-10 path) from this shell — required for the live magic-code round trip this task's &lt;verify&gt; depends on; confirmed reachable in 08-RESEARCH.md's Environment Availability table this session.</precondition>
  <files>web/src/lib/auth/LoginScreen.svelte, web/src/lib/components/ui/button/**, web/src/lib/components/ui/input/**, web/src/lib/components/ui/label/**, web/src/lib/components/ui/card/**, web/src/lib/components/ui/alert/**, web/package.json, web/bun.lock</files>
  <action>
Per C-08 (bun/bunx only), from `web/` run `bunx shadcn-svelte@latest add button input label card alert -y`
to generate the five component groups into `src/lib/components/ui/{button,input,label,card,alert}/`
from the already-initialized `nova`/`neutral` registry (C-11, Phase 7's `components.json`). This
pulls `bits-ui` as a fresh dependency — per 08-RESEARCH.md's Package Legitimacy Audit, verdict OK
(941,914 weekly downloads, github.com/huntabyte/bits-ui, no postinstall script): no [ASSUMED]/[SUS]
disposition on any package this task actually installs, so no blocking human checkpoint is required.
`tailwind-variants`/`clsx`/`tailwind-merge`/`@lucide/svelte` are already present from Phase 7. If the
CLI still prompts interactively for an overwrite confirmation despite `-y` (Phase 7's
`07-01-SUMMARY.md` precedent with `init`), pipe `y` via stdin instead. Confirm `web/package.json`
gained a `bits-ui` entry after install.

Then restyle `web/src/lib/auth/LoginScreen.svelte`'s markup only, satisfying AUTHUI-01/AUTHUI-02 —
the `<script>` block (lines 1-51: `step`/`email`/`code`/`erro`/`ocupado` state,
`enviarCodigo`/`verificarCodigo`/`onSubmitEmail`/`onSubmitCode`/`reenviar`) stays byte-identical, this
is a pure visual restyle. Wrap the existing outer `<div data-testid="login-screen">`'s two-step form
in `Card`/`CardContent` from `$lib/components/ui/card` — per ROADMAP Phase 8 success criterion 1,
`Card` must be present at EACH step, not only on error, so it wraps the whole form, not just the
error branch (08-RESEARCH.md's Alternatives Considered table justifies this reading of AUTHUI-01's
literal text). Replace each `<label>`/`<input>` pair with `Label` (`$lib/components/ui/label`) and
`Input` (`$lib/components/ui/input`), forwarding `id`, `data-testid` (`login-email`/`login-code`),
`bind:value`, `required`, and `disabled={ocupado}` unchanged. Replace both submit `<button>`s and the
resend `<button>` with `Button` (`$lib/components/ui/button`): CRITICAL — shadcn-svelte's generated
`Button.svelte` defaults `type` to `"button"`, not `"submit"` (08-RESEARCH.md Pitfall 1); both submit
`Button`s (email-step and code-step) MUST carry an explicit `type="submit"` prop or the
`<form onsubmit=...>` handler silently stops firing with zero console error. Keep the literal string
`data-testid="login-submit"` on BOTH submit `Button` instances — do not split into two different
testids; `auth.setup.ts` clicks `login-submit` twice (once per step) expecting the same testid each
time (08-RESEARCH.md Pitfall 2). The resend `Button` keeps `type="button"` and
`data-testid="login-resend"`. Add a `LoaderCircle` icon (`@lucide/svelte/icons/loader-circle`,
`class="size-4 animate-spin"`) as a conditional child of each submit `Button`, rendered only while
`ocupado` is true — giving AUTHUI-02's loading state its own distinct shadcn-composed visual (disabled
Button + spinner). Replace the error `<p data-testid="login-error">` with `Alert variant="destructive"`
(`$lib/components/ui/alert`) containing a `CircleAlert` icon (`@lucide/svelte/icons/circle-alert`) and
an `AlertDescription` carrying `data-testid="login-error"` verbatim around `{erro}` — do not change the
error-text source (the existing `err.body?.message` fallback), only its container. Do not touch
`web/src/App.svelte` or add a second `app-shell` testid anywhere in this file — that testid lives
exclusively in `App.svelte`'s wrapper div, out of this task's file scope.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/auth.setup.ts --project=setup</automated>
  </verify>
  <done>The real magic-code login (email step, then code step) completes end-to-end through the newly-generated shadcn Input/Label/Button/Card markup; `auth.setup.ts` passes unmodified — `app-shell` becomes visible and contains the authenticated email, `login-screen` becomes not visible — satisfying ROADMAP Phase 8 success criterion 1.</done>
</task>

<task type="auto">
  <name>Task 2: Prove AUTHUI-02's loading and error states with a new live-round-trip spec</name>
  <files>web/e2e/login-flow.spec.ts, web/playwright.config.ts</files>
  <action>
Add `web/e2e/login-flow.spec.ts` to the `anon` Playwright project (08-VALIDATION.md's Wave 0) by
editing `web/playwright.config.ts`: broaden the `anon` project's `testMatch` regex from
`/no-leakage\.spec\.ts|design-system\.spec\.ts/` to
`/no-leakage\.spec\.ts|design-system\.spec\.ts|login-flow\.spec\.ts/`, and broaden `authed`'s
`testIgnore` regex identically, so the new spec runs exactly once (under `anon`, never `authed`) — the
same additive-edit pattern Phase 7 used for `design-system.spec.ts`.

Write two tests in the new file, reusing `readLatestMagicCode`/`readMagicCodeAfter` from
`web/e2e/helpers/magic-code.ts` exactly as `auth.setup.ts` does (do not reimplement the Outlook peek
logic). Give each test a generous `test.setTimeout` (e.g. 90000ms, mirroring `auth.setup.ts`'s
precedent) for the real network + inbox-read round trip.

Test 1, "submit Button shows disabled + spinner while the send request is in flight": fill
`login-email`, click `login-submit`, and immediately assert `login-submit` is disabled and contains a
`.animate-spin` child element — a real transient state racing the live `db.auth.sendMagicCode`
network call, not a mock; add no artificial delay.

Test 2, "submitting a deliberately wrong code renders the destructive Alert": request a real magic
code (same live send + `readMagicCodeAfter` poll as Test 1/`auth.setup.ts`), then fill `login-code`
with an intentionally incorrect 6-digit value (e.g. increment the real code's last digit, wrapping
9→0) instead of the real code, click `login-submit`, then assert `login-error` becomes visible inside
`Alert[variant=destructive]`-styled markup (locate the `Alert` root via its shadcn-generated
`data-slot="alert"` attribute and confirm the destructive-variant class is present) and contains the
real backend's rejection text — a genuine live InstantDB rejection, not a mock, avoiding the ~60-90s
natural-expiry wait (08-RESEARCH.md Pitfall 3). Success state needs no new test here — it is already
covered by Task 1's `auth.setup.ts` run.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/login-flow.spec.ts --project=anon</automated>
  </verify>
  <done>Both new tests pass live against InstantDB: the loading-state assertion catches the disabled+spinner Button, and the error-state assertion catches a real live-backend rejection rendered via `Alert[variant=destructive]` containing `login-error` — satisfying AUTHUI-02 and ROADMAP Phase 8 success criterion 2's loading/error halves.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle Shell.svelte's nav/logout and prove SHELLUI-01/02 with a new authed-project spec</name>
  <files>web/src/lib/Shell.svelte, web/e2e/shell-nav.spec.ts</files>
  <action>
Restyle `web/src/lib/Shell.svelte`'s markup only, satisfying SHELLUI-01/SHELLUI-02 — the `<script>`
block (lines 1-44: `auth`, `ativo` state, the deliberately non-reactive `jobStarted`/`jobState`
job-tracking, and the `onMount` routine-job logic) and the hidden
`<div data-testid="routine-job-state">` sentinel stay byte-identical. Do NOT refactor `onMount` to
`$effect` (the file's own comment on lines 12-18 documents why this must stay `onMount`-based) and do
NOT touch `web/src/App.svelte`. Replace the logout `<button>` with `Button`
(`$lib/components/ui/button`, `variant="outline"`, `type="button"`), keeping `data-testid="logout"`
and the `onclick={() => db.auth.signOut()}` handler byte-identical. Wrap `<nav>` in
`class="flex gap-2"` (Tailwind flex layout per SHELLUI-01 — no dashboard/panel grid) and replace each
nav `<button>` with `Button` (`type="button"`), toggling
`variant={ativo === cfg.etype ? "secondary" : "ghost"}` per SHELLUI-02 (08-RESEARCH.md Pattern 2,
verified against the live `nova` `button.json` registry — `secondary` vs `ghost` gives a
resting-state background-color difference assertable via `getComputedStyle`, unlike a `default`-vs-
`ghost` pairing). Keep `data-testid={`nav-${cfg.etype}`}`, `aria-current={ativo === cfg.etype}`, and
`onclick={() => (ativo = cfg.etype)}` byte-identical. Leave the `{#key ativo}...{/key}` block (and the
`EntityScreen` it mounts) completely untouched — no `Card` wrapper here; SHELLUI-01's literal text
names only `Button` + flex/grid layout, and Phase 9/10 own entity-screen visual design (08-RESEARCH.md
Open Question 1).

Create `web/e2e/shell-nav.spec.ts` — no `playwright.config.ts` edit needed, it is picked up
automatically by the `authed` project's existing `testMatch: /.*\.spec\.ts/` (which auto-runs the
`setup` project first, per its `dependencies: ["setup"]`, producing a fresh live session). Declare
three tests in this order (single worker, sequential — `playwright.config.ts`'s
`workers: 1`/`fullyParallel: false` — so ending the session in the last test cannot affect the earlier
two):

1. "each nav Button renders its corresponding EntityScreen" — locate all `[data-testid^="nav-"]`
   elements (expect count 9, matching the 9 `defs/*.ts` entity configs); for each one in turn, read its
   own visible text (equal to that entity's `titulo`), click it, and assert `page.locator("h2")` now has
   that exact text. `EntityScreen.svelte`'s `<h2>{config.titulo}</h2>` is untouched by this phase and is
   a reliable per-entity signal. Do not import `web/src/lib/entities/registry.ts` into the spec — it
   uses `import.meta.glob`, a Vite-only construct unavailable in Playwright's plain Node/TS runtime.

2. "exactly one nav Button shows the active-state indicator at a time" — inside the same loop as test
   1 (or a second pass over the same nav), after each click assert `page.locator('[aria-current="true"]')`
   has count exactly 1 and its `data-testid` matches the just-clicked entry, and assert its computed
   `backgroundColor` differs from another (non-active) nav Button's computed `backgroundColor` —
   covering both the DOM (`aria-current`) and the visual (shadcn variant) halves of SHELLUI-02. Note
   `ativo` defaults to `entityConfigs[0].etype` on mount, so the first entry is already active before
   any click.

3. "clicking Logout ends the session and returns to the restyled LoginScreen" — declared LAST in the
   file — click `logout`, then assert `app-shell` is not visible and `login-screen` is visible.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/shell-nav.spec.ts --project=authed</automated>
  </verify>
  <done>All 9 nav Buttons navigate to their corresponding EntityScreen (h2 match), exactly one active-state indicator matches the current selection at every step (DOM + visual), and Logout ends the live session and returns to `login-screen` — all proven live against InstantDB, satisfying ROADMAP Phase 8 success criteria 3, 4, and 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (client) → InstantDB hosted backend | `db.auth.sendMagicCode`/`signInWithMagicCode`/`signOut` calls cross this boundary; unchanged by this phase — only the UI wrapping these existing calls changes |
| Local dev/build tooling → npm registry (via `bunx shadcn-svelte add`) | New `bits-ui` package pulled from the public npm registry into the build this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | LoginScreen `Alert`/`login-error` | low | accept | Rendered error text is unchanged from current behavior (`err.body?.message` fallback to a hardcoded PT-BR string) — this phase only restyles the container (`Alert`), never the content source; no new backend detail is exposed. |
| T-08-02 | Tampering | LoginScreen/Shell markup interpolation (`{erro}`, `{email}`, `{auth.user.email}`) | low | accept | Svelte's default text interpolation auto-escapes; no `{@html}` is introduced in either restyled file; unchanged from pre-restyle behavior. |
| T-08-03 | Elevation of Privilege | `instant.perms.ts` / auth boundary | low | accept | Not touched this phase — V4 access control is out of scope; only markup wrapping the existing `auth.*` calls changes. |
| T-08-SC | Tampering | `bun`/npm package install (`bits-ui`, via `shadcn-svelte add`) | high | mitigate | 08-RESEARCH.md's Package Legitimacy Audit already rated `bits-ui` "OK" (941,914 weekly downloads, `github.com/huntabyte/bits-ui`, no postinstall script) — no `[ASSUMED]`/`[SUS]` verdict on any package this phase's install task actually pulls, so no blocking-human checkpoint is required (also consistent with C-12's zero-human-UAT constraint). Task 1 confirms the installed `bits-ui` version in `web/package.json` matches the audited entry before committing. |
</threat_model>

<verification>
Full-suite regression (08-VALIDATION.md's phase-gate sampling): `cd web && bun run test:e2e` (runs
`setup` → `authed` → `anon`, all live against InstantDB) must be green, including the pre-existing
`auth.spec.ts`, `entities-*.spec.ts`, `routine-job*.spec.ts`, `no-leakage.spec.ts`, and
`design-system.spec.ts` alongside this plan's two new specs. Then `cd web && bun run check && bun run lint`
must both exit 0 (Biome + svelte-check — C-08/QUAL-01 baseline, zero new suppressions). Every
`data-testid` documented in 08-PATTERNS.md (`login-screen`, `login-email`, `login-submit`, `login-code`,
`login-error`, `login-resend`, `app-shell` [in `App.svelte`, untouched], `logout`, `nav-${etype}`,
`routine-job-state`) must resolve to the same semantics as before the restyle.
</verification>

<success_criteria>
Maps 1:1 to ROADMAP.md Phase 8's 5 success criteria:

1. A real magic-code login (email step, then code step) completes end-to-end against the live
   InstantDB app driving shadcn `Input`/`Label`/`Button`/`Card` markup at each step — proven by Task 1's
   `auth.setup.ts` run.
2. Loading, error, and success states are each visually distinguishable via distinct shadcn
   primitives/states, including a real induced error — proven by Task 1 (success) + Task 2 (loading,
   error).
3. `Shell.svelte` renders the entity nav using shadcn `Button` components in a flex layout (no
   dashboard/panel); clicking each of the 9 nav entries navigates to the corresponding `EntityScreen` —
   proven by Task 3.
4. Exactly one active-state indicator matches the current route as navigation changes — proven by
   Task 3.
5. Clicking Logout (a shadcn `Button`) ends the live session and returns to the restyled LoginScreen —
   proven by Task 3.
</success_criteria>

<output>
Create `.planning/phases/08-auth-shell-restyle/08-01-SUMMARY.md` when done
</output>
