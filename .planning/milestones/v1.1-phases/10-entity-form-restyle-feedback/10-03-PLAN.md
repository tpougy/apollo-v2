---
phase: 10-entity-form-restyle-feedback
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - web/src/lib/components/ui/sonner/sonner.svelte
  - web/src/lib/components/ui/sonner/index.ts
  - web/src/App.svelte
  - web/src/lib/entities/EntityScreen.svelte
  - web/src/lib/auth/LoginScreen.svelte
  - web/src/lib/Shell.svelte
  - web/e2e/entities-form-restyle.spec.ts
  - web/e2e/login-flow.spec.ts
  - web/e2e/shell-nav.spec.ts
  - web/package.json
  - web/bun.lock
autonomous: true
requirements: [FDBK-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "svelte-sonner is installed and mounted once in App.svelte, with theme=system and zero dependency on the mode-watcher package this project removed in Phase 7"
    - "Every entity CRUD write (create/edit/delete) and both auth actions (login success/error, logout) produce a visible Sonner toast whose content matches the outcome"
    - "Toast text always reuses the already-sanitized formError/extractErrorMessage() value — never a raw exception"
  artifacts:
    - web/src/lib/components/ui/sonner/sonner.svelte
    - web/src/lib/components/ui/sonner/index.ts
    - web/src/App.svelte (Toaster mounted outside SignedIn/SignedOut)
    - web/e2e/entities-form-restyle.spec.ts (toast assertions for fundos + instanciasRotina)
    - web/e2e/login-flow.spec.ts (2 new toast assertions)
    - web/e2e/shell-nav.spec.ts (logout toast assertion)
  key_links:
    - "handleSubmit/handleDelete's existing try/catch and formError assignments <-> new toast.success()/toast.error() calls, additive only, no control-flow change"
    - "db.auth.sendMagicCode/signInWithMagicCode/signOut (unchanged calls) <-> new toast.success()/toast.error() reactions in LoginScreen.svelte/Shell.svelte"
---

<objective>
Install `svelte-sonner` by hand (deliberately bypassing the stock `shadcn-svelte add sonner` registry entry, which pulls in `mode-watcher` and would reintroduce the exact dark-mode toggle machinery Phase 7 removed to satisfy C-11), mount a `<Toaster>` once at the app root, and wire `toast.success()`/`toast.error()` calls into every write path this phase's scope covers: entity create/edit/delete (`EntityScreen.svelte`) and both auth actions — login (`LoginScreen.svelte`) and logout (`Shell.svelte`, per ROADMAP Phase 10 SC5's literal "logging in/out" text).

Purpose: Deliver FDBK-01 — the last requirement this phase owns — without reopening any business logic (10-01/10-02 already converted every field-kind widget; this plan only adds UI-feedback side effects alongside existing success/error branches).
Output: A hand-written, `mode-watcher`-free Sonner wrapper; toast calls at every CRUD/auth write site; toast assertions added to `entities-form-restyle.spec.ts` (fundos full-CRUD + instanciasRotina status-only edit — two distinct capability classes per FDBK-01/SC5), `login-flow.spec.ts`, and `shell-nav.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-entity-form-restyle-feedback/10-RESEARCH.md
@.planning/phases/10-entity-form-restyle-feedback/10-01-SUMMARY.md
@.planning/phases/10-entity-form-restyle-feedback/10-02-SUMMARY.md
@web/src/App.svelte
@web/src/lib/entities/EntityScreen.svelte
@web/src/lib/auth/LoginScreen.svelte
@web/src/lib/Shell.svelte
@web/e2e/login-flow.spec.ts
@web/e2e/shell-nav.spec.ts
@web/e2e/fixtures/instancia-admin-fixture.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hand-install svelte-sonner (no mode-watcher); mount Toaster in App.svelte</name>
  <files>web/src/lib/components/ui/sonner/sonner.svelte, web/src/lib/components/ui/sonner/index.ts, web/src/App.svelte, web/package.json, web/bun.lock</files>
  <action>
    From `web/`, run `bun add svelte-sonner` directly — do NOT run `bunx shadcn-svelte@latest add sonner`, whose registry entry declares a devDependency this project's Phase 7 dark-mode conversion is incompatible with and would silently reintroduce (see 10-RESEARCH.md's "Explicitly NOT installed" table and Pitfall 1 for the full rationale).
    <!-- planner-discipline-allow: mode-watcher -->
    Create `web/src/lib/components/ui/sonner/sonner.svelte` by hand: import `Toaster as Sonner` and `type ToasterProps as SonnerProps` from `svelte-sonner`, destructure `let { ...restProps }: SonnerProps = $props();`, and render `<Sonner theme="system" class="toaster group" style="--normal-bg: var(--color-popover); --normal-text: var(--color-popover-foreground); --normal-border: var(--color-border);" {...restProps} />` — this file must contain no import from the package this task deliberately avoided installing (10-RESEARCH.md Pitfall 1's exact code block). Create `web/src/lib/components/ui/sonner/index.ts` re-exporting `{ default as Toaster }` from `./sonner.svelte`.
    Mount `<Toaster />` once in `web/src/App.svelte`, outside both `<SignedOut>` and `<SignedIn>` (so both `LoginScreen.svelte` and every authenticated write path can toast), importing it from `$lib/components/ui/sonner`.
  </action>
  <verify>
    <automated>cd .. && ! grep -qi "mode-watcher" web/package.json && grep -q "svelte-sonner" web/package.json && cd web && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>`web/package.json`/`bun.lock` gained exactly one new runtime dependency (`svelte-sonner`) and zero occurrences of the package this task deliberately avoided; `App.svelte` mounts `<Toaster />` unconditionally; `bun run build` succeeds with zero errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire toast calls into entity CRUD (handleSubmit/handleDelete); extend the new spec's fundos + instanciasRotina coverage</name>
  <files>web/src/lib/entities/EntityScreen.svelte, web/e2e/entities-form-restyle.spec.ts</files>
  <action>
    In `EntityScreen.svelte`'s `handleSubmit`, capture the pre-reset mode into a local const (e.g. `const wasCreate = mode === "create";`) immediately before the existing `mode = null; editingId = null;` success reset, then call `toast.success(wasCreate ? "Registro criado." : "Registro atualizado.")` right after — capturing before the reset matters because the ternary would otherwise read `mode` after it has already been nulled. Add `toast.error(formError)` immediately after every existing `formError = ...` assignment in `handleSubmit` (the xorLink required-check, the per-link required-check, the per-field required-check, the `parent_not_found` pre-check, and the catch block) so every path that blocks or fails a write also surfaces a toast, per FDBK-01's "every write" scope — this is additive only, no assignment values or control flow change. In `handleDelete`, add `toast.success("Registro excluído.")` immediately after the successful `db.transact(...)` call and `toast.error(formError)` in its catch block, mirroring the same pattern. Import `{ toast } from "svelte-sonner"` at the top of the file.

    Extend `entities-form-restyle.spec.ts`'s existing fundos test (from 10-01) with toast assertions: after the create-submit, assert `page.locator('[data-sonner-toast][data-type="success"]')` becomes visible; after the delete, assert a second success toast appears. Extend 10-02's validation-error test with an additional assertion that `page.locator('[data-sonner-toast][data-type="error"]')` also becomes visible for the same missing-required-field submission (per 10-RESEARCH.md's confirmed `data-sonner-toast`/`data-type`/`data-title` DOM attributes).

    Add a new `instanciasRotina` test to `entities-form-restyle.spec.ts`, seeding one instance via `seedInstance` from `./fixtures/instancia-admin-fixture.ts` (mirrors `entities-table-restyle.spec.ts`'s own precedent for this create-forbidden entity): open its row's edit action, assert the Dialog contains exactly one field control (`field-status`, a plain `Input` since this field is `kind: "text"`) and no create-affordance exists anywhere on the screen; change the status value, submit, assert a success toast appears and the new value persists on reload; tear down via `deleteInstance`. This is the "status-only" capability class FDBK-01/ROADMAP SC5 requires alongside fundos' full-CRUD class.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-form-restyle.spec.ts --project=authed</automated>
  </verify>
  <done>Creating, editing, and deleting a fundo each produce a visible success toast; the validation-error path also produces an error toast alongside its Alert; the `instanciasRotina` restricted edit produces a success toast on its single `status` field (ROADMAP Phase 10 SC5, two capability classes proven).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire toast calls into LoginScreen/Shell (both auth actions); extend login-flow.spec.ts and shell-nav.spec.ts</name>
  <files>web/src/lib/auth/LoginScreen.svelte, web/src/lib/Shell.svelte, web/e2e/login-flow.spec.ts, web/e2e/shell-nav.spec.ts</files>
  <action>
    In `LoginScreen.svelte`, import `{ toast } from "svelte-sonner"`. In `enviarCodigo()`, add `toast.success("Código enviado.")` immediately after the existing `step = "code";` line, and `toast.error(erro)` immediately after the existing `erro = ...` assignment in its catch block. In `verificarCodigo()`, add `toast.success("Login realizado.")` immediately after the existing `await db.auth.signInWithMagicCode(...)` call succeeds, and `toast.error(erro)` immediately after the existing `erro = ...` assignment in its catch block. No change to `ocupado`/`step`/`erro` state or the two functions' control flow.

    In `Shell.svelte`, import `{ toast } from "svelte-sonner"` and change the Logout Button's `onclick` from the bare `() => db.auth.signOut()` to a small inline function that calls `db.auth.signOut()` then `toast.success("Você saiu.")` — satisfies ROADMAP Phase 10 SC5's literal "logging in/out" wording (login's two toasts already covered above).

    Extend `login-flow.spec.ts`'s existing loading-state test with an assertion that `page.locator('[data-sonner-toast][data-type="success"]')` becomes visible once the send completes (step flips to "code"). Extend its existing wrong-code test with an assertion that `page.locator('[data-sonner-toast][data-type="error"]')` becomes visible alongside the existing `login-error` Alert assertion — these are the "two new toast assertions" this phase's scope requires on the pre-existing auth spec. Extend `shell-nav.spec.ts`'s existing "clicking Logout" test with an assertion that a success toast is visible before/as the screen transitions back to `login-screen` (assert the toast before the navigation-away assertion, since the toast and the auth-state flip can race).
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/login-flow.spec.ts --project=anon && bunx playwright test e2e/shell-nav.spec.ts --project=authed</automated>
  </verify>
  <done>Sending a magic code, submitting a deliberately wrong code, and clicking Logout each produce a visible Sonner toast whose type (success/error) matches the outcome — proven live via `login-flow.spec.ts`'s real magic-code round trip and `shell-nav.spec.ts`'s logout test (ROADMAP Phase 10 SC5's auth half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `db.transact()` / `db.auth.*` resolve/reject (unchanged calls) → new toast UI reaction | Purely a client-side UI side effect reacting to an already-resolved/rejected Promise — no new network call, no new backend involvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Information Disclosure | `toast.error(formError)` / `toast.error(erro)` echo error text into a new, more prominent UI surface (toast, `aria-live` region) | low | mitigate | Reuses the exact same already-sanitized `formError`/`erro`/`extractErrorMessage()` value the existing `Alert` already displays — never a raw exception object; no new leakage surface versus the pre-existing Alert-only display, per 10-RESEARCH.md's Security Domain |
| T-10-07 | Tampering (supply chain) | New runtime dependency `svelte-sonner` installed via `bun add`; hand-written wrapper deliberately omits the registry's own additional devDependency | low | mitigate | `svelte-sonner` audited OK in 10-RESEARCH.md's Package Legitimacy Audit (no postinstall script, recognized maintainer, high weekly downloads) — not `[ASSUMED]`/`[SUS]`, no human checkpoint required; the omitted package is deliberately never installed (Task 1's own grep check), preserving C-11's "no toggle" dark-mode requirement |
| T-10-SC | Tampering (supply chain) | Package legitimacy gate for this plan's one new npm install | low | mitigate | `svelte-sonner` verdict: OK (per 10-RESEARCH.md Package Legitimacy Audit, re-confirmed this session) — no blocking human checkpoint required |

</threat_model>

<verification>
`cd web && bun run test:e2e -- --grep "toast|Toast|Sonner"` is not a real filter this suite supports by tag — instead run the three targeted commands from each task above, then the full `bun run test:e2e` once at the end of this plan to confirm no regression in any spec this phase has touched so far (10-01/10-02/10-03's combined surface).
</verification>

<success_criteria>
- `svelte-sonner` is installed and the hand-written `Toaster` wrapper mounts with `theme="system"`, with zero dependency on the package Phase 7 removed.
- Creating, editing, deleting a `fundos` record and editing an `instanciasRotina` record each produce a visible, outcome-matching toast (ROADMAP Phase 10 SC5, entity side).
- Logging in (success + error) and logging out each produce a visible, outcome-matching toast (ROADMAP Phase 10 SC5, auth side).
</success_criteria>

<output>
Create `.planning/phases/10-entity-form-restyle-feedback/10-03-SUMMARY.md` when done
</output>
