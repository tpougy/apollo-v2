---
phase: 10-entity-form-restyle-feedback
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - web/e2e/entities-rotina-log.spec.ts
  - web/e2e/entities-projeto-etapa-tarefa.spec.ts
  - web/e2e/entities-ticket-subtarefa.spec.ts
autonomous: true
requirements: [ENTFRM-02, ENTFRM-03]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "entities-rotina-log.spec.ts, entities-projeto-etapa-tarefa.spec.ts, and entities-ticket-subtarefa.spec.ts all pass green against the fully restyled Dialog/Select/date-picker markup, with behavior/assertions unchanged beyond the interaction-strategy swap"
    - "Every .selectOption()/.locator('option') call site in these three files is converted to the shared role-based helpers from 10-01/10-02, not hand-rolled per call site"
    - "Every date-fill call site in these three files is converted to pickDate, with any downstream literal-date-string assertion updated to the dynamically-picked value"
  artifacts:
    - web/e2e/entities-rotina-log.spec.ts (updated)
    - web/e2e/entities-projeto-etapa-tarefa.spec.ts (updated)
    - web/e2e/entities-ticket-subtarefa.spec.ts (updated)
  key_links:
    - "web/e2e/helpers/form-controls.ts's pickDate/selectByText/openAndReadSelectOptions (built in 10-01/10-02) <-> every converted call site in these three files"
---

<objective>
Convert the remaining `.selectOption()`/`.locator("option")`/date-`.fill()` call sites in the three pre-existing e2e spec files this phase's Select and date-picker conversions break — `entities-rotina-log.spec.ts`, `entities-projeto-etapa-tarefa.spec.ts`, `entities-ticket-subtarefa.spec.ts` — to the shared role-based helpers built in 10-01/10-02 (`pickDate`, `selectByText`, `openAndReadSelectOptions`). `entities-fundos.spec.ts` was already fixed in 10-01 (its only breakage was the one date-fill call site); these three are the remainder, per 10-RESEARCH.md Pitfall 2.

Purpose: Leaving these three files red between this phase and Phase 11 would make every subsequent full-suite run indistinguishable from a real regression — 10-RESEARCH.md explicitly recommends fixing them within this phase's own plan rather than deferring to Phase 11's VERIFY-01.
Output: All three files green against the restyled markup, using the shared helpers (no duplicated per-call-site logic), plus a final full-suite run confirming no regression across every spec this phase has touched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-entity-form-restyle-feedback/10-RESEARCH.md
@.planning/phases/10-entity-form-restyle-feedback/10-01-SUMMARY.md
@.planning/phases/10-entity-form-restyle-feedback/10-02-SUMMARY.md
@web/e2e/entities-rotina-log.spec.ts
@web/e2e/entities-projeto-etapa-tarefa.spec.ts
@web/e2e/entities-ticket-subtarefa.spec.ts
@web/e2e/helpers/form-controls.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix entities-rotina-log.spec.ts's Select call sites (templatesRotina only — no date fields in this file)</name>
  <files>web/e2e/entities-rotina-log.spec.ts</files>
  <action>
    Import `openAndReadSelectOptions`/`selectByText` from `./helpers/form-controls.ts`. Convert the `tipoGeracaoSelect.locator("option").evaluateAll(...)` enumeration (lines ~123-127) to `const optionValues = await openAndReadSelectOptions(page, "field-tipoGeracao");` — drop the `.filter((v) => v !== "")` (the static-option Select renders no empty placeholder item, per 10-RESEARCH.md Pattern 3), keep the same `expect(optionValues.sort()).toEqual([...])` assertion. Convert `tipoGeracaoSelect.selectOption("du_fixo")` (line ~132) to `await selectByText(page, "field-tipoGeracao", "du_fixo");`, and the same pattern for the `"encadeado"` selection (line ~150). Convert `page.getByTestId("link-antecessor").selectOption({ label: nomeA })` (line ~152) to `await selectByText(page, "link-antecessor", nomeA);`. Convert the antecessor self-exclusion check's `.locator("option").allTextContents()` (lines ~165-168) to `const antecessorOptionLabels = await openAndReadSelectOptions(page, "link-antecessor");` (the empty `"—"` placeholder item this one DOES render is harmless — the assertion only checks `.not.toContain(nomeB)`). Leave every other line (checkbox interactions, CLI assertions, row/cell text assertions) untouched — this file has no date fields.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-rotina-log.spec.ts --project=authed</automated>
  </verify>
  <done>`entities-rotina-log.spec.ts` passes green against the restyled Select markup; every `.selectOption()`/`.locator("option")` call site now routes through the shared helpers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix entities-projeto-etapa-tarefa.spec.ts's date-fill and Select call sites</name>
  <files>web/e2e/entities-projeto-etapa-tarefa.spec.ts</files>
  <action>
    Import `pickDate`/`selectByText`/`openAndReadSelectOptions` from `./helpers/form-controls.ts`. Convert `page.getByTestId("field-dataInicioPrevista").fill("2026-03-10")` (line ~186) to `const dataInicioPrevistaValue = await pickDate(page, "field-dataInicioPrevista");`, and update the downstream literal-string assertion `await expect(reloadedRowA).toContainText("2026-03-10", { timeout: RESYNC_TIMEOUT });` (line ~193) to assert `dataInicioPrevistaValue` instead of the hardcoded string. Convert `page.getByTestId("field-dataPrevistaEstimada").fill("2026-04-01")` (line ~303) to `await pickDate(page, "field-dataPrevistaEstimada");` — no downstream literal-string assertion exists for this one field, confirmed by grep. Convert every `.selectOption({ label: ... })` call on `link-fundo`/`link-projeto`/`link-etapa` (lines ~200, 237, 304, 362) to `selectByText(page, testid, label)`. Convert the `tipoPrazoSelect.locator("option").evaluateAll(...)` enumeration (lines ~291-296) to `openAndReadSelectOptions(page, "field-tipoPrazo")` (drop the empty-value filter, same rationale as Task 1), and `tipoPrazoSelect.selectOption("hard")` (line ~301) to `selectByText(page, "field-tipoPrazo", "hard")`. Leave every other line (number-field fills, text-field fills, checkbox interactions, delete-confirm dialog handling) untouched.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-projeto-etapa-tarefa.spec.ts --project=authed</automated>
  </verify>
  <done>`entities-projeto-etapa-tarefa.spec.ts` passes green against the restyled Select and date-picker markup; the `dataInicioPrevista` persistence assertion checks the dynamically-picked value, not a hardcoded literal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix entities-ticket-subtarefa.spec.ts's date-fill and Select/xorLink call sites; run the full suite</name>
  <files>web/e2e/entities-ticket-subtarefa.spec.ts</files>
  <action>
    Import `pickDate`/`selectByText`/`openAndReadSelectOptions` from `./helpers/form-controls.ts`. Convert `page.getByTestId("field-dataRecebimento").fill("2026-02-01")` (line ~186) to `await pickDate(page, "field-dataRecebimento");` — no downstream literal-string assertion exists for this field, confirmed by grep. Convert the `tipoPrazoSelect.locator("option").evaluateAll(...)` enumeration (lines ~175-180) to `openAndReadSelectOptions(page, "field-tipoPrazo")` and `tipoPrazoSelect.selectOption("hard")` (line ~187) to `selectByText(page, "field-tipoPrazo", "hard")`. Convert every `xor-parent-type`/`link-tarefa`/`link-ticket` `.selectOption(...)` pair across this file's several subtarefa test cases (approximately lines 235-236, 273-274, 335-340, 373-374, 389-390, 428-429) to `selectByText(page, "xor-parent-type", "tarefa"|"ticket")` and `selectByText(page, "link-tarefa"|"link-ticket", chainTarefaTitulo|chainTicketTitulo)` — every one of these pairs is structurally identical, converting mechanically. Leave every other line untouched.

    After this task's own targeted run passes, run the complete suite (`bun run test:e2e`) once to confirm no regression across every spec this phase has touched: `entities-fundos.spec.ts` (10-01), `entities-form-restyle.spec.ts` (10-01/10-02/10-03), `login-flow.spec.ts`/`shell-nav.spec.ts` (10-03), and this plan's three files — a genuinely clean, full live run against InstantDB.
  </action>
  <verify>
    <automated>cd web && bunx playwright test e2e/entities-ticket-subtarefa.spec.ts --project=authed && bun run test:e2e</automated>
  </verify>
  <done>`entities-ticket-subtarefa.spec.ts` passes green against the restyled markup; the complete `web/e2e/` suite (`bun run test:e2e`) is fully green in a single run, proving this phase leaves zero red specs at its own boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test runner (Playwright, live) → InstantDB hosted app | Unchanged — these three files already exercise this boundary pre-restyle; this plan only changes the interaction strategy (role-based locators instead of native `<select>`/`<input type="date">` APIs), never the assertions' meaning or the live data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08 | Tampering | Shared e2e helper functions (`selectByText`, `pickDate`, `openAndReadSelectOptions`) click through real, live InstantDB-backed UI across three spec files | low | accept | Test-only code under `web/e2e/`, never imported by `web/src/` and therefore never bundled into the shipped app (mirrors `fixtures/instancia-admin-fixture.ts`'s own doc-comment precedent); no production trust boundary is crossed |
| T-10-SC | Tampering (supply chain) | Package legitimacy gate for this plan | low | mitigate | Zero new npm dependency — this plan only edits existing spec files and imports functions from a helper file two earlier plans already built; nothing to audit |

</threat_model>

<verification>
`cd web && bun run test:e2e` — the complete Playwright suite, all three projects (`setup`/`authed`/`anon`), fully green from a clean run against the live InstantDB app. This is the phase-boundary proof that no pre-existing spec was left red.
</verification>

<success_criteria>
- `entities-rotina-log.spec.ts`, `entities-projeto-etapa-tarefa.spec.ts`, `entities-ticket-subtarefa.spec.ts` all pass green post-restyle.
- Every `.selectOption()`/`.locator("option")`/date-`.fill()` call site broken by this phase's markup change is converted to the shared helpers, with zero duplicated per-call-site logic.
- `bun run test:e2e` is fully green at this phase's boundary (VERIFY-01's intent satisfied early, not deferred to Phase 11 with a known-red suite in between).
</success_criteria>

<output>
Create `.planning/phases/10-entity-form-restyle-feedback/10-04-SUMMARY.md` when done
</output>
