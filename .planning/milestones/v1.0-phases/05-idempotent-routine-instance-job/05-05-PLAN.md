---
phase: 05-idempotent-routine-instance-job
plan: 05
type: tdd
wave: 5
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - cli/apollo_cli/routine_job.py
  - cli/apollo_cli/entities/rotina.py
  - cli/tests/test_routine_job.py
  - cli/tests/test_rotina_instancia.py
autonomous: true
requirements: [JOB-02]
user_setup: []

must_haves:
  truths:
    - "cli/apollo_cli/routine_job.py returns results identical to web/src/lib/routineJob.ts for every case in shared/routine-job.testcases.json."
    - "`apollo rotina gerar-instancias` creates the expected instanciasRotina records in the live app."
    - "Running `apollo rotina gerar-instancias` twice in a row creates zero additional records and reports the second run's keys as existing."
    - "An instance manually set to 'concluida' still reads 'concluida' after a CLI job run, with dataPrevista, competencia and dedupeKey unchanged."
    - "`apollo rotina gerar-instancias` emits exactly one JSON document with created, existing and skipped keys."
    - "The CLI job authenticates with session_client() and never carries an admin token."
    - "The CLI job never deletes an instanciasRotina record."
    - statement: "Every dedupeKey the CLI computes for a given template set and date is byte-identical to the one the SPA computes for the same inputs."
      verification: explicit
  artifacts:
    - path: "cli/apollo_cli/routine_job.py"
      provides: "Python twin of the TS compute core + CLI-side orchestration"
      exports: ["compute_expected_instances", "run_routine_instance_job", "build_dedupe_key", "nth_business_day_of_month", "nth_calendar_day_of_month", "end_of_next_month", "shift_competencia"]
      min_lines: 150
    - path: "cli/apollo_cli/entities/rotina.py"
      provides: "apollo rotina gerar-instancias command"
      contains: "gerar-instancias"
    - path: "cli/tests/test_routine_job.py"
      provides: "Fixture parity suite + live CLI idempotency tests"
  key_links:
    - from: "cli/tests/test_routine_job.py"
      to: "shared/routine-job.testcases.json"
      via: "find_repo_root() / 'shared' / 'routine-job.testcases.json'"
      pattern: "routine-job.testcases.json"
    - from: "cli/apollo_cli/routine_job.py"
      to: "cli/apollo_cli/bizdays.py"
      via: "add_business_days / is_business_day"
      pattern: "from apollo_cli.bizdays import"
    - from: "apollo rotina gerar-instancias"
      to: "run_routine_instance_job"
      via: "click command"
      pattern: "run_routine_instance_job"
  prohibitions:
    - "The CLI job must never use login_client() or any admin token — it must be rejected by perms exactly as the SPA would be if it wrote a wrong donoId."
    - "The Python twin must never diverge silently from the TS implementation: divergence must fail the shared fixture suite, not be discovered in production."
    - "The CLI job must never delete or re-date an existing instanciasRotina record."
---

<objective>
Deliver JOB-02: a Python twin of the now-complete generation algorithm and the
`apollo rotina gerar-instancias` command that runs it, producing results byte-identical
to the SPA's for the same inputs, and proven non-duplicating against the live app from
the CLI channel.

Purpose: PROJECT.md's core value is UI↔CLI parity. The two runtimes cannot share code
(there is no shared JS/Python runtime — `bizdays.ts`/`bizdays.py` already established the
twin-implementation pattern in Phase 2), so parity has to be *proven*, and the only
honest proof is one fixture consumed by both suites.
Output: `cli/apollo_cli/routine_job.py`, the `gerar-instancias` command, and a pytest suite
with both a non-live parity half and a live idempotency half.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-idempotent-routine-instance-job/05-RESEARCH.md
@.planning/phases/05-idempotent-routine-instance-job/05-02-SUMMARY.md
@.planning/phases/05-idempotent-routine-instance-job/05-03-SUMMARY.md
@.planning/phases/05-idempotent-routine-instance-job/05-04-SUMMARY.md

@web/src/lib/routineJob.ts
@shared/routine-job.testcases.json
@cli/apollo_cli/bizdays.py
@cli/apollo_cli/crud_helpers.py
@cli/apollo_cli/instant_client.py
@cli/apollo_cli/entities/rotina.py
@cli/tests/test_bizdays.py
@cli/tests/test_rotina_instancia.py
@cli/tests/conftest.py

<interfaces>
<!-- Python twin contract. snake_case function names, camelCase DATA keys (matching -->
<!-- InstantDB's wire format and the shared fixture) — the same split cli/ already   -->
<!-- uses everywhere (see crud_helpers.create_entity's fields dicts).                -->

```python
# cli/apollo_cli/routine_job.py

TIPO_PRAZO_GERADO: Final[str] = "soft"
STATUS_INICIAL: Final[str] = "pendente"
REGRAS_COMPETENCIA_SUPORTADAS: Final[tuple[str, ...]] = ("M0", "M-1", "M-2", "M+1")

class ComputeResult(NamedTuple):
    expected: list[dict[str, Any]]   # sorted by dedupeKey
    skipped: list[dict[str, Any]]    # {"templateId": str, "reason": str}, sorted

def end_of_next_month(today: str) -> str: ...
def nth_business_day_of_month(year: int, month: int, n: int) -> str: ...   # month 1-based
def nth_calendar_day_of_month(year: int, month: int, n: int) -> str: ...   # month 1-based
def shift_competencia(data_prevista: str, regra_competencia: str) -> str | None: ...
def build_dedupe_key(template_id: str, competencia: str, data_prevista: str) -> str: ...
def to_iso_date(value: object) -> str: ...     # mirrors TS toIsoDate (10-char slice)
def compute_expected_instances(
    templates: list[dict[str, Any]],
    today: str,
    existing: list[dict[str, Any]],
) -> ComputeResult: ...

def run_routine_instance_job(
    client: Instant, dono_id: str, today: str, *, dry_run: bool = False
) -> dict[str, Any]: ...   # {"created": [...], "existing": [...], "skipped": [...]}
```

```python
# Consumed from Phase 2, unchanged (cli/apollo_cli/bizdays.py):
def is_business_day(value: str) -> bool: ...
def add_business_days(value: str, n: int) -> str: ...   # STRICTLY-AFTER semantics

# Consumed from Phase 3, unchanged (cli/apollo_cli/crud_helpers.py):
def client_for_session() -> tuple[Instant, Session]: ...
def emit(payload: object) -> None: ...
def validate_iso_date(ctx, param, value: str | None) -> str | None: ...
@contextmanager
def instant_errors() -> Iterator[None]: ...
```

```python
# InstantDB Python SDK (verified 1.0.63, cli/.venv/.../instantdb/_transact.py):
from instantdb import Instant, lookup
client.tx.instanciasRotina[lookup("dedupeKey", key)].update({...}).link({"template": tid})
# `create` REJECTS a lookup sentinel — the upsert path must use `update`.
```
</interfaces>

## Algorithm source of truth

`cli/apollo_cli/routine_job.py` implements the SAME algorithm documented in 05-02's
"Algorithm specification" and 05-04's "Encadeado semantics" (D-05-A..D-05-F), and the
SAME orchestration steps 1-7 documented in 05-03. Do not re-derive any of it; read those
plans plus their SUMMARY files and translate. Divergence is caught by the shared fixture,
not by review.

Python-specific notes:
- Use `calendar.monthrange(year, month)[1]` for the last day of the month, aliased as
  `import calendar as pycalendar` to avoid confusion with the vendored ANBIMA calendar
  concept that `bizdays.py` owns (RESEARCH's Supporting-library note).
- Use `datetime.date` for month arithmetic, matching `bizdays.py`'s existing convention.
- `dataPrevista` from InstantDB arrives as an ISO datetime string, not `YYYY-MM-DD`
  (`cli/tests/test_rotina_instancia.py` asserts with `.startswith`, not `==`). `to_iso_date`
  must normalize every DB-sourced date exactly as the TS `toIsoDate` does.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Python twin of the pure compute core, proven by the shared fixture (RED then GREEN)</name>
  <files>cli/tests/test_routine_job.py, cli/apollo_cli/routine_job.py</files>
  <read_first>
    - shared/routine-job.testcases.json (whole file — the complete specification after 05-04)
    - web/src/lib/routineJob.ts (the reference implementation you are translating, pure section only)
    - cli/apollo_cli/bizdays.py (the Phase 2 twin — mirror its module-docstring style and its "never a second calendar source" stance)
    - cli/tests/test_bizdays.py (how a shared-fixture parity suite is written on the Python side, including how it locates the shared/ file)
    - cli/apollo_cli/config.py (`find_repo_root`, the sanctioned way to reach `shared/`)
    - .planning/phases/05-idempotent-routine-instance-job/05-02-SUMMARY.md and 05-04-SUMMARY.md (the algorithm and encadeado prose)
  </read_first>
  <behavior>
    - Every `dayMath.nthBusinessDayOfMonth`, `nthCalendarDayOfMonth`, `endOfNextMonth`, and `shiftCompetencia` fixture case passes against the Python functions.
    - Every `scenarios` entry produces `expected` and `skipped` deeply equal to the fixture, including ordering.
    - `build_dedupe_key` produces the same string as the TS `buildDedupeKey` for the same three inputs.
    - `to_iso_date` handles an ISO datetime string, a plain date string, `None`, and a non-string without raising.
    - `compute_expected_instances([], today, [])` returns empty lists.
  </behavior>
  <action>
    Write `cli/tests/test_routine_job.py` FIRST, loading `shared/routine-job.testcases.json`
    via `find_repo_root()` and parametrizing over every `dayMath` array and every
    `scenarios` entry. Mark this half NOT live — it must run with no session and no
    network (`pytest -m "not live"` must exercise it), because the whole point of the twin
    is that it can be validated offline against the same data the TS suite uses.

    Compare full result structures with `==` on plain dicts/lists, not field-by-field
    assertions — an added or renamed key must fail the comparison, since a silently extra
    key on the Python side would become a silently extra attribute on a written record.

    Then implement `cli/apollo_cli/routine_job.py`'s pure section against the contract in
    this plan's `<interfaces>` block. Import `is_business_day`/`add_business_days` from
    `apollo_cli.bizdays` and nothing else calendar-related: no `from bizdays import Calendar`,
    no direct read of `shared/anbima-calendar.json`, no `Calendar.load("ANBIMA")`. C-03 makes
    a second calendar source a hard prohibition, and `bizdays.py`'s docstring already spells
    out why.

    Write a module docstring in `bizdays.py`'s style: name `web/src/lib/routineJob.ts` as the
    twin, name `shared/routine-job.testcases.json` as the single artifact preventing silent
    divergence, restate D-05-B/D-05-D/D-05-E/D-05-F for encadeado, and state that `dedupeKey`
    is plain concatenation whose uniqueness guarantee lives in the schema constraint.

    Keep the pure section strictly free of `Instant`, `httpx`, and filesystem access —
    mirror the TS file's `// --- I/O boundary ---` split with an equivalent Python section
    comment.
  </action>
  <acceptance_criteria>
    - `uv run --project cli pytest cli/tests/test_routine_job.py -m "not live" -x` exits 0 with every fixture case parametrized (not a single aggregate test): the run reports at least 40 collected test cases.
    - Offline proof: the non-live half passes with no session — `HOME=$(mktemp -d) uv run --project cli pytest cli/tests/test_routine_job.py -m "not live" -x` exits 0.
    - Calendar-source gates (all return 0): `grep -c 'Calendar.load' cli/apollo_cli/routine_job.py`, `grep -c 'from bizdays import' cli/apollo_cli/routine_job.py`, `grep -c 'anbima-calendar' cli/apollo_cli/routine_job.py`.
    - `grep -c 'from apollo_cli.bizdays import' cli/apollo_cli/routine_job.py` returns 1.
    - `grep -c 'hashlib\|sha256\|md5' cli/apollo_cli/routine_job.py` returns 0.
    - `uv run --project cli ruff check cli && uv run --project cli ty check cli` both exit 0 (C-08: 100% typed, zero warnings).
    - Commit sequence: one `test(05-05):` commit with the failing suite, then one `feat(05-05):` commit making it pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project cli pytest cli/tests/test_routine_job.py -m "not live" -x && uv run --project cli ruff check cli && uv run --project cli ty check cli</automated>
  </verify>
  <done>The Python compute core produces results identical to the TypeScript one for every case in the shared fixture, offline, with zero lint/type findings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire run_routine_instance_job and the `apollo rotina gerar-instancias` command</name>
  <files>cli/apollo_cli/routine_job.py, cli/apollo_cli/entities/rotina.py, cli/tests/test_rotina_instancia.py</files>
  <read_first>
    - cli/apollo_cli/entities/rotina.py (whole file — especially the module docstring, which currently states that `gerar-instancias` is Phase 5 scope and must not be stubbed; that statement now needs updating, not deleting)
    - cli/tests/test_rotina_instancia.py lines 41-46 (`test_gerar_instancias_does_not_exist_yet` — the Phase 3 guard that MUST be flipped, not deleted)
    - cli/apollo_cli/crud_helpers.py (`client_for_session`, `emit`, `instant_errors`, `validate_iso_date`)
    - cli/apollo_cli/instant_client.py (`session_client` vs `login_client` — the admin-token boundary)
    - .planning/phases/05-idempotent-routine-instance-job/05-03-PLAN.md — "Orchestration specification" steps 1-7 (the spec this mirrors)
    - .planning/phases/05-idempotent-routine-instance-job/05-04-PLAN.md — the templates-query widening (nested `antecessor`, union of antecessor ids in the existing-instances query)
  </read_first>
  <action>
    Implement `run_routine_instance_job(client, dono_id, today, *, dry_run=False)` in
    `cli/apollo_cli/routine_job.py` below the I/O boundary comment, following 05-03's
    orchestration steps 1-7 and 05-04's query widening exactly:
    query active templates (with the `antecessor` link selected) → short-circuit on zero →
    query existing instances for the union of active template ids and their antecessor ids →
    normalize dates with `to_iso_date` → `compute_expected_instances` → diff on dedupeKey →
    one `client.transact` of `lookup("dedupeKey", key)`-keyed `.update()` chunks with
    `donoId` always present and `.link({"template": template_id})` → catch a transact
    failure, re-query the keys, and report them as `existing` when a concurrent run already
    created them.

    Use `.update()` never `.create()` on the lookup path (the SDK rejects a lookup sentinel
    on `create` — verified in `_transact.py`). Never call `login_client()`. Never call
    `.delete()`. Wrap network calls in `instant_errors()` so failures map to the CLI's
    existing JSON-error + exit-code contract rather than a traceback.

    `dry_run=True` performs every query and the full diff but issues no transact, returning
    the same report shape with the would-be keys under `created`. It exists so an operator
    (or Claude) can inspect what a run would do against real data without writing — the
    only non-destructive inspection path this job otherwise has.

    Add the `gerar-instancias` command to the `rotina` group (top level of the group, NOT
    under `template` or `instancia` — PROJECT.md C-07 names it `apollo rotina gerar-instancias`)
    with options `--data-base` (ISO, `callback=validate_iso_date`, default `None` → today's
    UTC date) and `--dry-run/--no-dry-run` (default off). Emit exactly one JSON document via
    `emit({"created": [...], "existing": [...], "skipped": [...]})`, keeping the
    single-JSON-document-per-command contract every other command honors. The `skipped`
    array is what makes a misconfigured template diagnosable — it is the CLI's answer to
    "why did this template not generate?" (RESEARCH Open Question 3).

    Write rich `--help` covering: what the job does, the today→end-of-next-month range, that
    it never duplicates and never deletes, that `manual`/unrecognized `regraCompetencia`
    means no auto-generation, and that `encadeado` inherits its antecessor's competencia and
    counts business days (D-05-B/D-05-D).

    Update `cli/apollo_cli/entities/rotina.py`'s module docstring: the paragraph declaring
    `gerar-instancias` out of Phase 3 scope becomes a statement that it now exists and is
    the ONLY sanctioned creator of `instanciasRotina` records — preserving the reasoning
    about why hand-created instances break idempotency, since that reasoning is still the
    justification for `instancia` having no `criar`/`deletar`.

    Flip `test_gerar_instancias_does_not_exist_yet` in `cli/tests/test_rotina_instancia.py`
    into `test_gerar_instancias_exists_at_group_level`, asserting the command is present on
    `rotina.group.commands` and absent from `rotina.instancia.commands`. Do not delete it:
    the surrounding assertions that `instancia` exposes exactly `{listar, status}` are a
    live C-06 guard and must keep passing unchanged.
  </action>
  <acceptance_criteria>
    - `uv run --project cli apollo rotina gerar-instancias --help` exits 0 and its output contains `--data-base`, `--dry-run`, and the words `nunca duplica` (or the equivalent phrasing you chose, asserted in a test rather than by eye).
    - `grep -c 'login_client' cli/apollo_cli/routine_job.py` returns 0.
    - `grep -c 'admin_token\|INSTANT_APP_ADMIN_TOKEN' cli/apollo_cli/routine_job.py` returns 0.
    - `grep -v '^\s*#' cli/apollo_cli/routine_job.py | grep -c '\.delete('` returns 0.
    - `grep -v '^\s*#' cli/apollo_cli/routine_job.py | grep -c '\.create('` returns 0 — the upsert path uses `update` only.
    - `grep -c 'lookup("dedupeKey"' cli/apollo_cli/routine_job.py` returns 1.
    - `uv run --project cli pytest cli/tests/test_rotina_instancia.py -x` exits 0 with the flipped guard test and the unchanged `{listar, status}` assertions both passing.
    - `uv run --project cli pytest cli/tests/test_cli_surface.py -x` exits 0 (the schema-driven coverage/help-completeness suite from 03-06 still holds with the new command).
    - `uv run --project cli ruff check cli && uv run --project cli ty check cli` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project cli apollo rotina gerar-instancias --help >/dev/null && uv run --project cli pytest cli/tests/test_rotina_instancia.py cli/tests/test_cli_surface.py -x && uv run --project cli ruff check cli && uv run --project cli ty check cli && ! grep -q 'login_client' cli/apollo_cli/routine_job.py</automated>
  </verify>
  <done>`apollo rotina gerar-instancias` exists at the group level, emits one JSON report, runs under the session client with no admin token, has no create/delete path, and the Phase 3 structural guards still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Prove CLI-channel double-run idempotency and status preservation live</name>
  <files>cli/tests/test_routine_job.py</files>
  <read_first>
    - cli/tests/test_rotina_instancia.py (the live-test structure: `_create_template`, `_query_instancia`, `cleanup_records`, `pytestmark = pytest.mark.live`)
    - cli/tests/conftest.py (`run_cli`, `live_client`, `live_session`, `cleanup_records`, `unique_suffix`)
    - cli/apollo_cli/routine_job.py (the orchestration from Task 2)
    - .planning/phases/05-idempotent-routine-instance-job/05-03-PLAN.md Task 3 (the equivalent SPA-side proof this mirrors, so the two channels are argued the same way)
  </read_first>
  <action>
    Add a live half to `cli/tests/test_routine_job.py`, marked `pytest.mark.live`, mirroring
    05-03 Task 3's structure against the real InstantDB app:

    1. Seed three `phase05-cli-{suffix}` templates via `run_cli` — one `du_fixo`, one
       `corrido_fixo`, one `encadeado` linked via `--antecessor-id` — each with an
       `--offset-dias` that lands inside the range, and `--regra-competencia M0`. Register
       every created record in `cleanup_records`.
    2. `--dry-run` first: assert it reports a non-empty `created` list and that a live query
       shows ZERO instances for those templates — proving dry-run really does not write.
    3. Real run: `apollo rotina gerar-instancias`. Assert exit 0, one parseable JSON
       document, non-empty `created`, empty `existing`. Query the live app and assert each
       instance's `dedupeKey` equals `build_dedupe_key(templateId, competencia, to_iso_date(dataPrevista))`
       recomputed from that record's own fields, each `donoId == live_session.user_id`, each
       `status == "pendente"`, and each normalized `dataPrevista` inside
       `[today, end_of_next_month(today)]`.
    4. Set ONE instance to `concluida` via `apollo rotina instancia status`.
    5. Second run: assert `created` is empty and `existing` holds exactly the run-3 keys.
       Re-query and assert the id set is identical (nothing deleted, nothing recreated), no
       duplicate dedupeKeys, the mutated instance still reads `concluida`, and its
       `dataPrevista`/`competencia`/`dedupeKey` are unchanged.
    6. Register every generated instance id in `cleanup_records` (query them out of the live
       app after run 3) so a re-run of the suite never accumulates debris — instances have no
       `deletar` command by design, so the teardown must go through `live_client.tx` exactly
       as `test_rotina_instancia.py`'s existing fixtures do.

    Step 5's status assertion is the CLI-side mitigation of T-05-01 and is not redundant with
    05-03's: the two channels have independent write paths, and a status-clobbering bug in
    only one of them is exactly the kind of asymmetry a parity-first project must not ship.
  </action>
  <acceptance_criteria>
    - `uv run --project cli pytest cli/tests/test_routine_job.py -m live -x` exits 0 against the live app.
    - `uv run --project cli pytest cli/tests -x` exits 0 (whole CLI suite, no regressions).
    - Zero leftovers after the run: `uv run --project cli apollo rotina template listar | grep -c 'phase05-cli-'` returns 0, and `uv run --project cli apollo rotina instancia listar | grep -c 'phase05-cli-'` returns 0.
    - The dry-run non-write assertion is present: `grep -c 'dry-run\|dry_run' cli/tests/test_routine_job.py` returns >= 2.
    - The status-preservation assertion is present: `grep -c 'concluida' cli/tests/test_routine_job.py` returns >= 2.
    - `uv run --project cli ruff check cli && uv run --project cli ty check cli` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project cli pytest cli/tests -x && uv run --project cli ruff check cli && uv run --project cli ty check cli</automated>
  </verify>
  <done>Two consecutive `apollo rotina gerar-instancias` runs against the live app produce the same instance ids, zero duplicate dedupeKeys, and a preserved `concluida` status; `--dry-run` provably writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI process (holds a persisted session at `~/.config/apollo-cli/session`, and the repo `.env.instantdb` contains an admin token) → InstantDB | The admin token is physically reachable from this process; only convention and code review keep it out of the job's client |
| shared fixture → two independent implementations | The fixture is the only mechanism preventing silent TS/Python divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | CLI job client construction | mitigate | `client_for_session()`/`session_client()` only; grep gates in Task 2 assert zero occurrences of `login_client`, `admin_token`, and `INSTANT_APP_ADMIN_TOKEN` in `routine_job.py`. `session_client` passes an empty-string admin token specifically to defeat the SDK's env fallback |
| T-05-01 | Tampering | CLI job write payload | mitigate | Same structural diff as the SPA: existing dedupeKeys never enter a transact payload. Task 3 step 5 asserts a `concluida` status survives a CLI run |
| T-05-04 | Tampering | Accidental delete path | mitigate | Grep gate asserts zero `.delete(` in `routine_job.py`; Task 3 step 5 asserts an unchanged id set |
| T-05-30 | Tampering | Silent TS/Python divergence | mitigate | Every fixture case is parametrized on both sides; a divergent dedupeKey, date, competencia, or skip reason fails `pytest -m "not live"` offline before any live run |
| T-05-31 | Information Disclosure | `donoId` scoping in the CLI query | mitigate | Templates queried with an explicit `donoId` where-clause AND server-side perms filtering; Task 3 asserts every written record's `donoId` equals the session user id |
| T-05-32 | Repudiation | Unexplained non-generation | mitigate | `skipped` is a first-class key in the emitted JSON document with a machine-readable reason per template |
| T-05-33 | Tampering | `--dry-run` accidentally writing | mitigate | Task 3 step 2 asserts zero instances exist after a dry run, against the live app |
</threat_model>

<verification>
- `uv run --project cli pytest cli/tests/test_routine_job.py -m "not live" -x` — offline TS/Python parity across the full shared fixture.
- `uv run --project cli pytest cli/tests -x` — whole CLI suite, live and non-live, including the flipped Phase 3 guard.
- `uv run --project cli ruff check cli && uv run --project cli ty check cli` — C-08 gates.
- Admin-token, delete, and create grep gates from Task 2's acceptance criteria.
</verification>

<success_criteria>
- ROADMAP SC-4 is satisfied for the CLI-alone case: `apollo rotina gerar-instancias` generates and re-runs without duplicating. (The cross-channel half of SC-4 is proven in 05-06.)
- The Python twin is proven identical to the TS implementation offline, for every case in the shared fixture.
- The CLI job carries no admin token and has no create/delete path.
- The Phase 3 structural guard that `gerar-instancias` must not be stubbed is flipped into a guard that it exists at the right level — never deleted.
</success_criteria>

<artifacts>
## Artifacts this phase produces (contributed by this plan)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `cli/apollo_cli/routine_job.py` | source | 05-06 (cross-channel + concurrency proof), Phase 6 VERIFY-04 |
| `apollo rotina gerar-instancias` (with `--data-base`, `--dry-run`) | CLI surface | 05-06, Phase 6 |
| `cli/tests/test_routine_job.py` (parity + live halves) | test suite | 05-06 verification script |
| Flipped `gerar-instancias` structural guard | test | ongoing regression protection |
</artifacts>

<output>
After completion, create `.planning/phases/05-idempotent-routine-instance-job/05-05-SUMMARY.md`.
Record: the emitted JSON report shape with a real example, the count of parametrized parity
cases, the live run-1/run-2 evidence, and any place where the Python translation had to
differ mechanically from the TS (and why it is still behaviorally identical).
</output>
