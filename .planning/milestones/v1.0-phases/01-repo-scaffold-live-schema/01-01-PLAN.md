---
phase: 01-repo-scaffold-live-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/bun.lock
  - web/.gitignore
  - web/index.html
  - web/vite.config.ts
  - web/svelte.config.js
  - web/tsconfig.json
  - web/tsconfig.app.json
  - web/tsconfig.node.json
  - web/src/main.ts
  - web/src/App.svelte
  - web/src/app.css
  - web/src/vite-env.d.ts
  - web/src/lib/db.ts
  - shared/instant.schema.ts
  - shared/instant.perms.ts
autonomous: true
requirements: [SETUP-01, SETUP-03, SETUP-06, SETUP-07, SETUP-08]
user_setup: []

must_haves:
  truths:
    - "`bun install` in `web/` succeeds and `bun run build` produces `web/dist/` with exit code 0."
    - "`web/` is a pure Svelte 5 + Vite SPA: no `@sveltejs/kit` dependency and no `src/routes/` directory exist anywhere under `web/`."
    - "`import.meta.env.VITE_INSTANT_APP_ID` resolves to the real app id at build time even though the source file is named `.env.instantdb` (a filename Vite's own loader ignores)."
    - "The built `web/dist/` bundle contains the InstantDB app id and does NOT contain the `INSTANT_APP_ADMIN_TOKEN` value."
    - "`shared/instant.schema.ts` declares exactly the 9 entities `fundos`, `projetos`, `etapas`, `tarefas`, `templatesRotina`, `instanciasRotina`, `tickets`, `subtarefas`, `logInferenciaClaude`, each carrying an indexed `donoId` string field."
    - "`instanciasRotina.dedupeKey` is declared `.unique().indexed()`."
    - "`shared/instant.perms.ts` applies the identical `auth.id`/`donoId` rule set to all 9 domain entities and leaves `$users` untouched."
    - "`bun run instant:push` exits 0 and a subsequent `instant-cli pull` into a scratch directory returns a schema containing all 9 entity names and perms containing the donoId rule strings."
    - "Running `bun run instant:push` a second time in a row exits 0 (idempotent — schema push is additive and converges)."
    - "An unauthenticated (`--as-guest`) InstaQL query against `fundos` is rejected by the live permission rules."
    - statement: "The `svelte-check` gate reports zero errors against `web/src/**` and `shared/*.ts` including the cross-package import from `web/src/lib/db.ts` to `../../../shared/instant.schema`."
      verification: backstop
  artifacts:
    - path: "shared/instant.schema.ts"
      provides: "Single TS source of truth for the 9-entity InstantDB domain schema + 9 links"
      exports: ["default (schema)", "AppSchema"]
      contains: "logInferenciaClaude"
    - path: "shared/instant.perms.ts"
      provides: "donoId-scoped permission rules for all domain entities + attrs lockdown"
      exports: ["default (rules)"]
      contains: "auth.id == data.donoId"
    - path: "web/vite.config.ts"
      provides: "Explicit `.env.instantdb` load + narrow single-key `define` injection"
      contains: "VITE_INSTANT_APP_ID"
    - path: "web/src/lib/db.ts"
      provides: "Typed InstantDB client bound to the shared schema"
      exports: ["db", "id", "lookup"]
    - path: "web/package.json"
      provides: "Bun-managed SPA manifest + instant-cli push/pull/verify scripts"
      contains: "instant:push"
  key_links:
    - from: "web/src/lib/db.ts"
      to: "shared/instant.schema.ts"
      via: "relative import `../../../shared/instant.schema`"
      pattern: "\\.\\./\\.\\./\\.\\./shared/instant\\.schema"
    - from: "web/vite.config.ts"
      to: ".env.instantdb"
      via: "dotenv.parse of the explicitly resolved repo-root path"
      pattern: "\\.env\\.instantdb"
    - from: "web/package.json instant:push"
      to: "shared/instant.schema.ts and shared/instant.perms.ts"
      via: "INSTANT_SCHEMA_FILE_PATH / INSTANT_PERMS_FILE_PATH env overrides"
      pattern: "INSTANT_SCHEMA_FILE_PATH=\\.\\./shared/instant\\.schema\\.ts"
  prohibitions:
    - "MUST NOT ship placeholder-permissive rules (`view/create/update/delete: \"true\"`) for any domain entity, not even temporarily to unblock the push. The `ultima-missao` reference perms file is literally all-`\"true\"` and must not be copied; a live app with open rules silently voids the entire donoId isolation guarantee (C-05) while every gate still reports green."
    - "MUST NOT make the InstantDB admin token a runtime dependency of the SPA. The admin token bypasses all permission rules; wiring it into client code or a broad Vite `define` would silently convert the UI↔CLI parity guarantee into an admin-bypass, which is precisely what C-05 forbids."
    - "MUST NOT reduce the schema to a subset of the 9 entities \"for now\" and backfill later. C-02 locks the full schema in one pass; a partial push becomes the live source of truth that Phases 2-6 build on."
  assumptions_flagged:
    - "Canon-referral (dropped, covered by threat model T-01-01): secret-in-git handling is standard secret-management canon, tracked as an explicit threat rather than minted as a prohibition."
    - "`templatesRotina.ativo` is not in the SPEC schema table but IS required by SPEC §'Job de geração de instâncias de rotina' ('para cada templatesRotina ativo'). Added as a boolean; flagged so a reviewer can confirm."
    - "SPEC does not specify `onDelete` cascade semantics for any link. None are declared here; deliberate omission, revisit if Phase 3/4 needs cascade."
---

<objective>
Stand up the `web/` half of the monorepo and make the InstantDB schema and permission rules the LIVE source of truth for every later phase.

Purpose: This is the phase tracer slice. Everything downstream (Phase 2 calendar, Phase 3 CLI CRUD, Phase 4 SPA CRUD, Phase 5 job) reads its domain shape from `shared/instant.schema.ts` and is gated by `shared/instant.perms.ts`. Until both are pushed live and provably reflected back from the server, no later phase can be trusted.
Output: A buildable pure-Svelte-5 Vite SPA, the 9-entity schema + donoId perms in `shared/`, and both pushed to the live InstantDB app with a server-side round-trip proving it.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffold-live-schema/01-RESEARCH.md
@docs/superpowers/specs/2026-08-09-migracao-instantdb-design.md
</context>

<environment_facts>
Verified live on 2026-08-09 during planning — treat as established, do not re-discover:

- `/home/thomaz/pessoal/apollo-v2/.env.instantdb` exists at the repo root and defines exactly two keys: `NEXT_PUBLIC_INSTANT_APP_ID` and `INSTANT_APP_ADMIN_TOKEN`. The research doc guessed the key name `INSTANT_APP_ID` — that key does NOT exist. Read `NEXT_PUBLIC_INSTANT_APP_ID`.
- `.env.instantdb` is already gitignored via the `.env.*` line in `/home/thomaz/pessoal/apollo-v2/.gitignore` (`git check-ignore -v .env.instantdb` confirms). Never print either value into a tracked file, a log, or a SUMMARY.
- `instant-cli` v1.0.63 authenticates fine using ONLY the `INSTANT_APP_ADMIN_TOKEN` from the env file. `npx instant-cli login` (personal browser login) is NOT required — a `pull all` against the live app succeeded during planning with `Not logged in.` status. There is no human checkpoint in this plan.
- The live InstantDB app is currently EMPTY: its schema has only the system entities `$files`, `$streams`, `$users`, its perms file is the default all-comments stub, and `{"$users":{}}` returns `[]`. There is no real data on top of this schema, so pushing is NOT a costly one-way door at this point (REVERSIBILITY_GATES evaluated: no human checkpoint warranted).
- `instant-cli` global options confirmed: `-y --yes`, `--env <file>`, `-t --token`. `push` options: `-a --app`, `-p --package <…|svelte>`, `--skip-check-types`. `query` options: `--admin`, `--as-guest`, `--as-email <email>`, `--as-token <token>`. Env overrides `INSTANT_SCHEMA_FILE_PATH` / `INSTANT_PERMS_FILE_PATH` are documented in `push --help`.
- `instant-cli` refuses to run unless cwd contains a project marker (`package.json`) AND an `@instantdb/*` SDK is installed. That is why every instant-cli invocation in this plan runs with cwd = `web/`.
- `@biomejs/biome` resolves to 2.5.7 and supports `--config-path=PATH` (used in plan 03, not here).
</environment_facts>

<interfaces>
Contracts this plan establishes, consumed by plans 01-03 and by Phases 2-6.

From `shared/instant.schema.ts`:
- `export default schema` — the `i.schema()` result; imported by `web/src/lib/db.ts` and by `instant-cli push`.
- `export type { AppSchema }` — the inferred schema type used to type `init<AppSchema>()`.

From `shared/instant.perms.ts`:
- `export default rules` — an object `satisfies InstantRules`.

From `web/src/lib/db.ts`:
- `export const db` — the `init({ appId, schema })` client instance.
- `export { id, lookup }` — re-exported from `@instantdb/svelte` so no other module imports the SDK directly for id/lookup helpers.

Reference implementation to mirror for file layout and tsconfig shape (read, do not copy the perms file):
- `/home/thomaz/pessoal/ultima-missao/package.json`
- `/home/thomaz/pessoal/ultima-missao/tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`
- `/home/thomaz/pessoal/ultima-missao/index.html`, `src/main.ts`
- `/home/thomaz/pessoal/ultima-missao/instant.schema.ts` (structure only — its entities are a different domain)
- `/home/thomaz/pessoal/ultima-missao/src/lib/db.ts`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the pure Svelte 5 + Vite SPA under web/ with explicit .env.instantdb loading</name>
  <files>web/package.json, web/bun.lock, web/.gitignore, web/index.html, web/vite.config.ts, web/svelte.config.js, web/tsconfig.json, web/tsconfig.app.json, web/tsconfig.node.json, web/src/main.ts, web/src/App.svelte, web/src/app.css, web/src/vite-env.d.ts</files>
  <read_first>
    - /home/thomaz/pessoal/ultima-missao/package.json (scripts + dep layout to mirror)
    - /home/thomaz/pessoal/ultima-missao/tsconfig.json, tsconfig.app.json, tsconfig.node.json
    - /home/thomaz/pessoal/ultima-missao/index.html, /home/thomaz/pessoal/ultima-missao/src/main.ts
    - /home/thomaz/pessoal/ultima-missao/vite.config.ts, /home/thomaz/pessoal/ultima-missao/svelte.config.js
    - .planning/phases/01-repo-scaffold-live-schema/01-RESEARCH.md sections "Pattern 3" and "Pitfall 1"
    - .planning/PROJECT.md constraints C-01, C-03, C-08
  </read_first>
  <action>
Create `web/` as a Bun-managed, plain-Vite Svelte 5 SPA. Do NOT install or reference `@sveltejs/kit`, and do not create a `src/routes/` directory — C-01 locks "Vite puro, sem SvelteKit".

Initialize `web/package.json` with `"type": "module"`, `"private": true`, `"name": "apollo-web"`. Install with bun only (never npm/pnpm/yarn — C-08 makes bun the sole JS executor):
- dependency: `@instantdb/svelte` (^1.0.63)
- devDependencies: `@sveltejs/vite-plugin-svelte` (^7.3.0), `svelte` (^5.56.8), `vite` (^8.2.1), `typescript` (~5.9.3), `svelte-check` (^4.7.5), `@tsconfig/svelte` (^5.0.8), `@types/node` (^24), `@types/bun` (latest), `dotenv` (^17), `instant-cli` (^1.0.63)

`instant-cli` is pinned as a devDependency rather than invoked via `npx` so pushes are reproducible.

Scripts in `web/package.json`:
- `dev` = `vite`
- `build` = `vite build`
- `preview` = `vite preview`
- `check` = `svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json`
- `instant:push` = `INSTANT_SCHEMA_FILE_PATH=../shared/instant.schema.ts INSTANT_PERMS_FILE_PATH=../shared/instant.perms.ts instant-cli push all --env ../.env.instantdb --package svelte --yes`
- `instant:pull` = same env prefix and flags but `instant-cli pull all …` (this one overwrites `shared/` on purpose; only run it deliberately)
- `instant:verify` = `INSTANT_SCHEMA_FILE_PATH=.instant-verify/instant.schema.ts INSTANT_PERMS_FILE_PATH=.instant-verify/instant.perms.ts instant-cli pull all --env ../.env.instantdb --package svelte --yes` (pulls the LIVE state into a throwaway directory so it can be diffed against `shared/` without clobbering the source)

`web/.gitignore` must ignore `node_modules/`, `dist/`, `.instant-verify/`, `.svelte-kit/`.

`web/vite.config.ts` (this is the fix for RESEARCH Pitfall 1 — Vite's loader only recognizes `.env`, `.env.local`, `.env.[mode]`, `.env.[mode].local`, so the locked filename `.env.instantdb` is invisible to it):
- Resolve the env file path with `fileURLToPath(new URL("../.env.instantdb", import.meta.url))` — do not use `__dirname` (this file is ESM).
- Read that file with `node:fs` `readFileSync` and parse it with `dotenv`'s named `parse` export into a local constant. Do NOT call `dotenv.config()`, do NOT assign to `process.env`, and do NOT use Vite's `loadEnv` or `envPrefix` against this file — the file also holds `INSTANT_APP_ADMIN_TOKEN` and any broad injection would bake an all-permissions-bypassing credential into the browser bundle (threat T-01-02).
- Read the app id as `parsed.NEXT_PUBLIC_INSTANT_APP_ID` with `parsed.INSTANT_APP_ID` as a secondary fallback. If the resulting value is empty or undefined, `throw new Error(...)` naming the expected file path and key so a missing app id fails the build loudly instead of shipping `appId: undefined`.
- Export `defineConfig({ plugins: [svelte()], define: { "import.meta.env.VITE_INSTANT_APP_ID": JSON.stringify(appId) } })`. `define` must contain exactly this one key.

`web/svelte.config.js` mirrors the reference: a single default-exported empty options object with the `@sveltejs/vite-plugin-svelte` JSDoc type annotation. This is the one permitted `.js` file in `web/` — the Svelte toolchain loads it through bare Node with no transpile step, so it cannot be `.ts`. It contains configuration only, zero logic, which keeps C-08's "frontend logic is always `.ts`" intact. Record this exception in the SUMMARY.

`web/tsconfig.json` uses the reference's project-references shape (`files: []`, references to `./tsconfig.app.json` and `./tsconfig.node.json`).
`web/tsconfig.app.json` extends `@tsconfig/svelte/tsconfig.json` with `target: es2023`, `module: esnext`, `moduleDetection: force`, `types: ["svelte", "vite/client"]`, `allowArbitraryExtensions: true`, `noEmit: true`, and `tsBuildInfoFile: ./node_modules/.tmp/tsconfig.app.tsbuildinfo`. Diverge from the reference in two ways: set `include` to `["src/**/*.ts", "src/**/*.svelte", "../shared/*.ts"]` so the cross-package schema import in Task 2 type-checks, and OMIT `allowJs`/`checkJs` entirely (C-08 bans `.js` frontend logic, so there is nothing to check).
`web/tsconfig.node.json` copies the reference verbatim (`include: ["vite.config.ts"]`, `module: nodenext`, `types: ["node"]`, `noUnusedLocals`, `noUnusedParameters`, `erasableSyntaxOnly`, `verbatimModuleSyntax`, `noEmit`).

`web/src/vite-env.d.ts` declares an `ImportMetaEnv` interface with `readonly VITE_INSTANT_APP_ID: string` and an `ImportMeta` interface with `readonly env: ImportMetaEnv`, plus the `/// <reference types="svelte" />` and `/// <reference types="vite/client" />` triple-slash directives.

`web/index.html` uses `lang="pt-BR"`, title `Apollo v2`, a `<div id="app"></div>`, and `<script type="module" src="/src/main.ts"></script>`.
`web/src/main.ts` mounts `App.svelte` onto `#app` using the Svelte 5 `mount()` API from `svelte` and imports `./app.css`.
`web/src/app.css` holds a minimal base stylesheet (system font stack, `box-sizing: border-box` reset). No design work — Phase 4 and the future UI spec own that.
`web/src/App.svelte` for this task uses `<script lang="ts">` and renders a heading "Apollo v2" plus a paragraph showing `import.meta.env.VITE_INSTANT_APP_ID`. It must NOT import `./lib/db` yet — `shared/instant.schema.ts` does not exist until Task 2. Rendering the app id here is deliberate: it forces the injected value into `dist/` so the build-time gate below can assert both its presence and the admin token's absence.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bun install && bun run build && bun run check && test ! -d src/routes && ! grep -q '@sveltejs/kit' package.json && APPID=$(grep '^NEXT_PUBLIC_INSTANT_APP_ID=' ../.env.instantdb | cut -d= -f2-) && TOKEN=$(grep '^INSTANT_APP_ADMIN_TOKEN=' ../.env.instantdb | cut -d= -f2-) && grep -rqF "$APPID" dist/ && ! grep -rqF "$TOKEN" dist/ && echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && bun install` exits 0 and creates `web/bun.lock`.
    - `cd web && bun run build` exits 0 and creates `web/dist/index.html`.
    - `cd web && bun run check` exits 0 with zero errors reported by `svelte-check` and `tsc -p tsconfig.node.json`.
    - `grep -c '@sveltejs/kit' web/package.json` is 0 and `test -d web/src/routes` is false.
    - The literal value of `NEXT_PUBLIC_INSTANT_APP_ID` appears somewhere under `web/dist/`.
    - The literal value of `INSTANT_APP_ADMIN_TOKEN` appears NOWHERE under `web/dist/` (`grep -rF "$TOKEN" web/dist/` exits non-zero).
    - `web/vite.config.ts` has exactly one key inside its `define` object: `grep -v '^\s*//' web/vite.config.ts | grep -c 'import.meta.env.VITE_' equals 1`.
    - `git check-ignore -q web/node_modules web/dist web/.instant-verify` exits 0.
  </acceptance_criteria>
  <done>`web/` installs, builds, and type-checks clean as a SvelteKit-free Svelte 5 SPA, with the app id injected from `.env.instantdb` and the admin token provably absent from the bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Author shared/instant.schema.ts (9 entities + 9 links) and shared/instant.perms.ts (donoId rules), and bind the SPA client to them</name>
  <files>shared/instant.schema.ts, shared/instant.perms.ts, web/src/lib/db.ts, web/src/App.svelte</files>
  <read_first>
    - docs/superpowers/specs/2026-08-09-migracao-instantdb-design.md §"Schema do InstantDB" (lines 55-71) and §"Autenticação e permissões" (lines 73-84) — the authoritative field/link table
    - .planning/PROJECT.md constraints C-02, C-04, C-05
    - /home/thomaz/pessoal/ultima-missao/instant.schema.ts (structural template: `_schema`/`_AppSchema`/`AppSchema` triple + default export)
    - /home/thomaz/pessoal/ultima-missao/src/lib/db.ts (init pattern)
    - /home/thomaz/pessoal/ultima-missao/instant.perms.ts — READ ONLY as a shape reference. Its rules are all `"true"`; copying them would be a direct prohibition violation.
  </read_first>
  <action>
Create `shared/instant.schema.ts`. Import `{ i }` from `@instantdb/svelte` (verified during research to re-export the full schema-authoring surface — do NOT add `@instantdb/admin` as a second dependency). Use the reference's structural triple: build `const _schema = i.schema({ entities, links, rooms: {} })`, then `type _AppSchema = typeof _schema; interface AppSchema extends _AppSchema {}; const schema: AppSchema = _schema;` and `export type { AppSchema }; export default schema;`.

Declare exactly these 9 domain entities with these fields, per SPEC §"Schema do InstantDB". Every entity carries `donoId: i.string().indexed()` (C-04 denormalization). Do not add, rename, or omit fields beyond what is listed:

- `fundos`: `nome` string, `codigo` string indexed, `ativo` boolean, `donoId`, `createdAt` date
- `projetos`: `nome` string, `descricao` string optional, `status` string indexed, `dataInicioPrevista` date optional, `dataFimPrevista` date optional, `donoId`
- `etapas`: `nome` string, `ordem` number, `status` string indexed, `donoId`
- `tarefas`: `titulo` string, `descricao` string optional, `tipoPrazo` string, `dataPrevista` date optional, `dataPrevistaEstimada` date optional, `competencia` string optional, `status` string indexed, `donoId`
- `templatesRotina`: `nome` string, `tipoGeracao` string, `regraCompetencia` string, `propagarAtrasoSoft` boolean, `ativo` boolean, `donoId`
- `instanciasRotina`: `dedupeKey` string `.unique().indexed()`, `dataPrevista` date, `dataPrevistaEstimada` date optional, `competencia` string, `tipoPrazo` string, `status` string indexed, `donoId`
- `tickets`: `titulo` string, `corpo` string, `remetente` string, `dataRecebimento` date, `tipoPrazo` string, `dataPrevista` date optional, `status` string indexed, `donoId`
- `subtarefas`: `titulo` string, `concluida` boolean, `ordem` number, `donoId`
- `logInferenciaClaude`: `campo` string, `valorInferido` string, `trechoMotivador` string optional, `entidadeTipo` string, `entidadeId` string, `donoId`, `createdAt` date

`templatesRotina.ativo` is not in the SPEC's field table but is required by SPEC §"Job de geração de instâncias de rotina" ("Para cada `templatesRotina` ativo"). Add it and note the addition in the SUMMARY. Add a short `//` comment above each entity naming the SPEC row it implements. Add a comment on `instanciasRotina.dedupeKey` stating it is the Phase 5 idempotency key (`hash(templateId + competencia + dataPrevista)`) and that its uniqueness is what makes the lookup-upsert atomic.

Declare exactly these 9 links (forward `has: "one"` sides are the child; InstantDB links are optional by default, which is what lets `subtarefas` attach to a `tarefa` OR a `ticket`). Do NOT declare `onDelete` on any link — the SPEC does not specify cascade semantics:
- `fundoProjetos`: forward on `projetos` has one `fundo` / reverse on `fundos` has many `projetos`
- `fundoTemplatesRotina`: forward on `templatesRotina` has one `fundo` / reverse on `fundos` has many `templatesRotina`
- `fundoTickets`: forward on `tickets` has one `fundo` / reverse on `fundos` has many `tickets`
- `projetoEtapas`: forward on `etapas` has one `projeto` / reverse on `projetos` has many `etapas`
- `etapaTarefas`: forward on `tarefas` has one `etapa` / reverse on `etapas` has many `tarefas`
- `tarefaSubtarefas`: forward on `subtarefas` has one `tarefa` / reverse on `tarefas` has many `subtarefas`
- `ticketSubtarefas`: forward on `subtarefas` has one `ticket` / reverse on `tickets` has many `subtarefas`
- `templateInstancias`: forward on `instanciasRotina` has one `template` / reverse on `templatesRotina` has many `instancias`
- `templateAntecessor`: self-link, forward on `templatesRotina` has one `antecessor` / reverse on `templatesRotina` has many `sucessores`

Create `shared/instant.perms.ts`. Import `type { InstantRules }` from `@instantdb/svelte`. Declare ONE constant — name it `donoRules` — holding `allow: { view: "auth.id != null && auth.id == data.donoId", create: "auth.id != null && auth.id == newData.donoId", update: "auth.id != null && auth.id == data.donoId", delete: "auth.id != null && auth.id == data.donoId" }`, exactly as written in SPEC §"Autenticação e permissões". Then build the exported rules object by referencing that same `donoRules` constant for each of the 9 domain entity keys — do not retype or copy-paste the rule strings per entity (a single mistyped `donoId` on one entity silently opens cross-user access; threat T-01-03). Also add an `attrs` key with `allow: { create: "false" }` to block clients from minting new attributes at runtime (threat T-01-04). Do NOT add a `$users` key — InstantDB's default `$users` rule is what C-05 mandates, and an explicit entry risks weakening it. End with `satisfies InstantRules` and `export default rules`.

Create `web/src/lib/db.ts`: import `{ init, id, lookup }` from `@instantdb/svelte`, import the default export of `../../../shared/instant.schema`, export `const db = init({ appId: import.meta.env.VITE_INSTANT_APP_ID, schema })`, and re-export `{ id, lookup }`. All other `web/` modules must obtain `id`/`lookup` from here rather than importing the SDK directly.

Update `web/src/App.svelte` to import `db` from `./lib/db` and render the result of `db.useAuth()` — show "não autenticado" when there is no user, the user's email when there is, and the error message when auth errors. Use Svelte 5 runes only (`$state`, `$derived`, `$props`), never Svelte 4 `$:` reactive statements. Keep the app id paragraph from Task 1 so the `dist/` grep gate still holds. This is a scaffold smoke screen, not UI work — Phase 4 owns real screens.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bun run check && bun run build && cd /home/thomaz/pessoal/apollo-v2 && for e in fundos projetos etapas tarefas templatesRotina instanciasRotina tickets subtarefas logInferenciaClaude; do grep -q "^    $e: i.entity(" shared/instant.schema.ts || { echo "MISSING ENTITY $e"; exit 1; }; done && for l in fundoProjetos fundoTemplatesRotina fundoTickets projetoEtapas etapaTarefas tarefaSubtarefas ticketSubtarefas templateInstancias templateAntecessor; do grep -q "$l" shared/instant.schema.ts || { echo "MISSING LINK $l"; exit 1; }; done && grep -q 'dedupeKey: i.string().unique().indexed()' shared/instant.schema.ts && [ "$(grep -v '^\s*//' shared/instant.schema.ts | grep -c 'donoId: i.string().indexed()')" = "9" ] && [ "$(grep -v '^\s*//' shared/instant.perms.ts | grep -c 'auth.id == data.donoId')" = "3" ] && [ "$(grep -v '^\s*//' shared/instant.perms.ts | grep -c ': donoRules')" = "9" ] && ! grep -v '^\s*//' shared/instant.perms.ts | grep -qE '(view|create|update|delete): "true"' && grep -q '\$users' shared/instant.perms.ts && echo SHOULD_NOT_HAPPEN || echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `shared/instant.schema.ts` contains all 9 entity declarations `fundos:`, `projetos:`, `etapas:`, `tarefas:`, `templatesRotina:`, `instanciasRotina:`, `tickets:`, `subtarefas:`, `logInferenciaClaude:` as `i.entity(` calls.
    - `grep -v '^\s*//' shared/instant.schema.ts | grep -c 'donoId: i.string().indexed()'` equals exactly 9.
    - `shared/instant.schema.ts` contains the exact string `dedupeKey: i.string().unique().indexed()`.
    - All 9 link names `fundoProjetos`, `fundoTemplatesRotina`, `fundoTickets`, `projetoEtapas`, `etapaTarefas`, `tarefaSubtarefas`, `ticketSubtarefas`, `templateInstancias`, `templateAntecessor` appear in `shared/instant.schema.ts`.
    - `grep -c 'onDelete' shared/instant.schema.ts` equals 0.
    - `grep -v '^\s*//' shared/instant.perms.ts | grep -c ': donoRules'` equals exactly 9, and the rule strings `auth.id != null && auth.id == data.donoId` / `… newData.donoId` each appear only inside the single `donoRules` constant.
    - `grep -v '^\s*//' shared/instant.perms.ts | grep -E '(view|create|update|delete): "true"'` returns nothing (exit non-zero).
    - `grep -c '\$users' shared/instant.perms.ts` equals 0.
    - `shared/instant.perms.ts` contains `attrs` with `create: "false"`.
    - `cd web && bun run check` exits 0 (proves the `../../../shared/instant.schema` import resolves and types).
    - `cd web && bun run build` exits 0.
  </acceptance_criteria>
  <done>The full 9-entity domain schema and the identical-across-entities donoId permission rules exist in `shared/`, type-check from `web/`, and the SPA client is bound to them.</done>
</task>

<task type="auto">
  <name>Task 3: Push schema and perms to the live InstantDB app and prove the round-trip server-side</name>
  <files>web/.instant-verify/ (throwaway, gitignored)</files>
  <read_first>
    - web/package.json (the `instant:push` / `instant:verify` scripts written in Task 1)
    - shared/instant.schema.ts, shared/instant.perms.ts (as written in Task 2)
    - .planning/phases/01-repo-scaffold-live-schema/01-RESEARCH.md §"Pattern 2" and §"Pitfall 2"
    - The `<environment_facts>` block above — instant-cli auth, flags, and the current empty state of the live app
  </read_first>
  <action>
Push the Task 2 artifacts to the live InstantDB app and verify the server actually accepted them. This is the tracer's closing leg: the claim "the schema is live" must be proven by reading state back from InstantDB, not by trusting the push command's stdout.

Run every command with cwd = `/home/thomaz/pessoal/apollo-v2/web`. instant-cli requires a project marker plus an installed `@instantdb/*` SDK in cwd; running it from the repo root or from `shared/` fails with an unhelpful module-resolution error (RESEARCH Pitfall 2).

Step 1 — push: run `bun run instant:push`. Confirm exit code 0 explicitly; do not judge success from stdout wording. If it fails on type checking against the server, do NOT reach for `--skip-check-types` — a type-check failure means the schema file is wrong and must be fixed in `shared/instant.schema.ts`.

Step 2 — idempotency: run `bun run instant:push` a second time. It must exit 0 again. InstantDB schema push is additive and convergent, so the second run is a no-op; a non-zero exit or a reported destructive change means the schema file is unstable and must be investigated before the phase closes.

Step 3 — server round-trip: run `bun run instant:verify`, which pulls the LIVE schema and perms into `web/.instant-verify/` (never into `shared/` — `instant:pull` would clobber the source). Assert the pulled `web/.instant-verify/instant.schema.ts` contains all 9 domain entity names and that the pulled `web/.instant-verify/instant.perms.ts` contains the `auth.id == data.donoId` and `auth.id == newData.donoId` rule strings.

Step 4 — entity liveness: run an admin InstaQL query naming all 9 entities in one shot (`instant-cli query '{"fundos":{},"projetos":{},"etapas":{},"tarefas":{},"templatesRotina":{},"instanciasRotina":{},"tickets":{},"subtarefas":{},"logInferenciaClaude":{}}' --env ../.env.instantdb`). Every one of the 9 keys must appear in the JSON response with an empty array value. An entity that never landed would surface as a query error.

Step 5 — perms enforcement: run the same query for `fundos` with `--as-guest` (verified to exist on `instant-cli query`). This must NOT return a successful empty result set — it must be rejected by the live permission rules, because `auth.id != null` is false for a guest. This is the single strongest Phase 1 signal that the rules are actually enforced server-side rather than merely uploaded. Capture the exact rejection output verbatim into the SUMMARY so Phase 6's VERIFY-05 has a baseline to compare against.

Do not create any records in the live app. Phase 1 writes zero domain data; the app must remain empty for Phases 3-6 to test against a clean slate.

`web/.instant-verify/` is a disposable artifact and is already gitignored from Task 1 — leave it in place for inspection, do not commit it.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bun run instant:push && bun run instant:push && bun run instant:verify && for e in fundos projetos etapas tarefas templatesRotina instanciasRotina tickets subtarefas logInferenciaClaude; do grep -q "$e" .instant-verify/instant.schema.ts || { echo "NOT LIVE: $e"; exit 1; }; done && grep -q 'auth.id == data.donoId' .instant-verify/instant.perms.ts && grep -q 'auth.id == newData.donoId' .instant-verify/instant.perms.ts && bunx instant-cli query '{"fundos":{},"projetos":{},"etapas":{},"tarefas":{},"templatesRotina":{},"instanciasRotina":{},"tickets":{},"subtarefas":{},"logInferenciaClaude":{}}' --env ../.env.instantdb > /tmp/apollo-live-query.json && for e in fundos projetos etapas tarefas templatesRotina instanciasRotina tickets subtarefas logInferenciaClaude; do grep -q "\"$e\"" /tmp/apollo-live-query.json || { echo "QUERY MISSING: $e"; exit 1; }; done && ! bunx instant-cli query '{"fundos":{}}' --env ../.env.instantdb --as-guest > /tmp/apollo-guest-query.json 2>&1 && echo TASK3_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && bun run instant:push` exits 0.
    - A second consecutive `cd web && bun run instant:push` also exits 0 (idempotent).
    - `cd web && bun run instant:verify` exits 0 and writes `web/.instant-verify/instant.schema.ts` and `web/.instant-verify/instant.perms.ts`.
    - All 9 domain entity names appear in `web/.instant-verify/instant.schema.ts` (pulled from the live server).
    - `web/.instant-verify/instant.perms.ts` contains both `auth.id == data.donoId` and `auth.id == newData.donoId`.
    - The 9-entity admin InstaQL query returns a JSON object containing all 9 entity keys.
    - `bunx instant-cli query '{"fundos":{}}' --env ../.env.instantdb --as-guest` exits NON-ZERO (guest access is denied by the live rules).
    - `git status --porcelain shared/instant.schema.ts shared/instant.perms.ts` shows no unexpected rewrite of the source files by any pull command.
  </acceptance_criteria>
  <done>The 9-entity schema and the donoId permission rules are live on the InstantDB app, confirmed by pulling them back from the server, by an admin query resolving all 9 entities, and by a guest query being denied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → git history/remote | `.env.instantdb` holds `INSTANT_APP_ADMIN_TOKEN`; anything committed is permanently disclosed |
| `web/vite.config.ts` build step → shipped `dist/` bundle | Everything injected via `define` becomes world-readable JavaScript |
| browser SPA (untrusted client) → InstantDB | The ONLY enforcement point is `shared/instant.perms.ts` evaluated server-side |
| local dev/ops tooling (`instant-cli`) → live InstantDB app | Admin-token-authenticated; bypasses all permission rules by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.instantdb` | high | mitigate | Already covered by the `.env.*` line in the root `.gitignore` (verified `git check-ignore -v .env.instantdb` during planning). Plan 03 adds an explicit `.env.instantdb` line plus a `git ls-files --error-unmatch` tracking assertion to the closeout gate. Never echo either value into a SUMMARY, log, or tracked file. |
| T-01-02 | Information Disclosure | `web/vite.config.ts` → `web/dist/` | high | mitigate | Task 1 parses the env file with `dotenv.parse` into a local constant, never touches `process.env`, never uses `loadEnv`/`envPrefix`, and defines exactly one key (`import.meta.env.VITE_INSTANT_APP_ID`). Gate: build `dist/` and assert the literal `INSTANT_APP_ADMIN_TOKEN` value is absent (`! grep -rF "$TOKEN" dist/`). |
| T-01-03 | Elevation of Privilege | `shared/instant.perms.ts` | high | mitigate | Rules are authored once as the `donoRules` constant and referenced 9 times, eliminating per-entity typo drift (a single `dono_id` typo would silently open cross-user access). Gates: exactly 9 `: donoRules` references, zero `"true"` rule literals, and a live `--as-guest` query that must be denied. |
| T-01-04 | Tampering | InstantDB attrs (client-created attributes) | medium | mitigate | `shared/instant.perms.ts` sets `attrs: { allow: { create: "false" } }`, blocking clients from minting new attributes at runtime and forcing all schema evolution through `instant-cli push`. |
| T-01-05 | Spoofing | `web/src/lib/db.ts` | high | mitigate | The SPA client is constructed with `appId` only. No admin token is imported, referenced, or reachable from any file under `web/src/`. Enforced by the T-01-02 bundle grep. |
| T-01-06 | Information Disclosure | `instant-cli` credential scope | low | accept | The app admin token grants full access to this one app only, and lives on a single-user local machine with no shared CI runner (PROJECT.md single-user posture). Rotating it is a dashboard operation if ever needed. |
| T-01-07 | Repudiation | schema/perms push history | low | accept | No audit trail exists for who pushed what. Acceptable: single operator, and `shared/*.ts` is version-controlled so every pushed shape is reconstructable from git. |
</threat_model>

<verification>
1. `cd web && bun install && bun run build && bun run check` — all exit 0.
2. `web/dist/` contains the app id and does not contain the admin token.
3. `shared/instant.schema.ts` — 9 entities, 9 links, 9 indexed `donoId` fields, unique+indexed `dedupeKey`.
4. `shared/instant.perms.ts` — one `donoRules` constant referenced 9 times, no `"true"` literals, no `$users` override, `attrs.create` = `"false"`.
5. `bun run instant:push` exits 0 twice in a row.
6. `bun run instant:verify` pulls live state containing all 9 entities and both donoId rule strings.
7. Admin InstaQL query resolves all 9 entities; `--as-guest` query on `fundos` is denied.
</verification>

<success_criteria>
- SETUP-03 satisfied: `web/` installs and builds as a bun-managed, SvelteKit-free Svelte 5 + Vite SPA.
- SETUP-06 satisfied: the SPA runtime authenticates to InstantDB using only the app id from `.env.instantdb`; no admin token reaches the client.
- SETUP-07 satisfied: `shared/instant.schema.ts` defines all 8 domain entities plus `logInferenciaClaude` with their links and is live on InstantDB.
- SETUP-08 satisfied: `shared/instant.perms.ts` defines the identical donoId rules across all domain entities and is live, with guest access provably denied.
- SETUP-01 partially satisfied: `shared/` and `web/` exist per the locked layout (full layout gate closes in plan 03).
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan:

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `shared/instant.schema.ts` | TS module — `export default schema`, `export type AppSchema` | `web/src/lib/db.ts`, `instant-cli push`, Phases 3-6 |
| `shared/instant.perms.ts` | TS module — `export default rules` (`donoRules` constant) | `instant-cli push`, Phase 6 VERIFY-05 |
| `web/package.json` | Bun manifest — scripts `dev`, `build`, `preview`, `check`, `instant:push`, `instant:pull`, `instant:verify` | Plan 03, Phase 4 |
| `web/vite.config.ts` | Vite config — `.env.instantdb` parse + single-key `define` | Phase 4 build |
| `web/svelte.config.js` | Svelte config (the one permitted `.js` file, config-only) | `@sveltejs/vite-plugin-svelte`, `svelte-check` |
| `web/tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json` | TS project references; `tsconfig.app.json` includes `../shared/*.ts` | `svelte-check`, plan 03 Biome gate |
| `web/src/lib/db.ts` | Exports `db`, `id`, `lookup` | Phase 4 screens, Phase 5 job |
| `web/src/main.ts`, `web/src/App.svelte`, `web/src/app.css` | SPA entrypoint + smoke screen | Phase 4 replaces `App.svelte` |
| `web/src/vite-env.d.ts` | `ImportMetaEnv.VITE_INSTANT_APP_ID` declaration | all `web/` TS |
| `web/.gitignore` | ignores `node_modules/`, `dist/`, `.instant-verify/`, `.svelte-kit/` | plan 03 closeout |
| Live InstantDB app schema + perms | External state (not a file) | every later phase |

Named symbols introduced: `schema` / `AppSchema` (shared/instant.schema.ts); `donoRules` / `rules` (shared/instant.perms.ts); `db`, `id`, `lookup` (web/src/lib/db.ts).
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/01-repo-scaffold-live-schema/01-01-SUMMARY.md`.

Record in the SUMMARY: the verbatim `--as-guest` rejection output (Phase 6 VERIFY-05 baseline); the `web/svelte.config.js` `.js` exception and its rationale; the `templatesRotina.ativo` field addition; and the confirmed fact that `instant-cli` needs no personal login. Never record the app id or admin token values.
</output>
