---
phase: 01-repo-scaffold-live-schema
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - biome.json
  - web/package.json
  - .gitignore
  - README.md
  - .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh
autonomous: true
requirements: [SETUP-01, SETUP-05]
user_setup: []

must_haves:
  truths:
    - "`cd web && bun run lint` exits 0 with zero Biome findings across `web/src/**/*.ts`, `web/vite.config.ts`, and `shared/*.ts`."
    - "`cd web && bun run format:check` exits 0 — every checked file is already canonically formatted."
    - "`cd web && bun run check` exits 0 — `svelte-check` and `tsc -p tsconfig.node.json` report zero errors."
    - "No `.js` file exists anywhere under `web/src/` or `shared/` (C-08 bans `.js` frontend logic); the only permitted `.js` file in the repo is `web/svelte.config.js`, which the Svelte toolchain requires to be loadable by bare Node."
    - "Biome's configuration covers `shared/*.ts` as well as `web/`, so the schema and perms files cannot drift out of the formatter/linter's reach."
    - "The locked monorepo layout exists: `shared/`, `web/`, `cli/`, and `.env.instantdb` are all present at the repo root."
    - "`.env.instantdb` is gitignored AND untracked: `git check-ignore -q .env.instantdb` exits 0 and `git ls-files --error-unmatch .env.instantdb` exits non-zero."
    - "No file tracked by git contains the literal value of `INSTANT_APP_ADMIN_TOKEN`."
    - "A single script, `verify-phase-01.sh`, re-runs every SETUP-01 through SETUP-08 gate in sequence and exits 0."
    - "The repo has a root `README.md` documenting the three-package layout and the exact quality-gate command for each half."
    - statement: "Biome's Svelte support is flagged experimental upstream; the `.svelte` files in the scaffold pass `bun run lint` without a false positive forcing a rule exclusion."
      verification: backstop
  artifacts:
    - path: "biome.json"
      provides: "Single formatter + linter configuration covering both shared/ and web/"
      contains: "shared/**/*.ts"
    - path: ".planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh"
      provides: "One-command re-run of all 8 SETUP gates"
    - path: "README.md"
      provides: "Repo-root orientation: layout, per-package commands, quality gates, env file contract"
    - path: ".gitignore"
      provides: "Explicit `.env.instantdb` ignore plus per-package build artefact ignores"
  key_links:
    - from: "web/package.json lint script"
      to: "biome.json at the repo root"
      via: "biome --config-path=.. with explicit ../shared and ./src path arguments"
      pattern: "--config-path"
    - from: "verify-phase-01.sh"
      to: "cli/ and web/ gate commands"
      via: "sequential invocation of uv run and bun run gates"
      pattern: "uv run ty check"
  prohibitions:
    - "MUST NOT reach a green gate by narrowing what the gate looks at. Dropping `shared/*.ts` from Biome's paths, excluding `.svelte` from the linter without recording the deviation, or trimming `verify-phase-01.sh` all produce a passing phase whose green means nothing to Phases 2-6 — the exact silent failure the whole quality-gate constraint (C-08) exists to prevent."
    - "MUST NOT introduce a second JS package manager or executor. C-08 makes `bun` the sole JS/TS executor; a stray `npm install` or `package-lock.json` splits the dependency tree and makes the lockfile a lie about what actually runs."
  assumptions_flagged:
    - "RESEARCH assumption A4 (Biome's experimental Svelte support may false-positive on `.svelte`) is carried into this plan with a bounded, recorded escape hatch rather than being assumed away — see Task 1."
    - "Canon-referral (dropped, tracked as threat T-01-01 in plan 01 and re-gated here): secret-in-git detection is standard secret-management canon."
---

<objective>
Close Phase 1: give `web/` its formatter and linter, then prove the entire phase — all eight SETUP requirements — with one re-runnable script.

Purpose: Plans 01 and 02 each proved their own half. This plan installs the one piece C-08 still requires (a `web/` formatter + linter), makes it cover `shared/` too so the schema and perms files cannot drift out of reach, and collapses the phase's evidence into a single command that Phase 6 can re-run verbatim.
Output: `biome.json`, working `lint`/`format` scripts, a root `README.md`, hardened `.gitignore`, and `verify-phase-01.sh` exiting 0.

Runs in wave 2 because it modifies `web/package.json` (owned by plan 01 in wave 1) and its closeout gate exercises both halves.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-scaffold-live-schema/01-RESEARCH.md
@.planning/phases/01-repo-scaffold-live-schema/01-01-SUMMARY.md
@.planning/phases/01-repo-scaffold-live-schema/01-02-SUMMARY.md
</context>

<environment_facts>
Verified live on 2026-08-09 during planning — treat as established, do not re-discover:

- `@biomejs/biome` resolves to 2.5.7 and its CLI supports `--config-path=PATH`, where PATH may be the directory containing `biome.json`. This is what lets a repo-root `biome.json` govern a run invoked from `web/`.
- Biome's Svelte/HTML support requires the explicit `html.experimentalFullSupportEnabled` flag and is described upstream as not yet covering newer/rare syntax. Treat `svelte-check` as the authority for `.svelte` correctness.
- `/home/thomaz/pessoal/apollo-v2/.gitignore` currently contains: `node_modules/`, `dist/`, `.env`, `.env.*`, `!.env.example`, `.venv/`, `__pycache__/`, `*.pyc`, `.DS_Store`. The `.env.*` line already covers `.env.instantdb` (confirmed via `git check-ignore -v`).
- `.env.instantdb` defines `NEXT_PUBLIC_INSTANT_APP_ID` and `INSTANT_APP_ADMIN_TOKEN`. Never print either value into a tracked file or a SUMMARY.
- `uv` 0.9.21 and `bun` 1.3.12 are installed; system `python3` is 3.10 and must never be invoked directly.
</environment_facts>

<interfaces>
Contracts established by the prior plans that this plan consumes:

From plan 01 (`web/package.json`): scripts `dev`, `build`, `preview`, `check`, `instant:push`, `instant:pull`, `instant:verify`. Devdependency `@biomejs/biome` is already installed. `web/tsconfig.app.json` already includes `../shared/*.ts`.

From plan 02 (`cli/`): gate commands `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check`, plus `uv run apollo --help` / `doctor`.

Contract this plan adds to `web/package.json`:
- `lint` — Biome check (lint + format verification) over `../shared`, `./src`, `./vite.config.ts`
- `lint:fix` — the same with `--write`
- `format:check` — Biome formatter-only verification over the same paths
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Configure Biome at the repo root to cover both shared/ and web/, and drive it to zero findings</name>
  <files>biome.json, web/package.json</files>
  <read_first>
    - web/package.json, web/tsconfig.app.json, web/vite.config.ts, web/src/App.svelte, web/src/lib/db.ts (as written by plan 01)
    - shared/instant.schema.ts, shared/instant.perms.ts (as written by plan 01)
    - .planning/PROJECT.md constraint C-08
    - .planning/phases/01-repo-scaffold-live-schema/01-RESEARCH.md §"Pitfall 3" and assumption A4 (Biome + Svelte experimental status)
  </read_first>
  <action>
Create `biome.json` at the REPO ROOT, not inside `web/`. It must govern `shared/*.ts` as well as `web/`, and Biome resolves relative `files.includes` patterns against its config directory — a `web/biome.json` could not reach `../shared` cleanly. This placement is what stops the two most security-relevant files in the project, `shared/instant.schema.ts` and `shared/instant.perms.ts`, from silently sitting outside the formatter and linter.

`biome.json` contents:
- `$schema` pointing at `https://biomejs.dev/schemas/2.5.7/schema.json`
- `files.includes`: `shared/**/*.ts`, `web/src/**/*.ts`, `web/src/**/*.svelte`, `web/vite.config.ts`
- `files.ignoreUnknown`: true
- `html.experimentalFullSupportEnabled`: true (required for Biome to parse `.svelte` at all)
- `formatter`: enabled, `indentStyle: "space"`, `indentWidth: 2`, `lineWidth: 100` (matching the 100-column line length chosen for `cli/` in plan 02, so both halves of the monorepo read the same way)
- `linter`: enabled with `rules.recommended: true`
- `vcs`: `enabled: true`, `clientKind: "git"`, `useIgnoreFile: true` so Biome honours `.gitignore` and never walks `node_modules/`, `dist/`, or `web/.instant-verify/`

Add to `web/package.json` scripts (Biome is already a `web/` devDependency from plan 01, so the binary resolves from `web/node_modules/.bin`):
- `lint` = `biome check --config-path=.. ../shared ./src ./vite.config.ts`
- `lint:fix` = `biome check --write --config-path=.. ../shared ./src ./vite.config.ts`
- `format:check` = `biome format --config-path=.. ../shared ./src ./vite.config.ts`

Run `bun run lint:fix` once to normalise formatting, then drive `bun run lint` and `bun run format:check` to exit 0. Fix findings in the source files. Do NOT silence a finding by removing a path from the script arguments or from `files.includes` — that is the prohibition above.

Bounded escape hatch for RESEARCH assumption A4: if Biome's experimental Svelte parser produces a finding on `web/src/App.svelte` that is demonstrably a parser false positive (the same code is clean under `svelte-check` and the rule's documented intent does not apply), then — and only then — remove `web/src/**/*.svelte` from `linter.includes` while KEEPING it in `formatter.includes`, so `.svelte` files are still formatted and `svelte-check` remains the type/logic authority. Record the exact diagnostic verbatim and the reason in the SUMMARY. Never disable the linter wholesale, never delete the `.svelte` pattern from `files.includes`, and never resort to `// biome-ignore` comments to pass the gate.

Also verify and enforce the "no `.js` frontend logic" half of C-08: confirm that no `.js` file exists under `web/src/` or `shared/`. `web/svelte.config.js` is the single documented exception (the Svelte toolchain loads it through bare Node, so it cannot be `.ts`) and lives outside `src/`.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/web && bun run lint && bun run format:check && bun run check && cd /home/thomaz/pessoal/apollo-v2 && test -f biome.json && grep -q 'shared/\*\*/\*.ts' biome.json && grep -q 'useIgnoreFile' biome.json && [ -z "$(find web/src shared -name '*.js' -not -path '*/node_modules/*' 2>/dev/null)" ] && ! grep -rq 'biome-ignore' web/src shared && ! test -f web/package-lock.json && ! test -f web/yarn.lock && ! test -f web/pnpm-lock.yaml && echo TASK1_OK</automated>
  </verify>
  <acceptance_criteria>
    - `biome.json` exists at the repo root (not in `web/`) and its `files.includes` contains `shared/**/*.ts`, `web/src/**/*.ts`, `web/src/**/*.svelte`, and `web/vite.config.ts`.
    - `biome.json` sets `vcs.useIgnoreFile` to true.
    - `cd web && bun run lint` exits 0 with zero findings.
    - `cd web && bun run format:check` exits 0.
    - `cd web && bun run check` still exits 0 (Biome's formatting did not break `svelte-check` or `tsc`).
    - `find web/src shared -name '*.js' -not -path '*/node_modules/*'` returns nothing.
    - `grep -r 'biome-ignore' web/src shared` returns nothing.
    - `web/package-lock.json`, `web/yarn.lock`, and `web/pnpm-lock.yaml` all do not exist (bun remains the sole executor).
    - If the A4 escape hatch was used, `biome.json` still lists `web/src/**/*.svelte` under `files.includes` and under the formatter, and the SUMMARY quotes the triggering diagnostic verbatim.
  </acceptance_criteria>
  <done>A single repo-root Biome configuration formats and lints every TypeScript file in `shared/` and `web/`, runs clean, and coexists with `svelte-check` without either gate being narrowed.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the ignore rules, document the repo, and gate all eight SETUP requirements in one script</name>
  <files>.gitignore, README.md, .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh</files>
  <read_first>
    - /home/thomaz/pessoal/apollo-v2/.gitignore (current contents listed in `<environment_facts>`)
    - .planning/REQUIREMENTS.md §Setup (SETUP-01 through SETUP-08 — the exact wording each gate must satisfy)
    - .planning/ROADMAP.md §"Phase 1" Success Criteria (the 5 criteria this script must collectively prove)
    - .planning/phases/01-repo-scaffold-live-schema/01-01-SUMMARY.md and 01-02-SUMMARY.md (the commands each plan actually established)
    - .planning/PROJECT.md constraints C-01, C-05, C-08
  </read_first>
  <action>
Harden `.gitignore` at the repo root. Keep the existing lines and add: an explicit `.env.instantdb` line (belt-and-braces alongside the existing `.env.*` glob, so a future edit to that glob cannot silently expose the file), `web/.instant-verify/`, `.instant-verify/`, `**/node_modules/`, `**/dist/`, `.ruff_cache/`, `.ty_cache/`, and `*.egg-info/`. Do NOT add `uv.lock` or `bun.lock` — both lockfiles must be tracked for reproducibility.

Write the repo-root `README.md`. It documents, concretely:
- The three-package layout (`shared/`, `web/`, `cli/`) and what each owns, restating that `shared/` is the single place both runtimes read domain schema and calendar data from (C-01).
- Setup commands: `cd cli && uv sync`, `cd web && bun install`.
- The quality gates, verbatim, as the definition of "a file is done" (C-08): for `cli/` — `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check`; for `web/` — `bun run lint`, `bun run format:check`, `bun run check`.
- The schema workflow: edit `shared/instant.schema.ts` / `shared/instant.perms.ts`, then `cd web && bun run instant:push`; `bun run instant:verify` pulls live state into `web/.instant-verify/` for diffing; `bun run instant:pull` overwrites `shared/` and is deliberate-use-only.
- The `.env.instantdb` contract: it is gitignored and must never be committed; `NEXT_PUBLIC_INSTANT_APP_ID` is the only value the SPA and CLI runtimes use; `INSTANT_APP_ADMIN_TOKEN` exists solely for `instant-cli` schema/perms pushes and is never read by application code (C-05). Do NOT write either value into the README.
- A one-line note that `bun` is the only JS/TS executor and that all frontend logic is `.ts`, with `web/svelte.config.js` as the single toolchain-mandated `.js` exception.

Write `.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` as an executable `bash` script with `set -euo pipefail`, resolving the repo root from its own location so it runs from anywhere. It must echo a labelled line per requirement and run these checks in order, failing fast:
- SETUP-01: `shared/`, `web/`, `cli/` directories and `.env.instantdb` all exist; `shared/instant.schema.ts`, `shared/instant.perms.ts`, `web/src/lib/bizdays.ts`'s parent dir `web/src/lib/`, and `cli/apollo_cli/` exist per the C-01 tree. Additionally assert `git check-ignore -q .env.instantdb` exits 0 and `git ls-files --error-unmatch .env.instantdb` exits NON-zero (present but never tracked), and that the literal `INSTANT_APP_ADMIN_TOKEN` value does not appear in any git-tracked file (iterate `git ls-files` with `grep -lF`).
- SETUP-02: `cd cli && uv sync && uv run apollo --help` and `uv run apollo --version` both exit 0.
- SETUP-03: `cd web && bun install && bun run build` exit 0; `test ! -d web/src/routes`; `@sveltejs/kit` absent from `web/package.json`.
- SETUP-04: `cd cli && uv run ruff check . && uv run ruff format --check . && uv run ty check` all exit 0.
- SETUP-05: `cd web && bun run lint && bun run format:check && bun run check` all exit 0; no `.js` under `web/src/` or `shared/`.
- SETUP-06: `cd cli && uv run apollo doctor` exits 0; the built `web/dist/` contains the app id value and does NOT contain the admin token value (read both from `.env.instantdb` into shell variables; never echo them).
- SETUP-07: `cd web && bun run instant:verify` exits 0 and the pulled `web/.instant-verify/instant.schema.ts` contains all 9 entity names.
- SETUP-08: the pulled `web/.instant-verify/instant.perms.ts` contains `auth.id == data.donoId` and `auth.id == newData.donoId`; and `bunx instant-cli query '{"fundos":{}}' --env ../.env.instantdb --as-guest` exits NON-zero (live rules deny unauthenticated access).

The script must print `PHASE 01 VERIFIED` as its final line on success. It must never echo the app id or the admin token — compare with `grep -qF "$VAR"` and report only pass/fail. Make it executable (`chmod +x`).

Run the script and make it pass. If any check fails, fix the underlying repo, not the script.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2 && test -x .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh && bash .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh | tail -1 | grep -q 'PHASE 01 VERIFIED' && grep -q '^\.env\.instantdb$' .gitignore && git check-ignore -q .env.instantdb && ! git ls-files --error-unmatch .env.instantdb >/dev/null 2>&1 && TOKEN=$(grep '^INSTANT_APP_ADMIN_TOKEN=' .env.instantdb | cut -d= -f2-) && [ -z "$(git ls-files -z | xargs -0 grep -lF "$TOKEN" 2>/dev/null)" ] && test -f README.md && grep -q 'uv run ty check' README.md && grep -q 'bun run lint' README.md && grep -q 'instant:push' README.md && ! grep -qF "$TOKEN" README.md && echo TASK2_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a literal `.env.instantdb` line in addition to the existing `.env.*` glob, plus `web/.instant-verify/`, `.ruff_cache/`, `.ty_cache/`.
    - `.gitignore` does NOT ignore `cli/uv.lock` or `web/bun.lock`.
    - `git check-ignore -q .env.instantdb` exits 0 AND `git ls-files --error-unmatch .env.instantdb` exits non-zero.
    - `git ls-files -z | xargs -0 grep -lF "$INSTANT_APP_ADMIN_TOKEN_VALUE"` returns no files.
    - `README.md` exists at the repo root and contains all six gate commands (`uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check`, `bun run lint`, `bun run format:check`, `bun run check`) plus `instant:push`.
    - `README.md` contains neither the app id value nor the admin token value.
    - `.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` is executable and its output contains a labelled line for each of SETUP-01 through SETUP-08.
    - `bash verify-phase-01.sh` exits 0 and its final line is exactly `PHASE 01 VERIFIED`.
    - `verify-phase-01.sh` output contains neither credential value.
  </acceptance_criteria>
  <done>The repo is documented, secrets are provably untracked, and every one of the eight SETUP requirements is re-provable by a single command that Phase 6 can rerun unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index/history | Once a secret is committed it is permanently disclosed, regardless of later deletion |
| repo documentation (`README.md`, SUMMARY files) → shared/AI-readable context | Documentation is tracked; a credential pasted "as an example" is a committed secret |
| quality-gate configuration → what actually gets checked | A gate that excludes files is indistinguishable from a passing gate at the exit-code level |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01b | Information Disclosure | `.env.instantdb` in git | high | mitigate | Task 2 adds an explicit `.env.instantdb` line to `.gitignore` (independent of the `.env.*` glob) and the closeout script asserts both `git check-ignore -q` success and `git ls-files --error-unmatch` failure. Additionally, every git-tracked file is scanned for the literal admin token value. |
| T-01-11 | Information Disclosure | `README.md` / SUMMARY files | medium | mitigate | Task 2 explicitly forbids writing either credential value into the README, and the gate asserts the token value is absent from it. `verify-phase-01.sh` compares with `grep -qF "$VAR"` and reports pass/fail only, never echoing the value into autonomous-run logs. |
| T-01-12 | Tampering | quality-gate scope narrowing | medium | mitigate | `biome.json` lives at the repo root with `shared/**/*.ts` in `files.includes`, and the closeout gate asserts that pattern is present. The A4 escape hatch is bounded to `linter.includes` only, must keep `.svelte` in `files.includes` and in the formatter, and must be recorded verbatim in the SUMMARY. |
| T-01-13 | Elevation of Privilege | live perms regression | high | mitigate | `verify-phase-01.sh` SETUP-08 re-runs the `--as-guest` denial probe on every invocation, so any future push that accidentally loosens the rules fails the phase gate rather than passing silently. |
| T-01-14 | Tampering | second JS package manager | low | mitigate | Gate asserts `package-lock.json`, `yarn.lock`, and `pnpm-lock.yaml` are all absent from `web/`, keeping `bun.lock` the single truth about what runs (C-08). |
</threat_model>

<verification>
1. `cd web && bun run lint`, `bun run format:check`, and `bun run check` all exit 0.
2. `biome.json` at the repo root covers `shared/**/*.ts`; no `.js` under `web/src/` or `shared/`; no `biome-ignore` comments; no non-bun lockfile in `web/`.
3. `.gitignore` explicitly ignores `.env.instantdb`; the file is untracked; no tracked file contains the admin token value.
4. `README.md` documents the layout, all six gate commands, the schema push workflow, and the env contract — with no credential values.
5. `bash .planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` exits 0 with final line `PHASE 01 VERIFIED`, covering SETUP-01 through SETUP-08 including the live `--as-guest` denial probe.
</verification>

<success_criteria>
- SETUP-05 satisfied: Biome (formatter + linter) and `svelte-check` are configured for `web/` — and for `shared/` — and all exit 0.
- SETUP-01 satisfied: the locked monorepo layout is present and verified, with `.env.instantdb` present, gitignored, and untracked.
- All five ROADMAP Phase 1 success criteria are provable by a single command.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan:

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `biome.json` (repo root) | Formatter + linter config covering `shared/**/*.ts` and `web/` | Phase 4 SPA work, Phase 6 VERIFY-03 |
| `web/package.json` scripts `lint`, `lint:fix`, `format:check` | Bun script contract | Phase 4, Phase 6 VERIFY-03 |
| `README.md` (repo root) | Layout, setup, gate commands, schema push workflow, env contract | developer + every later phase |
| `.gitignore` (hardened) | Explicit `.env.instantdb`, `.instant-verify/`, tool caches | whole repo |
| `.planning/phases/01-repo-scaffold-live-schema/verify-phase-01.sh` | Executable one-command gate for SETUP-01..08 incl. the live guest-denial probe | `/gsd:verify-work`, Phase 6 VERIFY-02/03/05 |

Named symbols introduced: none (configuration, documentation, and shell tooling only).
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/01-repo-scaffold-live-schema/01-03-SUMMARY.md`.

Record in the SUMMARY: whether the RESEARCH A4 Biome/Svelte escape hatch was needed (and if so, the verbatim diagnostic that triggered it); the final `verify-phase-01.sh` output with all eight SETUP lines; and an explicit statement that the phase's five ROADMAP success criteria are each backed by a named check in that script. Never record the app id or admin token values.
</output>
