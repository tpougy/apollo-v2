---
phase: 04-web-spa-auth-crud-smoke-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/playwright.config.ts
  - web/tsconfig.e2e.json
  - web/src/App.svelte
  - web/src/lib/auth/LoginScreen.svelte
  - web/e2e/helpers/magic-code.ts
  - web/e2e/auth.setup.ts
  - web/e2e/auth.spec.ts
  - web/e2e/no-leakage.spec.ts
  - biome.json
  - .gitignore
autonomous: true
requirements: [WEB-01, WEB-10]
user_setup: []

must_haves:
  truths:
    - "A user landing on the SPA while unauthenticated sees the magic-code login screen and zero entity data."
    - "Submitting a real email address sends a real InstantDB magic-code email to tp@rbrasset.com.br."
    - "Submitting the real code received in that email authenticates the SPA session in a real Chromium browser."
    - "Reloading the page after login keeps the user authenticated (no login-screen flash, no re-auth)."
    - "A failed/expired code shows an error message and leaves the user on the login screen."
  artifacts:
    - path: "web/src/lib/auth/LoginScreen.svelte"
      provides: "Two-step magic-code form (email step -> code step) calling db.auth.sendMagicCode / db.auth.signInWithMagicCode"
      contains: "signInWithMagicCode"
    - path: "web/src/App.svelte"
      provides: "SignedOut -> LoginScreen, SignedIn -> authenticated shell region gate"
      contains: "SignedOut"
    - path: "web/playwright.config.ts"
      provides: "Headless Chromium config with webServer + setup/authed/anon projects"
      contains: "webServer"
    - path: "web/e2e/helpers/magic-code.ts"
      provides: "Outlook Classic COM peek helper that extracts the numeric code from the InstantDB email subject line (C-10)"
      contains: "orules.ps1"
    - path: "web/e2e/auth.setup.ts"
      provides: "Real magic-code round trip in Chromium; persists storageState for downstream specs"
      contains: "storageState"
    - path: "web/e2e/no-leakage.spec.ts"
      provides: "Unauthenticated-load assertion: login screen present, zero entity data in DOM"
  key_links:
    - from: "web/src/App.svelte"
      to: "web/src/lib/auth/LoginScreen.svelte"
      via: "SignedOut children snippet"
      pattern: "LoginScreen"
    - from: "web/src/lib/auth/LoginScreen.svelte"
      to: "web/src/lib/db.ts"
      via: "db.auth.sendMagicCode / db.auth.signInWithMagicCode"
      pattern: "db\\.auth\\.(sendMagicCode|signInWithMagicCode)"
    - from: "web/e2e/auth.setup.ts"
      to: "web/e2e/helpers/magic-code.ts"
      via: "readLatestMagicCode() called between sendMagicCode and code submission"
      pattern: "readLatestMagicCode"
  edge_coverage:
    - "WEB-01 / empty email submitted: form blocks submit, no network call issued."
    - "WEB-01 / wrong or expired code submitted: InstantDB error surfaced in the login screen, user stays signed out, form stays on the code step so a resend/retry is possible."
    - "WEB-01 / page reload immediately after sign-in: still authenticated (isLoading tick handled, no login flash)."
    - "WEB-01 / brand-new browser context restored from saved storageState (incl. IndexedDB): authenticated without a second magic code."
    - "WEB-10 / cold load with no stored session at all: only the login screen renders; no entity table, no entity row text, no request to InstantDB's query endpoint carrying app data."
  prohibitions:
    - "No hand-written localStorage/IndexedDB session read/write code anywhere in web/src (the SDK owns session persistence — C-05)."
    - "No mocked, stubbed, or hard-coded magic code in any spec — the code MUST come from the real inbox via the C-10 orules.ps1 peek channel."
    - "No reference to INSTANT_APP_ADMIN_TOKEN or @instantdb/admin anywhere under web/src or web/e2e."
    - "No data-bearing component rendered outside a SignedIn gate."
    - "web/e2e/.auth/ (real session tokens) is never committed to git."
---

<objective>
Establish the browser-side auth foundation for Phase 4 and prove it with a real magic-code
email round trip driven by a real headless Chromium instance.

This is the TRACER plan: nothing else in Phase 4 can be proven in a browser until Playwright
is installed, the dev server is drivable headlessly, and a real authenticated session can be
minted and reused by downstream specs.

Purpose: WEB-01 (magic-code login + session persistence) and WEB-10 (no data leakage while
unauthenticated) are the two requirements that cannot be inferred from the CLI's Phase 3
proof — they are browser behaviours and must be demonstrated in a browser.
Output: Playwright harness, `LoginScreen.svelte`, auth-gated `App.svelte`, three committed
e2e specs, and a saved `storageState` artifact every later plan's specs reuse.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-CONTEXT.md

<interfaces>
<!-- Verified API surface from web/node_modules/@instantdb/svelte (04-RESEARCH.md, HIGH confidence). -->
<!-- Use these directly. Do NOT explore node_modules to re-derive them. -->

From `@instantdb/svelte` (re-exported by `web/src/lib/db.ts` as `db`):
```typescript
db.auth.sendMagicCode({ email: string }): Promise<{ sent: true }>;
db.auth.signInWithMagicCode({ email: string, code: string }): Promise<{ user: User }>;
db.auth.signOut(): Promise<void>;
db.useAuth(): { isLoading: boolean; user: User | null; error: { message: string } | null };  // reactive object, NOT a hook
```

From `@instantdb/svelte` (Svelte 5 components, `children` is a Snippet):
```typescript
// <SignedIn {db}> ... </SignedIn>   renders children only when !isLoading && !error && user
// <SignedOut {db}> ... </SignedOut> renders children only when !isLoading && !error && !user
// During the initial isLoading tick NEITHER renders — this is correct and prevents a login flash.
```

Error shape thrown by both auth calls (see 04-RESEARCH.md Pattern 1):
```typescript
(err as { body?: { message?: string } }).body?.message
```

Existing app id plumbing (web/vite.config.ts, do not change):
```typescript
import.meta.env.VITE_INSTANT_APP_ID   // string, injected via define: from ../.env.instantdb
```
</interfaces>

Current SPA entry point (will be replaced by the auth gate in Task 2):
@web/src/App.svelte
@web/src/lib/db.ts
@web/package.json
@web/vite.config.ts
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright, wire the headless harness, and add the C-10 magic-code reader</name>
  <read_first>
    - web/package.json (existing scripts: dev, build, check, lint, format:check, test)
    - biome.json (`files.includes` currently covers only shared/**, web/src/**, web/vite.config.ts)
    - web/tsconfig.app.json (include list is src/**+../shared/*.ts — e2e/ is outside it)
    - .gitignore (repo root)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md § "Environment Availability" and § "Pitfall 3"
    - PROJECT.md constraint C-10 (verbatim orules.ps1 invocation)
  </read_first>
  <action>
    Install the Playwright test runner into `web/` as a devDependency: run `bun add -d @playwright/test`
    (the `@playwright/test` package, NOT bare `playwright` — the `playwright test` runner and the
    `test`/`expect` exports live there). Then run `bunx playwright install chromium` and record in the
    SUMMARY whether it reused the cached binaries under `~/.cache/ms-playwright` or downloaded fresh
    (this closes 04-RESEARCH.md assumption A2). Verify the installed version is >= 1.51 with
    `bunx playwright --version`; version >= 1.51 is REQUIRED because `context.storageState({ indexedDB: true })`
    (used in Task 3) only exists from that version — @instantdb/core persists its session in IndexedDB,
    so a storageState capture without `indexedDB: true` would silently save an unauthenticated state.

    Create `web/playwright.config.ts` with: `testDir: "./e2e"`, `fullyParallel: false`, `workers: 1`
    (a single real InstantDB user, single mailbox — parallel magic-code flows would race for the same
    inbox message), `reporter: "list"`, `use: { baseURL: "http://localhost:5174", trace: "retain-on-failure" }`,
    and a `webServer` entry running `bun run dev -- --port 5174 --strictPort` with
    `url: "http://localhost:5174"`, `reuseExistingServer: true`, `timeout: 120_000`. Define three projects:
    (a) `setup` — `testMatch: /auth\.setup\.ts/`; (b) `authed` — `dependencies: ["setup"]`,
    `use: { ...devices["Desktop Chrome"], storageState: "e2e/.auth/user.json" }`,
    `testMatch: /.*\.spec\.ts/` excluding the no-leakage spec; (c) `anon` —
    `testMatch: /no-leakage\.spec\.ts/`, `use: { ...devices["Desktop Chrome"], storageState: { cookies: [], origins: [] } }`,
    NO dependency on `setup`. Export the storageState path as a named const so specs import it rather
    than restating the string.

    Create `web/e2e/helpers/magic-code.ts` exporting `readLatestMagicCode(): Promise<string>`. It
    shells out with `node:child_process`'s `execFileSync` to `powershell.exe` with arguments
    `-NoProfile -Command` and the exact C-10 command string (`Set-Location` to
    `C:\Users\thomaz.pougy\Documents\RBR\Sandbox\outlook-rules`; then `.\orules.ps1 peek --folder Inbox
    --days 1 --grep 'nstant' --body 0 --max 5`), parses the most recent matching message's SUBJECT line
    for a 6-digit run (`/\b(\d{6})\b/`), and returns it. Throw a descriptive Error naming the raw
    powershell output when no 6-digit code is found. Also export
    `readMagicCodeAfter(sentAtMs: number, timeoutMs = 45_000): Promise<string>` which polls
    `readLatestMagicCode` every 2s until it returns a code different from the one observed before the
    send (pass the prior code in), so a stale code from an earlier run is never reused. Keep the helper
    completely free of pauses beyond that poll interval — C-10 documents ~60-90s code expiry.

    Add `web/tsconfig.e2e.json` extending `./tsconfig.node.json` (or `@tsconfig/svelte` if node config
    proves incompatible), `include: ["e2e/**/*.ts", "playwright.config.ts"]`, `noEmit: true`,
    `types: ["node"]`. Chain it into the existing `check` script so e2e TypeScript is type-checked by
    the same C-08 gate as the rest of `web/`: `check` becomes
    `svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.e2e.json`.
    Add scripts `test:e2e` = `playwright test` and `test:e2e:auth` = `playwright test --project=setup`.

    Extend `biome.json`'s `files.includes` with `web/e2e/**/*.ts` and `web/playwright.config.ts` so
    C-08's formatter+linter gate covers the new files. Append `web/e2e/.auth/`, `web/test-results/`, and
    `web/playwright-report/` to the repo-root `.gitignore` — `e2e/.auth/user.json` holds a REAL InstantDB
    refresh token for tp@rbrasset.com.br and must never enter git history (threat T-04-06).
  </action>
  <acceptance_criteria>
    - `cd web && bunx playwright --version` prints a version >= 1.51.0; exit 0.
    - `cd web && bunx playwright test --list` exits 0 and lists the `setup`, `authed`, and `anon` projects.
    - `cd web && bun run check` exits 0 (all three tsconfig passes green, including tsconfig.e2e.json).
    - `cd web && bun run lint && bun run format:check` both exit 0 with `web/e2e/**` in Biome scope
      (verify scope with `cd .. && biome check --reporter=summary web/e2e` reporting the new files, not "no files").
    - `powershell.exe -NoProfile -Command "Set-Location 'C:\Users\thomaz.pougy\Documents\RBR\Sandbox\outlook-rules'; .\orules.ps1 peek --folder Inbox --days 1 --grep 'nstant' --body 0 --max 5"`
      run directly from WSL exits 0 and prints at least the header of a message list (proves the C-10 channel is live before Task 3 depends on it).
    - `git check-ignore -q web/e2e/.auth/user.json` exits 0.
    - `git status --porcelain` shows no untracked file under `web/e2e/.auth/`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bunx playwright test --list && bun run check && bun run lint && bun run format:check</automated>
  </verify>
  <done>Playwright >= 1.51 installed with Chromium available, three projects resolvable, e2e TypeScript inside the C-08 gate, the C-10 inbox channel verified live, and the session-state directory git-ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LoginScreen.svelte and the SignedIn/SignedOut gate in App.svelte</name>
  <read_first>
    - web/src/App.svelte (current scaffold — the isLoading/error/user branch pattern to preserve)
    - web/src/lib/db.ts (exports `db`, `id`, `lookup`)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md § Pattern 1, Pattern 2, Pitfall 1, Pitfall 2
    - The `<interfaces>` block in this plan's `<context>` (exact SDK signatures — do not re-derive)
  </read_first>
  <behavior>
    - Initial render with no session: the email step is shown, with a labelled email input and a submit button.
    - Submitting an empty email: no `sendMagicCode` call is made (native `required` on the input blocks it).
    - Successful `sendMagicCode`: the form advances to the code step, showing a labelled code input and the target email.
    - Failed `sendMagicCode`: error text from `err.body.message` is rendered, step stays on `email`.
    - Failed `signInWithMagicCode`: error text is rendered, step stays on `code` so a resend/retry is possible.
    - Successful `signInWithMagicCode`: no manual navigation happens; `db.useAuth()` flips and `SignedIn` takes over.
    - A "reenviar código" action on the code step re-calls `sendMagicCode` for the same email (needed for the C-10 expiry retry path).
  </behavior>
  <action>
    Create `web/src/lib/auth/LoginScreen.svelte` with `<script lang="ts">`. Local state via Svelte 5 runes:
    `let step = $state<"email" | "code">("email")`, `let email = $state("")`, `let code = $state("")`,
    `let erro = $state<string | null>(null)`, `let ocupado = $state(false)`. Two async handlers
    `enviarCodigo()` and `verificarCodigo()` calling `db.auth.sendMagicCode({ email })` and
    `db.auth.signInWithMagicCode({ email, code })` respectively, each wrapped in try/catch extracting
    `(err as { body?: { message?: string } }).body?.message` with a Portuguese fallback string, and each
    toggling `ocupado` so double-submits are impossible. Do NOT write any localStorage/IndexedDB code —
    the SDK owns session persistence (C-05, 04-RESEARCH.md "Don't Hand-Roll").

    Markup: unstyled semantic HTML only (04-RESEARCH.md Open Question 2 resolution) — a `<form>` per step
    with `on:submit|preventDefault`-equivalent Svelte 5 syntax (`onsubmit` handler calling
    `event.preventDefault()`), `<label for>`+`<input id>` pairs, and stable hooks for Playwright:
    `data-testid="login-screen"` on the root, `data-testid="login-email"` / `data-testid="login-code"`
    on the inputs, `data-testid="login-submit"` on the submit button, `data-testid="login-resend"` on the
    resend button, and `data-testid="login-error"` on the error paragraph. Use `type="email"` + `required`
    on the email input and `inputmode="numeric"` + `required` on the code input.

    Replace `web/src/App.svelte` so it imports `SignedIn` and `SignedOut` from `@instantdb/svelte`, `db`
    from `./lib/db`, and `LoginScreen` from `./lib/auth/LoginScreen.svelte`. Render `<SignedOut {db}>`
    wrapping `<LoginScreen />`, and `<SignedIn {db}>` wrapping a placeholder authenticated region with
    `data-testid="app-shell"` that shows the authenticated user's email (read from `db.useAuth()`, but
    ONLY branch on `auth.user` after checking `!auth.isLoading` — Pitfall 2) plus a
    `data-testid="logout"` button calling `db.auth.signOut()`. Plan 04-02 replaces this placeholder's
    body with the real `Shell`; keep the `data-testid="app-shell"` hook stable so its specs keep working.
    Keep the existing `<h1>Apollo v2</h1>` heading outside both gates, and REMOVE the
    `import.meta.env.VITE_INSTANT_APP_ID` display line — printing the app id is unnecessary and it is the
    one identifier an unauthenticated visitor should not be handed (defence in depth for WEB-10).

    Do not create any component that issues a `db.useQuery` call outside the `SignedIn` gate anywhere in
    this plan (04-RESEARCH.md Anti-Patterns; threat T-04-01).
  </action>
  <acceptance_criteria>
    - `cd web && bun run check` exits 0.
    - `cd web && bun run lint && bun run format:check` both exit 0.
    - `grep -rn "localStorage\|indexedDB\|IDBDatabase" web/src` returns no matches (exit 1).
    - `grep -rniE "INSTANT_APP_ADMIN_TOKEN|@instantdb/admin" web/src web/e2e` returns no matches (exit 1).
    - `grep -c "useQuery" web/src/App.svelte web/src/lib/auth/LoginScreen.svelte` reports 0 for both files.
    - `grep -q "SignedOut" web/src/App.svelte && grep -q "SignedIn" web/src/App.svelte` exits 0.
    - `cd web && bun run build` exits 0 and `web/dist/` is produced.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun run check && bun run lint && bun run format:check && bun run build</automated>
  </verify>
  <done>The SPA renders only the login screen when signed out and only the placeholder shell when signed in, with zero hand-rolled session storage and zero queries outside the auth gate.</done>
</task>

<task type="auto">
  <name>Task 3: Prove WEB-01 and WEB-10 with a REAL magic-code round trip in Chromium</name>
  <read_first>
    - web/playwright.config.ts (from Task 1 — project names and storageState path)
    - web/e2e/helpers/magic-code.ts (from Task 1)
    - web/src/lib/auth/LoginScreen.svelte (from Task 2 — the data-testid hooks)
    - PROJECT.md constraint C-10 (expiry window and retry protocol)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md § Pitfall 3
    - .planning/phases/03-cli-auth-crud/03-06-SUMMARY.md § "Pinned EXPECTED_USER_ID"
  </read_first>
  <action>
    Create `web/e2e/auth.setup.ts` as the `setup` project's single test. Sequence, kept deliberately
    tight because codes expire in ~60-90s (C-10):
    1. `page.goto("/")`; assert `login-screen` is visible.
    2. Capture the currently-newest inbox code via `readLatestMagicCode()` (may be a stale code from a
       previous run, or absent — tolerate an absent code here).
    3. Fill `login-email` with `tp@rbrasset.com.br`, click `login-submit`, record `Date.now()`.
    4. Immediately call `readMagicCodeAfter(sentAt, priorCode)` — no `page.waitForTimeout` between the
       click and the poll.
    5. Fill `login-code` with the returned code and click `login-submit` immediately.
    6. If `login-error` becomes visible with a message matching `/expired|record-expired|inválido/i`,
       click `login-resend` and repeat steps 3-5 exactly once before failing. Do not add a third retry —
       a repeated expiry means the channel is too slow and should fail loudly.
    7. Assert `app-shell` is visible and contains the text `tp@rbrasset.com.br`.
    8. `await page.reload()`; assert `app-shell` is visible again and `login-screen` is NOT visible
       (WEB-01 session persistence, same context).
    9. Persist state: `await page.context().storageState({ path: STORAGE_STATE, indexedDB: true })`.
    Also write a plain-text evidence file `web/e2e/.auth/LOGIN-EVIDENCE.txt` (git-ignored) recording the
    timestamp, the email, and the authenticated user id read from the DOM, and echo that same
    information to stdout so it lands in the execution transcript.

    Create `web/e2e/auth.spec.ts` in the `authed` project: opens `/` in a fresh browser context restored
    from the saved storageState and asserts `app-shell` is visible without any magic-code interaction —
    this proves the persisted session survives a brand-new context, not just a reload.

    Create `web/e2e/no-leakage.spec.ts` in the `anon` project (explicitly empty storageState). Assertions:
    (a) `login-screen` is visible; (b) `app-shell` has count 0; (c) no `<table>` element exists anywhere
    in the DOM; (d) the full `document.body.innerText` does not contain any of the nine entity type names
    (`fundos`, `projetos`, `etapas`, `tarefas`, `templatesRotina`, `instanciasRotina`, `tickets`,
    `subtarefas`, `logInferenciaClaude`) — case-insensitive; (e) install a `page.on("response")` listener
    BEFORE `goto` and assert that no response from an `instantdb.com` host with a body larger than 0 bytes
    was received on a query/transact path (accept auth and websocket-handshake traffic; assert specifically
    that no `POST` to a `/runtime/session` or query endpoint returned entity payload). Prefer asserting on
    request URLs actually observed and record the observed list in the test output so a future regression
    is diagnosable.

    Do NOT mock, stub, or hard-code a magic code anywhere. Do NOT use the raw REST
    `send_magic_code`/`verify_magic_code` endpoints as a substitute for the browser flow — 04-RESEARCH.md
    is explicit that REST-level scripting does not prove WEB-01 (they may be used only as an auxiliary
    diagnostic if the browser flow fails and you need to isolate whether InstantDB or the SPA is at fault).
  </action>
  <acceptance_criteria>
    - `cd web && bunx playwright test --project=setup` exits 0 and its output contains the authenticated
      email `tp@rbrasset.com.br`.
    - `web/e2e/.auth/user.json` exists afterwards and is non-empty
      (`test -s web/e2e/.auth/user.json` exits 0).
    - `cd web && bunx playwright test --project=authed` exits 0 WITHOUT triggering a new magic-code email
      (assert by grepping the run output for absence of `sendMagicCode`/resend activity).
    - `cd web && bunx playwright test --project=anon` exits 0.
    - Fail-loud demonstration (record verbatim in the SUMMARY): temporarily change `App.svelte` to render
      the shell placeholder OUTSIDE the `SignedIn` gate, re-run `--project=anon`, and confirm it FAILS
      naming the leaked element; restore the file and confirm it passes again.
    - `grep -rn "123456\|MAGIC_CODE=\|mockCode\|fakeCode" web/e2e` returns no matches (exit 1) — no
      hard-coded or mocked code survived.
    - `git status --porcelain web/e2e/.auth` produces no output (the real session artifact stayed untracked).
  </acceptance_criteria>
  <verify>
    <automated>cd web && bunx playwright test</automated>
  </verify>
  <done>A real code from tp@rbrasset.com.br's real inbox authenticated a real Chromium session; the session survived both a reload and a fresh context; and an unauthenticated load provably renders the login screen with zero entity data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> InstantDB | All queries/transacts cross here carrying only the SDK-managed session token; InstantDB evaluates `instant.perms.ts` server-side |
| Unauthenticated visitor -> SPA bundle | The shipped JS bundle is fully public; anything baked into it is disclosed |
| WSL test harness -> Outlook Classic (COM) | The e2e helper reads one real mailbox to fetch one magic code (C-10 scope limit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `web/src/App.svelte` render tree | mitigate | Every data-bearing region nested under `<SignedIn {db}>`; `no-leakage.spec.ts` asserts zero entity data in the anonymous DOM and the fail-loud demo proves the assertion actually catches a regression |
| T-04-02 | Information Disclosure | Vite bundle / `web/dist` | mitigate | `vite.config.ts` parses `.env.instantdb` manually and injects only the app id (Phase 1 pattern, unchanged); Task 2 grep gate asserts no `INSTANT_APP_ADMIN_TOKEN` / `@instantdb/admin` reference under `web/src` or `web/e2e`; plan 04-06 re-greps the built bundle |
| T-04-03 | Spoofing | Magic-code flow | accept | Code issuance, expiry, and replay rejection are entirely InstantDB-managed; the SPA only surfaces the `record-expired` error and offers a resend. No app-controlled surface to harden |
| T-04-06 | Information Disclosure | `web/e2e/.auth/user.json` | mitigate | Directory added to `.gitignore` in Task 1; Task 1 and Task 3 both assert `git check-ignore` / clean `git status` for that path so a real refresh token can never be committed |
| T-04-07 | Elevation of Privilege | C-10 mailbox access | accept | The helper's powershell invocation is pinned to `peek --grep 'nstant' --body 0` on the Inbox — read-only, subject-line only, scoped to InstantDB messages, per the explicit user authorization in C-10 |
</threat_model>

<verification>
1. `cd web && bun run check && bun run lint && bun run format:check && bun run build` — all exit 0.
2. `cd web && bunx playwright test` — all three projects green.
3. `grep -rniE "INSTANT_APP_ADMIN_TOKEN|@instantdb/admin|localStorage" web/src web/e2e` — no matches.
4. `git status --porcelain` clean of `web/e2e/.auth/` and `web/test-results/`.
</verification>

<success_criteria>
- WEB-01: proven by `--project=setup` (real code, real browser, reload-persistent) and `--project=authed` (fresh context from persisted state).
- WEB-10: proven by `--project=anon` plus the recorded fail-loud demonstration.
- ROADMAP SC-1 and SC-4 for Phase 4 are satisfied by the artifacts of this plan alone.
- C-08 quality gates extended to cover `web/e2e/**` and still green.
</success_criteria>

## Artifacts this phase produces

| Artifact | Consumed by |
|----------|-------------|
| `web/playwright.config.ts` (setup/authed/anon projects, `STORAGE_STATE` const) | 04-02, 04-03, 04-04, 04-05 entity specs; Phase 6 VERIFY-01 |
| `web/e2e/helpers/magic-code.ts` (`readLatestMagicCode`, `readMagicCodeAfter`) | Any future re-auth; Phase 6 |
| `web/e2e/.auth/user.json` (git-ignored authenticated storageState) | Every downstream `authed`-project spec |
| `web/src/lib/auth/LoginScreen.svelte` | `App.svelte` `SignedOut` branch |
| `web/src/App.svelte` auth gate with stable `data-testid="app-shell"` | 04-02 replaces the placeholder body with `Shell.svelte` |
| `web/tsconfig.e2e.json` + extended `biome.json` scope + `test:e2e` script | 04-06 verification script; Phase 6 VERIFY-03 |

<output>
After completion, create `.planning/phases/04-web-spa-auth-crud-smoke-ui/04-01-SUMMARY.md`.
Record verbatim: the Playwright version installed, whether cached Chromium was reused (closes A2),
the authenticated user id observed in the browser (compare against Phase 3's pinned
`adf0d402-06df-4406-a5c7-ce82ee1bcb7e` and flag any mismatch loudly), how many magic-code attempts
were needed, and the fail-loud demonstration output for `no-leakage.spec.ts`.
</output>
