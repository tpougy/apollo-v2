---
phase: 04-web-spa-auth-crud-smoke-ui
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - web/src/lib/entities/defs/templatesRotina.ts
  - web/src/lib/entities/defs/instanciasRotina.ts
  - web/src/lib/entities/defs/logInferenciaClaude.ts
  - web/e2e/entities-rotina-log.spec.ts
autonomous: true
requirements: [WEB-06, WEB-07, WEB-09]
user_setup: []

must_haves:
  truths:
    - "An authenticated user can create, view, edit, and delete a templatesRotina in the browser, optionally linked to a fundo and to another template as antecessor."
    - "The instanciasRotina screen lists instances and lets the user change ONLY their status."
    - "The instanciasRotina screen offers no create and no delete action anywhere in the UI."
    - "The logInferenciaClaude screen is view-only: a table with no form, no edit, and no delete."
    - "A templatesRotina cannot select itself as its own antecessor."
  artifacts:
    - path: "web/src/lib/entities/defs/templatesRotina.ts"
      provides: "templatesRotina EntityConfig — nome, tipoGeracao (du_fixo|corrido_fixo|encadeado), regraCompetencia, propagarAtrasoSoft, ativo, links fundo + antecessor(self)"
      contains: "antecessor"
    - path: "web/src/lib/entities/defs/instanciasRotina.ts"
      provides: "instanciasRotina EntityConfig — capabilities create:false delete:false, updatableFields: ['status']"
      contains: "updatableFields"
    - path: "web/src/lib/entities/defs/logInferenciaClaude.ts"
      provides: "logInferenciaClaude EntityConfig — capabilities all false (view-only table)"
      contains: "logInferenciaClaude"
    - path: "web/e2e/entities-rotina-log.spec.ts"
      provides: "Live browser proof of template CRUD, instance status-only update, and the view-only log screen"
  key_links:
    - from: "web/src/lib/entities/defs/templatesRotina.ts"
      to: "templatesRotina (itself)"
      via: "LinkDef { label: 'antecessor', targetEtype: 'templatesRotina', excludeSelf: true }"
      pattern: "excludeSelf"
    - from: "web/src/lib/entities/defs/instanciasRotina.ts"
      to: "templatesRotina"
      via: "read-only template column in the list"
      pattern: "template"
  edge_coverage:
    - "WEB-06 / tipoGeracao select offers exactly du_fixo, corrido_fixo, encadeado — matching the CLI's click.Choice, no free text."
    - "WEB-06 / antecessor select excludes the record currently being edited (no self-referential link)."
    - "WEB-06 / template created with neither fundo nor antecessor: saves successfully with both columns blank."
    - "WEB-06 / propagarAtrasoSoft and ativo booleans both round-trip false across a reload."
    - "WEB-07 / instance list renders with zero create button and zero delete button in the DOM."
    - "WEB-07 / editing an instance exposes ONLY the status field — dedupeKey, dataPrevista, and competencia are not editable."
    - "WEB-07 / with zero instances in the app (none generated until Phase 5), the screen renders its empty state instead of erroring."
    - "WEB-09 / log screen renders no form element and no edit/delete buttons; entries created via the Phase 3 CLI are visible."
  prohibitions:
    - "No create or delete affordance for instanciasRotina anywhere in the UI — instances exist only via Phase 5's dedupeKey-keyed generation job (C-06); a hand-created instance would carry a wrong or absent dedupeKey and the next job run would duplicate alongside it."
    - "No write affordance of any kind for logInferenciaClaude — it is append-only via the CLI (CLI-10)."
    - "No gerar-instancias / generation logic stub of any kind — that is Phase 5 (JOB-01), not this phase."
    - "No edit to registry.ts, Shell.svelte, EntityScreen.svelte, or types.ts (would collide with the parallel wave-3 plans)."
    - "No donoId field in any of the three definitions."
---

<objective>
Add the three restricted-capability screens — `templatesRotina` (full CRUD with a self-link),
`instanciasRotina` (list + status-only update), and `logInferenciaClaude` (view-only) — and prove
in a real browser that the restrictions are actually enforced in the UI, not merely intended.

Purpose: WEB-06, WEB-07, WEB-09. These entities are where "minimal smoke UI" meets real invariants:
instances must never be hand-created (C-06 idempotency depends on it) and the inference log must stay
append-only. The generic engine's `capabilities` and `updatableFields` knobs exist for exactly this.
Output: three `defs/*.ts` modules and one e2e spec. Zero new components.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md
@shared/instant.schema.ts
@web/src/lib/entities/types.ts
@web/src/lib/entities/defs/fundos.ts
@web/src/lib/entities/EntityScreen.svelte
@web/e2e/entities-fundos.spec.ts

Phase 3 semantics reference — the same restrictions, already enforced on the CLI side:
@cli/apollo_cli/entities/rotina.py
@cli/apollo_cli/entities/log_inferencia.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: templatesRotina, instanciasRotina, and logInferenciaClaude EntityConfig definitions</name>
  <read_first>
    - web/src/lib/entities/types.ts (the EntityConfig contract as SHIPPED by 04-02, especially `capabilities` and `updatableFields`; follow the file, not the plan, if they diverged)
    - web/src/lib/entities/defs/fundos.ts (reference definition shape)
    - shared/instant.schema.ts for templatesRotina, instanciasRotina, logInferenciaClaude and links fundoTemplatesRotina, templateInstancias, templateAntecessor
    - cli/apollo_cli/entities/rotina.py (module docstring explains WHY instances have no criar/deletar: `_TIPO_GERACAO_CHOICES = ("du_fixo", "corrido_fixo", "encadeado")`; `rotina instancia status` updates only status)
    - cli/apollo_cli/entities/log_inferencia.py (append-only: registrar + listar, no editar, no deletar)
  </read_first>
  <action>
    Create three definition modules under `web/src/lib/entities/defs/`. Do not touch `registry.ts` —
    `import.meta.glob` discovers them.

    `templatesRotina.ts`: `etype: "templatesRotina"`, `titulo: "Templates de rotina"`, `ordem: 5`,
    all capabilities true. Fields: `nome` (text, required),
    `tipoGeracao` (select, required, `options: ["du_fixo", "corrido_fixo", "encadeado"]` — exactly the CLI's
    `_TIPO_GERACAO_CHOICES`), `regraCompetencia` (text, required — free-form, matching the CLI),
    `propagarAtrasoSoft` (boolean, required), `ativo` (boolean, required).
    Links: `{ label: "fundo", targetEtype: "fundos", targetLabelField: "nome", required: false }` and
    `{ label: "antecessor", targetEtype: "templatesRotina", targetLabelField: "nome", required: false,
    excludeSelf: true }` — the `templateAntecessor` self-link used by the `encadeado` generation type.
    `listColumns: ["nome", "tipoGeracao", "ativo", "fundo", "antecessor"]`.

    `instanciasRotina.ts`: `etype: "instanciasRotina"`, `titulo: "Instâncias de rotina"`, `ordem: 6`,
    `capabilities: { create: false, update: true, delete: false }`, `updatableFields: ["status"]`.
    Fields (all rendered read-only in the table; only `status` reachable in the edit form because of
    `updatableFields`): `dedupeKey` (text, required), `dataPrevista` (date, required),
    `dataPrevistaEstimada` (date, optional), `competencia` (text, required), `tipoPrazo` (text, required),
    `status` (text, required). Links: none declared for writing; include `template` in `listColumns` so the
    owning template is visible — confirm against the shipped `EntityScreen` how a read-only link column is
    expressed (a `LinkDef` with a capability-driven read-only render, or a plain list column) and use
    whichever the component already supports without modifying it.
    `listColumns: ["competencia", "dataPrevista", "status", "tipoPrazo", "template"]`.
    Add a module-level comment stating the C-06 rationale verbatim: instances are created exclusively by
    Phase 5's `dedupeKey`-keyed generation job; a hand-created instance would carry a wrong or absent
    `dedupeKey` and the next job run would create a duplicate alongside it.

    `logInferenciaClaude.ts`: `etype: "logInferenciaClaude"`, `titulo: "Log de inferências"`, `ordem: 9`,
    `capabilities: { create: false, update: false, delete: false }`. Fields: `campo` (text, required),
    `valorInferido` (text, required), `trechoMotivador` (textarea, optional),
    `entidadeTipo` (text, required), `entidadeId` (text, required), `createdAt` (date, required) — declared
    so the table can render them; with all capabilities false no form should be rendered at all.
    `listColumns: ["createdAt", "entidadeTipo", "campo", "valorInferido"]`.

    If the shipped `EntityScreen.svelte` does not already suppress the form when all capabilities are false,
    or does not honour `updatableFields` in edit mode, STOP and report it to the orchestrator rather than
    editing the shared component — it is owned by 04-02 and shared with two parallel wave-3 plans.
    Do not work around it inside a definition file.
  </action>
  <acceptance_criteria>
    - `cd web && bun run check && bun run lint && bun run format:check` all exit 0.
    - `cd web && bun run build` exits 0.
    - `grep -rn "donoId" web/src/lib/entities/defs/templatesRotina.ts web/src/lib/entities/defs/instanciasRotina.ts web/src/lib/entities/defs/logInferenciaClaude.ts` returns no matches (exit 1).
    - `grep -c 'create: false' web/src/lib/entities/defs/instanciasRotina.ts` reports 1 and
      `grep -c 'delete: false' web/src/lib/entities/defs/instanciasRotina.ts` reports 1.
    - `grep -c 'excludeSelf' web/src/lib/entities/defs/templatesRotina.ts` reports 1.
    - `grep -rniE "gerar.?instancias|generateInstances|dedupeKey *=" web/src` returns no matches (exit 1) — no Phase 5 generation logic leaked into Phase 4.
    - `git diff --name-only` for this task lists ONLY files under `web/src/lib/entities/defs/`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun run check && bun run lint && bun run format:check && bun run build</automated>
  </verify>
  <done>All three screens exist as declarative config with their capability restrictions expressed through the shared contract, and no Phase 5 generation logic anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Prove template CRUD, status-only instance updates, and the view-only log in Chromium</name>
  <read_first>
    - web/e2e/entities-fundos.spec.ts (authed-project spec pattern and cleanup discipline)
    - web/playwright.config.ts (project names)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md (data-testid vocabulary)
    - cli/README.md § "Superfície de comandos" (`apollo rotina template criar|deletar`, `apollo rotina instancia listar|status`, `apollo log-inferencia registrar|listar`)
  </read_first>
  <action>
    Create `web/e2e/entities-rotina-log.spec.ts` in the `authed` project. All records prefixed
    `phase04-e2e-`, deleted in an `afterEach` guard so re-runs are idempotent.

    Test 1 — templatesRotina CRUD: assert `field-tipoGeracao` offers exactly `du_fixo`, `corrido_fixo`,
    `encadeado`. Create template A with no fundo and no antecessor; assert the row appears with both link
    columns blank. Create template B with `antecessor` = A; assert B's antecessor column shows A's nome.
    Open B's edit form and assert the `link-antecessor` select does NOT contain B itself (`excludeSelf`).
    Set `ativo` false, reload, assert the column reads `não`. Delete both.

    Test 2 — instanciasRotina restrictions (WEB-07): navigate to the instances screen. Assert (a) there is
    no create form / no submit control for creating an instance — check for the absence of the create-form
    testid the shipped component uses; (b) `row-delete` has count 0 across the whole screen. If the live app
    currently has zero instances (expected — none exist until Phase 5), assert `empty-state` renders and
    then create ONE instance through a legitimate channel to exercise the status path: since neither the SPA
    nor the CLI may create instances by design, seed it with a direct admin-token write from a Node script
    run OUTSIDE `web/src` (a test-only fixture under `web/e2e/fixtures/`, using `@instantdb/admin`, which is
    already a `web/` devDependency), then delete it in teardown through the same admin path. Record clearly
    in the SUMMARY that this admin write is a TEST FIXTURE ONLY, never a runtime path, and confirm the
    fixture file is excluded from the browser bundle (it is under `e2e/`, never imported by `src/`).
    With that instance present: assert the row renders, click `row-edit`, and assert the edit form exposes
    ONLY `field-status` — specifically that `field-dedupeKey`, `field-dataPrevista`, and `field-competencia`
    have count 0. Change the status, submit, and verify server-side via
    `uv run --project cli apollo rotina instancia listar` that the status changed and `dedupeKey` and
    `dataPrevista` are byte-identical to before.

    Test 3 — logInferenciaClaude view-only (WEB-09): create a log entry through the Phase 3 CLI
    (`apollo log-inferencia registrar ...`), then in the browser navigate to the log screen and assert the
    entry is visible (this is also SC-3 evidence for this entity). Assert the screen contains no `<form>`
    element, `row-edit` count 0, and `row-delete` count 0.

    Do not add any UI, config, or script that creates an `instanciasRotina` record from the SPA or the CLI —
    the fixture path above is deliberately outside both channels precisely because both are forbidden from
    creating instances (C-06).
  </action>
  <acceptance_criteria>
    - `cd web && bunx playwright test --project=authed entities-rotina-log.spec.ts` exits 0.
    - The same command run twice in a row exits 0 both times.
    - After the run: `uv run --project cli apollo rotina template listar | grep -c phase04-e2e-` reports 0,
      `uv run --project cli apollo rotina instancia listar | grep -c phase04-e2e-` reports 0, and
      `uv run --project cli apollo log-inferencia listar | grep -c phase04-e2e-` reports 0.
    - `grep -rn "@instantdb/admin" web/src` returns no matches (exit 1) — the admin token never reaches app code.
    - `cd web && bun run build && grep -rlF "$(grep '^INSTANT_APP_ADMIN_TOKEN=' .env.instantdb | cut -d= -f2-)" web/dist` returns no matches (exit 1).
    - Fail-loud demonstration recorded in the SUMMARY: temporarily set `instanciasRotina`'s
      `capabilities.delete` to true, re-run Test 2, confirm the `row-delete` count-0 assertion FAILS,
      restore, confirm green.
    - `cd web && bunx playwright test` (all projects) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bunx playwright test</automated>
  </verify>
  <done>Template CRUD (including the self-link exclusion) works live; the instance screen provably offers status-only editing with no create/delete anywhere; the log screen is provably read-only and shows CLI-written entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input -> `db.transact` | Template fields and the fundo/antecessor link ids cross here |
| Test fixture (`web/e2e/fixtures/`) -> InstantDB admin API | The ONLY admin-token path in this phase; test-only, never bundled |
| Browser -> InstantDB | `donoId` rules enforced server-side on every read and write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | Admin token reaching the browser bundle | mitigate | `@instantdb/admin` is used only from `web/e2e/fixtures/` (never imported by `web/src`); Task 2 asserts `grep -rn "@instantdb/admin" web/src` is empty AND greps the built `web/dist` for the literal token value, reusing Phase 1's `verify-phase-01.sh` technique |
| T-04-13 | Tampering | Hand-created `instanciasRotina` breaking Phase 5 idempotency | mitigate | `capabilities.create: false` and `delete: false` in the definition; Task 2 asserts zero create form and zero delete buttons in the rendered DOM, with a recorded fail-loud demo proving the assertion bites |
| T-04-14 | Repudiation | Edited or deleted `logInferenciaClaude` entries destroying the audit trail | mitigate | All three capabilities false; Task 2 asserts no form, no edit button, no delete button on that screen (client-side). Server-side the `donoId` update/delete rules still permit the owner to write — matching the CLI, which also relies on surface restriction rather than a perms-level append-only rule; noted here as a known parity-preserving limit, not a silent gap |
| T-04-04 | Tampering | `antecessor` self-link | mitigate | `excludeSelf: true` removes the record from its own options; the inherited `queryOnce` existence check guards the chosen target |
| T-04-03 | Elevation of Privilege | These three definitions | mitigate | `FieldDef` cannot express an owner id; Task 1 grep gate asserts no `donoId` under `defs/` |
</threat_model>

<verification>
1. `cd web && bun run check && bun run lint && bun run format:check && bun run build` — all exit 0.
2. `cd web && bunx playwright test` — every project green.
3. `grep -rn "@instantdb/admin" web/src` — no matches; built bundle grep for the literal admin token — no matches.
4. `grep -rniE "gerar.?instancias|generateInstances" web/src` — no matches (Phase 5 scope not leaked forward).
5. Zero `phase04-e2e-` records remain across `apollo rotina template|instancia listar` and `apollo log-inferencia listar`.
</verification>

<success_criteria>
- WEB-06 satisfied: templatesRotina create/view/edit/delete live, including the fundo link and the
  self-referential `antecessor` link with self-exclusion.
- WEB-07 satisfied: instances list and update status only, with create and delete provably absent from the UI.
- WEB-09 satisfied: logInferenciaClaude renders CLI-written entries with no write affordance at all.
</success_criteria>

## Artifacts this phase produces

| Artifact | Consumed by |
|----------|-------------|
| `web/src/lib/entities/defs/templatesRotina.ts` | Registry auto-discovery; 04-06 coverage test; Phase 5 JOB-01 (reads active templates) |
| `web/src/lib/entities/defs/instanciasRotina.ts` (create:false, delete:false, status-only update) | Registry auto-discovery; 04-06 coverage test; Phase 5 attaches generation without loosening these capabilities |
| `web/src/lib/entities/defs/logInferenciaClaude.ts` (view-only) | Registry auto-discovery; 04-06 coverage test |
| `web/e2e/fixtures/` admin-only instance seeder (test-only, never bundled) | Phase 5 JOB-01 idempotency tests; Phase 6 VERIFY-04 |
| `web/e2e/entities-rotina-log.spec.ts` | 04-06 full-suite gate; Phase 6 VERIFY-01 |

<output>
After completion, create `.planning/phases/04-web-spa-auth-crud-smoke-ui/04-05-SUMMARY.md`.
Record verbatim: how the read-only `template` column on the instances screen was expressed without editing
the shared component, the exact admin-fixture seeding path used for Test 2 (and the explicit statement that
it is test-only and unbundled), the built-bundle admin-token grep result, and the fail-loud demonstration output.
</output>
