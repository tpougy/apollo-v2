---
phase: 04-web-spa-auth-crud-smoke-ui
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - web/src/lib/entities/defs/projetos.ts
  - web/src/lib/entities/defs/etapas.ts
  - web/src/lib/entities/defs/tarefas.ts
  - web/e2e/entities-projeto-etapa-tarefa.spec.ts
autonomous: true
requirements: [WEB-03, WEB-04, WEB-05]
user_setup: []

must_haves:
  truths:
    - "An authenticated user can create, view, edit, and delete a projeto in the browser, optionally linked to a fundo."
    - "An authenticated user can create, view, edit, and delete an etapa in the browser, linked to a projeto, with a numeric ordem."
    - "An authenticated user can create, view, edit, and delete a tarefa in the browser, linked to an etapa, with hard/soft tipoPrazo and optional dates."
    - "The projeto -> etapa -> tarefa chain is navigable: an etapa's projeto and a tarefa's etapa render in their list columns."
  artifacts:
    - path: "web/src/lib/entities/defs/projetos.ts"
      provides: "projetos EntityConfig — nome, descricao?, status, dataInicioPrevista?, dataFimPrevista?, link fundo"
      contains: "projetos"
    - path: "web/src/lib/entities/defs/etapas.ts"
      provides: "etapas EntityConfig — nome, ordem (number), status, link projeto"
      contains: "etapas"
    - path: "web/src/lib/entities/defs/tarefas.ts"
      provides: "tarefas EntityConfig — titulo, descricao?, tipoPrazo (hard|soft), dataPrevista?, dataPrevistaEstimada?, competencia?, status, link etapa"
      contains: "tarefas"
    - path: "web/e2e/entities-projeto-etapa-tarefa.spec.ts"
      provides: "Live browser CRUD round trip across the three linked entities"
  key_links:
    - from: "web/src/lib/entities/defs/etapas.ts"
      to: "projetos"
      via: "LinkDef { label: 'projeto', targetEtype: 'projetos' }"
      pattern: "projetos"
    - from: "web/src/lib/entities/defs/tarefas.ts"
      to: "etapas"
      via: "LinkDef { label: 'etapa', targetEtype: 'etapas' }"
      pattern: "etapas"
    - from: "web/src/lib/entities/registry.ts"
      to: "web/src/lib/entities/defs/*.ts"
      via: "import.meta.glob auto-discovery — no registry edit required"
      pattern: "import.meta.glob"
  edge_coverage:
    - "WEB-03 / projeto created with no fundo link (link is optional): saves successfully and the fundo column renders empty, not 'undefined'."
    - "WEB-03 / optional date fields left blank: no key is written for them (not an empty-string or Invalid Date)."
    - "WEB-04 / etapa ordem is written as a number: reloading shows the numeric value, and the schema's i.number() accepts it (no string coercion rejection)."
    - "WEB-04 / etapa created with a projeto link where the projeto id does not resolve: the existence check blocks the transact with a visible error instead of writing a dangling link."
    - "WEB-05 / tarefa tipoPrazo select offers exactly 'hard' and 'soft' — matching the CLI's click.Choice, no free text."
    - "WEB-05 / tarefa with only dataPrevistaEstimada set (dataPrevista blank): saves and renders correctly."
    - "WEB-03/04/05 / editing changes only the edited fields; the record id and unedited fields are untouched."
  prohibitions:
    - "No new table/form markup — these three screens render exclusively through EntityScreen.svelte."
    - "No edit to registry.ts, Shell.svelte, EntityScreen.svelte, or types.ts (auto-discovery makes it unnecessary; editing them would collide with the parallel wave-3 plans)."
    - "No donoId field in any of the three definitions."
    - "No hard-coded status vocabulary that contradicts the CLI — status is free-form on all three entities (CLI uses a plain --status string, not click.Choice)."
---

<objective>
Add the projeto -> etapa -> tarefa screens as pure `EntityConfig` definitions and prove all three
CRUD round trips live in Chromium.

Purpose: WEB-03, WEB-04, WEB-05 — the structured-work chain of the domain, and the first test of
whether the generic engine handles optional fields, optional links, number fields, and multiple
optional date fields without new machinery.
Output: three `defs/*.ts` modules and one e2e spec. Zero new components.
</objective>

<execution_context>
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomaz/pessoal/apollo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-RESEARCH.md
@.planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md
@shared/instant.schema.ts
@web/src/lib/entities/types.ts
@web/src/lib/entities/defs/fundos.ts
@web/src/lib/entities/EntityScreen.svelte
@web/e2e/entities-fundos.spec.ts

Phase 3 semantics reference — mirror the required/optional split and validation rules exactly:
@cli/apollo_cli/entities/projeto.py
@cli/apollo_cli/entities/etapa.py
@cli/apollo_cli/entities/tarefa.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: projetos, etapas, and tarefas EntityConfig definitions</name>
  <read_first>
    - web/src/lib/entities/types.ts (the EntityConfig contract as SHIPPED by 04-02 — use it verbatim; if it diverged from 04-02's plan, follow the file, not the plan)
    - web/src/lib/entities/defs/fundos.ts (the reference definition shape)
    - shared/instant.schema.ts lines for projetos, etapas, tarefas and links fundoProjetos, projetoEtapas, etapaTarefas
    - cli/apollo_cli/entities/projeto.py, etapa.py, tarefa.py (which flags are `required=True` on criar vs `default=None` on editar; `_TIPO_PRAZO_CHOICES = ("hard", "soft")`; `validate_iso_date` accepts YYYY-MM-DD only)
  </read_first>
  <action>
    Create three definition modules under `web/src/lib/entities/defs/`, each default-exporting an
    `EntityConfig`. Do not touch `registry.ts` — `import.meta.glob` picks them up automatically.

    `projetos.ts`: `etype: "projetos"`, `titulo: "Projetos"`, `ordem: 2`, all capabilities true.
    Fields: `nome` (text, required), `descricao` (textarea, optional), `status` (text, required — free-form,
    matching the CLI's plain `--status` string), `dataInicioPrevista` (date, optional),
    `dataFimPrevista` (date, optional). Links: one `LinkDef` `{ label: "fundo", targetEtype: "fundos",
    targetLabelField: "nome", required: false }` (the CLI's `--fundo-id` is optional).
    `listColumns: ["nome", "status", "fundo", "dataInicioPrevista", "dataFimPrevista"]`.

    `etapas.ts`: `etype: "etapas"`, `titulo: "Etapas"`, `ordem: 3`, all capabilities true.
    Fields: `nome` (text, required), `ordem` (number, required), `status` (text, required).
    Links: `{ label: "projeto", targetEtype: "projetos", targetLabelField: "nome", required: false }` —
    match the CLI, where `--projeto-id` is optional but validated when supplied.
    `listColumns: ["ordem", "nome", "status", "projeto"]`.
    Note the field named `ordem` collides in name with `EntityConfig.ordem` (nav sort) — they are different
    keys on different objects; do not conflate them, and confirm the shipped `EntityScreen` renders the
    field-level `ordem` from `config.fields`, not the nav value.

    `tarefas.ts`: `etype: "tarefas"`, `titulo: "Tarefas"`, `ordem: 4`, all capabilities true.
    Fields: `titulo` (text, required), `descricao` (textarea, optional),
    `tipoPrazo` (select, required, `options: ["hard", "soft"]` — exactly the CLI's `_TIPO_PRAZO_CHOICES`),
    `dataPrevista` (date, optional), `dataPrevistaEstimada` (date, optional),
    `competencia` (text, optional — free-form, matching the CLI), `status` (text, required).
    Links: `{ label: "etapa", targetEtype: "etapas", targetLabelField: "nome", required: false }`.
    `listColumns: ["titulo", "status", "tipoPrazo", "dataPrevista", "etapa"]`.

    If any of the three needs behaviour the shipped `EntityConfig` cannot express, STOP and report it
    rather than adding bespoke markup — the correct fix is a contract extension coordinated with the
    other wave-3 plans, not a one-off component.
  </action>
  <acceptance_criteria>
    - `cd web && bun run check && bun run lint && bun run format:check` all exit 0.
    - `cd web && bun run build` exits 0.
    - `grep -rn "donoId" web/src/lib/entities/defs/` returns no matches (exit 1).
    - `git diff --name-only` for this task lists ONLY files under `web/src/lib/entities/defs/`
      (no change to registry.ts, types.ts, EntityScreen.svelte, or Shell.svelte).
    - `grep -c '"hard"' web/src/lib/entities/defs/tarefas.ts` reports >= 1 and
      `grep -c '"soft"' web/src/lib/entities/defs/tarefas.ts` reports >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bun run check && bun run lint && bun run format:check && bun run build</automated>
  </verify>
  <done>Three screens exist purely as declarative config, auto-discovered by the registry, with the required/optional split matching the CLI exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Prove projeto/etapa/tarefa CRUD live in Chromium</name>
  <read_first>
    - web/e2e/entities-fundos.spec.ts (the authed-project spec pattern and cleanup discipline to copy)
    - web/playwright.config.ts (project names)
    - .planning/phases/04-web-spa-auth-crud-smoke-ui/04-02-SUMMARY.md (the exact data-testid vocabulary EntityScreen exposes)
  </read_first>
  <action>
    Create `web/e2e/entities-projeto-etapa-tarefa.spec.ts` in the `authed` project, with all records
    prefixed `phase04-e2e-` and deleted in an `afterEach` guard (idempotent re-runs).

    Test 1 — projetos: create with nome + status only (no fundo link, both dates blank) and assert the row
    appears with an empty fundo cell rendering as blank, not the literal `undefined` or `null`. Then edit to
    set `dataInicioPrevista`, reload the page, and assert the date column shows the same `YYYY-MM-DD` value
    that was typed (proves the ISO round trip from 04-02's date helpers). Then create a second projeto WITH
    a fundo link selected and assert the fundo's nome renders in the column. Delete both.

    Test 2 — etapas: create an etapa with `ordem` 10 linked to the projeto from Test 1's setup; assert the
    row shows `10` and the projeto nome. Edit `ordem` to 20, reload, assert it persisted as a number by
    asserting the cell text is exactly `20` (not `"20"` with quotes, not blank). Delete.

    Test 3 — tarefas: assert the `field-tipoPrazo` select has exactly two options, `hard` and `soft`.
    Create a tarefa with `tipoPrazo=hard`, `dataPrevistaEstimada` set and `dataPrevista` blank, linked to
    Test 2's etapa; assert it appears and the `dataPrevista` column is blank. Edit the status, assert
    `data-eid` is unchanged after the edit (proves update, not recreate). Delete.

    Test 4 — dangling link guard: attempt an etapa create whose selected projeto was deleted mid-test
    (delete it via the Phase 3 CLI between the select and the submit), and assert a visible
    `data-testid="entity-error"` appears and no new row is created — proving the `queryOnce`
    existence check from 04-02 actually fires (threat T-04-04). If that race is not reliably reproducible
    through the UI, drive the same assertion by evaluating the existence check directly in the page context
    and record in the SUMMARY why the UI path was not used.

    Do not mock InstantDB. Every assertion runs against the real app with the real authenticated session
    restored from `e2e/.auth/user.json`.
  </action>
  <acceptance_criteria>
    - `cd web && bunx playwright test --project=authed entities-projeto-etapa-tarefa.spec.ts` exits 0.
    - The same command run twice in a row exits 0 both times.
    - After the run: `uv run --project cli apollo projeto listar | grep -c phase04-e2e-` reports 0, and the
      same for `apollo etapa listar` and `apollo tarefa listar`.
    - Fail-loud demonstration recorded in the SUMMARY: temporarily change `defs/tarefas.ts` to mark
      `tipoPrazo` as a plain text field, re-run Test 3, confirm the two-option assertion FAILS, restore, confirm green.
    - `cd web && bunx playwright test` (all projects) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd web && bunx playwright test</automated>
  </verify>
  <done>All three screens are proven to create, read, update, and delete real records in a real browser, with links, optional dates, numbers, and the dangling-link guard all exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form input -> `db.transact` | Field values and the selected `projeto`/`etapa`/`fundo` link ids cross here |
| Browser -> InstantDB | `donoId` view/create/update/delete rules enforced server-side on every operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | `etapas`/`tarefas`/`projetos` link selection | mitigate | The `queryOnce` existence check inherited from `EntityScreen.svelte` runs before every `.link()`; Task 2 Test 4 asserts it actually fires by deleting the target mid-flight |
| T-04-03 | Elevation of Privilege | These three definitions | mitigate | `FieldDef` cannot express an owner id and Task 1 greps assert no `donoId` in `defs/`; injection stays exclusively in `EntityScreen.svelte`'s submit path |
| T-04-10 | Tampering | Free-form `status` and `competencia` values | accept | The CLI accepts the same free-form strings (`--status` is a plain option, not a `click.Choice`); introducing a stricter vocabulary in the SPA only would BREAK the C-05 parity guarantee. Server-side `donoId` rules still bound the blast radius to the user's own records |
| T-04-01 | Information Disclosure | These screens' queries | mitigate | Rendered only via `Shell` inside `<SignedIn {db}>`; 04-01's `no-leakage.spec.ts` is re-run in this plan's full-suite gate |
</threat_model>

<verification>
1. `cd web && bun run check && bun run lint && bun run format:check && bun run build` — all exit 0.
2. `cd web && bunx playwright test` — every project green.
3. `git diff --stat` touches only `web/src/lib/entities/defs/` and `web/e2e/` for this plan.
4. Zero `phase04-e2e-` records remain in `apollo projeto|etapa|tarefa listar`.
</verification>

<success_criteria>
- WEB-03, WEB-04, WEB-05 satisfied: each entity's create/view/edit/delete proven live in Chromium.
- The generic engine absorbed optional fields, optional links, a number field, and multiple optional
  date fields with zero new components — confirmed by the diff scope assertion.
</success_criteria>

## Artifacts this phase produces

| Artifact | Consumed by |
|----------|-------------|
| `web/src/lib/entities/defs/projetos.ts` | Registry auto-discovery; 04-06 coverage test; 04-04's subtarefa parent chain |
| `web/src/lib/entities/defs/etapas.ts` | Registry auto-discovery; 04-06 coverage test |
| `web/src/lib/entities/defs/tarefas.ts` | Registry auto-discovery; 04-04's `subtarefas` XOR parent (tarefa branch); 04-06 coverage test |
| `web/e2e/entities-projeto-etapa-tarefa.spec.ts` | 04-06 full-suite gate; Phase 6 VERIFY-01 |

<output>
After completion, create `.planning/phases/04-web-spa-auth-crud-smoke-ui/04-03-SUMMARY.md`.
Record verbatim: any place the shipped `EntityConfig` contract had to be stretched (and how it was handled
without editing shared files), the dangling-link guard demonstration, and the fail-loud demonstration output.
</output>
