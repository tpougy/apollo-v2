# Requirements Archive: v1.5 Correções descobertas no onboarding real do calendário de rotinas (RBR)

**Archived:** 2026-09-22
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Apollo v2 — Milestone v1.5

**Milestone:** v1.5 — Correções descobertas no onboarding real do calendário de rotinas (RBR)
**Status:** Roadmap complete → Ready for planning

## Context

Esta milestone nasceu de um relatório de uso real, não de uma ideia de produto: um
agente cadastrou, via `apollo` CLI, o calendário de rotinas de controladoria de
fundos da RBR — 18 fundos, 84 `templatesRotina`, 168 `instanciasRotina`
(competências `2026-07`/`2026-08`), a partir de uma planilha com 87 eventos
recorrentes. O motor de geração funciona (as 84 datas de agosto conferiram
1-a-1 com a planilha, e o job provou-se idempotente na segunda execução). Os 8
achados abaixo foram reportados pelo usuário e **verificados diretamente no
código** desta sessão antes de virarem requisito — nenhum foi aceito por
inferência.

Três são falhas silenciosas (a CLI aceita entrada que o job depois rejeita ou
ignora, sem nenhuma mensagem no momento da escrita); dois são lacunas de
cobertura do calendário real (offset negativo, periodicidade semanal); os
demais são atrito operacional que só aparece em volume. Nenhum achado é coberto
por uma constraint já travada em `PROJECT.md` — as constraints (C-03, C-06,
C-09, separação pure-core/I-O-boundary) limitam a solução, mas nenhuma resolve
o problema sozinha.

**Decisões de implementação registradas** (não são perguntas em aberto — já
foram decididas nesta sessão, antes do roadmap):

- **JOB-01** (validar vs. normalizar `status` de instância): normalizar a
  comparação (`_is_concluida`, case/acento-insensível), não fechar o
  vocabulário de `status`. `status` é texto livre por decisão de produto em
  `projeto`/`etapa`/`tarefa`/`ticket` também — fechar só em
  `instanciasRotina` criaria uma exceção inconsistente. O problema real não é
  a liberdade de texto, é a comparação com uma única grafia exata dentro do
  job.

- **VAL-02** (implementar vs. reservar `propagarAtrasoSoft`): marcar como
  reservado, não implementar a propagação. Implementar reabriria C-09
  (decisão travada, fora de escopo desta migração) e é uma feature de produto
  nova, não uma correção do que já existe.

- **JOB-02** (offset negativo vs. `tipoGeracao` novo para `du_fixo`): estender
  `du_fixo` para aceitar `offsetDias <= 0`, não criar um tipo novo.
  `add_business_days` (`bizdays.py`/`.ts`) já aceita `n` negativo — só falta
  liberar a validação e uma função de cálculo "a partir do fim do mês". Um
  tipo novo duplicaria toda a superfície de CLI/testes/fixtures para expressar
  o que continua sendo semanticamente "o N-ésimo dia útil do mês".

- **SEM-01** (fazer vs. não fazer periodicidade semanal): fazer. Confirmado
  com o usuário que o custo de um `tipoGeracao` novo se justifica mesmo para 1
  evento em 87 (`Atualiz Calc RF`, toda sexta-feira) — decisão explícita do
  usuário, não do agente.

- **LIFE-03** (app de teste dedicado vs. fluxo de limpeza no mesmo app):
  revisado com o usuário durante a Fase 30 — **não criar um segundo app
  InstantDB**. Decisão explícita do usuário: "Quero que você use o mesmo app
  de prod para testar, não temos dados importantes lá ainda. Basta montar um
  fluxo de limpeza após rodar os testes." O texto original de LIFE-03 (abaixo)
  é substituído em espírito por essa decisão — o objetivo ("nenhuma escrita de
  teste aparece na listagem de dados de produção") permanece, mas o mecanismo
  muda de "app distinto" para "limpeza confiável pós-teste no mesmo app",
  detalhado em `.planning/phases/30-ciclo-de-vida-e-higiene-de-dados/30-CONTEXT.md`.

## v1 Requirements

### Validação na escrita (VAL)

- [x] **VAL-01**: `apollo rotina template criar`/`editar --regra-competencia`
  usa `click.Choice` (ou validação equivalente) sobre
  `REGRAS_COMPETENCIA_SUPORTADAS` (`M0`, `M-1`, `M-2`, `M+1`), recusando na
  hora qualquer outro valor com uma mensagem que lista os valores aceitos.
  Elimina o modo de falha atual: um template com `regraCompetencia` inválida
  é aceito e só falha, em silêncio, muito depois, dentro de
  `gerar-instancias` (`skipped: regra_competencia_nao_suportada`). `--help` e
  `docs/ai-usage/CLAUDE.md` deixam de descrever o campo como
  "free-form/não parseado".

- [x] **VAL-02**: `--propagar-atraso-soft/--nao-propagar-atraso-soft` (em
  `criar`/`editar`) tem seu `--help` reescrito para deixar explícito que o
  valor é armazenado mas **não lido** por `gerar-instancias` — reservado para
  uma futura propagação de atraso soft, hoje fora de escopo (C-09). Nenhuma
  mudança de comportamento; só elimina a impressão de que a flag já faz algo.

- [x] **VAL-03**: corrigir o docstring de `apollo rotina instancia status`
  (`entities/rotina.py`), que hoje descreve `dedupeKey` como um "hash" —
  contradiz o docstring canônico de `routine_job.py`, que é explícito sobre
  ser concatenação simples, não hash.

### Robustez do job (JOB)

- [x] **JOB-01**: a resolução de sucessores `encadeado` (D-05-E) reconhece
  `status = "concluida"` de forma normalizada (trim + casefold + remoção de
  acento) em vez de comparar com a string exata, via uma função central
  (`_is_concluida`/equivalente) espelhada nos dois runtimes
  (`routine_job.py`/`routineJob.ts`). `status` continua texto livre na
  escrita — nenhum enum novo. `apollo rotina instancia status --help`
  documenta esse acoplamento (quais grafias contam como "concluída" para
  fins de geração de sucessores).

- [x] **JOB-02**: `du_fixo` aceita `offsetDias <= 0`, com a semântica "contado
  a partir do último dia útil do mês" (`0` = último DU do mês; negativo = N
  dias úteis antes do último), implementada como uma nova função em
  `bizdays.py`/`bizdays.ts` construída sobre `add_business_days` (que já
  suporta contagem negativa). `--help` de `template criar`/`editar` documenta
  a nova semântica de `--offset-dias` para `du_fixo`.

- [x] **JOB-03**: cada entrada do relatório `skipped` de `gerar-instancias`
  inclui o `nome` do template, além de `templateId`/`reason`, em todo ponto
  de emissão de `compute_expected_instances`/`computeExpectedInstances` — uma
  extensão aditiva do contrato JSON já documentado no `--help` de
  `gerar-instancias`.

### Periodicidade semanal (SEM)

- [x] **SEM-01**: novo `tipoGeracao = "semanal"` para `templatesRotina`,
  ancorado em um dia da semana (`--dia-semana`) em vez de um offset mensal,
  cobrindo eventos como "toda sexta-feira" sem forçar uma aproximação
  artificial num tipo mensal. Inclui a regra de competência aplicável a esse
  tipo (a decidir durante o planejamento da fase), cálculo espelhado nos dois
  runtimes, e fixtures novas em `shared/routine-job.testcases.json`.

### Controle de geração (RANGE)

- [x] **RANGE-01**: `apollo rotina gerar-instancias` aceita um recorte
  explícito do range de geração (ex. `--competencia AAAA-MM` e/ou
  `--de`/`--ate`) sem alterar como uma instância é datada nem como o
  `dedupeKey` é derivado — o recorte filtra quais candidatas, dentre as
  calculadas para o range default, são de fato diffed/gravadas nesta
  chamada. Rodar com e sem recorte cobrindo o mesmo intervalo produz o mesmo
  resultado final (idempotência preservada, C-06).

### Ciclo de vida e higiene de dados (LIFE)

- [x] **LIFE-01**: `apollo rotina template deletar` passa a ter um
  comportamento explícito e documentado quando existem `instanciasRotina`
  vinculadas ao template — no mínimo, bloquear por padrão informando a
  contagem de instâncias afetadas, com uma flag explícita para prosseguir.
  Nunca mais deleta um template deixando instâncias órfãs sem qualquer
  sinalização.

- [x] **LIFE-02**: existe um comando dedicado para localizar e remover
  instâncias órfãs (cujo `template` vinculado não resolve mais) — escopo
  estritamente de limpeza de resíduo já existente, não uma via alternativa de
  criação/edição de `instanciasRotina` (a restrição "sem `criar`/`deletar`
  manual" de C-06 continua valendo para o fluxo normal).

- [x] **LIFE-03** (escopo revisado — ver nota de decisão no Context acima): os
  testes marcados `live` (pytest) e a suíte Playwright de `web/e2e` continuam
  rodando contra o MESMO app de produção, mas todo sweep que chama `rotina
  template deletar` agora passa `--force`, e os 5 arquivos que semeiam
  `instanciasRotina` com `dedupeKey` prefixado ganharam uma varredura direta
  (`sweepInstancesByDedupeKeyPrefix`) — elimina o vetor que produziu o resíduo
  `phase23-e2e-dedupe-weekday-...` encontrado na base real durante o
  onboarding (confirmado removido; produção com 0 órfãs).

### Cadastro em lote (BATCH)

- [x] **BATCH-01**: existe um caminho de entrada em lote (ex. um comando
  `importar --from-json <arquivo>` por entidade, ou um `apollo import`
  cross-entity) que valida o lote inteiro antes de escrever, resolve ids de
  pai referenciados dentro do próprio lote (ex. um `fundo` e os `templates`
  que o citam no mesmo arquivo), e permite retomar após uma falha parcial sem
  duplicar o que já foi persistido.

## Future Requirements

<!-- Table-stakes deferidos, não nesta milestone -->

- Propagação real de atraso soft entre templates encadeados (reabriria C-09
  — fora de escopo aqui; VAL-02 apenas documenta o campo como reservado)

- Suporte a múltiplos `app_id`/multi-app InstantDB simultâneo (LIFE-03 não
  introduziu um segundo app — escopo revisado para reutilizar o mesmo app de
  produção com limpeza pós-teste, ver nota de decisão no Context acima)

## Out of Scope

<!-- Exclusões explícitas desta milestone, com razão -->

- Qualquer mudança de comportamento observável de comandos fora de `rotina`
  (`fundo`, `projeto`, `etapa`, `tarefa`, `ticket`, `subtarefa`,
  `log-inferencia`) — escopo é o motor de rotinas e seu entorno de CLI

- Cálculo algorítmico de feriados ou qualquer segunda fonte de calendário
  (C-03, travado) — todas as novas funções de data continuam lendo
  exclusivamente `anbima-calendar.json` via `bizdays.py`/`.ts`

- Qualquer mudança em `routine_job.py`/`routineJob.ts` que faça o job
  deletar ou sobrescrever `status` de uma instância existente — LIFE-01/
  LIFE-02 vivem fora do job de geração (job continua só criando via upsert)

- Migração de dados já cadastrados no onboarding real (os 18
  fundos/84 templates/168 instâncias já existentes não são tocados
  retroativamente por nenhuma fase — as correções valem para cadastro daqui
  para frente; um eventual re-cadastro pontual das duas Prévias DU-2 e do
  evento semanal, hoje impossíveis, é trabalho manual do usuário após
  JOB-02/SEM-01 existirem)

## Traceability

| Requirement | Phase |
|-------------|-------|
| VAL-01 | Phase 26 |
| VAL-02 | Phase 26 |
| VAL-03 | Phase 26 |
| JOB-01 | Phase 27 |
| JOB-02 | Phase 27 |
| JOB-03 | Phase 27 |
| SEM-01 | Phase 28 |
| RANGE-01 | Phase 29 |
| LIFE-01 | Phase 30 |
| LIFE-02 | Phase 30 |
| LIFE-03 | Phase 30 |
| BATCH-01 | Phase 31 |
