---
milestone: v1.4
audited: 2026-08-12
status: passed
scores:
  requirements: 10/10
  phases: 2/2
  integration: 10/10
  flows: 3/3
gaps: {}
tech_debt: []
---

# Milestone v1.4 Audit — CLI instalável via `uv tool install`, sem dependência de admin token no fluxo de uso real

## Summary

Both phases (24, 25) independently verified `passed` by `gsd-verifier`; a dedicated cross-phase integration check confirmed the two phases genuinely compose in the same installed artifact, with zero orphaned code, zero duplicated logic, and a fresh live `uv build` → `uv tool install` → run-outside-repo → `uv tool uninstall` round trip re-proving the combined result. No critical gaps, no tech debt.

## Requirements Coverage (3-source cross-reference)

| Req ID | Description | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------------|-------|------------------|----------------------|-------------------|---------------|
| PKG-01 | Vendor ANBIMA calendar into package, read via `importlib.resources` | 24 | passed | listed | `[x]` | **satisfied** |
| PKG-02 | Byte-parity test between the two calendar copies | 24 | passed | listed | `[x]` | **satisfied** |
| PKG-03 | Embedded default `app_id` in `config.py` | 24 | passed | listed | `[x]` | **satisfied** |
| PKG-04 | `.env.instantdb`/`APOLLO_ENV_FILE` override the embedded default | 24 | passed | listed | `[x]` | **satisfied** |
| PKG-05 | Real `uv build`/`uv tool install` round trip outside the repo | 24 | passed | listed | `[x]` | **satisfied** |
| AUTH-01 | `apollo auth login` via public `/runtime/auth/*` endpoints, no admin token | 25 | passed | listed | `[x]` | **satisfied** |
| AUTH-02 | `apollo auth login`'s observable output/exit codes unchanged | 25 | passed | listed | `[x]` | **satisfied** |
| AUTH-03 | No CLI command depends on `INSTANT_APP_ADMIN_TOKEN` to operate | 25 | passed | listed | `[x]` | **satisfied** |
| AUTH-04 | `admin_token_present`/`apollo doctor` keep working | 25 | passed | listed | `[x]` | **satisfied** |
| AUTH-05 | Admin-token structural test gates tightened correctly | 25 | passed | listed | `[x]` | **satisfied** |

No orphaned requirements — every REQ-ID in the traceability table has a matching entry in a phase's VERIFICATION.md.

## Phase Verification Summary

| Phase | Name | Verification Status | Must-Haves |
|-------|------|----------------------|------------|
| 24 | Packaging & Installability | passed | 9/9 |
| 25 | Public Auth Login | passed | 5/5 |

## Cross-Phase Integration

Dedicated `gsd-integration-checker` pass (this audit): 1 seam checked (`auth.py` → `config.py`'s `load_instant_config()`), fully wired, zero duplication. Re-ran the full composed live proof independently:
- `uv build` → `uv tool install --force` → from `/tmp` (outside repo, no `.env.instantdb`, `INSTANT_APP_ADMIN_TOKEN` unset): `apollo doctor` correctly reports embedded-default app_id + admin token absent; `apollo --version`/`apollo auth login --help` run cleanly, proving the installed artifact's import chain (`auth.py` → `httpx`/`instantdb` → `config.load_instant_config`) works packaged, not just in the dev tree.
- `uv tool uninstall apollo-cli` left the machine clean.
- Full `cli/` test suite re-run with `INSTANT_APP_ADMIN_TOKEN` unset: 418 passed, 2 skipped, 1 xfailed, 0 failed.
- Zero orphaned/dead code from the old admin-SDK-based login path.

## E2E Flows

| Flow | Status |
|------|--------|
| Build → install → run outside repo (no `shared/`, no `.env.instantdb`) → `doctor`/`--version` clean | ✓ Complete |
| Full magic-code login round trip (real email, zero admin token) | ✓ Complete (proven live in Phase 25's own verification) |
| Full offline + live test suite, admin token absent | ✓ Complete |

## Tech Debt

None identified.

## Conclusion

Milestone v1.4 is ready to close. No gaps, no tech debt, no pending human verification.
