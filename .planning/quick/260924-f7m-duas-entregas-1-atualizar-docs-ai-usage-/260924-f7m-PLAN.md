---
phase: 260924-f7m
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ai-usage/README.md
  - docs/ai-usage/CLAUDE.md
  - cli/apollo_cli/data/scaffold/__init__.py
  - cli/apollo_cli/data/scaffold/README.md
  - cli/apollo_cli/data/scaffold/CLAUDE.md
  - cli/tests/test_scaffold_vendored_parity.py
  - .planning/STATE.md
  - cli/apollo_cli/init.py
  - cli/apollo_cli/cli.py
  - cli/tests/test_init.py
autonomous: true

must_haves:
  truths:
    - "docs/ai-usage/README.md and docs/ai-usage/CLAUDE.md contain zero remaining occurrences of \"fundo\"/\"fundos\" (case-insensitive) anywhere — every CLI-syntax reference (apollo fundo *, --fundo-id) AND every generic-prose reference (domain diagram, opening paragraph, privacy note) renamed to entidade/entidades per D3+D6; the rotina/import command-surface drift found by research stays untouched (explicitly out of scope)."
    - "cli/apollo_cli/data/scaffold/README.md and cli/apollo_cli/data/scaffold/CLAUDE.md are byte-identical to docs/ai-usage/README.md and docs/ai-usage/CLAUDE.md at all times, enforced by a passing automated pytest parity test (mirrors the ANBIMA calendar precedent)."
    - "apollo init <path> creates <path> if it doesn't exist and writes README.md + CLAUDE.md into it with content byte-identical to the vendored scaffold, live-proven via a real CLI invocation against a real temp directory."
    - "Running apollo init again against a folder whose README.md/CLAUDE.md were locally edited (content differs from the vendored template) refuses with a non-zero exit and a structured JSON error, writing nothing, unless --force is passed — live-proven for both the refusal and the --force override."
    - "apollo init always exits 0 and reports real auth status as JSON: when a valid session exists it reports the authenticated email (live-verified via the same load_session()+verify_token() mechanism apollo auth whoami uses); when no session exists (including a simulated fresh machine via APOLLO_SESSION_FILE) it reports the exact two apollo auth login commands to run next, and never crashes or exits non-zero for this expected first-run state."
    - "docs/ai-usage/CLAUDE.md's remaining known staleness on the rotina/import command surface (semanal periodicity, --dia-semana, extended --offset-dias semantics, --competencia/--de/--ate, template deletar --force, instancia limpar-orfas, the whole apollo import command) is recorded as a visible, tracked deferred item in .planning/STATE.md, not silently left undocumented."
  artifacts:
    - docs/ai-usage/README.md
    - docs/ai-usage/CLAUDE.md
    - cli/apollo_cli/data/scaffold/README.md
    - cli/apollo_cli/data/scaffold/CLAUDE.md
    - cli/tests/test_scaffold_vendored_parity.py
    - cli/apollo_cli/init.py
    - cli/tests/test_init.py
  key_links:
    - "docs/ai-usage/{README,CLAUDE}.md (source of truth, D2) -> cli/apollo_cli/data/scaffold/{README,CLAUDE}.md (vendored copy, byte-identical, enforced by cli/tests/test_scaffold_vendored_parity.py) -> apollo_cli.init.run_init()'s importlib.resources.files(\"apollo_cli.data.scaffold\") read -> apollo init <path>'s written output files"
    - "apollo_cli.init.run_init()'s check-then-write overwrite guard (exit code 6, mirrors rotina.py template deletar's exit-code-5 idiom) -> cli.py's @apollo.command(name=\"init\") thin registration (mirrors the import/import_batch precedent's emit()-based JSON output, NOT doctor's plain-text click.echo) -> crud_helpers.emit() JSON stdout"
    - "apollo_cli.session.load_session()/MissingSessionError/CorruptSessionError + Instant(...).auth.verify_token(...) (apollo_cli.auth.whoami's exact mechanism), reused read-only and wrapped never-fatal inside apollo_cli.init._auth_status() — NOT crud_helpers.require_session(), which SystemExits on the very failure mode init must tolerate"
---

<objective>
Two deliverables, per CONTEXT.md D1-D6 (locked, do not re-litigate):

1. Update `docs/ai-usage/README.md`/`CLAUDE.md` (the "Apollo Tasks" onboarding template) to catch up with the `fundo`->`entidade` rename shipped in quick task `260922-vbt` — every CLI-syntax reference AND every generic-prose reference (D6: rename the prose too, no half-renamed state), while explicitly leaving the newer, unrelated rotina/import command-surface drift out of scope (D6: tracked as a deferred item instead of silently ignored). These docs become a vendored copy shipped inside the CLI package itself, at `cli/apollo_cli/data/scaffold/`, read via `importlib.resources` (D2, mirrors the existing ANBIMA calendar vendoring + byte-parity-test precedent).

2. A new top-level `apollo init <path>` command (D1) that scaffolds an "Apollo Tasks" folder: creates `<path>` if missing, writes the vendored `README.md`/`CLAUDE.md` into it (refusing to overwrite locally-edited copies unless `--force`), then reports real, read-only auth status so the user knows whether to run `apollo auth login` next — never crashing on an unauthenticated first-run machine (D4).

Purpose: keep the onboarding template a Claude Code operator opens in a fresh "Apollo Tasks" folder factually correct against the current CLI surface, and let a new user go from "nothing" to "a folder ready to open in Claude Code" with one command instead of manually copying files.
Output: `docs/ai-usage/{README,CLAUDE}.md` renamed and accurate; a vendored, parity-tested copy under `cli/apollo_cli/data/scaffold/`; a working, live-tested `apollo init <path>` command.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260924-f7m-duas-entregas-1-atualizar-docs-ai-usage-/260924-f7m-CONTEXT.md
@.planning/quick/260924-f7m-duas-entregas-1-atualizar-docs-ai-usage-/260924-f7m-RESEARCH.md
@docs/ai-usage/README.md
@docs/ai-usage/CLAUDE.md
@cli/apollo_cli/bizdays.py
@cli/tests/test_calendar_vendored_parity.py
@cli/apollo_cli/cli.py
@cli/apollo_cli/auth.py
@cli/apollo_cli/session.py
@cli/apollo_cli/crud_helpers.py
@cli/apollo_cli/entities/rotina.py
@cli/tests/conftest.py
@cli/tests/test_batch_import.py

Read first (do not re-read once loaded):
- `docs/ai-usage/README.md` (146 lines) / `docs/ai-usage/CLAUDE.md` (216 lines) — full current text, every "fundo" occurrence already exhaustively grepped (Task 1's action below gives the complete line-by-line diff, nothing left to discover).
- `cli/apollo_cli/bizdays.py:1-57` — the exact `importlib.resources.files("apollo_cli.data")...read_text(encoding="utf-8")` vendoring pattern Task 1/2 mirror for `.md` files instead of the calendar JSON.
- `cli/tests/test_calendar_vendored_parity.py` (27 lines, full) — the exact byte-parity test shape Task 1's new test doubles for 2 files.
- `cli/apollo_cli/cli.py` (131 lines, full) — `doctor` (plain `click.echo`, NOT a JSON precedent — see RESEARCH.md's "Important correction to D4's stated JSON-output precedent") vs `import`/`import_batch` (the real JSON-via-`emit()` precedent `init` follows).
- `cli/apollo_cli/auth.py:145-177` (`whoami`) and `cli/apollo_cli/session.py` (full) — the exact `load_session()`/`MissingSessionError`/`CorruptSessionError`/`Instant(app_id=..., admin_token="").auth.verify_token(...)` mechanism `init`'s auth-status check reuses read-only, wrapped so it never raises.
- `cli/apollo_cli/entities/rotina.py:79-88, 458-505` — the exact `--force/--no-force` + distinct-exit-code idiom (`_EXIT_INSTANCES_LINKED = 5`) `init`'s overwrite guard mirrors, using exit code `6` (confirmed unused: grep `Final[int]` across `cli/apollo_cli/*.py`/`entities/*.py` at execution time before hardcoding, in case a later plan claimed it since research — 1/2/3/4/5 were the only taken values at planning time).
- `cli/tests/conftest.py` (full) — `run_cli`/`live_session`/`json_out`/`cleanup_records`/`unique_suffix` fixtures Task 2's tests reuse.
- `cli/tests/test_batch_import.py:1-45` — the `pytestmark = pytest.mark.live` module-level convention and file-per-standalone-command precedent Task 2's new test file follows.

This is a code/docs-only quick task, zero new external dependencies. No live production writes anywhere in this plan — `apollo init`'s auth-status check is read-only (one `verify_token` GET-equivalent against the public, unauthenticated InstantDB endpoint, never a login/transact).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rename docs to entidade, vendor a byte-parity-tested copy into the CLI package</name>
  <files>docs/ai-usage/README.md, docs/ai-usage/CLAUDE.md, cli/apollo_cli/data/scaffold/__init__.py, cli/apollo_cli/data/scaffold/README.md, cli/apollo_cli/data/scaffold/CLAUDE.md, cli/tests/test_scaffold_vendored_parity.py, .planning/STATE.md</files>
  <action>
Step A — edit `docs/ai-usage/README.md` (D3+D6), applying exactly these 6 line-level replacements, nothing else:
- Line 4: "pessoal para organizar fundos, projetos, rotinas e tarefas usando o" -> "pessoal para organizar entidades, projetos, rotinas e tarefas usando o".
- Line 84: "- Qualquer outro arquivo de apoio (notas, listas de fundos/rotinas a" -> "- Qualquer outro arquivo de apoio (notas, listas de entidades/rotinas a".
- Line 91: "os fundos/projetos/rotinas/tarefas que quer cadastrar (colar e-mails," -> "as entidades/projetos/rotinas/tarefas que quer cadastrar (colar e-mails,".
- Line 95: "- criar as entidades na ordem certa (fundo -> projeto -> etapa -> tarefa ->" -> "- criar as entidades na ordem certa (entidade -> projeto -> etapa -> tarefa ->" (only the parenthetical's first word changes; the arrows/rest of the line are unchanged; "->" here stands for the file's literal "→" glyph).
- Line 104: "apollo fundo listar" -> "apollo entidade listar" (inside the "Comandos úteis" fenced code block).
- Line 144 (D6 — generic prose, rename too, do not leave as-is): "notas com nomes reais de fundos/clientes em texto livre, e mantenha o" -> "notas com nomes reais de entidades/clientes em texto livre, e mantenha o".
Line 110's existing "apollo &lt;entidade&gt; &lt;ação&gt; --help" generic placeholder is already correct — do not touch it.

Step B — edit `docs/ai-usage/CLAUDE.md` (D3+D6), applying exactly these replacements:
- Line 3: "Você está ajudando o usuário a fazer o **onboarding inicial** dos fundos," -> "Você está ajudando o usuário a fazer o **onboarding inicial** das entidades,".
- Line 5 (D6 — generic prose/opening paragraph, rename too): "controladoria de fundos. Este repositório não contém o código do Apollo; ele" -> "controladoria de entidades. Este repositório não contém o código do Apollo; ele".
- Line 32 (domain-hierarchy diagram root node): "fundo" -> "entidade".
- Line 33: " └─ projeto (opcionalmente ligado a um fundo)" -> " └─ projeto (opcionalmente ligado a uma entidade)".
- Line 38: "ticket (demanda avulsa, ex. e-mail recebido; opcionalmente ligado a um fundo)" -> "ticket (demanda avulsa, ex. e-mail recebido; opcionalmente ligado a uma entidade)".
- Line 41: "templatesRotina (modelo de rotina recorrente, opcionalmente ligado a um fundo)" -> "templatesRotina (modelo de rotina recorrente, opcionalmente ligado a uma entidade)".
- Lines 59-64: the "### `apollo fundo criar`" heading plus its 3-line fenced field block (containing, in order, "--nome TEXT (obrigatório)", "--codigo TEXT (obrigatório, curto, tipo sigla)", "--ativo / --inativo (default: --ativo)") becomes "### `apollo entidade criar`" followed by a 4-line fenced field block, in this exact order: "--nome TEXT (obrigatório)", "--codigo TEXT (obrigatório, curto, tipo sigla)", "--tipo-entidade TEXT (obrigatório, ex.: Fundo, Cliente, Area)", "--ativo / --inativo (default: --ativo)" — the new line's field name/order/wording matches the real CLI verbatim (`cli/apollo_cli/entities/entidade.py:40-52`, already read).
- Line 73 (inside the `apollo projeto criar` block): "--fundo-id TEXT (precisa já existir — use `apollo fundo listar` para achar o id)" -> "--entidade-id TEXT (precisa já existir — use `apollo entidade listar` para achar o id)".
- Line 105 (inside `apollo ticket criar`): "--fundo-id TEXT (precisa já existir)" -> "--entidade-id TEXT (precisa já existir)".
- Line 124 (inside `apollo rotina template criar`): "--fundo-id TEXT" -> "--entidade-id TEXT".
- Lines 138-139: "1. Pergunte/receba a lista de fundos primeiro (tudo mais pode linkar a um" / "   fundo). Crie-os com `apollo fundo criar`." -> "1. Pergunte/receba a lista de entidades primeiro (tudo mais pode linkar a" / "   uma entidade). Crie-os com `apollo entidade criar`.".
- Lines 141-142: "   ele pertence a um fundo já criado; use `apollo fundo listar` para" / "   recuperar o `id` antes de usar `--fundo-id`." -> "   ele pertence a uma entidade já criada; use `apollo entidade listar` para" / "   recuperar o `id` antes de usar `--entidade-id`.".
- Line 151: "6. Rotinas são a última coisa a cadastrar (dependem de fundo, se ligadas a" -> "6. Rotinas são a última coisa a cadastrar (dependem de entidade, se ligadas a".
- Line 176: "- Escolher um `--fundo-id`/`--projeto-id`/`--etapa-id` por similaridade de" -> "- Escolher um `--entidade-id`/`--projeto-id`/`--etapa-id` por similaridade de".
- Line 194 (the `--entidade-tipo` enumeration list, confirmed against `shared/instant.schema.ts`'s 9 live namespace keys this session — the other 7 names are unchanged): "`fundos`, `projetos`, `etapas`, `tarefas`, `tickets`, `subtarefas`," -> "`entidades`, `projetos`, `etapas`, `tarefas`, `tickets`, `subtarefas`,".
Everything else in both files (subtarefa/ticket/rotina sections beyond the listed lines, the log-inferencia rules, the "what not to do" list) stays byte-for-byte unchanged — this is D3's explicit boundary. Do NOT touch the `--tipo-geracao`/`--dia-semana`/`--offset-dias`/`gerar-instancias`/`template deletar --force`/`limpar-orfas`/`apollo import` drift RESEARCH.md separately found (Step D below tracks it instead).

Step C — verify zero renames were missed: after Steps A/B, grepping both files case-insensitively for "fundo" must return nothing (every one of the 21 occurrences enumerated above is the complete, exhaustive set found via `grep -n -i fundo` over both files this session).

Step D — vendor the now-updated docs (D2), AFTER Steps A-C are complete so the vendored copy reflects the final renamed content, not the pre-rename version. Create `cli/apollo_cli/data/scaffold/__init__.py` as an empty file (mirrors `cli/apollo_cli/data/__init__.py`, making `apollo_cli.data.scaffold` a real importable subpackage — no `pyproject.toml` edit needed, `[tool.uv.build-backend] module-root = ""` already ships every non-`.py` file under `apollo_cli/`, confirmed this session). Copy `docs/ai-usage/README.md` byte-for-byte to `cli/apollo_cli/data/scaffold/README.md`, and `docs/ai-usage/CLAUDE.md` byte-for-byte to `cli/apollo_cli/data/scaffold/CLAUDE.md` (exact copy, no templating/placeholder substitution — both are generic instructional content per D2).

Step E — write `cli/tests/test_scaffold_vendored_parity.py`, mirroring `cli/tests/test_calendar_vendored_parity.py`'s exact shape (module docstring explaining the byte-parity gate, `from apollo_cli.config import find_repo_root`, `from importlib import resources`). Parametrize (or write two separate test functions) over both filenames ("README.md", "CLAUDE.md"): for each, `source_path = find_repo_root() / "docs" / "ai-usage" / filename`, `vendored_resource = resources.files("apollo_cli.data.scaffold").joinpath(filename)`, assert `source_path.read_bytes() == vendored_resource.read_bytes()`, with an assertion message naming both paths and pointing at D2 — same assertion-message style as the calendar test. This test needs no `pytest.mark.live` (pure filesystem read, no network) — same as the calendar test's own precedent.

Step F — D6's second resolution: add one new row to `.planning/STATE.md`'s "Deferred Items" table (the existing `| Category | Item | Status | Deferred At |` table), with Category `Docs`, Item recording that `docs/ai-usage/CLAUDE.md` is now known-stale on the rotina/import command surface (semanal periodicity, `--dia-semana`, extended `--offset-dias` semantics, `--competencia`/`--de`/`--ate`, `template deletar --force`, `instancia limpar-orfas`, the whole `apollo import` command — all postdate these docs and are unrelated to the fundo rename this task addressed) as of this quick task's date, Status "Deferred — explicit out-of-scope per CONTEXT.md D6, future pass should reconcile deliberately", and Deferred At "260924-f7m close". Insert it as a new row in the existing table (do not restructure the table, do not touch any other row).
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2 && ! grep -qi "fundo" docs/ai-usage/README.md docs/ai-usage/CLAUDE.md && grep -q "260924-f7m" .planning/STATE.md && cd cli && uv run pytest tests/test_scaffold_vendored_parity.py -v</automated>
  </verify>
  <done>Zero case-insensitive "fundo" occurrences remain in either doc file. `cli/apollo_cli/data/scaffold/{README,CLAUDE}.md` exist and are byte-identical to the renamed docs, proven by a passing `test_scaffold_vendored_parity.py`. `.planning/STATE.md`'s Deferred Items table has one new row for the rotina/import doc-drift item, referencing this quick task's id.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `apollo init &lt;path&gt;` — scaffold + force guard + read-only auth status, live-tested</name>
  <files>cli/apollo_cli/init.py, cli/apollo_cli/cli.py, cli/tests/test_init.py</files>
  <action>
Step A — create `cli/apollo_cli/init.py` (D1: standalone module, mirrors `batch_import.py`'s shape of "module owns its own error emission + `SystemExit`, `cli.py` just calls the function then `emit()`s the returned dict on success"). Module docstring states its purpose (scaffold an "Apollo Tasks" folder) and D4's "never crashes on an unauthenticated first run" guarantee.

Imports: `from __future__ import annotations`; `from importlib import resources`; `from pathlib import Path`; `from typing import Final`; `import click`; `import httpx`; `from instantdb import Instant, InstantAPIError`; `from apollo_cli.config import load_instant_config`; `from apollo_cli.session import CorruptSessionError, MissingSessionError, load_session`.

Define `_EXIT_TARGET_EXISTS_DIFFERS: Final[int] = 6` with a comment mirroring `rotina.py`'s `_EXIT_INSTANCES_LINKED` comment style: distinct from `EXIT_NO_SESSION=1`/Click's own UsageError=2/`EXIT_API_ERROR=3`/`EXIT_NETWORK_ERROR=4`/`_EXIT_INSTANCES_LINKED=5` — confirm via `grep -n "Final\[int\]" cli/apollo_cli/*.py cli/apollo_cli/entities/*.py` immediately before writing this line that `6` is still unclaimed (it was at planning time; re-verify, do not blindly trust this plan if something changed).

Define `_SCAFFOLD_FILES: Final[tuple[str, str]] = ("README.md", "CLAUDE.md")`.

Define `_scaffold_text(filename: str) -> str`: returns `resources.files("apollo_cli.data.scaffold").joinpath(filename).read_text(encoding="utf-8")`.

Define `_auth_status() -> dict[str, object]`: a read-only, NEVER-raising reimplementation of `auth.whoami`'s full body (D4 step 3 — the SAME mechanism, `load_session()` then `Instant(app_id=config.app_id, admin_token="").auth.verify_token(session.refresh_token)`), but every failure branch returns a status dict instead of emitting/raising. Try `session = load_session()`; on `(MissingSessionError, CorruptSessionError)`, return `{"authenticated": False}`. Otherwise try `config = load_instant_config()`, build the SAME unauthenticated `Instant(app_id=config.app_id, admin_token="")` client `whoami` uses (never `session_client()`/`login_client()`), call `client.auth.verify_token(session.refresh_token)`; on success return `{"authenticated": True, "email": user["email"]}`; on `(InstantAPIError, httpx.HTTPError)` (expired/invalid token, or no network) return `{"authenticated": False}` — do NOT reuse `crud_helpers.require_session()`/`auth._emit_api_error`/`_emit_network_error` anywhere in this function, they all `raise SystemExit`, exactly what D4 forbids here.

Define `run_init(target: Path, *, force: bool) -> dict[str, object]` — the function `cli.py`'s `init` command delegates to:
1. `target.mkdir(parents=True, exist_ok=True)` (D4 step 1, unconditional — happens even if the write-guard below later refuses).
2. Check-then-write, in that order, for BOTH files before writing either (Pitfall 3: never partially write one file then discover the second conflicts) — for each of `_SCAFFOLD_FILES`, read `(target / filename)`'s existing text if `.is_file()`, compare to `_scaffold_text(filename)`; build two lists: `to_write` (missing, or present-and-identical — the latter is a no-op but not a conflict) and `conflicts` (present, content differs, `force` is `False`).
3. If `conflicts` is non-empty: `click.echo(json.dumps({"error": "scaffold_files_exist", "path": str(target.resolve()), "conflicting_files": sorted(conflicts), "hint": "pass --force to overwrite (any other file in the folder is left untouched)"}, sort_keys=True), err=True); raise SystemExit(_EXIT_TARGET_EXISTS_DIFFERS)` — zero files written this call (needs its own `import json` at the top of the module for this one call site, since `run_init` composes this JSON itself rather than importing `crud_helpers._emit_err`, a private name in another module).
4. Otherwise, for each filename: if its existing content already equals the vendored text, record it under `unchanged` and skip writing (silent no-op, mirrors `template deletar --force`'s "zero linked instances = silent no-op" precedent); otherwise `(target / filename).write_text(_scaffold_text(filename), encoding="utf-8")` and record it under `written`.
5. Compute `auth_status = _auth_status()`.
6. Build and return the success payload: `{"path": str(target.resolve()), "written": sorted(written), "unchanged": sorted(unchanged), "authenticated": auth_status["authenticated"], "readme": str((target / "README.md").resolve()), "hint": "Abra esta pasta no Claude Code — o README.md explica os próximos passos."}`, PLUS (only when `auth_status["authenticated"]` is `True`) `"email": auth_status["email"]`, OR (only when `False`) `"next_steps": ["apollo auth login --email <voce@exemplo.com>", "apollo auth login --email <voce@exemplo.com> --code <codigo>"]` — the exact two commands D4 mandates, verbatim.

Step B — register in `cli/apollo_cli/cli.py` (D1, mirrors `import`/`import_batch`'s thin-registration shape, placed after the existing `import_batch` command and before `def main()`). Add `from apollo_cli.init import run_init` as the last line of the existing import block (alphabetically after `entities`). Add:
`@apollo.command(name="init")` with `@click.argument("path", type=click.Path(file_okay=False, path_type=Path))` (no `exists=True` — the whole point is it may not exist yet) and `@click.option("--force/--no-force", default=False, help="Overwrite README.md/CLAUDE.md even if their current content differs from the vendored scaffold template. Any OTHER file already in the folder is never touched. With identical content already present, this flag is a silent no-op.")`. Function `def init(path: Path, force: bool) -> None:` with a real docstring (>= 20 chars — `test_every_command_has_rich_help_text` in `test_cli_surface.py` walks every registered command including this new one) explaining it scaffolds an "Apollo Tasks" folder (README.md + CLAUDE.md) at PATH and reports auth status. Body: `report = run_init(path, force=force); emit(report)` (reuses the already-imported `crud_helpers.emit`, D4's JSON-output requirement — NOT `doctor`'s plain `click.echo`).

Step C — create `cli/tests/test_init.py`, mirroring `test_batch_import.py`'s file-per-standalone-command precedent and fixture usage (`run_cli`, `live_session`, `tmp_path`, `monkeypatch` — the last is pytest's built-in). Module docstring + `pytestmark = pytest.mark.live` at module level (every test here invokes the real CLI via `run_cli`, and at least one needs a real session; matches D5's live-verification convention — this file needs no `cleanup_records`, `init` never creates any InstantDB record).

Test 1, `test_init_creates_folder_and_writes_vendored_scaffold_files` (D5-a): `target = tmp_path / "apollo-tasks"`; `result = run_cli(["init", str(target)])`; assert `result.result.exit_code == 0`; `report = result.json_out()`; assert `sorted(report["written"]) == ["CLAUDE.md", "README.md"]` and `report["unchanged"] == []`; assert `(target / "README.md").read_text(encoding="utf-8") == resources.files("apollo_cli.data.scaffold").joinpath("README.md").read_text(encoding="utf-8")` (and the same for `CLAUDE.md`) — the exact vendored content, not merely "some content" (needs `from importlib import resources` imported in this test file).

Test 2, `test_init_force_refusal_then_override_round_trip` (D5-b): `target = tmp_path / "apollo-tasks-force"`; `target.mkdir()`; write a deliberately different `README.md` at `target / "README.md"` (e.g. `"minhas notas pessoais\n"`, content that is NOT the vendored text) via plain `Path.write_text`; `run_cli(["init", str(target)])` (no `--force`) must exit with code `6`, and `(target / "README.md").read_text()` must still be the untouched `"minhas notas pessoais\n"` (proving zero writes on refusal); then `run_cli(["init", str(target), "--force"])` must exit `0`, and `(target / "README.md").read_text()` must now equal the vendored `README.md` content.

Test 3, `test_init_reports_authenticated_status_when_session_present` (D5-c, real session branch — requires `live_session` fixture, so it skips cleanly if this dev machine has no persisted session, matching `test_batch_import.py`'s own convention): `target = tmp_path / "apollo-tasks-auth"`; `result = run_cli(["init", str(target)])`; assert exit code `0`; `report = result.json_out()`; assert `report["authenticated"] is True` and `report["email"] == live_session.email` (the REAL logged-in email on this machine, never a hardcoded/fake value, per D5).

Test 4, `test_init_reports_guidance_when_unauthenticated` (D5-c, simulated-fresh-machine branch): `monkeypatch.setenv("APOLLO_SESSION_FILE", str(tmp_path / "does-not-exist" / "session"))`; `target = tmp_path / "apollo-tasks-noauth"`; `result = run_cli(["init", str(target)])`; assert exit code `0` (the core D4 guarantee: never crashes/exits non-zero for this expected state); `report = result.json_out()`; assert `report["authenticated"] is False`; assert `report["next_steps"] == ["apollo auth login --email <voce@exemplo.com>", "apollo auth login --email <voce@exemplo.com> --code <codigo>"]` (the exact two commands, verbatim).
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2/cli && uv run pytest tests/test_init.py -v -m live && uv run pytest tests/test_cli_surface.py -v</automated>
  </verify>
  <done>`apollo init &lt;path&gt;` creates the folder and writes byte-identical vendored README.md/CLAUDE.md, live-proven. Re-running against a locally-edited folder refuses (exit 6, JSON error, zero writes) without `--force` and succeeds with it. Auth status is reported honestly both ways: real email when a live session exists, the exact two `apollo auth login` commands when none does (including the simulated-fresh-machine case) — `init` never exits non-zero for the unauthenticated case. `test_cli_surface.py`'s help-completeness/JSON-output/suppression gates all still pass with `init` registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `apollo init <path>` -> local filesystem | User-supplied `PATH` argument is used directly for `mkdir`/file writes — no privilege boundary crossed (same permissions as the invoking user), but a local write nonetheless |
| `apollo init`'s auth-status check -> InstantDB public API | Read-only `verify_token` call against the SAME unauthenticated, no-admin-token client `whoami` already uses — never a login/transact, never writes remotely |
| `docs/ai-usage/*.md` (repo source) -> `cli/apollo_cli/data/scaffold/*.md` (shipped package artifact) | Vendoring boundary — the parity test is the only thing preventing silent drift between what a developer edits and what every `apollo init` invocation actually writes to a user's disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-f7m-01 | Tampering | `run_init`'s overwrite guard | medium | mitigate | Check-then-write ordering (Pitfall 3): both files' existing content is read and compared BEFORE any write; any conflict blocks the ENTIRE call (exit 6, zero writes) unless `--force` — a user's own edits to a previously-scaffolded folder are never silently clobbered. |
| T-f7m-02 | Denial of Service (crash on expected state) | `run_init`'s `_auth_status()` | high | mitigate | `_auth_status()` catches `MissingSessionError`/`CorruptSessionError`/`InstantAPIError`/`httpx.HTTPError` internally and always returns a status dict — never `require_session()`/`whoami`'s own `SystemExit`-raising helpers. An unauthenticated first-run machine (the most common real-world case) must exit 0, live-proven by Test 4. |
| T-f7m-03 | Information Disclosure | `_auth_status()`'s token handling | low | accept | Inherits `whoami`'s already-proven hygiene unchanged: the refresh token itself is read from the session file and sent to `verify_token`, but never echoed, logged, or included in `run_init`'s returned JSON — only `email`/`authenticated` are ever emitted. |
| T-f7m-04 | Tampering | Arbitrary user-supplied `PATH` argument | low | accept | Standard local single-user CLI behavior (same trust model as `mkdir -p`/`git init` itself) — no privilege escalation, writes only whatever permissions the invoking user already has; the user explicitly directs the target path. |
| T-f7m-05 | Tampering (docs/package drift) | `cli/apollo_cli/data/scaffold/*.md` silently diverging from `docs/ai-usage/*.md` | medium | mitigate | `cli/tests/test_scaffold_vendored_parity.py` fails loudly (byte comparison) the moment one side is edited without the other — same mechanism already proven for the ANBIMA calendar vendoring. |

</threat_model>

<verification>
1. Task 1: `grep -qi fundo docs/ai-usage/README.md docs/ai-usage/CLAUDE.md` finds nothing; `cli/tests/test_scaffold_vendored_parity.py` passes; `.planning/STATE.md` contains the new deferred-item row.
2. Task 2: `cli/tests/test_init.py`'s 4 live tests pass — real vendored-content write, force-refusal-then-override round trip, both auth-status branches (real session and simulated-fresh-machine).
3. `cli/tests/test_cli_surface.py` (help-completeness, JSON-output-only, suppression-count gates) passes with `init` registered as a new top-level command.
</verification>

<success_criteria>
- `docs/ai-usage/{README,CLAUDE}.md` have zero remaining `fundo`/`fundos` references (CLI syntax and generic prose alike), while the unrelated rotina/import doc drift stays untouched and is now tracked in STATE.md's Deferred Items.
- `cli/apollo_cli/data/scaffold/{README,CLAUDE}.md` are byte-identical to the docs source, enforced by an automated test.
- `apollo init <path>` works end-to-end, live-verified: creates the folder, writes the exact vendored files, refuses-then-force-overwrites a locally-edited folder, and always exits 0 while honestly reporting real auth status (authenticated email, or the exact two login commands to run next).
</success_criteria>

<output>
Create `.planning/quick/260924-f7m-duas-entregas-1-atualizar-docs-ai-usage-/260924-f7m-SUMMARY.md` when done
</output>
