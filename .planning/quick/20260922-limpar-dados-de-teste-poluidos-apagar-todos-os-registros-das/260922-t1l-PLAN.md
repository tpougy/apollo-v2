---
quick_id: 260922-t1l
type: execute
autonomous: true
depends_on: []
files_modified:
  - cli/scripts/cleanup_rotina_test_data.py
files_deleted:
  - cli/scripts/cleanup_rotina_test_data.py
must_haves:
  truths:
    - "Querying instanciasRotina in production (admin client, no where filter) returns 0 rows after cleanup."
    - "Querying templatesRotina in production (admin client, no where filter) returns 0 rows after cleanup."
    - "The counts for fundos, projetos, etapas, tarefas, tickets, subtarefas, and logInferenciaClaude are byte-identical before and after the cleanup run — proving the delete never touched them."
    - "The before-cleanup counts for templatesRotina/instanciasRotina were captured and are non-zero, proving the run actually deleted real rows rather than no-op'ing against an already-empty table."
    - "No permanent script or CLI command is left behind — the cleanup tool is transient and removed once production is confirmed clean."
  artifacts: []
  key_links:
    - "cli/scripts/cleanup_rotina_test_data.py -> apollo_cli.instant_client.login_client() -> repo-root .env.instantdb's INSTANT_APP_ADMIN_TOKEN -> InstantDB /admin/query + /admin/transact, scoped to templatesRotina and instanciasRotina only"
---

<objective>
Permanently delete every `templatesRotina` and `instanciasRotina` record in the
production InstantDB app — the test-onboarding routine calendar (STATE.md:
"18 fundos, 84 templatesRotina, 168 instanciasRotina" from the v1.5 onboarding
run, plus whatever accumulated since) — while leaving the other 7 entities
(`fundos`, `projetos`, `etapas`, `tarefas`, `tickets`, `subtarefas`,
`logInferenciaClaude`) completely untouched.

This must use InstantDB's ADMIN client (`apollo_cli.instant_client.login_client()`,
backed by `INSTANT_APP_ADMIN_TOKEN` in the repo-root `.env.instantdb`), never
the session-scoped client — the goal is "TODOS os registros" across every
`donoId`, not just the current CLI session's own rows. `apollo_cli`'s own CRUD
surface (`session_client()`, `rotina.py`'s `limpar-orfas`) is intentionally
scoped to the authenticated user and to orphan instances only, so it cannot
satisfy this "delete literally everything in these two tables" requirement —
a standalone script reusing the admin client constructor is required instead.

Purpose: remove test-onboarding pollution from production before real routine
data is entered, without risking any of the other 7 real entities.
Output: 0 surviving `templatesRotina`/`instanciasRotina` rows in production,
proven by an independent post-hoc admin query; no permanent script left in
the repo (transient, deleted once the cleanup is confirmed).
</objective>

<context>
Read first (do not re-read once loaded):
- `cli/apollo_cli/instant_client.py` — `login_client()` is the ONLY sanctioned
  admin-token client constructor; it bypasses every `instant.perms.ts` rule.
  Import it directly; never construct `Instant(...)` by hand and never read
  `INSTANT_APP_ADMIN_TOKEN` yourself — `login_client()` already does both via
  `load_instant_config()`/`_read_admin_token()`.
- `cli/apollo_cli/entities/rotina.py`'s `limpar_orfas` (around line 561) — the
  existing default-lists/`--confirmar`-deletes convention and the
  single-JSON-document-per-invocation output shape (`json.dumps(..., sort_keys=True)`)
  this script's own output should mirror.
- `cli/tests/test_auth_rejection.py`'s `_package_python_files()` (around line
  222) — the admin-token-confinement AST gate walks ONLY
  `cli/apollo_cli/**/*.py`. This is exactly why the new script must live
  OUTSIDE `cli/apollo_cli/` (at `cli/scripts/`) — placing it inside the
  package would make that gate fail on the new `login_client()` call site.
- `shared/instant.schema.ts` (entities block, lines ~11-120) — the exact 9
  entity name strings: `fundos`, `projetos`, `etapas`, `tarefas`,
  `templatesRotina`, `instanciasRotina`, `tickets`, `subtarefas`,
  `logInferenciaClaude`. Use these exact strings, case-sensitive.
- `instantdb` Python SDK's `Instant.transact` (installed at
  `cli/.venv/lib/python3.12/site-packages/instantdb/_sync/client.py`) accepts
  either a single tx chunk or a `list` of chunks in one call — this is how
  many rows get deleted per network round trip.

Both tasks run against the LIVE production InstantDB app configured in the
repo-root `.env.instantdb` — no admin-token mocking, matching this project's
established live-verification convention (see PROJECT.md precedent; v1.5
Phase 30 similarly ran its orphan cleanup live against production, per
STATE.md's "[Phase 30]" decision notes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the admin cleanup script and capture live before-counts (dry run only)</name>
  <precondition>Repo-root `.env.instantdb` exists and contains `INSTANT_APP_ADMIN_TOKEN` — check with: `cd /home/thomaz/pessoal/apollo-v2 && cli/.venv/bin/python -c "from apollo_cli.config import load_instant_config as l; print(l().admin_token_present)"` must print `True` before proceeding.</precondition>
  <files>cli/scripts/cleanup_rotina_test_data.py</files>
  <action>
Create a standalone Python script at `cli/scripts/cleanup_rotina_test_data.py`
— deliberately OUTSIDE the `apollo_cli` package (see `<context>` for why) and
NOT named `test_*.py` (pytest would otherwise try to collect it). Module
docstring must state up front that this is a ONE-OFF, TRANSIENT maintenance
tool for quick task 260922-t1l, deleted once the cleanup is confirmed — not a
permanent CLI command.

Imports: `argparse`, `json`, and `login_client` from `apollo_cli.instant_client`
(nothing else from `apollo_cli` is needed — no session, no `apollo auth login`
dependency at all).

Define two module-level tuples: `_ETYPES_DELETE = ("templatesRotina", "instanciasRotina")`
(the ONLY two entities this script ever writes to) and `_ETYPES_CANARY =
("fundos", "projetos", "etapas", "tarefas", "tickets", "subtarefas",
"logInferenciaClaude")` (read-only sentinels proving nothing else moved).

A `_counts(client)` helper builds one query dict merging both tuples (9 keys,
each mapped to `{}`), calls `client.query(...)` once, and returns a dict of
`{etype: len(rows)}` for all 9 entities using `.get(etype, [])` the same
defensive-empty-list style as `limpar_orfas`.

`main()`: `argparse` with one flag, `--confirmar` (`action="store_true"`,
default `False` — mirrors `rotina.py`'s `limpar-orfas --confirmar` convention:
default is list/count-only, zero writes). Call `login_client()` once. Compute
`before = _counts(client)`. Print exactly one JSON document via
`json.dumps({"before": before, "confirmado": args.confirmar}, sort_keys=True)`.
If `--confirmar` was NOT passed, return immediately after that print — this
run must perform zero writes and zero `client.transact` calls.

(The `--confirmar` delete path itself belongs to Task 2's action below — Task
1 only needs the script to exist and its dry-run path to work; do not
implement the confirmar branch as a no-op stub, implement it fully now since
Task 2 depends on it, but Task 1 only VERIFIES the dry-run side.)

For the confirmar branch (implement fully, even though Task 1 doesn't
exercise it): for each etype in `_ETYPES_DELETE`, query `{etype: {}}` again to
get every id (admin client, no `where` — every `donoId`), then delete in
batches of at most 200 ids per `client.transact([...])` call (build a list of
`client.tx[etype][eid].delete()` chunks per batch — never call `.transact`
once per row). After both entities are fully deleted, compute `after =
_counts(client)` and print `json.dumps({"after": after}, sort_keys=True)`.
Then assert, raising `AssertionError` (uncaught — a non-zero exit is the
correct failure signal here) if `after["templatesRotina"] != 0`, if
`after["instanciasRotina"] != 0`, or if `after[etype] != before[etype]` for
ANY etype in `_ETYPES_CANARY` (this is the "nothing else was touched"
guarantee, checked live, not just by code review).
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2 && A=$(cli/.venv/bin/python cli/scripts/cleanup_rotina_test_data.py) && B=$(cli/.venv/bin/python cli/scripts/cleanup_rotina_test_data.py) && [ "$A" = "$B" ] && echo "$A"</automated>
  </verify>
  <done>Script exists, imports/constructs the admin client only via `login_client()` (no bare `Instant(...)`, no `INSTANT_APP_ADMIN_TOKEN` reference of its own), and two consecutive dry runs against production return byte-identical JSON (`"confirmado": false`, non-zero `templatesRotina`/`instanciasRotina` before-counts) — proving the dry-run path performs zero writes.</done>
</task>

<task type="auto">
  <name>Task 2: Run the real deletion, verify independently, remove the transient script</name>
  <reversibility rating="one-way">Permanently deletes production rows with no undo path; the exact scope (only templatesRotina/instanciasRotina, confirm counts=0 before/after) was explicitly specified by the user in this quick-batch item's own description — the door is already walked through, so no additional checkpoint is inserted here (see planner-reversibility.md "Gating a decision already made").</reversibility>
  <files>cli/scripts/cleanup_rotina_test_data.py</files>
  <action>
Run the script from Task 1 with `--confirmar` against the production app from
`.env.instantdb`. It performs the delete for `templatesRotina` and
`instanciasRotina` only and internally asserts `after` counts are exactly 0
for both and unchanged for all 7 canary entities (see Task 1's action) — a
non-zero exit here means the invariant broke and this task is NOT done; do
not proceed to script removal in that case, surface the assertion failure
instead.

Once the confirmar run exits 0, delete `cli/scripts/cleanup_rotina_test_data.py`
from the working tree — it is a one-off tool for this cleanup, not a
permanent addition to the CLI package, and the codebase has no established
"scripts/" convention to extend.

Then run one independent, ad-hoc read-only admin query (a `python -c`
one-liner, NOT the now-deleted script, so the "0 rows" claim doesn't rest on
the same code path that just wrote the deletes) confirming `templatesRotina`
and `instanciasRotina` both report 0 rows in production.
  </action>
  <verify>
    <automated>cd /home/thomaz/pessoal/apollo-v2 && cli/.venv/bin/python cli/scripts/cleanup_rotina_test_data.py --confirmar && rm cli/scripts/cleanup_rotina_test_data.py && test ! -f cli/scripts/cleanup_rotina_test_data.py && cli/.venv/bin/python -c "from apollo_cli.instant_client import login_client; c = login_client(); r = c.query({'templatesRotina': {}, 'instanciasRotina': {}}); t = len(r.get('templatesRotina', [])); i = len(r.get('instanciasRotina', [])); print(t, i); assert t == 0 and i == 0"</automated>
  </verify>
  <done>Script's own `--confirmar` run exits 0 (internal assertion passed: both target entities at 0, all 7 canary entities unchanged). `cli/scripts/cleanup_rotina_test_data.py` no longer exists in the working tree. An independent post-removal `python -c` admin query (not the deleted script) confirms `templatesRotina` and `instanciasRotina` both report 0 rows in production.</done>
</task>

</tasks>

<verification>
1. `git status --porcelain cli/scripts/` shows nothing (the transient script was created and removed within this plan, never left behind).
2. An admin query for `templatesRotina`/`instanciasRotina` (ad-hoc `python -c`, using `apollo_cli.instant_client.login_client()`) returns 0 rows for both, run independently of both tasks above as a final sanity check.
3. Optional, best-effort only (skip if no `apollo auth login` session is active in this environment): `cli/.venv/bin/apollo rotina template listar --limit 1` and `cli/.venv/bin/apollo rotina instancia listar --limit 1` (the normal session-scoped CLI surface) each return an empty list — confirms the cleanup is visible through the product's own read path too, not just the admin client. Not required for task completion; items 1-2 and the two tasks' own `<verify>` blocks are the binding proof.
</verification>

<success_criteria>
- Production `templatesRotina` count is 0.
- Production `instanciasRotina` count is 0.
- Production counts for `fundos`, `projetos`, `etapas`, `tarefas`, `tickets`, `subtarefas`, `logInferenciaClaude` are unchanged from their pre-cleanup values.
- No new permanent file remains in the repo from this cleanup (`cli/scripts/cleanup_rotina_test_data.py` removed).
</success_criteria>

<output>
Create `.planning/quick/20260922-limpar-dados-de-teste-poluidos-apagar-todos-os-registros-das/260922-t1l-SUMMARY.md` when done, with `status: complete` in frontmatter, recording the exact before/after counts observed live.
</output>